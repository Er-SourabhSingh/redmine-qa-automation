# Bug Index — Redmineflux Helpdesk

| Bug ID | Title | Status | Severity | Redmine Version | File Path |
|--------|-------|--------|----------|-----------------|-----------|
| BUG-HLP-001 | New Customer form shows a permanently stuck Login validation message that doesn't reflect actual submission outcome (misleading, not a hard block — some attempts succeed despite it) | Open | Medium | Forge (flux-fiqll6jza49) | bugs/open/BUG-HLP-001.md |
| BUG-HLP-002 | Support Level "Escalation To" dropdown is unscoped ONLY via the unlinked global route — caused by BUG-HLP-005, not independent; recommend closing when BUG-HLP-005 is fixed | Open | Low (downgraded — root cause is BUG-HLP-005) | Forge (flux-fnwchpuwl49 / re-verified flux-fudbk2hlu49) | bugs/open/BUG-HLP-002.md |
| BUG-HLP-003 | Active checkbox cannot be unchecked via Edit on Organization, SLA, and Canned Response only (Product and Support Level confirmed NOT affected); a working list-level toggle exists as a workaround for Organization/SLA, but Canned Response has no workaround (complete block) | Open | High | Forge (flux-fw2qhf0ux49 / flux-fxd0g3s4049 / flux-fudbk2hlu49) | bugs/open/BUG-HLP-003.md |
| BUG-HLP-004 | Customer form's SLA/Support Level dropdowns still list cross-project options (disabled, not removed) — the plugin's own `hidden` attribute on those options is defeated by CSS | Open | Low | Forge (flux-fudbk2hlu49) | bugs/open/BUG-HLP-004.md |
| BUG-HLP-005 | SLA and Support Level can be created via unlinked global routes that bypass the intended project-scoped navigation — for SLA this produces a permanently orphaned, unmanageable record | Open | Medium | Forge (flux-fudbk2hlu49) | bugs/open/BUG-HLP-005.md |
| BUG-HLP-006 | A customer cannot open a ticket they created — the ticket link in their own restricted ticket list points to the standard `/issues/:id` route, which the Customer role is not authorized to access. **Does NOT reproduce on Local (2026-08-26 retest)** — Local's customer ticket list links to the branded `/projects/:id/helpdesk/issues/:id` route instead, which customers can access; needs a Forge retest before closing | Open | Critical | Forge (flux-forgyim9c49) | bugs/open/BUG-HLP-006.md |
| BUG-HLP-007 | Submitting an Internal Note crashes with a 500 (`journals.private_notes` NOT NULL violation, submitted as `""`) — reproduces only via the branded `/projects/:id/helpdesk/issues/:id` route (`RfProjectHelpdeskIssuesController`), not via core `/issues/:id`; real-world impactful because every notification email's "View Ticket" link uses the branded route | Open | High | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-007.md |
| BUG-HLP-008 | "Helpdesk Conversion" tab is an incomplete/inaccurate email audit trail — a customer's real inbound-email reply to an existing ticket is never logged there (only visible under Notes), and one outbound agent-reply entry is duplicate-logged mislabeled as an inbound "Customer replied to Support" message; likely shares a root cause with the `EmailPollerWorker` warning | Open | Medium | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-008.md |
| BUG-HLP-009 | Adding a note via the standard Redmine "Edit" form auto-transitions the ticket's Status to "Waiting for Customer Response" even though nothing is actually communicated to the customer through any channel (no Notes visibility — expected — but also no Helpdesk Conversion entry, no email) | Open | High | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-009.md |
| BUG-HLP-010 | Organization Website and Phone Number allow exact duplicates across different organizations with no warning — matches current documented spec (only Name is unique) but filed per explicit user product-judgment direction as a real-world data-integrity gap | Open | Low | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-010.md |
| BUG-HLP-011 | An organization linked to a customer can be deleted with only a generic "are you sure" prompt — no warning that a customer is linked, silently clearing the customer's Organization reference to "—" | Open | Medium | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-011.md |
| BUG-HLP-012 | ~~Customer Edit form's Information section editable by an out-of-project Manager~~ — **Closed: Not a Bug.** Customers are a global entity (Login/Email unique install-wide); the Add-project mechanism on the same form is correctly scoped to the Manager's own project, confirming Information fields are the intentionally-global part of the record, Project-access rows are the intentionally-scoped part | Closed | — | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-012.md |
| BUG-HLP-013 | **Revised 2026-08-27 per user product-judgment direction.** Portal Preview (`/rf_customers/:id/portal`) is not considered part of this plugin's intended functionality (see `HELPDESK_SCOPE.md`), yet the route is fully reachable and functional (read-only, correct scoping, 403 on out-of-entitlement project) despite having no UI entry point anywhere — the defect is now that the route exists at all, not that it lacks a button. Needs a product/dev decision: remove the route, or reverse the scope call and add the missing entry point. TC-HLP-117 → FAIL, TC-HLP-124 → PASS (secondary) | Open | Medium | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-013.md |
| BUG-HLP-014 | A customer's own Tickets list shows "0 tickets" even when their tickets genuinely exist and are individually reachable. **Root cause confirmed via source + `rails runner`**: `TicketFilter#base_scope` starts from Redmine's native `Issue.visible(user)`, which requires real project Membership — but customers are (by design) linked only via `RfProjectCustomer`, never a Redmine Member, so `Issue.visible` returns zero unconditionally. Every other customer check in the plugin (page access, ticket access, Dashboard count) correctly uses `RfProjectCustomer` instead; only this one list fell back to Redmine's native scope. **Confirmed systemic** — reproduced identically with a brand-new customer (`delta.customer`) with zero prior history, so this affects every customer account, not a fixture-specific artifact. | Open | High | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-014.md |
## Notes
- Open bugs: bugs/open/
- Closed bugs: bugs/closed/
- Screenshots: screenshots/<BUG-ID>/
