# Copy this file to .env and fill in values from the active credentials file:
#   QA_CREDENTIALS_LOCAL.md  (ENVIRONMENT=local, localhost:3012, redmine-docker-6)
#   QA_CREDENTIALS_FORGE.md  (ENVIRONMENT=forge)
#
# Never commit the real .env file. Never hardcode these values in test/page-object code.
# Agent/Customer credentials are NOT here — they're fixed local fixtures, see
# testdata/helpdesk.local.fixtures.ts (AGENTS/CUSTOMERS/MAIL_PASSWORD).

ENVIRONMENT=local
BASE_URL=http://localhost:3012

ADMIN_USERNAME=admin
ADMIN_PASSWORD=
