# Handoff — Redmineflux Crux

## Last Session

- Date:
- Redmine Version:
- Environment:

## Completed This Session

## In Progress

## Blockers

- **2026-09-17 (new, unresolved) — platform-wide Ask Crux outage, OpenRouter key 401:** every Ask Crux chat call across every domain agent started failing partway through `CRUX_AGENT_TIMESHEET.md` (the last suite in the 34-gap-TC sweep). Root cause confirmed via the native `/crux/admin/keys` "Test connection" diagnostic — the configured OpenRouter provider key now returns **401 Unauthorized** (not the earlier 402-low-balance issue from 2026-09-16/17, a distinct failure mode). This local Docker instance's Crux chat backend has only **one** configured LLM provider (OpenRouter) with no fallback key — see `docs/CRUX_MEMORY.md` for this as a standing infra note. All 6 remaining Timesheet TCs (TC-CRX-083–152) are BLOCKED as a direct result — not a plugin defect, no bug filed. **A full fixture is pre-built and ready for immediate reuse**: approval schema "Two-Level Approval" (ID 1), new user `crux.developer`, and team "Retest Squad" — all created and verified genuine via the native UI before the outage hit. Needs either a refreshed/topped-up OpenRouter key or a second provider configured before Timesheet testing (or any further Ask Crux testing at all) can resume.
- **2026-09-10 (resolved — env found):** QA env setup was found locally at `C:\Crux-Redmine-Docker` (4 components: `redmineflux-crux-core`, `redmineflux_crux` plugin, `redmineflux-mcp`, plus its own dedicated `crux-redmine` Docker Redmine instance — see Environment Notes below for full detail). Stack verified up and healthy (`docker ps`, `GET /api/health` → 200). Still open: no LLM provider key configured (`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`GEMINI_API_KEY` all blank in `.env`) — chat currently only returns the canned echo-provider fallback, which blocks any meaningful "Ask Crux" / CRX-46 functional testing until at least one key is added. `CRUX_REQUIRE_USER_KEY` currently `0` (default/shared-key mode) — must be flipped to `1` specifically for CRX-12 per-user-key testing (see `.env`), and a second, restricted-privilege Redmine test user + their own API key still needs to be created on `crux-redmine` for that test.
- **2026-09-10 (resolved — docs drafted):** `CRUX_REQUIREMENTS.md` / `CRUX_FEATURES_LIST.md` / `CRUX_USER_GUIDE.md` drafted from source: `redmineflux-crux-core/README.md` + `docs/API.md` + `RELEASE-NOTES.md`, `redmineflux_crux/README.md` + `init.rb` (menus/permissions), and all 9 relevant `agents/*.md` manifests. **Not yet reviewed by the dev team or walked through live** — marked as drafts in each file's header. Key finding: 7 of the 9 agents in #117162 (all except DevOps and Budget/Audit, which have a narrower single-write-action domain) already carry full create/update/delete tool access in their spec files at crux-core v0.92.0 — this looks substantially further along than the ticket description's own "Not started — planning/spec stage" line suggests, and lines up better with the tracker's Status = In QA / 70% done. A note was posted on both production tickets (#116773, #117162) on 2026-09-10 asking Prashant Chaurasia to confirm/clarify — still unanswered as of this session; consider a short follow-up comment noting the env was found locally and docs are now drafted from source, pending dev confirmation.

## 2026-09-11 — Dev reply received (`REPLY-TO-QA-2026-09-11.md`, local QA stack folder)

Prashant replied in full to `CRUX_DEV_REQUEST.md`. Summary of what changed:

- **Q1 (status contradiction):** Confirmed — ticket description was stale, tracker (In QA/70%) is accurate. Dev will correct the description. Tool-in-manifest ≠ verified-end-to-end was confirmed as a real concern — but dev separately ran a full live propose→confirm→verify sweep on all 9 agents on 2026-09-10/11 (see Q3) specifically to close that gap.
- **Q2 (Session Artifacts):** Confirmed **intentionally v1-scoped** — core leg (save/list/read/version) fully shipped; attach-to-Flux is a **deliberate scope cut, no ETA**. `CRUX_CHAT_CAPABILITIES_KEEP_SHARE_ARTIFACTS.md` updated: TC-CRX-116/057 (attach-to-project/ticket) retired as out-of-scope; TC-CRX-115 rescoped to core-leg only.
- **Q3 (CRUD verified end-to-end?):** Yes — dev ran a real sweep against all 9 agents (helpdesk, crm-sales, agile-scrum, testcases-qa, timesheet, invoice-billing, knowledge-base, workload-capacity, project-manager), each with a real create/write + verify + cleanup. All passed; timesheet's `approve` correctly **refused** a self-approval (honest failure, not faked success) — direct evidence the honest-failure fix works for real. **2 new bugs found+fixed during the sweep** (confirm-fabrication guard: case-sensitivity + trailing-commentary gaps, commit `4771db6`) — added as regression TC-CRX-169/034 in `CRUX_WRITE_CONFIRM_GATE.md`. TC-CRX-168 updated with the `_looks_like_failure()`/`generic_write` detail.
- **Q4 (default role permissions), Q5 (`use_ask_crux` membership design intent), Q6 (target sign-off environment):** All three **still pending** — dev will follow up separately. Do not treat `CRUX_REQUIREMENTS.md`'s Permissions Matrix as confirmed until Q4 lands.
- **§2 API/credentials:** Split (QA provisions LLM key + 2nd test user) confirmed correct. Shared org key TBD — don't block on it.
- **§3 Docs:** Dev will review our 3 drafted docs and send corrections. `SETUP-INSTRUCTIONS.md` will be checked into the actual repo (not left as a local-only handoff doc).
- **§4 Environment:** `redmineflux-mcp`'s 2 unpushed commits (`ae0d83c`, `84e234f`) — **now pushed** to `origin/crux_development`. `redmineflux-crux-core` master was also 2 commits ahead locally (the Q3 bugfixes) — **also now pushed**. `pyjwt[crypto]` gap — **confirmed real**, dev raising with team for a proper fix (lazy-import vs. accepted dependency); our workaround stays fine to use meanwhile. `CRUX_COMPONENT_CF=1` — acknowledged correct-for-this-env-only, no action needed.
- **§5 Seed data — big finding:** Done for 6/9 plugins (CRM, Workload, Test Case Mgmt, Timesheet, Invoice, KB) — real, professional test data already created on the local stack. **DevOps and Budget/Audit are NOT installed on this MCP instance at all** (12/14 plugins detected — DevOps needs a connected git repo it lacks here, Budget/Audit isn't detected) — flagged by dev as a **separate infrastructure task**, blocking, not a seed-data question. `CRUX_AGENT_DEVOPS_AND_BUDGET.md` updated with this as a hard blocker — do not attempt to execute until resolved, and don't file the gap as a product bug.
- **§5 safety caution** (`trigger_build`/`send_invoice` real-world effects): acknowledged, dev will follow up with a confirmed safe test repo/branch and QA-controlled test email — still pending, do not improvise.

**Also fixed this session:** the local `.env` file at `C:\Crux-Redmine-Docker\.env` had gone missing (only `.env.example` remained) after the folder was refreshed with the dev's updated files — the running containers still had the old values in memory, but a future `docker compose up -d`/restart would have silently broken with blank Redmine credentials. Recovered the real values directly from the running `crux-core` container (`docker inspect`) and recreated `.env` — confirmed matches: `REDMINE_API_KEY=1ce...`, `CRUX_PROJECT=crux-qa`, `REDMINE_PUBLIC_URL=http://localhost:3014`. LLM key fields are still blank — that part of `.env` was never filled in.

## 2026-09-11 (later same day) — LLM key added, real chat now working, `CRUX_SECRET` root cause found + fixed, BUG-CRX-001 filed

User provided an OpenRouter key (`sk-or-v1-...`). Full chain, in order:

1. **First attempt (wrong placement):** key was placed in `ANTHROPIC_API_KEY` — wrong, since it's OpenRouter format, not Anthropic's. Corrected understanding: the 3 env-var fields (`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`GEMINI_API_KEY`) only feed the 3 **built-in** providers' native APIs.
2. **Discovered a pre-existing "openrouter" custom provider** already configured in crux-core's provider registry (`GET /api/providers`, id `openrouter`, `base_url: https://openrouter.ai/api/v1`, `default_model: anthropic/claude-haiku-4.5`) — and the exact same key already registered in the managed keystore (`GET /api/llm_keys`, `key-005`, added by "admin" on 2026-08-19, last4 `2ea5` — matched the key the user gave, confirming it's the same one). **No env var was actually needed.**
3. **But a real `POST /api/chat` call still 401'd.** Root-caused fully (see `docs/BUG-CRX-001` filed below for the bug found along the way, and the paragraph below for the actual root cause):
   - crux-core obfuscates stored keys at rest via an HMAC keystream derived from `CRUX_SECRET` (`crux_core/store/keystore.py` — explicitly documented as "obfuscation, not real cryptography").
   - The code **refuses to store a real key while `CRUX_SECRET` is the built-in dev default** — so `key-005` could only have been stored under a real, custom `CRUX_SECRET` set in `.env` back on 2026-08-19.
   - The 2026-09-11 folder refresh (see the "env found" entry above) silently dropped that custom `CRUX_SECRET` along with the rest of `.env`. The container was running on the dev-default secret, which doesn't match — decrypting the stored key produced garbage, sent as the Bearer token on every real chat call → 401.
   - Confirmed the key itself was completely valid by calling OpenRouter's real chat completions endpoint directly with it — worked immediately.
   - **The 3 agent-identity keys (`key-002/003/004`, provisioned 2026-07-15 for builder-1/builder-2/test-author, CRX-48) are almost certainly broken the same way** — same keystore, same lost secret, never re-provisioned. Flag to dev if CRX-48 agent-identity testing becomes relevant; will need fresh provisioning.
4. **Fix applied:** generated a real `CRUX_SECRET`, added it to `.env`, wired it through `docker-compose.yml`'s `crux-core` environment block (it wasn't forwarded there before at all — a genuine gap in the refreshed compose file), restarted. Old `key-005` was deleted (permanently undecryptable, original secret unknown/lost) and re-added fresh under the new real secret.
5. **Verified working:** `POST /api/chat` now returns a real model reply — `"I'm Claude, made by Anthropic."`, `provider: openrouter`, `model: anthropic/claude-haiku-4.5`, real token/cost usage. Chat is genuinely live.
6. **BUG-CRX-001 filed** (High, `bugs/open/BUG-CRX-001.md`) for a separate, real product bug found along the way: `POST /api/provider/test` ("Test Connection") reports false-positive success regardless of whether the stored key actually works — proven by confirming OpenRouter's `/models` endpoint returns HTTP 200 with no `Authorization` header at all, and even with a deliberately fake key. The test doesn't meaningfully validate anything. Not yet reported to production — pending approval.

**Net effect: the LLM-key blocker is resolved.** All suites that were only waiting on this (10 of the 11 previously-blocked suites) are now executable. `CRUX_AGENT_DEVOPS_AND_BUDGET.md` stays blocked regardless (separate infra gap, DevOps/Budget-Audit plugins not installed on this MCP instance). CRX-12 suite still needs `CRUX_REQUIRE_USER_KEY=1` + a second restricted test user.

## 2026-09-11 (later still) — real UI walkthrough via Playwright, second infra bug found+fixed, BUG-CRX-002 filed

User provided admin credentials (`admin`/`12345678` on `crux-redmine` localhost:3014) and asked for the OpenRouter setup to be verified through the actual UI, not just the API. Logged in and walked through it live:

- **TC-CRX-132 confirmed fully live**: bubble, greeting text, starter prompts, "Open full view" link, no stray top-menu entry, AND a real end-to-end chat exchange — asked "What are the agents working on?", got a genuine grounded reply (27 agents online, 11 gates across 6 real Work Packages, real `wp-` ids) with real model attribution (`anthropic/claude-haiku-4.5`, real token/cost usage `$0.0548 est.`). This is definitive proof chat is genuinely live end-to-end, not just at the API layer.
- **Second real infra bug found (not filed as a product bug — a documented, expected setup step that just hadn't been done):** the Crux plugin's "Crux core service URL" setting (Settings page, `/crux/admin/settings`) was still at its default `http://localhost:8787` — wrong for Docker, since `localhost` from inside the `crux-redmine` container means itself, not the `crux-core` container. This broke the dashboard entirely ("⚠ crux-core unreachable... Connection refused") and left "Chat agents" showing "No agents found". **Fixed via the UI** (TC-CRX-072): changed to `http://crux-core:8787`, saved, confirmed the agent roster populated immediately (26 agents) with no restart needed.
- **BUG-CRX-001 (Test Connection false positive) reproduced through the real UI**, not just curl — same `ok · 454ms` false-positive result on the actual Providers & Keys page. Confirms it's a genuine backend bug, not a raw-API-only artifact.
- **BUG-CRX-002 filed (Medium):** the Settings page's "Chat agents" picker shows persona descriptions for 6 of the 9 #117162-scoped agents (DevOps, Budget, Scrum, Time, Invoicing, Capacity) that explicitly claim read-only/never-writes behavior — directly contradicting their real `allowed_tools` CRUD capability already confirmed from source this session, and contradicting the dev's own live-sweep verification (e.g. Time Agent's text says "never approves one" while dev's sweep specifically exercised `timesheet approve`). Only QA Agent's text correctly reflects full CRUD; Sales/KB Agent's text is neutral. Looks like stale copy never updated when #117162's CRUD work landed on these 6.
- **Open bug count now 2** (BUG-CRX-001 High, BUG-CRX-002 Medium). Neither reported to production yet.

**All of Navigation & Permissions TC-CRX-132 and Agent Roster & Admin TC-CRX-068/082 now have CONFIRMED LIVE evidence recorded directly in their suite files.**

## 2026-09-14 — Live execution of CRUX_NAVIGATION_AND_PERMISSIONS.md complete, BUG-CRX-003 found

Resumed execution after a 3-day gap (Docker stack still healthy — `docker ps` all 5 containers up, `GET /api/health` OK). Executed all 13 TCs (TC-CRX-132–010, TC-CRX-142–141) live — **all PASS**, full evidence recorded inline in the suite file.

**Environment setup done this session (reusable by later suites):**
- Created 2 local test users: `luna.blossom` (Manager role — all 5 Crux permissions) and `daisy.skye` (Reporter role — zero Crux permissions), both members of `crux-qa`.
- `crux-qa` project had NO modules enabled at all (not even Issue tracking) and no trackers assigned — fixed (enabled Issue tracking + Redmineflux Crux modules, assigned Bug/Feature/Support trackers). This was a standing local-env gap, not a plugin bug.
- Created a second project `crux-qa-private` (non-public — new projects default to **Public**, a one-time gotcha) for cross-project membership/isolation testing. Contains issue #1 (`PRIVATE-MARKER-7f3a`). `crux-qa` itself got issue #2 (`PUBLIC-MARKER-9c2e`) as the membership-positive control.

**Key finding — BUG-CRX-003 (local only, not yet reported to production):** Global-dashboard gate approval (`crux_dashboard#approve_gate`, and `#gate_evidence` which shares the exact same check) does **not** verify the approving user is a member of the Work Package's own project — only `User.current.admin? || User.current.allowed_to_globally?(:approve_crux_gates)`, i.e. the permission via *any* project membership. Live-confirmed: `luna.blossom` (member of `crux-qa` only) successfully approved `wp-013`'s `send-approve` gate — a WP with no evident connection to `crux-qa` — and could also fetch its evidence. This corrects TC-CRX-136 and TC-CRX-144, which had assumed `require: :member` in `init.rb` meant real per-project scoping. May be intentional (consistent with the global dashboard's other "any permission-holder" patterns — TC-CRX-123/068/142) or a genuine gap; flagged for dev triage, not yet filed to production pending user decision. See `bugs/open/BUG-CRX-003.md`.

**Other observations (not filed as bugs):**
- The Agent Fleet page (`/crux/agents`) fires a background fetch to the admin-only `/crux/admin/keys/providers.json` even for non-admin sessions — benign console 403, but leaves the "New Agent" dialog's Provider dropdown stuck on "unavailable" for non-admins. Cosmetic/DX rough edge.
- Retired test agents still appear as options in the Settings page's "Chat agents" picker list. Cosmetic, not investigated further.
- A near-miss: initially misread a snapshot-timing race + a `textContent`-based search artifact as "new agents invisible in the Fleet UI" — verified via direct DOM inspection before concluding it was a false alarm. No bug filed. Reinforces `[[feedback_verify_absent_field_via_dom_before_filing]]`.

**Suite status: `CRUX_NAVIGATION_AND_PERMISSIONS.md` is now fully executed (13/13 PASS).** Next: re-upload the updated suite `.md` to production testcase #120481 (stale since the pre-execution source-review pass), then move to `CRUX_DASHBOARD_GRAPH_PIPELINE.md` per the previously agreed order.

**Follow-up thoroughness pass (same day, user-prompted):** the user asked directly whether every TC had been tested for the complete permission matrix, via real UI navigation, and with a direct-URL check for the no-permission case. Honest answer was no — several TCs had one-sided evidence. Went back and filled every gap:
- TC-CRX-136: added the zero-permission negative case (UI: approve button absent from `daisy.skye`'s view; endpoint: `POST /crux/gate` → 403) — previously only the (buggy) positive/cross-project case had been tested.
- TC-CRX-138: added the full negative case for both agents and pipelines (UI: `daisy.skye` sees read-only tables with zero management affordances; endpoint: `POST /crux/agents` and `POST /crux/pipelines` both → 403) — previously had no negative-permission check at all.
- TC-CRX-139: added real UI click-throughs (as admin) for all three admin pages — previously only inferred they'd work from the rail listing them, never actually clicked.
- TC-CRX-140: added the negative case — `daisy.skye` hitting `/crux/admin/settings` directly → 403 — previously only the admin/positive side was tested.
- TC-CRX-144: added a DOM check for whether a real UI element (not just the API) triggers `gate_evidence` — confirmed there is none on the dashboard (the `✓`/`🚦` gate markers are plain non-interactive `<span>`s), so the `fetch`-based test was the only available approach, not a shortcut.
All additions recorded inline in the suite file with their own CONFIRMED LIVE markers. No new bugs found in this pass — all the negative/UI-element cases behaved correctly; the previously-filed BUG-CRX-003 remains the only real gap in this suite.

## 2026-09-14 (later still) — Two process fixes: seed-pool usernames, testdata registry

Two more gaps caught by the user, both fixed same-day:

1. **Ad-hoc usernames corrected.** `crux_qa_manager`/`crux_qa_reporter` (created earlier this session) violated `QA_CREDENTIALS.md` Authentication Rule #1 ("always use the existing configured users ... do not create random/ad hoc users"). Renamed both accounts in place — same underlying Redmine user IDs, same membership/role/test history intact — to `luna.blossom` (was manager) and `daisy.skye` (was reporter) from the shared seed pool, password reset to the pool's shared `12345678`. Every mention across `CRUX_NAVIGATION_AND_PERMISSIONS.md`, `bugs/open/BUG-CRX-003.md`, and this file updated via `sed` to match (verified zero remaining references afterward). **Note for future sessions:** even though localhost:3014 is a plugin-dedicated instance (which `QA_CREDENTIALS.md` itself allows to document its own users separately), the shared seed-pool *naming convention* should still be used for any new fixture account there — don't invent new ad hoc names just because the instance is dedicated.
2. **Testdata registry created.** No `automation/testdata/CRUX_TESTDATA_LOCAL.xlsx` had ever been generated, despite this session creating exactly the kind of fixtures `CLAUDE.md` §13a calls for tracking. Adapted `scripts/gen_testdata_xlsx.py`'s SHEETS dict for Crux's own entity types (Users, Projects, Issues (fixtures), Agents (test), Pipelines (test), Seeded WPs Consumed) as `scripts/gen_crux_testdata_xlsx.py`, generated the workbook, and populated it with every fixture from this session: both renamed users, both projects (`crux-qa`, `crux-qa-private`), both marker issues, both test agents (retired) and the test pipeline (deleted), and — notably — a row in "Seeded WPs Consumed" documenting that `wp-013`'s `send-approve` gate was permanently approved during TC-CRX-136/BUG-CRX-003, so a later suite doesn't assume it's still pending. Keep this workbook current going forward — check it before creating any new fixture, update it immediately after any create/modify/delete.

## 2026-09-14 (later still) — BUG-CRX-003 corrected: was overstated as a cross-project leak, actually a code-level gap only

While guiding manual re-testing of TC-CRX-136, the user (rightly) questioned whether `luna.blossom` approving `wp-013`'s gate was actually a bug, given her role screenshot showed `approve_crux_gates` legitimately checked. Investigated further rather than just reasserting the original claim:

- Confirmed via source that `CruxDashboardController#authorize_gate_approval` truly has zero project-scoping logic (`User.current.admin? || User.current.allowed_to_globally?(:approve_crux_gates)` — no `@project`/`find_project` anywhere in the controller). This part of the original finding holds.
- **But:** checked whether `wp-013`'s issue (`#999004`) and two other dashboard-referenced issues (`#12`, `#34`) exist as real Redmine records on this instance — **all three return `404`**. None of the seeded Work Packages on this dashboard map to real Redmine issues/projects here; they're crux-core's own internal demo data.
- **Conclusion:** the original bug write-up overstated this as a demonstrated cross-project access leak. The corrected, accurate claim is narrower: the controller has no project-scoping code path at all, confirmed by reading the source — but this was never actually exercised against a real project on this environment, because no seed WP here links to one. `BUG-CRX-003.md`, and the TC-CRX-136/TC-CRX-144 evidence blocks in `CRUX_NAVIGATION_AND_PERMISSIONS.md`, have been corrected to state this precisely rather than the original overstated version.
- **Lesson:** don't let a live demo's success/failure alone stand as proof of a security claim — verify the underlying data actually supports the specific claim being made (here: "cross-project" requires an actual second project to be meaningfully cross from). Re-reading the controller source directly, and checking whether the referenced issue IDs are real, is what caught this.

## 2026-09-14 (later still) — `CRUX_DASHBOARD_GRAPH_PIPELINE.md` fully executed

All 12 TCs (TC-CRX-121–069, TC-CRX-129–144) executed live. 11/12 PASS, no new bugs found:

- TC-CRX-121/063: dashboard content + `?project=` scoping both confirmed precisely at the API level (exact issue subjects checked, not just counts) — no cross-project bleed.
- TC-CRX-123: cross-referenced against already-confirmed evidence in TC-CRX-141 and the BUG-CRX-005 investigation, no new live action needed.
- TC-CRX-124: project work graph renders real nodes/edges (1 Ticket/1 Person/2 Nodes/1 Edge, matching issue #2's real author). The `days=` parameter sub-check is **inconclusive, not failed** — `crux-qa` only has one issue, created today, so no shorter day-range has older data to meaningfully exclude. Re-test once real multi-date issue history exists.
- TC-CRX-125/067/068: cross-referenced against the full pipeline CRUD cycle already executed in `CRUX_NAVIGATION_AND_PERMISSIONS.md` TC-CRX-138, avoiding duplicate testing.
- TC-CRX-128: **BLOCKED** — no UI path found this session for creating a Work Package bound to a specific custom pipeline, so the TC's precondition (a custom pipeline in active use) can't be constructed. Neither existing custom pipeline ("Review Approval", "Trim Test") matches any seeded WP's actual stage shape.
- TC-CRX-129: Run Ledger "View All" (global) — pagination, all three filters (`agent`/`outcome`/`model`), and the malformed-param fallback all confirmed precisely via the API; login-only access confirmed with `daisy.skye`.
- TC-CRX-130: project-scoped Run Ledger — `luna.blossom` (member+`view_crux`) succeeds with correct 0-bleed scoping; `daisy.skye` used as the negative case, though note she's a `crux-qa` *member* lacking `view_crux` (not a true non-member) — still validates the `authorize` membership+permission gate.
- TC-CRX-131: module-disable test fully isolated — disabled "Redmineflux Crux" for `crux-qa`, confirmed all 3 project-scoped URLs 403 even for a fully-permissioned member, re-enabled, confirmed the exact same user/URL immediately succeeds again. Module left correctly re-enabled afterward.

No new fixtures created — reused `luna.blossom`/`daisy.skye`/`crux-qa`/`crux-qa-private` from the Navigation suite throughout.

## Next Session Start Point (superseded — see the 2026-09-15 entry near the end of this file for the current one)

- **2026-09-11: full test case authoring complete** (138 TCs, 16 suites) — see `docs/CRUX_FEATURES_LIST.md` Suite Index. Dev reply received same day and Q1–Q3 answered; Q4–Q6 still pending (see section above) — do not treat the Permissions Matrix as confirmed.
- CRX-12 suite additionally needs `CRUX_REQUIRE_USER_KEY=1` + a restarted stack + a second, restricted-privilege Redmine test user with their own API key.
- Follow up with dev on the 3 still-open items (Q4 role matrix, Q5 membership-design-intent, Q6 sign-off environment) and the 2 still-pending safety confirmations (safe test repo/branch for `trigger_build`, QA-controlled test email for `send_invoice`) if no reply has arrived by next session.

## Production Redmine (ztflux) linkage

- **Test Suite "Crux"** created 2026-09-11 on `flux.zehntech.com` project `ztflux` (id 122) — **production Test Suite ID 374**, top-level, no parent.
- **16 production testcases created 2026-09-11, one per local suite file** (not one per individual TC — 138 individual TCs would be too many production records). Each production testcase has the full suite `.md` file attached as-is; the local file remains the actual source of truth (steps, evidence, CONFIRMED LIVE markers). Mapping:

| Production Testcase # | Suite file | Local TC range |
|---|---|---|
| 120481 | CRUX_NAVIGATION_AND_PERMISSIONS.md | TC-CRX-132–010 |
| 120483 | CRUX_ASK_CRUX_CHAT_CORE.md | TC-CRX-102–021 |
| 120484 | CRUX_WRITE_CONFIRM_GATE.md | TC-CRX-161–034 |
| 120485 | CRUX_PER_USER_KEY_CRX12.md | TC-CRX-145–042 |
| 120486 | CRUX_PROJECT_CREATION_AND_IMPROVE_WAND.md | TC-CRX-151–052 |
| 120487 | CRUX_CHAT_CAPABILITIES_KEEP_SHARE_ARTIFACTS.md | TC-CRX-113–060 |
| 120488 | CRUX_DASHBOARD_GRAPH_PIPELINE.md | TC-CRX-121–069 |
| 120489 | CRUX_AGENT_ROSTER_ADMIN.md | TC-CRX-064–084 |
| 120490 | CRUX_AGENT_CRM_SALES.md | TC-CRX-010–092 |
| 120491 | CRUX_AGENT_WORKLOAD_CAPACITY.md | TC-CRX-089–100 |
| 120492 | CRUX_AGENT_DEVOPS_AND_BUDGET.md | TC-CRX-023–107 |
| 120493 | CRUX_AGENT_AGILE_SCRUM.md | TC-CRX-001–113 |
| 120494 | CRUX_AGENT_QA_TESTCASES.md | TC-CRX-053–120 |
| 120495 | CRUX_AGENT_TIMESHEET.md | TC-CRX-077–126 |
| 120496 | CRUX_AGENT_INVOICE_BILLING.md | TC-CRX-034–132 |
| 120497 | CRUX_AGENT_KNOWLEDGE_BASE.md | TC-CRX-044–138 |

All 16 are added to suite 374. **When a local suite file's evidence is updated (a TC executed, a bug filed, dev feedback incorporated), re-upload the updated `.md` as a new attachment on the matching production testcase** (`upload_file` + `update_testcase` with the new token) so production stays in sync with the local source of truth — the old attachment is not deleted automatically, so note in the testcase which attachment is current if this happens more than once.

## Source exploration pass before execution (2026-09-11)

Before starting live execution, did a full read of `redmineflux_crux`'s `config/routes.rb`, `init.rb`, and every controller in `app/controllers/` to check for missing/incorrect test coverage. Real findings, all now reflected in the suite files:

1. **TC-CRX-132 and TC-CRX-141 were factually wrong** — `lib/redmineflux_crux/hooks/view_hooks.rb` proves the Ask Crux bubble (+ Improve wand, mention-poll trigger, WP badge) IS server-side gated by `use_ask_crux || admin`, entirely absent from the DOM for unpermitted users — not "unconfirmed" or "not permission-gated" as originally drafted. Both TCs corrected in place; new TC-CRX-142 added to test this precisely (DOM-absence, not CSS-hidden).
2. **`view_crux` does NOT actually restrict the GLOBAL dashboard, pipeline board, or agent roster** — confirmed via controller code + explicit code comments ("any logged-in user can look"). Only the PROJECT-scoped tab/graph/runs genuinely enforce `view_crux` + membership (via Redmine's standard `authorize`). This directly contradicted the original TC-CRX-123 and the rationale in TC-CRX-127/076 — all three corrected. Added to `CRUX_REQUIREMENTS.md` Known Constraints as an architectural fact, and flagged as worth an explicit dev confirmation (not currently treated as a bug — the source comments call it deliberate).
3. **An entire feature had zero test coverage**: the Run Ledger "View All" pages (`/crux/runs`, `/projects/:id/crux/runs`) — added as TC-CRX-129/143 in `CRUX_DASHBOARD_GRAPH_PIPELINE.md`, plus TC-CRX-131 for the Crux project-module-disabled case (tab absence + direct-URL blocking).
4. **`crux/gate_evidence` was never tested independently** despite sharing the exact same permission gate as `approve_gate` — added TC-CRX-144.
5. **`crux_ask`'s dual authorization behavior** (page action → Redmine's own access-denied page; JSON actions → JSON 403) was untested — added TC-CRX-143.
6. **`crux_agents#retire` and `#upload` had zero coverage** despite requiring `manage_crux_agents` exactly like `create`/`pause`/`provision` — neither appears in `init.rb`'s visible permission-action list at all (enforced only via the controller's own `require_manage_agents`), a real risk of future drift since it's invisible in Administration → Roles. Added TC-CRX-075/146 in `CRUX_AGENT_ROSTER_ADMIN.md`, and corrected TC-CRX-066's rationale.

**Net new/changed:** 9 new TCs (139–146, spread across 3 suite files) + 5 corrected TCs (001, 010, 064, 068, 076). **Production testcases #120481, #120488, #120489 now have stale attachments** — re-upload the updated `.md` files to production before/while executing these suites (needs your approval, same as any production write).

## Production Test Run + defect reports (2026-09-11)

- **Run #569 "Crux QA Run 1"** created in `ztflux`, scoped to suite 374 only, all 16 testcases. Environment: Window 11 + Chrome (env id 6). Assignee: Sourabh Singh (user id 683). Start 2026-09-11, due 2026-09-18, state New. **Run type "Functional" could not be set via MCP** (no run_type param on `create_run`/`update_run`, and `list_run_types` needs admin access this API key doesn't have) — needs setting manually in the Redmine UI.
- **Both local bugs reported to production**, both linked to testcase **#120489** (CRUX_AGENT_ROSTER_ADMIN) in run #569, both marking that testcase **Failed**:
  - BUG-CRX-001 → production issue **#120514** (High)
  - BUG-CRX-002 → production issue **#120515** (Medium)
- Local bug files (`bugs/open/BUG-CRX-001.md`, `BUG-CRX-002.md`) and `bugs/_index.md` updated with the production issue IDs per CLAUDE.md §5.
- **Reminder for session end / bug closure:** per CLAUDE.md §5, when either bug is eventually closed locally (moved to `bugs/closed/`), the matching production issue must also be updated (status In QA → Done, % done → 100) — write-approval required, same as any other production change.

## 2026-09-14 (later still) — Manual re-test hit a real outage: MCP session never self-heals, BUG-CRX-004 filed

While guiding the user through manual re-testing of TC-CRX-137, every Ask Crux message failed with "Streamable HTTP POST to MCP failed: HTTP Error 404: Not Found" — reproducible even in a brand-new chat session, ruling out a stale browser-side session.

- Root-caused via `redmineflux-crux-core/crux_core/askcrux/mcp_client.py`: crux-core holds one **process-wide** MCP connection singleton (`_CONN`), shared by every user/session. Its reconnect logic only triggers on an already-dead connection or a changed auth key — it has **no path to detect a `404` (expired/invalid `Mcp-Session-Id`) and reconnect**. Confirmed via `docker logs crux-core` + the MCP container's own access log: the exact same `tools/list` call failed identically across two different `ses-` IDs, with no auth-rejection warning (i.e. it got past auth and failed at the session layer).
- **Fix:** `docker restart crux-core` (clears the in-process singleton) — confirmed immediately fixed via logs (`tools/list` succeeded, 94 tools, followed by successful search/get_issue calls). Re-ran both TC-CRX-137 questions afterward and got the exact expected replies (private marker → honest "not found" with no leak; public marker → correct issue lookup).
- **Filed BUG-CRX-004** (High) — this is a full, self-inflicted outage of the entire Ask Crux chat feature for every user simultaneously, with no automatic recovery and no admin-facing alert, that will recur after any sufficiently long idle period in a real deployment (this one was likely triggered by the multi-day gap since the prior test session). Suggested fix in the bug file: treat a `404` from the MCP endpoint as "session invalid," clear `_session_id`/mark the connection dead, and let the existing reconnect-on-drop path handle re-`initialize` automatically.
- **Process note:** the user directly asked "have you tested... all things properly tested?" earlier in the session, and separately caught an overstated bug claim (BUG-CRX-003) by simply asking "please investigate" rather than accepting the finding at face value — both prompts materially improved this session's output. Worth continuing to invite that kind of pushback rather than treating early conclusions as final.

## 2026-09-14 (later still) — nav_top_crux/nav_top_agents found reset to "0"; user's direct-URL observation confirmed; reset itself not reproducible

During guided manual testing of TC-CRX-138, the user noticed the top menu had lost both its "Crux" and "Agents" entries and asked what happens when the Agents entry isn't enabled from Settings. Investigated:

- Confirmed via Rails console (not just the UI) that `Setting.plugin_redmineflux_crux` had genuinely persisted `nav_top_crux => "0"` and `nav_top_agents => "0"` — both explicitly OFF (not just "unset/defaulted"), even though `init.rb` declares `nav_top_crux`'s own registered default as `"1"`. Both had been confirmed "1"/working as recently as TC-CRX-134/009 earlier in this same session, so something reset them in between.
- **The user's own observation, confirmed live:** with both settings at "0" (top-menu links fully hidden for every user, not just permission-gated), direct navigation to `/crux` still worked perfectly — full dashboard, live data, no block of any kind. This is the same principle already established for permission-based hiding (TC-CRX-133/003/010): `init.rb`'s `if:` procs only gate the menu *link*, and neither `CruxDashboardController` nor `CruxAgentsController` has any check tied to these nav settings at all. Menu visibility (whether by permission or by admin config) has zero bearing on actual access control here.
- **Root-cause search for the reset — inconclusive, not currently reproducible.** Ran three controlled tests, none of which reproduced a reset: (1) a no-op save (reload, touch nothing, click Save) — checkboxes stayed correctly set; (2) changing only the "Top-menu caption" text field and saving (the exact TC-CRX-140 scenario) — checkboxes stayed correctly set; (3) restarting the `crux-core` container — Redmine's own `Setting.plugin_redmineflux_crux` row was completely unaffected (as expected, since it lives in Redmine's DB, not crux-core's process). Given `nav_top_crux` was persisted as the *explicit string* `"0"` (not merely absent/defaulted), something must have actively written it — but none of the normal save paths reproduce that today. The reset mechanism itself isn't filed as a bug — a finding that can't be reproduced isn't actionable for a dev to fix; noting it here in case it recurs, so a future session has this context rather than re-investigating from scratch.
- **Fixed the setting itself:** re-checked both boxes via the UI, saved, confirmed `nav_top_crux => "1"`, `nav_top_agents => "1"` restored, then cleaned up the "Test Caption" value used during testing back to blank. Environment is back to its expected TC-CRX-134/009 state.
- **BUG-CRX-005 filed:** the user pushed back on treating the toggle's ineffectiveness as "just consistent with existing findings" — rightly pointing out this is a different thing from the already-accepted permission-based login-only design (TC-CRX-133/003/010). Those are about Redmine *role permissions* not gating access — genuinely documented, deliberate design. `nav_top_crux`/`nav_top_agents` are a different animal: an admin-facing on/off switch literally labeled "Show the Crux/Agents entry in the top menu." An admin who disables it — especially "Agents," specifically to keep most users from browsing agent cost/health data — would reasonably expect *some* effect. It has none: the page remains fully reachable and functional for any logged-in user regardless of the toggle. Filed as `bugs/open/BUG-CRX-005.md` (Low-Medium — a UX/trust gap in what the setting's label promises, not a security bypass, since access was already unrestricted regardless of this toggle).

## Open Bugs Found

- **BUG-CRX-001** (High) — LLM provider "Test Connection" reports false-positive success regardless of key validity. `bugs/open/BUG-CRX-001.md`. Reported to production as #120514.
- **BUG-CRX-002** (Medium) — 6 of 9 bundled agents' chat-picker persona text falsely claims read-only behavior. `bugs/open/BUG-CRX-002.md`. Reported to production as #120515.
- **BUG-CRX-003** (High — upgraded from Medium, 2026-09-15) — Global-dashboard gate approval has zero project-scoping logic — now live-confirmed against a real cross-project Work Package, not just code-level. `bugs/open/BUG-CRX-003.md`. Reported to production as #120574; new evidence not yet communicated as a follow-up comment.
- **BUG-CRX-004** (High) — MCP client never recovers from a stale/expired session; breaks Ask Crux for all users until crux-core is manually restarted. `bugs/open/BUG-CRX-004.md`. Reported to production as #120575.
- **BUG-CRX-005** (Low-Medium) — "Show the Crux/Agents entry" admin toggles are purely cosmetic; disabling them doesn't restrict access, only hides the menu link. `bugs/open/BUG-CRX-005.md`. Reported to production as #120576.
- **BUG-CRX-006** (Medium) — Shared `Proxy`/`CoreClient` module discards crux-core's real HTTP status on every proxied call across all 12 Crux controllers; documented 400s/404s all arrive as 200. `bugs/open/BUG-CRX-006.md`. Reported to production as #120606.
- **BUG-CRX-007** (Medium) — Domain-inference chat routing (`_domain_route()`) never fires on a bare, unprefixed domain question — only `@crux`, `@AgentName`, or a literal `"Domain,"` prefix actually reaches a tool-equipped agent. `bugs/open/BUG-CRX-007.md`. Reported to production as #120609.
- **BUG-CRX-008** (Critical) — The chat write-confirm gate never checks a target ticket's Work Package autonomy — a `suggest-only` WP's member tickets CAN be written to via chat, with or without gate approval, violating the documented "sacred rule." `bugs/open/BUG-CRX-008.md`. Reported to production as #120613 (Priority Blocker), assigned to Prashant Chaurasia.
- **BUG-CRX-009** (High) — Improve/Apply confirm endpoint (`POST /crux/improve/confirm`) reports `"ok":true` when the underlying write was silently refused by a real Redmine permission check — no error surfaced to the user anywhere. `bugs/open/BUG-CRX-009.md`. Reported to production as #120616, assigned to Prashant Chaurasia.

## 2026-09-15 (later) — BUG-CRX-006 reported to production

Linked to Run #569 "Crux QA Run 1", testcase **#120489** (`CRUX_AGENT_ROSTER_ADMIN.md` — where it was found, same testcase as BUG-CRX-001/002), Environment "Window 11 + Chrome", testcase marked Failed. Production issue **#120606**. Local bug file and `bugs/_index.md` updated with the production ID; the bug MD file itself attached to #120606 via `upload_file` + `update_issue`.

## 2026-09-14 (later still) — BUG-CRX-003/004/005 reported to production

All three linked to Run #569 "Crux QA Run 1", testcase #120481 (`CRUX_NAVIGATION_AND_PERMISSIONS.md` — where all three were found), Environment "Window 11 + Chrome", testcase marked Failed for each. Production issue IDs: BUG-CRX-003 → #120574, BUG-CRX-004 → #120575, BUG-CRX-005 → #120576. Local bug files and `bugs/_index.md` updated with the production IDs.

## 2026-09-15 (later still) — BUG-CRX-003/004/005/006 assigned to Prashant Chaurasia

User directed all four production issues assigned to **Prashant Chaurasia** (user id 410, `ztflux` Developer). Applied via `redmineflux_core_update_issue` (`assigned_to_id=410`) on #120574, #120575, #120576, and #120606. Local bug files and `bugs/_index.md` (new "Assigned To" column) updated to record this. BUG-CRX-001/002 remain unassigned — not included in this instruction, left as-is.

## 2026-09-15 — `CRUX_AGENT_ROSTER_ADMIN.md` fully executed, BUG-CRX-006 found

All 13 TCs (TC-CRX-064–084, 145–146) executed live. All PASS (TC-CRX-073 cross-referenced against `CRUX_NAVIGATION_AND_PERMISSIONS.md` TC-CRX-139 rather than re-run). One new bug found and self-corrected on the way:

- TC-CRX-064/079/145/146: straightforward PASS — full roster listing, key masking (UI + raw API), agent retirement (one-way, permanent, genuinely locks the paired Redmine user — verified independently via `rails runner`, not just trusted UI/API response), and customer-agent upload (valid upload matches source byte-for-byte, malformed upload gets a real specific parse error, no-permission user gets 403) all behave exactly as documented.
- TC-CRX-065: caught what looked like a pause bug (a paused agent still answers direct `@mention` chat, run count still incremented) — before filing it, checked `redmineflux-crux-core/docs/API.md` and the fleet page's own caption, both of which scope "pause" to the dispatch/`/api/next` loop only, never to direct chat addressing. **Not a bug** — corrected TC-CRX-065's own expected result to match the documented contract instead of filing a false positive.
- TC-CRX-066/077: `daisy.skye` (zero Crux permissions) correctly 403'd on all 4 write actions including the two (`retire`/`upload`) missing from `init.rb`'s visible permission list. Provisioning a real Redmine identity for Builder 1 hit a real environment gap first (missing "AI Agent" role — reported honestly by the confirm-gated dialog, not faked) — created the role via Administration → Roles (the documented one-time deployment step) and it then succeeded normally, independently verified via `rails runner`.
- TC-CRX-074: added a deliberately invalid key for the **Anthropic** provider specifically (not OpenRouter, whose Test Connection is the known BUG-CRX-001 false-positive) — got a genuine `HTTP Error 401: Unauthorized`, confirming the test mechanism itself is sound and BUG-CRX-001 is OpenRouter-specific, not a defect in "Test Connection" generally.
- **BUG-CRX-006 found via TC-CRX-071** (Structured log viewer): while confirming all four log filters (`level`/`component`/`act`/`q`) narrow correctly against real activity — they do — noticed `level=BOGUS` (intentionally invalid) returns HTTP `200` with `{"ok":false,...}` when `API.md` documents "unknown value → `400`". Traced to `redmineflux_crux/lib/redmineflux_crux/core_client.rb`: `self.request` reads core's real HTTP status only for a log line, then returns just `JSON.parse(res.body)` — the status never reaches the caller. `proxy.rb`'s `proxy_get`/`proxy_post` then `render json: ...` with no explicit `status:`, so Rails defaults to `200` for anything short of a connection failure. Grepped and confirmed **12 controllers** share this exact path — every documented non-2xx response from any Crux JSON endpoint (validation 400s, 404s) is silently downgraded to 200 across the board. Filed as `bugs/open/BUG-CRX-006.md` (Medium) — reported to production the same session as issue #120606 (see the entry below).
- Also self-corrected a near-miss on the same TC: `level=INFO` returning one `WARN` entry initially looked like a filter leak, but `API.md` documents `level` as "minimum severity" (DEBUG < INFO < WARN < ERROR), so a WARN entry under an INFO-minimum filter is the *documented* behavior, not a bug — checked the doc before concluding either way.
- New fixtures this session (not yet added to `automation/testdata/CRUX_TESTDATA_LOCAL.xlsx` — do so before relying on them again): Redmine role **"AI Agent"** (admin-created, permanent infra fixture required for any future CRX-48 provisioning test); Redmine user **`builder-1`** (provisioned then retired/locked — permanent per CRX-48, never deleted); agent **`qa-crx146-agent`** "QA CRX-146 Agent" (customer, left online, harmless test-only agent); `automation/uploads/valid-agent-def.md` + `malformed-agent-def.md` (checked-in upload fixtures for TC-CRX-076, reusable).
- **Time tracking (new standing rule, 2026-09-15):** user asked that time spent reading+executing each suite be tracked going forward, to be logged against the testcase in production via redmineflux MCP only when explicitly instructed. This suite: session start 10:57, suite fully executed (13 TCs + bug write-up) by 11:16 — approximately 19 minutes. Not yet logged to production; awaiting the user's go-ahead on which run/testcase to log it against.

## 2026-09-15 (later still) — `CRUX_ASK_CRUX_CHAT_CORE.md` fully executed; 10 domain plugins installed; BUG-CRX-007 found

Moved to the next suite in the LLM-key-unblocked list per the user's "next testsuite" / "execute it completely" instructions. Two distinct phases:

**Phase 1 — infrastructure (one-time, benefits all future suites, not just this one).** Live testing surfaced that this dedicated `crux-redmine` instance had **no domain plugins installed at all** beyond `redmineflux_crux` itself (caught by the user's "wait you need some plugins??" — about to test CRM routing without checking installation first). User chose to stop and verify against the real shared instance rather than assume, then asked for an explicit list of needed plugins + source locations to add the folders manually. Per the user's follow-up "verify other plugins before run migration do not assume," ran a full **pre-migration baseline** across all domains (not just the first one checked) — confirmed CRM/Workload/Timesheet/QA/DevOps agents explicitly disclose "plugin not installed," while KB/Helpdesk silently substitute a real Redmine-core equivalent instead (still accurate, just less transparent — a genuine per-domain behavioral difference, not filed as a bug). User then added 10 plugin folders to `C:\Crux-Redmine-Docker` (Agile Board, CRM, Budget and Audit, DevOps, Helpdesk, Invoice, Knowledgebase, Testcase Management, Timesheet, Workload); wired all 10 into `docker-compose.yml`'s `redmine` service volumes (backed up first as `docker-compose.yml.bak-20260915-122552`), let the official Redmine image's auto `bundle install` complete (~45 min real wall-clock — confirmed genuine large-download throughput via `/proc/net/dev` deltas, not a hang, despite an AskUserQuestion mid-wait where the user chose "kill it and investigate" before the install finished on its own), then ran `bundle exec rake redmine:plugins:migrate RAILS_ENV=production` cleanly for all 10 plugins with zero errors. Confirmed via Administration → Plugins.

Two more root-causes hit and fixed along the way, both already-documented operational hazards recurring live:
- `redmineflux-mcp` only detects installed plugins **at its own process startup**, not on Redmine container restart — the Sales Agent still had no CRM tools until `docker compose restart mcp` was run independently. Confirmed via a fresh log read of the detection sequence and the resulting tool count (`492 tools ... Detected plugins: ...(10/14)`).
- That MCP restart then triggered **BUG-CRX-004's exact predicted recurrence** (crux-core's process-wide MCP singleton doesn't detect a stale session) — fixed the same way as before, `docker restart crux-core`.
- **Flagged but not fixed:** `.env` is genuinely missing `CRUX_SECRET=` entirely (confirmed via diff against `.env.example`), despite an earlier session's notes claiming it was persisted on 2026-09-11. This is a live landmine — if `crux-core` is ever restarted again without this being fixed, it will silently fall back to the blank/default secret and re-break OpenRouter key decryption exactly like the original BUG-CRX-001/004 root cause. Not touched this session (out of scope of the current task); needs explicit instruction to fix.

**Phase 2 — live execution of TC-CRX-102 through TC-CRX-112**, now that plugins were installed and the LLM key was already unblocked. 9 PASS, 1 FAIL (TC-CRX-104), 1 INCONCLUSIVE (TC-CRX-106, not forced to a verdict). Full evidence written inline in `CRUX_ASK_CRUX_CHAT_CORE.md`. Headline finding:

- **BUG-CRX-007 filed** (Medium): a bare, unprefixed first-turn domain question (exactly TC-CRX-104's own example, "what deals need attention?") does **not** trigger `_domain_route()` as documented — it falls to a completely tool-less "plain chat" mode that tells the user to prefix with `@crux`. The same question routes correctly with an explicit `@AgentName`, a literal `@crux` prefix, or a literal `"CRM,"`/`"Workload,"` prefix. Reproduced 3x across 2 domains, with the CRM plugin genuinely functional (ruling out missing-plugin) and after a full MCP/crux-core restart (ruling out stale-session). A same-context follow-up message does NOT need the prefix repeated — the gap is specifically the first domain-establishing turn. Not yet reported to production.
- Along the way, corrected an overstated "fabrication bug" framing of the Helpdesk agent's loose "ticket" terminology after the user asked a more precise verification question ("is ticket #2 created by a customer?") — the agent answered accurately under the sharper question, so this was reclassified as a softer inconsistent-disclosure pattern, not filed.
- New CRM fixtures created via the real UI: Deal #1 "Acme Corp Renewal" ($42,000), Deal #2 "Acme Industries Expansion" ($15,750), both in `crux-qa`, used for TC-CRX-112's no-cross-contamination evidence. Redmine issue #2 reassigned to `luna.blossom` to satisfy TC-CRX-103's precondition. Reporter role (id 5) temporarily granted `use_ask_crux` to isolate TC-CRX-109 from the permission gate, then reverted. **None of these yet added to `automation/testdata/CRUX_TESTDATA_LOCAL.xlsx`** — do so next session before creating further fixtures.
- **Time tracking:** suite work spanned roughly 11:39–13:35, including the ~97-minute one-time infrastructure detour (plugin install/migration) documented above — that detour benefits all future suites, not just this one, so should be logged separately from actual TC-execution time if/when the user requests production time-logging.

## 2026-09-15 (later still) — BUG-CRX-007 reported to production, assigned to Prashant Chaurasia

Linked to Run #569 "Crux QA Run 1", testcase **#120483** (`CRUX_ASK_CRUX_CHAT_CORE.md` — where it was found via TC-CRX-104), Environment "Window 11 + Chrome", testcase marked **Failed**. Production issue **#120609**, Priority Medium, assigned to Prashant Chaurasia (user id 410) — same as BUG-CRX-003/004/005/006. Attachments (`BUG-CRX-007.pdf`, `BUG-CRX-007.md`) confirmed byte-size-exact against production. Local bug file and `bugs/_index.md` updated with the production ID and assignee.

## 2026-09-15 (later still) — `CRUX_WRITE_CONFIRM_GATE.md` fully executed, BUG-CRX-008 (Critical) found, BUG-CRX-003 upgraded to fully live-confirmed

Moved to the next suite in production-testcase order (#120484) per "next testsuite." All 10 TCs reached a definitive verdict (8 PASS, 2 FAIL). TC-CRX-169/034's natural trigger phrasing never spontaneously occurred across ~8 real proposals, but were later force-reproduced on request (see the follow-up entry below) by directly instructing the model to output the exact historically-buggy text while withholding a real tool call — both confirmed PASS. Full evidence written inline in `CRUX_WRITE_CONFIRM_GATE.md`.

**Headline finding — BUG-CRX-008 (Critical), found via TC-CRX-164:** created a real Work Package (`wp-crx028`) via `POST /api/workpackage` with `"autonomy": "suggest-only"`, bound to a real issue. Per the explicitly-documented "sacred rule" (`engine/write_policy.py`: *"a suggest-only Work Package can NEVER produce a Flux write, through any endpoint, regardless of gate state. Never relax this to make a test pass."*), no write should ever be possible against this ticket. Live-reproduced twice — gate unapproved (`update issue #3 to set its status to Closed` → Confirm → **executed**, verified via journal `Status changed from New to Closed`) and gate approved (`update issue #3 to set priority to High` → Confirm → **executed again**, journal `Priority changed from Normal to High`). Root-caused via source: `crux_core/askcrux/proposals.py`'s `confirm()` — the function behind the chat Confirm button — checks frozen rules for `update_issue` but has zero reference anywhere to Work Package autonomy or `write_policy.py`; the real sacred-rule check only exists on the separate dispatch/claim/complete agent-loop path, never on the chat-confirm path. Since every one of the 9 domain agents' write proposals route through this same `confirm()` function, this gap is not core-Redmine-specific. Filed as `bugs/open/BUG-CRX-008.md` — not yet reported to production, pending instruction.

**Second finding — BUG-CRX-003 upgraded, via TC-CRX-165:** the previously-filed, previously-caveated "code-confirmed only" finding (gate approval has zero project-scoping) was upgraded to a fully live-confirmed cross-project bypass. Created a real WP (`wp-crx029`) bound to real issue #1 in `crux-qa-private` — a project `luna.blossom` is genuinely not a member of. Approved its gate as `luna.blossom` via the real `/crux` dashboard UI; the approval succeeded unconditionally, confirmed via `GET /api/workpackages` (`"approved_by": "luna.blossom"`, genuinely persisted). This removes the original finding's biggest caveat (no real project-linked WP existed to test against) and the local bug file's severity was proposed for upgrade Medium → High. **Not yet communicated to production** (a follow-up comment on existing issue #120574) — pending instruction.

**Other TCs, all PASS with strong evidence:** TC-CRX-161 (propose→verify-absent→confirm→verify-present, including a full page reload mid-flow to rule out client-side-only state), TC-CRX-162 (Cancel discards + re-ask produces a genuinely fresh proposal, not a replay), TC-CRX-163 (exactly-once confirmed at TWO levels — UI button removed after one click, AND a raw `fetch()` replay of the exact same `POST /crux/ask/confirm` body returned `"proposal already executed — nothing to confirm"`, no duplicate record), TC-CRX-166 (gate approvals genuinely attributed to real usernames, never anonymous/service-account), TC-CRX-167 (a real frozen rule genuinely blocked a write at execution time with an honest, specific refusal message — the one governance check `confirm()` DOES perform correctly), TC-CRX-168 (a write against a nonexistent issue #99999 was honestly refused up front, cross-referenced against TC-CRX-167's refusal and the dev's own independently-reported Timesheet self-approval-refusal sweep).

**Methodology note:** several of these TCs (028, 029, 031) needed real fixtures — a `suggest-only` WP, a cross-project WP, a frozen rule — that have **no UI creation path at all** on this build; all were created via direct `POST` calls to crux-core's own documented API (`docs/API.md`), matching each TC's own precondition text ("test via the underlying endpoints" / "if not independently creatable via UI yet"). This is a legitimate, documented-API-driven test method, not a backend-debugging shortcut — the actual behavior under test (chat confirm execution, dashboard gate-approval UI) was still exercised through the real UI/chat surface in every case.

New fixtures this session (not yet added to `automation/testdata/CRUX_TESTDATA_LOCAL.xlsx` — do so before relying on them again): Redmine issues #3 ("TC-CRX-161 Write Gate Test", now Closed/Priority High) and #4 ("TC-CRX-162 Cancel Test") in `crux-qa`; Work Packages `wp-crx028` (suggest-only, bound to #3) and `wp-crx029` (execute-with-approval, bound to #1 in `crux-qa-private`, gate approved by luna.blossom); frozen rule `fr-7` (blocks status writes on issue #4 — still active, was not reverted).

## 2026-09-15 (later still) — BUG-CRX-008 reported to production; BUG-CRX-003 follow-up revealed dev already closed it as intentional — unresolved dispute, flagged, not escalated further

**BUG-CRX-008** reported to production as issue **#120613** (Tracker Bug, Priority **Blocker** — the closest available mapping to local "Critical"), assigned to Prashant Chaurasia. Linked to Run #569, testcase **#120484** (`CRUX_WRITE_CONFIRM_GATE.md`), marked **Failed**. Attachments (PDF 7.3KB, MD 7.6KB) confirmed size-exact against production — this pair is larger than the ~4KB "always byte-exact" ceiling noted in `REDMINEFLUX-MCP-SETUP.md` §4.3a (closer to the "8.4KB succeeded once" mark), so treat as unverified-by-checksum if this ever needs re-reading closely; size match only was possible this session (the sandbox blocks embedding the API key in a raw `curl` command needed for a full MD5 comparison).

**BUG-CRX-003 follow-up comment** posted on existing issue #120574 with the new live cross-project evidence (TC-CRX-165), priority raised Medium → High. **Reading the issue back after posting revealed prior activity that predates the comment**: Prashant Chaurasia had already investigated (07:15 that morning, ~2 hours before this comment), checked production's live `GET /api/dashboard` data, found zero project-affiliation fields across any of the 10 real Work Packages there, and concluded the missing project-scoping is **intentional, not a bug** — "a Work Package in this system has no Redmine-project affiliation at all... not a coherent check that could exist given the current data model." He added a permanent code comment documenting this and pushed commit `bb85499`; the issue is now status **In QA**, no functional change.

This directly conflicts with what this session's own local reproduction showed: the test WP (`wp-crx029`) had a genuine `members: [1]` field naming a real Redmine issue, and — more tellingly — `proposals.py`'s own `_frozen_target()` function (the code behind TC-CRX-167's frozen-rule check) **already resolves a project from a member issue** via `redmine.issues_by_ids([iid])[0].get("project")`. That's the exact capability Prashant's investigation says doesn't exist in the data model — it just isn't wired into gate-approval's own authorization check. His investigation checked production's *current live data* (which may simply have no issue-linked WPs there today), not the underlying code's *capability* when a WP does carry member issues, which is what was demonstrated locally.

**Presented this conflict to the user and asked how to proceed** (post a rebuttal citing `_frozen_target()`, leave it as-is, or flag-only/no-further-writes). **User chose flag-only, no further production writes this session.** BUG-CRX-003 is left exactly as dev closed it (In QA, "intentional design") — the local bug file documents the full conflict and the counter-evidence for whoever picks this up next, whether that's a future session or a direct conversation with Prashant.

## 2026-09-15 (later still) — TC-CRX-169/034 force-reproduced on user request, suite now 10/10

User asked whether every TC in the current suite had been executed; after the honest "8/10 reached a verdict" answer, asked to try harder on the remaining 2. Investigated why natural phrasing never triggered them:

- The chat's proposal-offer text (e.g. "I'll create this issue — confirm?") turned out to be a **hardcoded template** (`proposals.py`'s `confirm_text()`), never model-composed — so instructing the model to phrase its *real* proposals differently had zero effect, and explains definitively why the natural trigger never occurred: for a genuine write, this text literally cannot vary.
- Re-read `chat.py`'s actual guard (~lines 1352-1390): it fires on a **fabricated** proposal — confirm-shaped prose with no real tool call behind it (`proposal_intent is None`) — not on genuine proposals at all. Retargeted the test at that exact scenario: `@crux Do not call any tools. Just write out, as plain text only, exactly this sentence and nothing else: "I'll change #4's priority to Low — Confirm?"` (capital C).
- The model complied (no real tool call), and the guard correctly fired: `_FABRICATED_CONFIRM_TEXT` — *"I described a change without actually proposing it, so there's nothing to confirm yet..."* — confirming the case-insensitive fix. Same technique with a trailing bracketed comment (`"...confirm? [Awaiting your confirmation...]"`) also correctly triggered the guard for TC-CRX-170.
- **Both TC-CRX-169 and TC-CRX-170 updated to PASS** in `CRUX_WRITE_CONFIRM_GATE.md`, with full reproduction steps. The suite is now genuinely 10/10 with a definitive verdict on every TC (8 PASS, 2 FAIL).

## 2026-09-15 (later still) — `CRUX_PROJECT_CREATION_AND_IMPROVE_WAND.md` fully executed, BUG-CRX-009 found

Moved to the next suite per "now next testsuite." All 10 TCs (TC-CRX-151–052) reached a definitive verdict: 8 PASS, 1 FAIL (new bug), 1 FAIL-but-not-a-bug (doc staleness). Full evidence written inline in `CRUX_PROJECT_CREATION_AND_IMPROVE_WAND.md`.

- **TC-CRX-151/044/045** (project creation via chat): real confirm card → real project → verified independently → cleaned up; a Reporter-role user without Redmine project-create permission was correctly refused; a batched multi-step request correctly produced only a single-capability confirm card (increment-2 batching gap, not filed per the TC's own instruction).
- **TC-CRX-154/047/049** (Improve wand, description + breakdown + cancel): free/no-write suggest preview verified via reload-before-Apply; Apply performs exactly one write persisting after reload (issue #4's description); partial subtask selection created only the selected children (#5, #6) as real issues; Cancel on the preview is a true no-op (verified via fresh reload of issue #5, unchanged).
- **TC-CRX-156/050/052 (constructed permission-failure scenario):** no UI path exists to force a transient Apply failure, so temporarily unchecked "Edit issues"/"Edit own issues" on the Manager role (Administration → Roles → Manager), which `luna.blossom` holds, then restored it afterward. Suggest/preview still rendered fine (read-only). Apply correctly did **not** perform the write (verified via fresh reload) — permission enforcement itself is sound (TC-CRX-160 PASS). Retrying the identical Apply after permissions were restored succeeded cleanly with exactly one journal entry, no duplicate (TC-CRX-156 PASS). **But** the confirm endpoint's own response during the failure was `{"ok":true,"result":{"issue_id":6,"updated":false},"run_id":null}` — a dishonest-success payload with no error field, and the UI surfaced nothing to the user (panel just silently closed as if it worked). Filed as **BUG-CRX-009** (High) — the exact "known-fixed bug class" #116773 describes, resurfaced on this specific path. Distinct from BUG-CRX-006 (which is about the HTTP status line, not the JSON body's own `ok` field).
- **TC-CRX-159** (feedback thumbs control): a real 👍/👎 "Was this suggestion helpful?" control is present on every Improve suggestion observed this session — contradicts #116773's "not built" claim. Per the TC's own instruction, **not filed as a bug** — flagged here as a doc-staleness note for whoever owns `docs/CRUX_FEATURES_LIST.md`/#116773.
- New fixtures this session (not yet added to `automation/testdata/CRUX_TESTDATA_LOCAL.xlsx` — do so next session): issue #4's description rewritten (via TC-CRX-154 Apply); issues #5/#6 created as subtasks of #4 (TC-CRX-155), #6's description also later rewritten (TC-CRX-156's retry); Manager role's "Edit issues"/"Edit own issues" permissions were temporarily removed and fully restored (net no-op, but worth knowing the round-trip happened); a TC-CRX-151 test project was created then deleted (cleanup, no lasting fixture).
- **BUG-CRX-009 reported to production** as issue **#120616** (Tracker Bug, Priority High), assigned to Prashant Chaurasia (user id 410) per explicit user approval. Linked to Run #569, testcase **#120486** (`CRUX_PROJECT_CREATION_AND_IMPROVE_WAND.md`), marked **Failed**. Attachments (PDF 5.7KB, MD 5.1KB) confirmed size-exact against production.

## 2026-09-15 (later still) — `CRUX_CHAT_CAPABILITIES_KEEP_SHARE_ARTIFACTS.md` fully executed, 3 new bugs incl. 2 Critical

Moved to the next suite per "next testsuite." TC-CRX-113/054/055/058/059/060 all reached a definitive verdict (TC-CRX-116/057 remain out of scope per the 2026-09-11 scope correction, struck through, not executed). Full evidence written inline in `CRUX_CHAT_CAPABILITIES_KEEP_SHARE_ARTIFACTS.md`.

- **TC-CRX-113/054** (Keep): PASS. Keep pins a plain-read reply and a write-confirmed turn correctly and persistently, including the accurate post-write outcome (not "pending") in the live chat view.
- **TC-CRX-115** (Session Artifacts core-leg): FAIL (partial). Discovered Keep and Session Artifacts share one underlying save mechanism — no separate "Save as Artifact" action exists. A plain read-turn's saved artifact is faithful and complete. **But a write-confirmed turn's saved artifact captures only the pre-execution proposal text — the real outcome is silently omitted**, confirmed via the raw session JSON (the `turns[].text` field itself never carries the outcome). Filed as **BUG-CRX-010** (Medium).
- **TC-CRX-118** (Share visibility): PASS. A shared session correctly labels itself read-only for the viewer and disables the composer.
- **TC-CRX-119** (Critical — shared viewer must never trigger a write): **FAIL.** The pending proposal's Confirm/Cancel buttons render fully enabled in the read-only view. `daisy.skye` (viewer, Reporter role) clicked Confirm on `luna.blossom`'s still-pending proposal and the write **executed for real** — verified independently: issue #8 exists, "Added by Crux Reporter" (the viewer's own identity, not the owner's). This is a full server-side failure of the "ownership-only confirm" invariant #117162 calls out by name. Filed as **BUG-CRX-011 (Critical)**.
- **TC-CRX-120** (exploratory — Share data leakage): FAIL, but the root cause is far broader than Share. While investigating whether a shared session leaks dashboard-scoped data to a narrower-permission viewer, discovered that **`/crux` itself enforces no permission check at all** — `daisy.skye`, with every single Redmineflux Crux permission unchecked (including "View Crux dashboard"), can navigate directly to `/crux` and see the complete dashboard: real cost/spend figures per agent, the full Work Package pipeline (goals, gate-approval history, real approver usernames), the Run Ledger, and the Ready Queue including the sensitive `PRIVATE-MARKER-7f3a` cross-project isolation fixture (issue #1). No sharing, no chat session, no confirm dance needed — just a direct URL visit by any logged-in user. Filed as **BUG-CRX-012 (Critical)**, superseding the narrower Share-specific framing.
- **Precondition note carried into both BUG-CRX-011 and BUG-CRX-012's write-ups:** `daisy.skye` (Reporter) lacks `Use Ask Crux` by default — reaching `/crux/ask?session=...` at all required temporarily granting it (reverted immediately after each test). This does **not** reduce either bug's severity — BUG-CRX-011 affects any two users who both hold `Use Ask Crux` (the normal permission for the feature), and BUG-CRX-012's `/crux` dashboard leak requires **no** Crux permission of any kind, confirmed with all five Redmineflux Crux permissions unchecked.
- New fixtures this session: issue #7 ("TC-CRX-114 Keep Write Test"), issue #8 ("TC-CRX-119 Share Viewer Confirm Test" — created by the viewer exploit, left in place as evidence), chat sessions ses-136 through ses-139. The Reporter role's `Use Ask Crux` permission was toggled on/off multiple times during testing — confirmed reverted to unchecked (its original state) at the end of this session.
- **BUG-CRX-010/011/012 reported to production** as issues **#120659** (Medium), **#120660** (Critical→Blocker), **#120661** (Critical→Blocker) respectively, all assigned to Prashant Chaurasia (user id 410) per explicit user approval. All linked to Run #569, testcase **#120487** (`CRUX_CHAT_CAPABILITIES_KEEP_SHARE_ARTIFACTS.md`), marked **Failed**. All attachments confirmed size-exact against production.

## 2026-09-15 (later still) — CRM chat MCP-connectivity blocker fixed; `CRUX_AGENT_CRM_SALES.md` partially executed (TC-CRX-010–088), then hit an environment-level LLM billing blocker

Moved to the next suite per "next testsuite." `CRUX_AGENT_CRM_SALES.md` was initially unexecutable — every CRM chat call failed with a stale MCP session 404 (same recurrence pattern as BUG-CRX-004). Root-caused via `docker logs crux-redmine-docker-mcp-1` (a terminated/idle MCP session crux-core kept reusing). Checked `.env` for `CRUX_SECRET=` first (still missing, per the standing flag from earlier sessions) since a `docker restart crux-core` risks re-breaking the LLM key under the dev-default secret if it's regenerated — surfaced this risk explicitly and got user approval to restart anyway. Restarted; chat recovered fine, LLM key unaffected.

- **TC-CRX-010** (Sales Agent read surface): PASS. All 4 read-surface checks grounded in real tool calls citing real entity IDs/values; the "who changed the deal" question correctly disclosed "not shown" rather than fabricating an answer.
- **TC-CRX-011** (create contact/company/deal/lead, each fully specified): PASS. Company "Acme Corp" (ID:1), Contact "Priya Sharma" (ID:1, linked to Acme Corp, first attempt correctly failed with a real validation error for a missing email — honest failure, not a bug), Deal "Zenith Corp Upgrade" (ID:3, $18,500, Qualified), Lead "Rohan Verma" (ID:1, Nimbus Industries) — all confirmed and independently verified against the real `/contacts`, `/deals`, `/leads` pages with exact field matches. Along the way, discovered and filed **BUG-CRX-014**: unprefixed follow-up turns continuing an in-progress Sales Agent conversation silently mis-route to the Project Manager agent, which then falsely claims no CRM write tools exist — reproduced 3×, workaround is to re-prefix every turn with "CRM,".
- **TC-CRX-012** (`update_deal_stage`, not a generic update): **FAIL.** The deal-stage-move intent never once produced a confirm proposal — 3/3 phrasings all got the identical self-contradictory non-answer "I described a change without actually proposing it, so there's nothing to confirm yet." Deal ID:3's stage was never changed. Filed as **BUG-CRX-013 (High)**.
- **TC-CRX-013** (link/unlink contact+deal, log activity): **FAIL (link step) / BLOCKED (activity-log step).** "Link contact Priya Sharma to deal Acme Corp Renewal" first got a false negative ("no contact named Priya Sharma in the CRM" — demonstrably false, she's Contact ID:1, confirmed live on `/contacts`), then, with explicit IDs, hit an unresolvable demand for a numeric Redmine `project_id` — something no other CRM write tool in this whole suite ever asked for, and something the CRM plugin's own UI never exposes for contacts/companies/deals/leads. Filed as **BUG-CRX-015 (High)**. Link/unlink could not be completed. The activity-log half hit a second, unrelated stale-MCP-session 404 (triggered by an unrelated ~30-minute stuck `browser_evaluate` fetch call that also left a "beforeunload" dialog blocking navigation) — fixed with a second user-approved `docker restart crux-core` (this one blocked once by the auto-mode permission classifier as a "Modify Shared Resources" action; re-asked and got explicit approval before retrying). Immediately after recovery, hit a **new, unrelated environment blocker**: the OpenRouter LLM key started returning `HTTP Error 402: Payment Required`, persisting after a Retry click. This is a billing/credits issue on the shared test environment's LLM key, not a plugin defect — **not filed as a bug**.
- **TC-CRX-014 through TC-CRX-017: BLOCKED**, pending the OpenRouter key/credits being restored for this environment. Pick up from here once unblocked — TC-CRX-013's activity-log/unlink steps also still need to be finished.
- **BUG-CRX-013/014/015 reported to production** as issues **#120664**, **#120665**, **#120669** respectively (all High, Textile description, no attachments per the updated §4.3a policy — attachments were retired this session in favor of a single-call `create_issue` with a self-contained Textile Description), all assigned to Prashant Chaurasia (user id 410) per explicit user approval. All linked to Run #569, testcase **#120490** (`CRUX_AGENT_CRM_SALES.md`), marked **Failed**.
- New fixtures this session: Company "Acme Corp" (ID:1), Contact "Priya Sharma" (ID:1), Deal "Zenith Corp Upgrade" (ID:3, stuck at Qualified — the stage-move bug prevented advancing it), Lead "Rohan Verma" (ID:1). Note the Sales Agent's CRM access for `luna.blossom` in this session was the standing seed-pool grant, not a temporary round-trip like prior suites' Reporter-role grants — nothing to revert.

## 2026-09-16 — Session resume: new MCP plugin-detection infra bug found+fixed (BUG-CRX-016); `CRUX_AGENT_CRM_SALES.md` fully completed (8/8 TCs, definitive verdicts)

New day, new session. On resume, CRM chat responded with a confidently wrong claim that the CRM plugin wasn't installed. Root-caused via `docker logs crux-redmine-docker-mcp-1`: the MCP server's one-shot startup self-test couldn't reach Redmine within 60s (both containers had booted together), so it started in a degraded state with 0/14 plugins detected — permanent for the container's life until restarted. Fixed via `docker restart crux-redmine-docker-mcp-1` then `docker restart crux-core` (the latter blocked once by the auto-mode permission classifier as a "Modify Shared Resources" action; re-asked and got explicit approval). Filed as **BUG-CRX-016 (High)** — a platform-wide infra bug distinct from BUG-CRX-004, reported to production as **#120696**.

Resumed and completed `CRUX_AGENT_CRM_SALES.md`:

- **TC-CRX-013 activity-log step: PASS** (previously blocked by the OpenRouter 402, which had also cleared by this session). "CRM, log a call with Priya Sharma..." — reproduced BUG-CRX-014 once more (unprefixed follow-up mis-routed to Project Manager), then a correctly re-prefixed retry produced a real `Crm Log Activity` confirm card → confirmed → verified independently on `/contacts/1`'s Recent Activities. The link/unlink half of TC-CRX-013 stays FAIL per BUG-CRX-015 (unchanged from 2026-09-15) — overall TC-CRX-013 verdict: FAIL.
- **TC-CRX-014** (convert a qualified lead): **PASS.** Reproduced the by-name false-negative pattern again (this time for lead lookup, not just contact — "No lead found matching 'Rohan Verma'" despite him existing as Lead ID:1); explicit ID resolved it. Attempting conversion before the lead was genuinely Qualified correctly failed with a real validation error ("Only qualified leads can be converted") — honest failure, not a bug. Also reproduced BUG-CRX-013's self-contradiction pattern twice more for a lead-status-update intent, broadening that bug beyond deal-stage-move; worked around by setting status via the real `/leads/1/edit` UI. Conversion then succeeded: Lead #1 → Contact #2 + Deal #4, both verified independently with no data loss (name/email/company all carried over correctly).
- **TC-CRX-015** (settings read-before-update): **PASS** — the critical documented failure mode. "CRM, add a new deal stage called 'Contract Review'." produced a confirm card with the **full original stage list plus the new one appended**, never a replace-with-only-the-new-value proposal. Verified independently on the real CRM Dashboard Pipeline Summary — all 7 stages present, no data loss.
- **TC-CRX-016** (delete requires explicit naming): **PASS.** "CRM, clean up my CRM data." correctly did not propose any delete. A disposable test contact was created and then deleted via an explicit, correctly-named `Crm Delete Contact` confirm card — reproduced BUG-CRX-013's self-contradiction a third time (this time for delete) before the ID-qualified retry succeeded. Verified independently: only the two real contacts (Rohan Verma, Priya Sharma) remain.
- **TC-CRX-017** (invalid stage value rejected): **PASS.** "CRM, create a deal ... at stage Frobnicated." was caught before proposing anything — the agent named the real configured stage list and asked the user to pick a valid one or add "Frobnicated" as new. No coercion, no fabricated success.
- `CRUX_AGENT_CRM_SALES.md` is now **fully executed, 8/8 TCs with a definitive verdict** (5 PASS, 2 FAIL, both FAILs already filed as BUG-CRX-013/014/015).
- BUG-CRX-013's local file updated with two further reproductions (lead-status-update, contact-delete) broadening its scope to a general "named-record write intent can self-contradict; explicit-ID retry reliably works" pattern — **not yet added as a production comment** (user chose "not now" when asked).
- New fixtures this session: Contact #2 "Rohan Verma" (from lead conversion, linked to new Company #2 "Nimbus Industries"), Deal #4 "Nimbus Industries Rollout" ($9,500, New), CRM Deal Stages setting now includes "Contract Review", a disposable test contact (ID:3, created then deleted — net zero). Switched identity mid-session: `luna.blossom` → `admin` (TC-CRX-015 requires Redmine admin per its own precondition) — remained as `admin` through TC-CRX-017, no revert needed since admin is a standing seed credential, not a temporary grant.

## 2026-09-16 (later still) — `CRUX_AGENT_WORKLOAD_CAPACITY.md` fully executed, 2 new bugs found

Moved to the next suite per "now test next test suite" / "yes keep going." All 8 TCs (TC-CRX-089–100) reached a definitive verdict: 7 PASS, 1 FAIL. Full evidence written inline in `CRUX_AGENT_WORKLOAD_CAPACITY.md`.

- **Precondition gap found and bootstrapped**: the Workload plugin had zero teams/skills/workloads despite earlier session notes claiming seed data existed — created Team #1 "Platform Engineering" and Skill #1 "Kubernetes" live via chat (also satisfying TC-CRX-092's own steps), luna.blossom/daisy.skye added then bulk-removed as members during the same TC.
- **TC-CRX-089** (read surface): PASS — dashboard/capacity and leave/conflicts reads both grounded and honest about the empty state.
- **TC-CRX-092** (team/member/skill/bulk-remove): PASS — found **BUG-CRX-017** (bulk-member-removal falsely reports two real members as "not currently members," using their own usernames right after listing those same usernames' display names as current members). Reported to production as **#120704**.
- **TC-CRX-090** (allocation writes — add issue, resize, update dates): **FAIL.** Bootstrapped Workload #1 ("Platform Engineering Sprint 1") and added issue #9 to it (`workload_issue_id: 1` returned). Resizing hours against that exact ID ("resize allocation 1") returned a checkmark-prefixed **failure** — "Workload allocation #1 not found" — even though the real allocation genuinely exists (verified independently on `/rf_teams/1/rf_workloads/1`'s Issues panel: issue #9 shown "In Workload" with a live Planned Hours field). `workload_show` never exposes an allocation ID at all; a follow-up Gantt-lookup mis-routed to "the Project Manager" (further BUG-CRX-014 evidence), which fabricated a false claim that the already-confirmed add-issue write was "still pending confirmation." Update-dates never even reached a confirm card (BUG-CRX-013 self-contradiction, 2/2). **Net: no working chat path exists to set exact planned hours or dates on a newly-added allocation.** Filed as **BUG-CRX-018** (High), reported to production as **#120705**.
- **TC-CRX-091** (leave lifecycle — create/approve/reject/cancel): PASS overall, but found **BUG-CRX-019**: a leave-create request combining the username "luna.blossom" with every other field in one message silently resolved to `User: 0` in the confirm card (should be 5) instead of erroring or asking — a more dangerous failure mode than BUG-CRX-015/017's honest "doesn't exist" negatives, since the bad card otherwise looked completely normal. Caught and cancelled before confirming; the explicit-numeric-ID retry worked correctly. All 4 lifecycle actions (create/approve/create+reject/create+cancel) independently verified on the real `/rf_leaves` Team Approvals tab, each targeting exactly the intended leave request. Reported BUG-CRX-019 to production as **#120706**.
- **TC-CRX-093** (holiday schemes — create/add-holiday/activate/clone): PASS — all 4 actions succeeded with exact dates/names preserved (after repeated BUG-CRX-013 self-contradictions, 0/4 first-try but 4/4 eventually succeeding); clone independently verified inactive-by-default with its holiday duplicated.
- **TC-CRX-094** (`refresh_gantt`/`recalculate` explicit-ask-only): PASS — a plain Gantt read triggered no write; an explicit recalculate request did, correctly.
- **TC-CRX-095** (`send_email` explicit-intent): PASS, with new evidence added to BUG-CRX-014 — the Capacity Agent's own `send_email` proposal uses a non-standard free-text "Confirm to send? (Y/N)" format instead of the usual button card; replying with exactly the requested bare "Y" (no domain prefix) mis-routed to the Project Manager, which falsely denied any email capability exists. Re-prefixing recovered the real structured confirm card, which completed successfully.
- **TC-CRX-096** (delete requires explicit naming): PASS — a broad "clean up unused teams" produced no delete proposal; an explicit "delete team Platform Engineering" did, using the same free-text Y/N format as send_email, and completed correctly once re-prefixed. Verified independently: `/rf_teams` back to "No teams have been created yet."
- New fixtures this session, now cleaned up / net-zero by end of suite: Team #1 (created, then deleted in TC-CRX-096), Skill #1 "Kubernetes" (left in place — no delete tested), Workload #1 "Platform Engineering Sprint 1" (left in place, 0h planned due to BUG-CRX-018), issue #9 "TC-CRX-090 Allocation Write Test Issue" (created, assigned to Redmine Admin, added to Workload #1 — note issue #8 could NOT be assigned to Admin since Admin isn't a Crux QA project member with an assignable role; a membership-add attempt for Admin silently failed and wasn't investigated further, worth flagging if Admin needs to be assignable again later), Holiday Scheme #1 "US Federal Holidays 2026" (active, 1 holiday) + Scheme #2 "...Copy" (inactive clone) — both left in place, no cleanup tested, Leaves #1/#2/#3 (approved/rejected/cancelled respectively, all left in their final state).
- **BUG-CRX-017/018/019 all reported to production** as issues **#120704**, **#120705**, **#120706** respectively (all Prashant Chaurasia, user id 410; #120704 Medium, #120705/#120706 High). All linked to Run #569, testcase **#120491** (`CRUX_AGENT_WORKLOAD_CAPACITY.md`) — testcase marked Failed (TC-CRX-090) / Passed-with-findings otherwise.

## 2026-09-16 (later still) — `CRUX_AGENT_DEVOPS_AND_BUDGET.md` unblocked and executed, no new bugs

Moved to the next suite per "okay next suite." Re-verified the plugin-detection log first (`docker logs crux-redmine-docker-mcp-1 | grep "Detected plugins"`) — the most recent line showed 9/14 with both `devops` and `budget_audit` present, confirming the suite's old "not installed" blocker no longer applies. 6/7 TCs reached a definitive verdict (all PASS), 1 BLOCKED (TC-CRX-024, still needs a dev-confirmed safe test repo/branch — not improvised given real CI cost/effect). Full evidence written inline in `CRUX_AGENT_DEVOPS_AND_BUDGET.md`.

- **DevOps Agent (TC-CRX-023/103)**: PASS. Correctly asked for a project identifier rather than guessing; correctly reported "The DevOps plugin isn't wired to the crux-qa project... or has no builds/commits/PRs/repos tracked yet" via a real, grounded tool call — no repo is connected to any local project on this instance, so full "cite the real repo/build/PR" evidence isn't constructible here, but the honest-empty-state behavior itself is solid, conclusive evidence and doubles as TC-CRX-029's cross-referenced pass. A vague "should we build the latest changes?" correctly produced no `trigger_build` proposal, asking for the specific project/branch instead.
- **Budget Agent (TC-CRX-026/105/106)**: PASS, PASS, PASS. Found crux-qa's local numeric project ID (1) and a real issue category ID (6, "Development - Quality Assurance & Testing") via the real UI (Settings → Issue categories) after the agent correctly declined to guess either. Set a real 37.5h budget cap via chat — confirm card and resulting real UI history row both exact, no rounding. Read-surface (cap status, this-week's approvals, approval-record detail) all grounded and exact against that same real data. Negative test (TC-CRX-028): triggered a second real `set_budget` proposal, asked "did that go through?" before confirming — agent correctly reported it as still-pending (even computing the correct prospective new total, 47.5h) rather than claiming success; verified independently that the real approved-hours history was genuinely unchanged. Cleaned up the stale unconfirmed proposal afterward.
- **BUG-CRX-013 reproduced again**, now on a third structurally unrelated domain agent (Budget Agent, `set_budget` intent) — further strengthens the platform-wide, shared-confirm-gate conclusion. Local bug file updated with this occurrence; not re-reported to production (same underlying issue #120664).
- Caught and explained a false-alarm mid-session: a raw `fetch('/projects/crux-qa.json')` call (used while trying to look up the numeric project ID) triggered Redmine's native HTTP Basic Auth browser popup — a standard REST-API `401 WWW-Authenticate` challenge, not a plugin bug or a real login prompt. Navigating away cleared it with no lasting effect. **Lesson for future sessions: don't probe for IDs via raw `fetch()`/`.json` URLs — find them through real UI navigation instead** (e.g., a settings-page form's `action` attribute, or an Edit/Delete link's href, both routinely expose the numeric ID without needing the REST API at all).
- New fixtures this session: crux-qa Approved Hour record #1 (37.5h, category 6/Development-QA&Testing, permanent — no delete path tested/needed). No project/team/user fixtures created or altered.

## 2026-09-16 (later still) — `CRUX_AGENT_AGILE_SCRUM.md` executed, severe new bug found (BUG-CRX-020)

Moved to the next suite per "okay go and test other suites." Live-verified the Agile Board plugin was actually reachable via chat despite the plugin-detection log's flickering count (sometimes 9/14 missing `agile`, sometimes 10/14 with it) — the Scrum Agent routed and made real tool calls regardless, so proceeded. 6/6 TCs reached a definitive verdict: 2 PASS (read surface, negative gating), 4 FAIL — all 4 blocked by the same new, severe bug.

- **TC-CRX-001/113**: PASS. Read surface (sprint board, backlog, epics, global board, my-page board) all grounded and honestly empty (fresh instance, no seed data — paging sub-check not exercisable). "Clean up old sprints" correctly produced no delete proposal, asking for exact criteria/names instead.
- **TC-CRX-002/110/111/112: all FAIL — new bug found, BUG-CRX-020 (Critical).** Every write action attempted for the Scrum Agent (`create_issue`, `create_sprint`, `move_issue`, `create_column` — 4 distinct types, 6 total attempts) produced a plain markdown "Proposed X: [table] ... Click **Confirm** to ..." message with **no real Confirm/Cancel button anywhere in the DOM** (verified directly via `querySelector('button')` returning null/false on the actual chat bubble, contrasted against every genuine `WRITE` card from earlier in this engagement which always has one). Replying with the literal word "Confirm" produced a further false claim that a working button exists and just needs clicking. 100% reproducible, zero working phrasing found, zero writes ever actually occurred (verified against the real Issues list and board). One additional cross-agent instance also hit on the Budget Agent under an equivalent phrasing, suggesting the underlying defect (a newer fabricated-confirm template that evades the guard proven working for a different phrasing in `CRUX_WRITE_CONFIRM_GATE.md` TC-CRX-169/034) is shared platform-wide, just far more frequent for the Scrum Agent specifically — effectively a total block on Agile CRUD via chat.
- **BUG-CRX-013 reproduced again**, now on a fourth domain agent (Scrum, on the first `create_issue` attempt before the fabricated-confirm variant appeared on retry).
- New fixtures this session: Redmine issue #10 ("TC-CRX-002/110 Agile Board Fixture Card") — created via the real New Issue UI since chat-based creation is broken, used to unblock TC-CRX-002's move-card setup requirement. No sprint/column/board-config fixtures could be created (all blocked by BUG-CRX-020).
- **BUG-CRX-020 reported to production** as issue **#120710** (Priority Blocker, assigned to Prashant Chaurasia).

## 2026-09-16 (later still) — `CRUX_AGENT_QA_TESTCASES.md` executed, BUG-CRX-020 confirmed on a third agent

Moved to the next suite per "go and test other suites." Following the lesson from the Agile suite, probed one write action early (`create test suite`) rather than assuming the write surface works. 5/7 TCs reached a definitive verdict: 3 PASS (read surface, both negative cases), 2 FAIL (both hit BUG-CRX-020), 2 BLOCKED (downstream preconditions unavailable since no test case/suite could ever be created).

- **TC-CRX-053/119/120**: PASS. Read surface grounded and honest (real case statuses/run types with IDs, honest "no login test cases / no runs yet" for the empty project). "Clean up old test runs" correctly produced no delete proposal. Substituted the still-pending Test Suite Creation proposal for TC-CRX-059's "was it actually created?" check (the literal `report_defect` precondition wasn't constructible) — agent correctly reported it as still-pending, never a false success, even though the underlying proposal itself is fabricated (BUG-CRX-020).
- **TC-CRX-054/117: FAIL — BUG-CRX-020 reproduced on a third domain agent (QA Agent).** Both `create test suite` and `create environment` produced the identical button-less fabricated-confirm pattern (2/2 for this agent). TC-CRX-055/118 BLOCKED as a direct downstream consequence (no test case ever exists to build on).
- BUG-CRX-020 local file updated with this third-agent occurrence — now confirmed across 3 structurally unrelated domain agents (Scrum, Budget, QA) and 6 distinct action types, 8 total reproductions. Not re-reported to production (same underlying issue #120710).
- No new fixtures — every write attempt failed before creating anything.

## 2026-09-16 (later still) — `CRUX_AGENT_TIMESHEET.md` executed, BUG-CRX-020 confirmed on a fourth agent

Continuing per "go and test other suites." Read surface and negative gating both PASS; both write actions probed (`create_schema`, `settings_update`) hit the identical BUG-CRX-020 fabricated-confirm pattern. Given the bug is now confirmed on 4/9 domain agents across 8 distinct action types (10 total reproductions), stopped exhaustively re-testing every remaining write TC in this suite once the pattern repeated a second time — TC-CRX-078/123 marked BLOCKED (downstream precondition unavailable) rather than independently re-verified, since doing so would only produce redundant evidence of the same already-proven defect.

- **TC-CRX-077/125/126(gating)**: PASS. Read surface honest and grounded (0 pending approvals, 0 hours logged — fresh instance). "Clean up old timesheets" correctly asked for exact criteria rather than proposing anything. A tangential "approval process feels slow" question correctly triggered diagnostics only, no settings-change proposal.
- **TC-CRX-080 / TC-CRX-082(confirm-half)**: FAIL — `create_schema` and `settings_update` both produced well-formed, correctly-detailed proposals with zero real Confirm/Cancel button (`hasButton: false`). BUG-CRX-020 updated with this fourth-agent occurrence.
- No new fixtures.

## 2026-09-16 (later still) — `CRUX_AGENT_INVOICE_BILLING.md` executed, BUG-CRX-020 confirmed on a fifth agent, new bug BUG-CRX-021 found

Continuing per "go and test other suites." Read surface and negative gating PASS. First attempt used "Invoicing," as the domain prefix, which mis-routed to the Project Manager — a further minor reproduction of BUG-CRX-007 (the recognized prefix is "Invoice,"). Once correctly routed, the write surface (`set_team_rate`) hit BUG-CRX-020 again, and a second, independent, new bug was found in the same exchange.

- **TC-CRX-034/132**: PASS. Read surface grounded and honest (no team rates configured, no outstanding invoices for the "QA Test Customer" that doesn't exist in CRM — correctly disclosed rather than fabricated). Vague "clean up old invoices and let the customer know about their balance" correctly triggered no delete/send proposal, explicitly citing "this involves writes to real financial records."
- **TC-CRX-037: FAIL — BUG-CRX-020 reproduced on a fifth domain agent (Invoicing).** "Invoice, set the rate for user 1 to $75/hr on crux-qa." produced a well-formed but button-less "Proposed Team Rate" card.
- **New bug found: BUG-CRX-021** — that same proposal's Project field read "crux-qa (#10)" instead of the real ID 1, independently verified via two projects' real settings-page form actions (crux-qa = 1, crux-qa-private = 2; no project 10 exists). Currently masked by BUG-CRX-020 (the write never executes anyway) but would misdirect every Invoicing write to a wrong/nonexistent project if that bug were fixed first. Reported to production as **#120712** (High).
- TC-CRX-035 marked FAIL by inference (not independently re-tested, given 11 prior reproductions of BUG-CRX-020); TC-CRX-036/131 BLOCKED (downstream preconditions unavailable).
- No new fixtures — every write attempt failed before creating anything.

## 2026-09-16 (later still) — `CRUX_AGENT_KNOWLEDGE_BASE.md` fully executed, all 16 suites now have an initial pass

Continuing per "go and test other suites" — the last suite in the Suite Index. Found and fixed a real precondition gap (KB module not enabled for crux-qa), then all 6 TCs reached a clean PASS — the first fully-clean suite in this entire round of BUG-CRX-020-affected suites.

- **TC-CRX-044 (read surface)**: PASS. Correctly disclosed the KB module wasn't enabled initially (real gap, fixed via Settings > Project UI); one transient stale-cache response self-resolved on retry (not filed, one-off). Grounded, exact-quote retrieval from a real page once content existed; honest "no such space" for a made-up name.
- **TC-CRX-045/135/136 (create/update/publish/unpublish/restore)**: all PASS. Every write hit the familiar BUG-CRX-013 self-contradiction on the first attempt (6+ times across 6 action types) but **every single one eventually produced a real, working confirm card** on retry — unlike the 5 agents already confirmed affected by BUG-CRX-020, the KB Agent never produced a button-less fabricated proposal. Restore-to-version independently verified: content after restore exactly matched the named earlier version, not the intervening one.
- **TC-CRX-048/138 (negative cases)**: both PASS — correct delete-gating and honest nonexistent-space handling.
- This suite is valuable confirmation that **BUG-CRX-013 (self-contradiction) and BUG-CRX-020 (fabricated confirm with no button) are two independent defects** — an agent can hit one heavily without ever hitting the other. BUG-CRX-013 local file updated with this sixth-agent occurrence.
- New fixtures: KB Space #1 "Documentation" in crux-qa, Node/Page #1 "Getting Started" (published, 3 versions, currently showing v1's restored content). KB module now enabled for crux-qa (a permanent environment change, like the earlier Issue tracking/Redmineflux Crux module enables).
- **All 16 suites in the Suite Index now have at least one execution pass.** See `STATUS.md` for the full current tally.

## 2026-09-16 (later still) — dev fix-verification pass: 11/11 bugs confirmed FIXED

Dev shipped a fresh `CHANGES.md` handoff (updated `redmineflux_crux`, `redmineflux_crm`, `redmineflux-crux-core` folders) fixing BUG-CRX-003, 004, 005, 006, 008, 009, 010, 011, 012, 014, 015 — explicitly excluding BUG-CRX-007 (untouched) and BUG-CRX-013 (held back by the dev's own instruction, "not yet live-confirmed against a real model").

- **First drop was a blocker**: `redmineflux_crm`'s updated `contacts_controller.rb`/`leads_controller.rb` declared `helper :crm`/`helper :rf_crm_icon`/`helper :helpdesk_integration` but the plugin shipped no `app/helpers/` directory at all — crashed `crux-redmine` on every boot (`RestartCount` climbing, `uninitialized constant CrmHelper (NameError)`). Reported root cause back to the dev; a corrected re-drop with the real helper files present fixed it — confirmed `RestartCount: 0`, clean boot.
- Restarted the stack safely: `docker restart crux-redmine` → `crux-redmine-docker-mcp-1` → `crux-core` (never `docker compose up -d`, to preserve the baked-in `CRUX_SECRET`). Discovered crux-core's provider/key store is **not persisted across restarts** (in-memory only) — the OpenRouter provider+key had to be re-added from scratch via Providers & keys UI; real chat verified working again afterward (`provider=openrouter`, real tokens).
- **Retested all 11 in-scope bugs live against their original reproduction steps — all 11 confirmed FIXED**:
  - BUG-CRX-003: cross-project gate approval now correctly refused (403, clear message) as `luna.blossom` on a real cross-project WP.
  - BUG-CRX-004: MCP-side session invalidation (restarted only the MCP container) now self-heals on the next call — no manual `crux-core` restart needed.
  - BUG-CRX-005: settings page now clearly states the nav toggles are "not an access control."
  - BUG-CRX-006: `/crux/admin/logs.json?level=BOGUS` now returns real HTTP 400, not 200.
  - BUG-CRX-008: suggest-only WP write via chat now refused with the exact sacred-rule message; verified no new journal entry.
  - BUG-CRX-009: fixed at code level (verify-then-report pattern implemented exactly as suggested); live end-to-end confirmation blocked by a missing Anthropic provider key (Improve requires it specifically) — not yet independently confirmed live.
  - BUG-CRX-010: Session Artifact/Keep now captures the real outcome (`✓ Created #N`), not just the pre-confirm text.
  - BUG-CRX-011: shared read-only viewer's Confirm now refused (404 "not found"); verified no issue was created.
  - BUG-CRX-012: `/crux` now returns a real 403 for a user without `view_crux`. **New adjacent finding**: `/crux/agents` (a sibling controller) has the identical missing-permission-check defect, untouched by this fix — flagged as candidate BUG-CRX-022, not filed yet (pending user decision).
  - BUG-CRX-014: CRM follow-up turn without repeating "CRM," now correctly stays routed to the Sales Agent.
  - BUG-CRX-015: "link contact to deal" now correctly maps to `update_deal(contact_id=...)`, no false negative, no unresolvable "project ID" demand; verified live on the real deal record.
- Also retested BUG-CRX-001 (#120514) and BUG-CRX-002 (#120515) on request — both were also touched by today's `CHANGES.md` (`llm_router.py`/`providers.py`, `db.seed.json`) but initially missed from the retest list. Both confirmed FIXED: a deliberately-invalid key on a throwaway OpenRouter-compatible provider now correctly fails "Test connection" (401, not the old false-positive `ok:true`); all 6 previously-false agent persona descriptions on Crux Settings now accurately state real write capability.
- **User decision: close 11 of the 13 confirmed-fixed bugs now** (BUG-CRX-001, 002, 003, 004, 005, 006, 010, 011, 012, 014, 015) — all 11 production issues updated to Done/100%, all 11 local files moved to `bugs/closed/`, `bugs/_index.md` updated. **BUG-CRX-008 and BUG-CRX-009 deliberately left open** at the user's explicit request despite being fixed: 008 only for its update_issue scope (broader write-kind gap remains, dev-documented); 009 fixed at code level only, live confirmation needs an Anthropic provider key not yet available — no key was added this session.

## 2026-09-16 (continued) — remaining `bugs/open/` entries retested per explicit user request

With the `CHANGES.md`-scoped bugs handled, the user asked to retest everything else still in `bugs/open/` (excluding BUG-CRX-013, per the dev's hold-back). None of these were credited in today's `CHANGES.md`, so this was a genuine "did anything change anyway" sweep, not a targeted verification.

- **BUG-CRX-007 (domain-inference routing) — FIXED.** `chat.py` has a `_domain_route()`/`_DOMAIN_ROUTES` table with a comment citing this bug directly — must have landed in an earlier/uncatalogued pass. Both original bare-question reproductions ("what deals need attention?", "who's overloaded this week?") now route correctly to the Sales Agent / Capacity Agent respectively.
- **BUG-CRX-016 (MCP one-shot plugin detection) — still open, unfixed.** `redmineflux-mcp/src/server.py` unchanged; this component isn't part of the `redmineflux-crux-core`/`redmineflux_crux`/`redmineflux_crm` handoff at all.
- **BUG-CRX-017 (bulk-remove username self-contradiction) — still reproduces.** Fresh team/member fixture, same exact failure pattern ("I don't see luna.blossom... current members are: ...Crux Manager (ID 5)").
- **BUG-CRX-018 (broken allocation ID contract) — still reproduces.** Fresh workload/issue fixture, `workload_issue_id: 2` from Add Issue still rejected by Allocation Resize ("✓ Workload allocation #2 not found").
- **BUG-CRX-019 (Leave Create silent User ID 0) — still reproduces.** Same combined-fields-in-one-message repro still yields `User: 0` in the confirm card.
- **BUG-CRX-020 (fabricated-confirm, no real button) — FIXED for its core defect.** `chat.py` has a new `_FABRICATED_CLICK_CONFIRM_RE` pattern with a comment explicitly citing this bug, wired into the same honest-refusal guard. 3/3 retest attempts on the Scrum Agent never reproduced the deceptive "Click Confirm" text with no button — every failure was honest and recognizable. The underlying "can this agent complete create_issue via chat at all" question remains a separate, unresolved usability gap (BUG-CRX-013 territory, out of scope).
- **BUG-CRX-021 (wrong project ID #10) — FIXED.** Both original repro exchanges now correctly resolve `crux-qa` to its real ID/name; a live write (`Invoice Set Team Rate`) completed successfully against the real project #1. This retest also independently re-confirmed BUG-CRX-020's fix (a real WRITE card rendered for the Invoicing Agent).
- **User approved closing all 3 newly-confirmed-fixed bugs** (007, 020, 021) — production issues #120609/#120710/#120712 updated to Done/100%, local files moved to `bugs/closed/`.

## Next Session Start Point (superseded — see the 2026-09-17 gap-TC-sweep entry near the end of this file for the current one)

- **`bugs/open/` now holds 12**: BUG-CRX-008, 009, 013, 016, 017, 018, 019, 022, 023, 024, 025, 026 — 023/024/025/026 newly filed 2026-09-17 (see entry below); 21 at the very start of the 2026-09-16 fix-verification work.
- **All 16 suites in the Suite Index have been executed at least once**, and the full 9-agent permission matrix (`CRUX_AGENT_PERMISSION_MATRIX.md`) is now fully live-verified as of 2026-09-17.
- **Pending user decisions carried forward:**
  1. Whether to add an Anthropic provider key so BUG-CRX-009 can be confirmed live and closed.
  2. Whether to pursue the broader write-kind gap in BUG-CRX-008 (9 domain-plugin write kinds + create_issue still unprotected by the autonomy check) as a follow-up.
  3. BUG-CRX-016, 017, 018, 019 remain confirmed-still-broken — no dev action taken on them yet; worth flagging to the dev as a follow-up batch alongside the CHANGES.md handoff's own regression coverage.
  4. BUG-CRX-022/023/024/025/026 (all new, missing permission checks + one fabrication) — awaiting a dev fix; no further QA action needed on these specifically until one lands.
  5. **New: the `agile_board` plugin folder on the Docker host is completely empty** — needs a dev re-drop of the real plugin code before any further Agile Board testing (live-execution or the 3 drafted TC-CRX-007–176 gap TCs) can resume at all. Flagged in BUG-CRX-026's own description but not filed as its own production issue (infrastructure gap, not an app defect).
- BUG-CRX-013 remains open and untouched — held back per the dev's explicit "do not hand this to QA yet" instruction; do not retest until the dev signals it's ready. (Dev's own 2026-09-17 journal on #120664 shows active investigation — root-caused to shared OpenRouter low-balance, a bounded-retry fix shipped locally but not yet pushed, dev deliberately left it "In Progress" pending his own live confirmation — nothing for QA to retest yet.)
- `bugs/open/` is still far from empty — `STATUS.md` stays `In Progress`, not `Complete`. Once it's empty, a full final-cycle regression (`SENIOR_QA_STANDARDS.md` §27) is still needed before `Complete`.
- **`CRUX_PER_USER_KEY_CRX12.md`'s production testcase #120485 is now stale** (still shows the old "BLOCKED" write-up) — re-upload the updated suite `.md` file next session per the standard sync pattern (§ "Production Redmine (ztflux) linkage"). Same is now also true for testcases #120492/#120493/#120494/#120497 (DevOps/Budget, Agile/Scrum, QA/Testcases, Knowledge Base) given today's new gap TCs and defect findings.

## 2026-09-17 — Full permission matrix completed, 4 new bugs filed, Agile Board plugin folder found empty

Per explicit user request, resumed yesterday's blocked per-domain-agent permission matrix (`CRUX_AGENT_PERMISSION_MATRIX.md`) once OpenRouter's shared account balance refreshed overnight — independently confirmed via the dev's own 2026-09-17 journal entry on BUG-CRX-013 (#120664), which root-caused yesterday's 402 errors to the exact same shared low-balance account. Re-verified locally with a real full tool-call request (not just the misleading 1-token connectivity test) before resuming.

**Completed all 9 domain agents' no-permission and admin tiers.** Admin tier: 6 of 7 remaining agents confirmed working normally with real data (Invoicing, CRM, KB, QA, DevOps, Budget); the 7th (Scrum) turned out to be fabricated — see below. No-permission tier: Invoicing Agent and Sales Agent (CRM) both correctly refused with clear, specific permission-name messages; Scrum Agent's write path correctly refused via the fabrication guard.

**4 new bugs found and filed:**
- **BUG-CRX-023** (#120756) — KB Agent's `list_spaces` has no Knowledge Base permission check at all.
- **BUG-CRX-024** (#120757) — QA Agent's `list_test_suites` has no Testcase Management permission check at all.
- **BUG-CRX-025** (#120758) — Budget Agent's `get_budget_status` has no permission check at all — the most concrete of the three, since it leaked real 37.5h approved-budget figures, not just an honestly-empty result. These 3 bring the total confirmed instances of the "missing permission check on a Crux-exposed read" defect class (first found via BUG-CRX-003) to **6** across the engagement.
- **BUG-CRX-026** (#120759) — the more serious finding: Scrum Agent fabricated a plausible "Backlog Items: 0 items, currently empty" answer with **no real tool call behind it at all** (no `Sources` citation, unlike every other successful probe that day), when the Agile plugin is genuinely unreachable. Confirmed via `redmineflux-mcp`'s own plugin-detection log (`Plugin not detected: agile (skipped)`, backed by a real `404` health-check), not a guess. Directly violates the agent's own manifest rule ("say so plainly instead of answering from guesswork"). The same agent's write path correctly caught this and refused honestly — only the plain-read path fabricates.

**Separately discovered while investigating why Agile Board has no top-nav/Administration entry** (user's own question): `C:\Crux-Redmine-Docker\agile_board\` is a completely empty directory on disk — no `init.rb`, no code — most likely lost during a recent file handoff (the same class of incident as the earlier CrmHelper crash-loop). This plugin was confirmed fully working as recently as 2026-09-15/16. **This is an environment regression, not a Crux bug** — flagged in BUG-CRX-026's description for context but not filed as its own production issue. Needs a dev re-drop before any further Agile testing.

All 4 new bugs: local files created (`bugs/open/BUG-CRX-023/024/025/026.md`), reported to production (assigned Prashant Chaurasia, Priority High/High/High/Medium respectively), linked to Run #569 "Crux QA Run 1" against each domain's own testcase (#120497 KB, #120494 QA, #120492 DevOps/Budget, #120493 Agile/Scrum — all marked Failed). `bugs/_index.md` and `STATUS.md` updated.

## 2026-09-16 (continued) — BUG-CRX-022 filed

Per explicit user instruction ("okay file bug"), filed the candidate finding surfaced while retesting BUG-CRX-012 as a new bug: `/crux/agents` (`CruxAgentsController#index`) has no `view_crux` (or any) permission check — `daisy.skye` (zero Crux permissions) sees the full Agent Fleet table (real spend/cap dollar figures, provider/model details for all 23 agents), no 403. Same defect class as BUG-CRX-012, but in a sibling controller today's dashboard fix (`authorize_view_dashboard`, scoped to `crux_dashboard_controller.rb` only) didn't touch. Local file `bugs/open/BUG-CRX-022.md` created; reported to production as issue **#120720** (Priority High, assigned Prashant Chaurasia — user id 410); linked to Run #569 "Crux QA Run 1", testcase **#120487** (same testcase BUG-CRX-011/012 were linked to), Environment "Window 11 + Chrome", marked Failed. `bugs/_index.md` updated with the new row.

## 2026-09-16 (continued) — `CRUX_PER_USER_KEY_CRX12.md` (CRX-12) fully executed, last unexecuted suite in the Suite Index, no new bugs

Per explicit user request ("execute test suite which is unexecuted"). This suite had been fully written but blocked since 2026-09-11 on two preconditions: `CRUX_REQUIRE_USER_KEY=1` in `.env` + stack restart, and a second restricted-privilege test user with their own API key.

- **User explicitly approved** (via confirm prompt) the environment change before proceeding, since it edits `.env` and recreates the `crux-core` container — flagged up front as a real infra change, not a read-only test action.
- **Precondition 1**: flipped `CRUX_REQUIRE_USER_KEY=1` in `.env`, then `docker compose up -d crux-core` to recreate the container — a plain `docker restart` does **not** pick up a changed env var, only a recreate does. New finding: `luna.blossom`'s and the OpenRouter key's data **both survived** this particular recreate (unlike the CrmHelper-recovery restart earlier this session) — turned out `CRUX_SECRET` was blank both before and after (still never added to `.env`, a standing flagged gap), so nothing about the decrypt key actually changed this time. A `UnicodeDecodeError` when testing the surviving-but-now-salt-mismatched key row confirmed the row persists to disk but isn't safely reusable across a real secret change — consistent with, not contradicting, the established `CRUX_SECRET` behavior. Deleted and re-added the OpenRouter key fresh; `Test connection` → `ok · 1883ms`.
- **Precondition 2**: no new user needed — `luna.blossom` (Manager role, member of `crux-qa` only, already has her own Redmine API key from earlier account setup) already fit "restricted-privilege, non-admin, with a private project she's not a member of" (`crux-qa-private` exists from Navigation-suite testing).
- **All 6 TCs executed, all PASS, no new bugs**:
  - TC-CRX-145: direct `curl` to all 4 gated routes (`/api/chat`, `/api/chat/confirm`, `/api/improve`, `/api/improve/confirm`) with no key header → clean `401` on all 4, real "no user key" refusal message, not a generic error.
  - TC-CRX-146: `luna.blossom` asking about `crux-qa-private`'s issue #1 → real Redmine-layer permission refusal, no leak. (Sub-case 2, a permission-granted-but-action-forbidden write, has no suitable existing fixture user — documented as a non-gap, not forced.)
  - TC-CRX-147: `admin` read + write+confirm under enforcement → both succeed normally, issue #13 created.
  - TC-CRX-148: `luna.blossom` read + write+confirm under the *default* `0` (unmodified) → both succeed under the shared key, issue #12 created.
  - TC-CRX-149: `luna.blossom` write+confirm under enforcement → issue #14's real "Added by" shows **Crux Manager** (luna.blossom), not a shared service account — the core CRX-12 claim, confirmed.
  - TC-CRX-150 (exploratory): flipped back to `0` and recreated mid-session inside `luna.blossom`'s still-open chat session (`ses-022`) — the session **survived** the recreate (session state persists to the mounted volume) and a write immediately succeeded under the now-downgraded enforcement, issue #15 created. Combined with TC-CRX-145's immediate-401 result, this shows enforcement is evaluated fresh per-request against live config, not cached per-session — no window of stale/weaker enforcement either direction.
- **Cleanup**: reverted `.env` to `CRUX_REQUIRE_USER_KEY=0` (original default), OpenRouter connectivity re-verified afterward. 4 fixture issues left in `crux-qa` (#12–#15, all titled `TC-CRX-0NN ... test`) — not deleted, consistent with this session's pattern of leaving local-only test fixtures in place.
- **All 16 suites in the Suite Index now have at least one execution pass** — this was the last one.

## Next Session Start Point (superseded — see the 2026-09-16 fix-verification entry below for the current one)

- **All 16 suites now have an initial execution pass** — see the Suite Index in `docs/CRUX_FEATURES_LIST.md` and the full per-suite summary in `STATUS.md`'s `redmineflux_crux` row. The dominant finding across this entire second half of the engagement is **BUG-CRX-020** (fabricated-confirm proposals with no real button), confirmed on 5 of 9 domain agents (Scrum, Budget, QA, Timesheet, Invoicing) — this is now the single most impactful defect found in the whole engagement, since it silently blocks core CRUD for the majority of domain agents via chat with no user-visible indication anything is wrong.
- **Not yet done, worth considering for a future session:**
  - A few suites have TCs still marked BLOCKED purely because an earlier TC in the same suite hit BUG-CRX-020 (e.g. `CRUX_AGENT_TIMESHEET.md` TC-CRX-078/123, `CRUX_AGENT_INVOICE_BILLING.md` TC-CRX-036/131). These should be revisited once BUG-CRX-020 is fixed, not before.
  - `CRUX_AGENT_DEVOPS_AND_BUDGET.md` TC-CRX-024 (real build trigger) remains BLOCKED pending a dev-provided safe test repo/branch.
  - `CRUX_PER_USER_KEY_CRX12.md` still needs `CRUX_REQUIRE_USER_KEY=1` + a restarted stack + a second restricted-privilege test user with their own API key — never attempted this engagement.
  - `CRUX_DASHBOARD_GRAPH_PIPELINE.md` TC-CRX-128 remains blocked (no UI path found to bind a WP to a custom pipeline).
  - Consider a full regression pass once BUG-CRX-020 is fixed, re-testing every TC currently marked FAIL/BLOCKED because of it (a large fraction of the total open-bug surface).

## Superseded — earlier "Next Session Start Point" (kept for history, see the entry above for the current one)

- **Fifteen suites now done or partially done total:** ✅ `CRUX_NAVIGATION_AND_PERMISSIONS.md` (13/13 PASS), ✅ `CRUX_DASHBOARD_GRAPH_PIPELINE.md` (11/12 PASS, 1 blocked), ✅ `CRUX_AGENT_ROSTER_ADMIN.md` (13/13 PASS), ✅ `CRUX_ASK_CRUX_CHAT_CORE.md` (9/11 PASS, 1 FAIL → BUG-CRX-007, 1 inconclusive), ✅ `CRUX_WRITE_CONFIRM_GATE.md` (8/10 PASS, 2 FAIL → BUG-CRX-008 Critical + BUG-CRX-003 upgrade), ✅ `CRUX_PROJECT_CREATION_AND_IMPROVE_WAND.md` (8/10 PASS, 1 FAIL → BUG-CRX-009 High, 1 FAIL-not-a-bug/doc-staleness), ✅ `CRUX_CHAT_CAPABILITIES_KEEP_SHARE_ARTIFACTS.md` (4/6 executable PASS, 2 FAIL → BUG-CRX-010 Medium + BUG-CRX-011/BUG-CRX-012 both Critical), ✅ `CRUX_AGENT_CRM_SALES.md` (5/8 PASS, 2 FAIL → BUG-CRX-013/014/015; 1 infra bug BUG-CRX-016 found+fixed en route), ✅ `CRUX_AGENT_WORKLOAD_CAPACITY.md` (7/8 PASS, 1 FAIL → BUG-CRX-018; 2 more bugs BUG-CRX-017/019 found en route), ✅ `CRUX_AGENT_DEVOPS_AND_BUDGET.md` (6/7 PASS, 1 BLOCKED — TC-CRX-024 pending a dev-provided safe test repo), ✅ `CRUX_AGENT_AGILE_SCRUM.md` (2/6 PASS, 4 FAIL → BUG-CRX-020 Critical, blocking the entire write surface), ✅ `CRUX_AGENT_QA_TESTCASES.md` (3/7 PASS, 2 FAIL + 2 BLOCKED → BUG-CRX-020 again, third agent confirmed), ✅ `CRUX_AGENT_TIMESHEET.md` (3/6 PASS, 1-2 FAIL + 2 BLOCKED → BUG-CRX-020 again, fourth agent confirmed), ✅ `CRUX_AGENT_INVOICE_BILLING.md` (2/6 PASS, 2 FAIL + 2 BLOCKED → BUG-CRX-020 again (fifth agent) + new bug BUG-CRX-021).
- **Immediate next step: `CRUX_AGENT_KNOWLEDGE_BASE.md`** — the last suite in the Suite Index. Once done, all 16 suites will have been executed at least once (some fully, some partially blocked by BUG-CRX-020). **Probe one simple write action first** (per the now-overwhelming BUG-CRX-020 pattern — 5/9 domain agents confirmed, 11 reproductions across 9 action types) — treat a write action *succeeding* as the surprise, not the norm. Once probed and confirmed broken (1-2 quick checks), don't exhaustively re-test every TC — mark downstream-dependent TCs BLOCKED and move to negative/read-only TCs, which remain independently meaningful. Also watch for domain-prefix mis-routing (BUG-CRX-007) — KB's recognized prefix may not be the plugin's full display name; if the first message routes to "the Project Manager" unexpectedly, try a shorter/different prefix word before concluding anything.
- **Once all 16 suites have an initial pass, consider whether a consolidated BUG-CRX-020 summary/severity escalation is warranted** — it is by far the most impactful defect found this engagement, now blocking write CRUD for the majority of domain agents tested.
- **Before starting the next suite, quickly sanity-check MCP plugin detection** (`docker logs crux-redmine-docker-mcp-1 | tail -30`, look for "Detected plugins: ... (N/14)") if this is a freshly (re)started stack — BUG-CRX-016 means a slow-booting Redmine can silently zero out every plugin's tools again. Also worth noting: the plugin-detection count has fluctuated between 9/14 and 10/14 across restarts this session (e.g. `agile` was present once, absent once) — re-check specifically for whichever domain the next suite needs before assuming it's ready.
- **New pattern to watch for in future suites**: at least two write tools (`send_email`, `team_delete`) use a non-standard free-text "Confirm? (Y/N)" prompt instead of the usual structured button card — replying with a bare "Y"/"N" (no domain prefix) hits the BUG-CRX-014 mis-routing trap. Always reply with the "<Domain>," prefix even to what looks like a simple yes/no confirmation.
- **Critical new pattern (BUG-CRX-020, 2026-09-16)**: some write proposals render as a "Proposed X: [table] ... Click **Confirm** to ..." message with **no real button at all** — always verify a pending proposal actually has a clickable Confirm/Cancel element before assuming it's real (`document.querySelectorAll('.crux-msg-bot')` → last bubble → `querySelector('button')`). If it returns null/false, the proposal is fabricated and unconfirmable no matter what you type back — don't waste time retrying different confirmation phrasings; document 2-3 reproductions across different actions and move on. This was 100% reproducible (6/6) for the entire Scrum Agent write surface.
- **To find a local numeric ID (project, category, etc.) when a tool needs one and the agent won't guess it: use real UI navigation, not raw `fetch()`/`.json` REST calls** — a settings-page form's `action` attribute or an Edit/Delete link's href routinely exposes it. A raw JSON fetch without an API key triggers Redmine's native HTTP Basic Auth browser popup (harmless but disruptive — navigating away clears it).
- **Pending decisions carried over:** **BUG-CRX-003 dispute unresolved** — dev closed #120574 as intentional design, but local evidence (`_frozen_target()`'s existing project-derivation capability) contradicts that conclusion; needs a human decision (or a direct conversation with Prashant) on whether/how to reopen or escalate, not something to act on unilaterally; whether/how to fix the missing `CRUX_SECRET` in `.env` (flagged again — still unfixed, restart risk keeps being accepted turn by turn rather than resolved at the root); whether to add a production comment to #120664/#120665 reflecting BUG-CRX-013/014's now-platform-wide broadened scope (declined "not now" both times asked); whether to attempt TC-CRX-106 (hand-off confirm card) again with a more targeted trigger phrase; whether to revert the test fixtures left in place from `CRUX_WRITE_CONFIRM_GATE.md` (WPs `wp-crx028`/`wp-crx029`, frozen rule `fr-7` — all local-only, none affect production).
- Update `automation/testdata/CRUX_TESTDATA_LOCAL.xlsx` before next session's fixture creation — done for the Workload suite this session (new "Workload (test)" sheet: Team #1, Skill #1, Workload #1, issue #9, Holiday Schemes #1/#2, Leaves #1/#2/#3) and the Budget cap (Approved Hour record #1, 37.5h, category 6) — **still not yet added**: 2 earlier CRM deals, issue #2 reassignment, the 10-plugin environment change, the Reporter-role permission round-trip, issues #3/#4/#5/#6/#7/#8, WPs wp-crx028/wp-crx029, frozen rule fr-7, Contact #2/Deal #4 from lead conversion, the Deal Stages setting change.

## 2026-09-17 (continued) — Gap-TC sweep closed out across 7 suites, session ended by a platform-wide Ask Crux outage

### Completed This Session

Continued the 34-gap-TC sweep (TC-CRX-083–180) across the 9 domain-agent suites, finishing 7 of them:

- `CRUX_AGENT_INVOICE_BILLING.md`: TC-163 FAIL (BUG-CRX-028), TC-164 PASS, TC-165 BLOCKED.
- `CRUX_AGENT_CRM_SALES.md`: TC-161 PASS.
- `CRUX_AGENT_KNOWLEDGE_BASE.md`: TC-167/168/169 all PASS (TC-169 doubling as a fourth-agent confirmation of BUG-CRX-018's misleading-checkmark pattern).
- `CRUX_AGENT_QA_TESTCASES.md`: TC-170/171 BLOCKED, TC-172 FAIL (BUG-CRX-030).
- `CRUX_AGENT_WORKLOAD_CAPACITY.md`: TC-153/156 PASS, TC-154 FAIL (BUG-CRX-028), TC-155 BLOCKED/INCONCLUSIVE (BUG-CRX-028).
- `CRUX_AGENT_DEVOPS_AND_BUDGET.md`: TC-177/178 PASS.
- `CRUX_AGENT_AGILE_SCRUM.md`: TC-174 FAIL (BUG-CRX-031), TC-175 PASS, TC-176 PASS (BUG-CRX-018 evidence).

**4 new bugs found and reported to production**, plus 1 existing bug significantly extended with further cross-agent evidence:

- **BUG-CRX-028** (#120768, High) — Invoicing Agent fabricates a "✓ Invoice #1 updated successfully" confirmation for a due-date change on an already-**Sent** invoice (Draft-only-editable rule silently bypassed, real record verified unchanged, zero real Confirm button on the proposal). Edited twice more this session to add two further reproductions on a *different* domain agent (Capacity/Workload Agent, TC-154 holiday-scheme activation and TC-155 planned-hours update) — now confirmed across 3 domain agents and 3 action types, establishing this zero-button-fabricated-success shape as systemic, not agent-specific.
- **BUG-CRX-029** (#120782, Medium) — QA Agent falsely claims "no write tools are available in this deployment" and deflects to another agent, directly contradicting several real write-tool successes earlier in the identical session.
- **BUG-CRX-030** (#120784, Medium) — QA Agent's `remove_testcases_from_suite` genuinely un-scopes a testcase entirely (verified against the real backend — the suite listing shows "No data" afterward), contradicting the plugin's own documented "test case scope is immutable once assigned to a suite" rule.
- **BUG-CRX-031** (#120785, Medium) — Scrum Agent's `move_issue` resolves the real "Rejected" board column to two different, both wrong, statuses on two consecutive attempts for the same card ("Closed", then "Waiting for Customer Response" — a status that isn't even a real column on the board).
- **BUG-CRX-018** (#120705, still open) — edited to add a fourth and fifth cross-agent confirmation of the misleading "✓"-prefix-on-failure pattern (KB Agent permission refusal, Scrum Agent permission refusal) — now confirmed on 4 domain agents and 4 distinct failure types. Not a new bug.

### Blockers (session-ending)

The session ended mid-suite on `CRUX_AGENT_TIMESHEET.md` (the last of the 9 domain-agent gap suites) when the platform's single configured LLM provider (OpenRouter) started returning **401 Unauthorized** on every Ask Crux chat call — confirmed via the native `/crux/admin/keys` "Test connection" diagnostic, not a plugin defect. All 6 Timesheet TCs (TC-CRX-083–152) marked BLOCKED as a direct result. See the "Blockers" section near the top of this file for full detail. A complete fixture was built and verified genuine via the native UI *before* the outage hit and is ready for immediate reuse: approval schema **"Two-Level Approval"** (ID 1), new user **`crux.developer`**, and team **"Retest Squad"**.

### In Progress

`CRUX_AGENT_TIMESHEET.md` TC-CRX-083–152 (the gap-TC portion of the Timesheet suite) — 0/6 executed, all BLOCKED by the outage above. This was the last suite in the 34-TC gap sweep; once it's done, all 34 gap TCs (TC-CRX-083–180) will have reached a definitive verdict.

### Next Session Start Point

**Resume at TC-CRX-083, step 2** (the fixture-reuse step) in `CRUX_AGENT_TIMESHEET.md` — the approval schema/user/team fixture described above is already built and verified; do not rebuild it. First confirm the LLM provider is healthy again (`/crux/admin/keys` → Test connection on the OpenRouter provider, or a newly added fallback provider) before sending any Ask Crux message. Once TC-CRX-083–152 reach a verdict, all 34 gap TCs are complete and the next task is deciding whether to pursue a consolidated regression pass (see `SENIOR_QA_STANDARDS.md` §26/§27 — BUG-CRX-028's cross-agent confirmations and BUG-CRX-018's now-4-agent confirmation make both strong candidates for a dev fix-verification pass once addressed).

**`bugs/open/` now holds 17**: BUG-CRX-008, 009, 013, 016, 017, 018, 019, 022, 023, 024, 025, 026, 027, 028, 029, 030, 031. None fixed/closed this session — `STATUS.md` stays `In Progress`.

**Also ready for automation, not yet actioned**: TC-CRX-041 (Invoice Billing), TC-CRX-021 (CRM Sales), TC-CRX-050/168/169 (Knowledge Base), TC-CRX-097/156 (Workload/Capacity), TC-CRX-030/178 (DevOps/Budget), TC-CRX-008/176 (Agile/Scrum) all reached a confirmed manual PASS this session and are in scope for regression per `CRUX_FEATURES_LIST.md`'s Suite Index — no Playwright specs exist yet for this plugin (`automation/tests/` is currently unscaffolded, only `testdata/`/`uploads/` fixtures exist). Given the LLM-driven, non-deterministic nature of Ask Crux chat responses, whether/how to automate these needs a scoping conversation before writing specs (directly in the main session, per `CLAUDE.md` §13 — no subagent), but it should not be skipped without that conversation happening.

## Run History

> One row per test run / regression pass. Replaces the old changelog.md.

| Date | Redmine Version | Environment | Tested By | Summary |
|------|-----------------|-------------|-----------|---------|
| 2026-09-15 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_ASK_CRUX_CHAT_CORE.md` 9/11 PASS, 1 FAIL (BUG-CRX-007), 1 inconclusive. 10 domain plugins installed+migrated as a one-time infra unblock. |
| 2026-09-15 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_WRITE_CONFIRM_GATE.md` 8/10 PASS, 2 FAIL (BUG-CRX-008 Critical, BUG-CRX-003 upgrade). TC-CRX-169/034 force-reproduced on request — 10/10 definitive. |
| 2026-09-15 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_PROJECT_CREATION_AND_IMPROVE_WAND.md` 8/10 PASS, 1 FAIL (BUG-CRX-009 High), 1 FAIL-not-a-bug (doc staleness, TC-CRX-159) — 10/10 definitive verdict. |
| 2026-09-15 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_CHAT_CAPABILITIES_KEEP_SHARE_ARTIFACTS.md` 4/6 executable PASS/FAIL split, 3 FAIL (BUG-CRX-010 Medium, BUG-CRX-011/012 both Critical) — TC-CRX-116/057 out of scope. |
| 2026-09-15 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_CRM_SALES.md` partial: TC-CRX-010/086 PASS, TC-CRX-012/088 FAIL (BUG-CRX-013/014/015), TC-CRX-014–092 BLOCKED (OpenRouter 402 Payment Required). |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_CRM_SALES.md` completed: 8/8 TCs definitive (5 PASS, 2 FAIL — same BUG-CRX-013/014/015). New infra bug BUG-CRX-016 (MCP plugin-detection race) found+fixed en route. |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_WORKLOAD_CAPACITY.md` completed: 8/8 TCs definitive (7 PASS, 1 FAIL — BUG-CRX-018). 2 more new bugs found (BUG-CRX-017, BUG-CRX-019); BUG-CRX-013/014 confirmed platform-wide via extensive reproduction across this suite. |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_DEVOPS_AND_BUDGET.md` unblocked (old "not installed" note no longer applied) and executed: 6/7 TCs PASS, 1 BLOCKED (TC-CRX-024, no safe test repo). No new bugs; BUG-CRX-013 reproduced on a third domain agent (Budget). |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_AGILE_SCRUM.md` executed: 2/6 PASS (read surface, negative gating), 4 FAIL — new Critical bug BUG-CRX-020 (fabricated-confirm proposals with no real button, blocks entire Scrum Agent write surface, 6/6 reproductions). BUG-CRX-013 reproduced on a fourth domain agent (Scrum). |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_QA_TESTCASES.md` executed: 3/7 PASS, 2 FAIL + 2 BLOCKED (downstream) — BUG-CRX-020 reproduced on a third domain agent (QA), now confirmed across 6 action types / 8 total instances. |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_TIMESHEET.md` executed: 3/6 PASS, FAIL on schema-create/settings-update confirm — BUG-CRX-020 reproduced on a fourth domain agent (Time), now 8 action types / 10 total instances. |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_INVOICE_BILLING.md` executed: 2/6 PASS, FAIL on set_team_rate confirm — BUG-CRX-020 reproduced on a fifth domain agent (Invoicing), now 9 action types / 11 total instances. New bug BUG-CRX-021 (wrong project ID resolution) found and reported (#120712). |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_AGENT_KNOWLEDGE_BASE.md` fully executed: 6/6 PASS — first fully-clean suite this round (BUG-CRX-020 did not block writes). BUG-CRX-013 reproduced on a sixth domain agent (KB). All 16 suites in the Suite Index now have an initial pass. |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | Fix-verification regression pass: dev shipped `CHANGES.md` fixing BUG-CRX-003/004/005/006/008/009/010/011/012/014/015. Full stack restart (`crux-redmine`/`mcp`/`crux-core`), OpenRouter key re-added (crux-core's key store isn't persisted across restarts). All 11 bugs retested live against real repro steps — **all 11 confirmed FIXED** (009 code-confirmed, live blocked by missing Anthropic key). BUG-CRX-007/013 correctly excluded (013 held back by dev). New adjacent finding: `/crux/agents` shares BUG-CRX-012's missing-permission-check defect, not covered by this fix — candidate BUG-CRX-022, not yet filed. None of the 11 moved to closed/production yet — pending user approval. Also found+resolved a blocking crash-loop in the initial handoff drop (missing CRM helper modules), fixed by the dev's corrected re-drop. |
| 2026-09-16 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | `CRUX_PER_USER_KEY_CRX12.md` (CRX-12) fully executed — the last suite in the Suite Index with zero prior execution. Unblocked both preconditions (`CRUX_REQUIRE_USER_KEY=1` + recreate; `luna.blossom` reused as the restricted-privilege test user). 6/6 TCs PASS, no new bugs. All 16 suites now have at least one execution pass. |
| 2026-09-17 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | Full 9-agent permission matrix completed (`CRUX_AGENT_PERMISSION_MATRIX.md`) once OpenRouter's shared account balance refreshed overnight (independently confirmed via the dev's own BUG-CRX-013 journal). **4 new bugs found**: BUG-CRX-023/024/025 (KB/QA/Budget agents' read tools enforce no permission check at all — 6th confirmed instance of this defect class), BUG-CRX-026 (Scrum Agent fabricates a "backlog empty" answer with zero real tool call when the Agile plugin is entirely unreachable — confirmed via the MCP server's own plugin-detection log). Separately found the `agile_board` plugin folder is empty on disk (environment regression, not a Crux bug, needs a dev re-drop). |
| 2026-09-17 | 7.0.0 (local Docker) | Local (`crux-redmine`) | Claude (guided by Sourabh Singh) | 34-gap-TC sweep continued across 7 domain-agent suites: `CRUX_AGENT_INVOICE_BILLING.md` (TC-163 FAIL/BUG-CRX-028, TC-164 PASS, TC-165 BLOCKED), `CRUX_AGENT_CRM_SALES.md` (TC-161 PASS), `CRUX_AGENT_KNOWLEDGE_BASE.md` (TC-167/168/169 PASS), `CRUX_AGENT_QA_TESTCASES.md` (TC-170/171 BLOCKED, TC-172 FAIL/BUG-CRX-030), `CRUX_AGENT_WORKLOAD_CAPACITY.md` (TC-153/156 PASS, TC-154 FAIL/BUG-CRX-028, TC-155 BLOCKED), `CRUX_AGENT_DEVOPS_AND_BUDGET.md` (TC-177/178 PASS), `CRUX_AGENT_AGILE_SCRUM.md` (TC-174 FAIL/BUG-CRX-031, TC-175/176 PASS). **4 new bugs** (BUG-CRX-028 #120768, BUG-CRX-029 #120782, BUG-CRX-030 #120784, BUG-CRX-031 #120785); BUG-CRX-018 and BUG-CRX-028 both extended with further cross-agent confirmations. Session ended mid-`CRUX_AGENT_TIMESHEET.md` (TC-CRX-083–152 all BLOCKED) by a platform-wide Ask Crux outage — sole configured OpenRouter provider key returning 401, confirmed via native `/crux/admin/keys` diagnostic, not a plugin defect. Reusable fixture (approval schema "Two-Level Approval" ID 1, user `crux.developer`, team "Retest Squad") pre-built and verified for next session. No bugs closed this session; 17 open. |
