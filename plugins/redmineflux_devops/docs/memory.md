# Plugin Memory — Redmineflux DevOps

> Plugin-specific observations only. Global rules live in root MEMORY.md.

## Known Quirks

- The plugin has 7 permissions in code but the user-guide lists 10 permission entries (some are sub-permissions or aliases). QA tests use the 7 canonical permission names from requirements.md: `view_devops`, `manage_devops_settings`, `trigger_builds`, `approve_deployments`, `manage_releases`, `manage_incidents`, `view_security_scans`.
- "DevOps Engineer" is a custom role that must be created manually in Redmine — it is not a built-in role. Test setup must create this role if it does not exist.
- Token encryption uses AES-256-GCM; token fields in the plugin settings UI are write-only (cannot be read back after save). Test validation: save token, then verify it is not displayed in plaintext anywhere on the page.
- HMAC-SHA256 webhook signature is validated via `X-Hub-Signature-256` header (not `X-Hub-Signature`). Tests that send malformed webhooks must use this exact header name.
- Deployment freeze window blocks ALL deployments including those with `approve_deployments` permission — only admin can override.
- DORA metric recalculation is not always real-time; allow up to 60 seconds after a deployment event before asserting metric values in tests.
- The SSRF guard blocks webhook callback URLs pointing to private IP ranges (10.x, 172.16-31.x, 192.168.x, 127.x). Tests for SSRF guard should use these ranges as negative cases.
- Flaky test detection requires ≥3 consecutive alternating pass/fail results to flag a test as flaky. Single failures are not flagged.
- The "Covered by TC" column in features-list.md is intentionally blank — QA engineers fill it in after test execution.
- SUPPORTED_PROVIDERS = [github, gitlab, bitbucket, jenkins, sonarqube, fossa, prometheus, datadog, zabbix, pagerduty] — NOT snyk, trivy, or argocd (those return 400). TC-RDV-006 test data was incorrect for those 3.
- Auth header map per provider: github→X-Hub-Signature-256 (HMAC), gitlab→X-Gitlab-Token (plain), bitbucket→X-Hub-Signature (HMAC), jenkins→X-Jenkins-Token (plain, from global setting), sonarqube→X-Sonar-Webhook-HMAC-SHA256 (HMAC, global setting — BUG-RDV-003), fossa→X-FOSSA-Signature (HMAC, repo setting — BUG-RDV-004), prometheus/datadog/zabbix/pagerduty→X-DevOps-Token (plain, global setting).
- webhook_secret is stored and returned in plaintext in the Edit Repository form (only api_token_plain is encrypted). This is an observation — not confirmed as a bug.
- New connection initial status is `pending` — it only changes to `connected` after the Test Connection button is clicked (TC-RDV-011). TC-RDV-009 expected "connected" immediately; that expectation was incorrect.
- repo_name field in the Add/Edit Repository form must be "owner/repo" format (e.g., "Er-SourabhSingh/devops-plugin-test") NOT just the repo name alone. The server-side test_connection builds the GitHub API URL as `https://api.github.com/repos/#{repo_name}` — if owner is missing it returns a non-200 and the connection is marked "error". This was the root cause of repeated Test Connection failures in Session 3.
- Delete confirmation modal is #devops-repository-delete-modal (class "devops-repodel-backdrop"), not a native <dialog>. The confirm button is an `input[type=submit]` inside a Rails form with _method=delete. JS clicking visible buttons on the page can accidentally re-trigger row-level Delete buttons; use form.submit() on #devops-repository-delete-form directly.
- Developer role (ID 7) had view_devops added during Session 3 for TC-RDV-017 testing. It was NOT removed at session end — current state: Developer has view_devops.
- Flash message on connection creation is "Repository connection created successfully." (not "Connection saved successfully.") — minor wording difference from TC expectation.
- Test Connection button (TC-RDV-011): POST to /projects/<id>/devops_repositories/<repo_id>/test_connection returns {"status":"connected","message":"Connection verified — API access confirmed"}. Status badge updates to "connected" on page reload.
- Flash message on connection update is "Repository connection updated successfully." (verified TC-RDV-012).
- The DevOps roles admin panel shows 10 permissions, not 7. The 3 extras are: "Trigger deployments", "Approve releases", "Override security gate" — filed as BUG-RDV-006. All 7 documented permissions are present alongside them.
- Permission check order: trigger_builds check is done BEFORE resource lookup (TC-RDV-018 confirmed 403 returned without DB query). approve_deployments check may be AFTER resource lookup — 404 returned for non-existent build ID (TC-RDV-017).
- Build records in phoenix-platform (as of 2026-05-22): Build #42 (ID 451, github_actions, failed, master branch), Build #1 (ID 452, jenkins, pending). No deployment records exist.
- REST API endpoint /devops/builds.json returns 403 for unauthenticated (no cookie + no key) and for invalid API key (TC-RDV-019, TC-RDV-020). Use credentials:'omit' in fetch tests to strip session cookie.
- luna.blossom = Manager role on phoenix-platform (full DevOps access). daisy.skye = Developer (no DevOps permissions by default; view_devops was NOT added). autumn.grace = QA role (view_devops added for TC-RDV-016/017/018 testing).
- Developer role (ID 7) has no DevOps permissions by default. QA role (ID 11) had view_devops added during session 2 to enable TC-RDV-016 testing.

- Webhook background job throws ActiveRecord::RecordInvalid for minimal GitHub push payloads — event row is created (duplicate detection works) but enrichment step fails. Filed as BUG-RDV-007.
- TC-RDV-009 confirmed flash wording: "Repository connection created successfully." New connection initial status is "pending". Test connection created (ID 24) and deleted during session 4 — environment restored to 3 connections (IDs 19, 20, 21).
- TC-RDV-008 confirmed: event log caps at exactly 100 rows after 105 webhooks sent (scope :recent working correctly).

## Confirmed Working

- Webhook receiver: GitHub, GitLab, Bitbucket, Jenkins, Prometheus, Datadog, Zabbix, PagerDuty all accept valid payloads with 202 (TC-RDV-001, TC-RDV-006)
- HMAC replay protection: duplicate delivery_id returns 200 with `{"status":"ok","duplicate":true}` — no double-insert (TC-RDV-003)
- Async response: 202 returned in <100 ms before background job completes (TC-RDV-005, measured 59 ms)
- Event log `.recent` scope correctly limits display to 100 rows ordered by received_at DESC (TC-RDV-008)
- Token encryption: edit form renders token field empty with type=password and placeholder "(unchanged)"; raw PAT not in HTML source (TC-RDV-010)
- Connection Manager: Add Repository form creates connections for GitHub/GitLab/Bitbucket; flash "Repository connection created successfully." on success (TC-RDV-009)
- Plugin global settings: Jenkins webhook token and Monitoring webhook token saved correctly (masked with "•••••• (set — leave blank to keep)" after save)
- Test Connection button: correctly verifies PAT/API access against provider; flash "Connection test successful." on success; status updates to "connected" (TC-RDV-011, Session 3)
- Test Connection with invalid token: flash "Connection test failed. Check your credentials." (generic — does not include specific HTTP status); status updates to "error" (TC-RDV-012, Session 3)
- Edit Repository: updates persisted correctly, token remains masked, flash "Repository connection updated successfully." (TC-RDV-012 old session / TC-RDV-011)
- Delete Repository: admin can delete connections; row removed from list; confirmation dialog uses custom JS modal (#devops-repository-delete-modal) with Rails _method=delete form inside (TC-RDV-014)
- Role-based access control: users without DevOps permissions get 403 on all DevOps pages (TC-RDV-013)
- Non-member of private project gets 403 on all DevOps endpoints (TC-RDV-016)
- Developer with view_devops can access Deployments page (200); no Approve/Reject buttons shown when no deployment records exist (TC-RDV-017)
- trigger_builds permission: correctly blocks POST /devops_builds/{id}/trigger with 403 before resource lookup (TC-RDV-018)
- REST API: 403 returned for unauthenticated and invalid-key requests (TC-RDV-019, TC-RDV-020)
- All 6 original bugs (BUG-RDV-001 to BUG-RDV-006) confirmed present on new Forge (Session 3)
- BUG-RDV-007 confirmed on new Forge (Session 4) — webhook background job ActiveRecord::RecordInvalid

- REST API IDOR confirmed (BUG-RDV-008): daisy.skye (phoenix-platform only) reads builds from agileboard and commits from mobileappdev using her API key. All 9 non-member projects tested — all return 200 with data. Project membership check is completely absent from REST API controllers.
- DORA metrics route missing (BUG-RDV-009): GET /devops/metrics/dora.json → 404. All other 9 REST endpoints return 200. Only this route is absent.
- ActivityProvider not registered (BUG-RDV-010): No DevOps activity filters appear for any user in /projects/<id>/activity sidebar. Affects TC-RDV-024, TC-RDV-026, TC-RDV-038. ViewHook-based DevOps panel on issue pages (TC-RDV-025) is a separate mechanism and works correctly.
- Webhook event log access control incorrect (BUG-RDV-011): /projects/<id>/devops_webhooks returns 200 for Developer with view_devops only — should require manage_devops_settings. Controller uses wrong permission check.
- Notification preferences (/my/devops_notifications): save/persist works; flash text is "Account was successfully updated." (NOT "DevOps preferences updated." — previous note was wrong). Save route is PATCH (POST to /my/devops_notifications returns 404). Slack/Teams notification channels absent from UI — only email and in-app available.
- Plugin settings page: stale_pr_threshold field not rendered in /settings/plugin/redmineflux_devops. Validator logic confirmed working on fields that do exist.
- Admin audit log (/admin/devops_audit): 270 events visible to Admin; non-admin redirected to /login. Confirmed working correctly.
- Build trigger endpoint returns 422 when Jenkins is not connected — rate limit logic (60 req/min) is never reached in this state.
- REST API pagination structure confirmed: builds.json returns total_count, limit, offset fields correctly. Data is 0 due to BUG-RDV-007 but response structure is valid.

- **Suite 02 — Commit storage systemic failure (BUG-RDV-007 / BUG-RDV-022)**: ALL push webhooks (single and multi-commit) return 202 but zero commits are stored in the DB. Background job raises RecordInvalid and silently rolls back. The ~50 commits visible in `/projects/<id>/devops_commits` are pre-seeded data — NOT from test-run webhooks. Any TC that asserts a commit appeared after a push webhook will be BLOCKED until this is fixed.
- **Suite 02 — Authorization gaps widespread (BUG-RDV-017)**: PR Dashboard (`/projects/<id>/devops_pull_requests`) and Commits page (`/projects/<id>/devops_commits`) return HTTP 200 with full data to non-members and users without `view_devops`. Both controllers are missing the before_action permission guard — same root cause as BUG-RDV-011.
- **Suite 02 — PR Dashboard filter/sort wrong (BUG-RDV-016)**: The PR dashboard at `/devops_pull_requests` shows ALL statuses (open, merged, closed, draft) without any default filter. Sort order is Updated desc, not age asc (oldest unreviewed first) as the spec requires.
- **Suite 02 — PR multi-issue linking broken (BUG-RDV-019)**: The PR title/body parser only extracts the FIRST issue reference. A PR titled "Fixes #518 and refs #519" updates issue #518 but leaves issue #519 untouched. The regex stops after the first match.
- **Suite 02 — PR reviewer data not stored (BUG-RDV-014)**: After a `pull_request_review` webhook (202), reviewer names and approval status are not stored or rendered on the PR card. The reviewer data in the payload is silently discarded.
- **Suite 02 — Commit stats route missing (BUG-RDV-018)**: GET `/devops_commit_stats` returns 404. Daily/weekly aggregation charts, top contributors, and time window filter are entirely absent at the routing level.
- **Suite 02 — Auto-transition journal wrong (BUG-RDV-015)**: Journal text is "[DevOps] Status changed automatically — PR merged" (spec: "Status changed by DevOps integration (PR #N merged)"). Author is "Redmine Admin" (user #1 used as fallback) — spec requires a dedicated system/DevOps actor, not a real user account.
- **Suite 02 — Auto-transition UI simplified (BUG-RDV-021)**: The auto-transition settings page only shows a global enable checkbox + a single target status select. The per-rule configuration UI (Add Rule, Source Status, Trigger type) described in the spec does not exist.
- **Suite 02 — GitLab commit SHA links wrong (BUG-RDV-020)**: Stored GitLab commit links use `https://gitlab.com/<ns>/<repo>/commit/<sha>` — missing the `/-/` prefix required by current GitLab URL standards. Should be `/-/commit/<sha>`.
- **Issue URL routing quirk**: Issue pages for the "Environment Request" tracker type are only accessible at `/issues/<id>` (Redmine global route) — NOT at `/projects/<identifier>/issues/<id>` (returns 404 for that tracker type). Use global `/issues/<id>` URLs when navigating directly to individual issues in test steps.
- **Local Docker connection IDs (Session 7 state)**: GitHub ID 4 (`Er-SourabhSingh/devops-plugin-test`), GitHub ID 7 (`acme/phoenix-frontend`), GitLab ID 5 (`test/repo`). All use webhook secret `s3cr3tKey`.

- **Suite 03 — Badge polling uses identifier not numeric ID (BUG-RDV-023)**: `data-project-id` on the badge element contains the project's string identifier ("phoenix-platform"). The JS polling loop calls `/devops/builds.json?project_id=phoenix-platform` which returns 403. Badge never auto-updates without page reload.
- **Suite 03 — JUnit ingestion broken — missing rexml gem (BUG-RDV-026)**: `POST /devops/builds/<id>/test_results` returns HTTP 422 with `{"error":"XML parser dependency missing: cannot load such file -- rexml/document"}`. The `rexml` gem must be added to the plugin's Gemfile. All flaky test detection is blocked.
- **Suite 03 — Pipeline stages not implemented (BUG-RDV-029)**: Build detail page has no "Pipeline Stages" section. `BuildStageIngestor` service not present. Affects TC-RDV-125, 126, 144, 148.
- **Suite 03 — Build queue widget missing (BUG-RDV-027)**: `/projects/<id>/devops/dashboards` is a blank custom dashboard builder — no pre-built DevOps widgets. Queue depth and wait estimate (rfd-077) not implemented.
- **Suite 03 — Artifact downloads missing (BUG-RDV-028)**: Build detail page shows "No artifacts available" for all builds. GitHub Actions artifact API not called.
- **Suite 03 — Build duration not stored (BUG-RDV-031)**: Webhook `run_duration_ms` field silently ignored. All builds show "N/A" duration on detail page and "-" in the list. Stats bar shows "0s avg duration".
- **Suite 03 — Slack/Teams notifications absent (BUG-RDV-030)**: `/my/devops_notifications` shows only Email + In-app. Slack and Teams columns not rendered.
- **Suite 03 — Permission enforcement confirmed working**: QA role (view_devops, no trigger_builds) cannot see Rebuild button and trigger endpoint returns 403. TC-RDV-132 PASS. BUG-RDV-025 was NOT filed — original TC-RDV-108 conclusion was erroneous (admin session was likely still active).
- **Suite 03 — Failed build auto-comment works**: When a build webhook with conclusion=failure is received, a journal entry "[DevOps] Build #N failed on BRANCH. Error: log unavailable" is posted on the linked issue. Author is admin (user #1), not a system actor. Log excerpt shows "unavailable" because GitHub API can't serve logs for fake run IDs.
- **Suite 03 — XSS prevention confirmed**: Branch names containing `<script>` tags in webhook payloads are HTML-escaped in the builds list (hasEscapedScript=true, hasUnescapedScript=false). TC-RDV-142 PASS.
- **Suite 03 — Build stats bar present**: Builds list shows "N builds / X% success rate / Y failed / Zs avg duration" above the table. TC-RDV-133 PASS.
- **Suite 03 — IDOR protection for build detail pages confirmed**: Accessing a build belonging to project A via project B's URL returns HTTP 404. TC-RDV-147 PASS.
- **Suite 03 — Local users for phoenix-platform**: `qa_user` (QA role, ID 13) has view_devops. `reporter_user` (QA Engineer role, ID 15) gets 403 on DevOps pages — QA Engineer role does not have view_devops. Passwords reset to Test1234! during Suite 03 testing.

- **Suite 04 — ArgoCD not in SUPPORTED_PROVIDERS (BUG-RDV-033)**: POST with `provider=argocd` returns HTTP 400 immediately. Canary rollout, progressive deployment, and ArgoCD sync features are entirely untestable. SUPPORTED_PROVIDERS list confirmed: [github, gitlab, bitbucket, jenkins, sonarqube, fossa, prometheus, datadog, zabbix, pagerduty].
- **Suite 04 — Issue-deployment linking not implemented (BUG-RDV-035)**: Deployment detail page has no "Issues Included" section. Redmine issue pages have no deployment badge or sidebar widget. Feature described in rfd-014 not present.
- **Suite 04 — Deploy button not visually disabled for locked/frozen environments (BUG-RDV-032)**: Server correctly returns HTTP 423 (freeze) and flash message (lock), but the Deploy button on the environment card remains enabled. UI gives no pre-emptive visual feedback.
- **Suite 04 — Concurrent pending_approval deployments not blocked (BUG-RDV-037)**: Pre-flight check does not detect an existing pending_approval deployment. Multiple simultaneous pending approvals accumulate for the same environment.
- **Suite 04 — Deployment detail commit SHA not shown; URL field absent (BUG-RDV-034)**: COMMIT field shows "—" even when commit_sha is stored (confirmed via TC-RDV-195 webhook). No "Deployment URL" field on detail page.
- **Suite 04 — Original deployment has no "Reverted by" reverse link (BUG-RDV-036)**: Rollback detail correctly shows "Rollback of #N" link. But the original deployment #N shows no "Reverted by #M" reverse link. Audit trail is uni-directional.
- **Suite 04 — Approval gate well-implemented**: approval_required flag, HTTP 403 for wrong roles, approve/reject workflow, audit trail all work correctly. rate limit (5/hr/env) enforced. Freeze HTTP 423, lock flash message both correct.
- **Suite 04 — Webhook deployment record creation confirmed (TC-RDV-195)**: GitHub deployment_status webhook populates all 8 fields: version (from deployment.ref), commit_sha (from deployment.sha), deployed_by (from sender.login), environment name, status, url (from target_url), finished_at. Use this event to create deployment records in tests.
- **Suite 04 — Local user dev_user (ID 12)**: Developer role in phoenix-platform. Password reset to Test1234! during Suite 04 session 9.
- **Suite 04 — flux-erp-system environment created**: "ERP Staging" (env ID 5) created during TC-RDV-209 IDOR test setup. No deployments were created in flux-erp-system (deploy POST silently ignored when no deploy_provider is configured).
- **Suite 04 — Rollback reason is required**: POST to /create_rollback without rollback_reason returns HTTP 422. Always include rollback_reason in rollback test steps.
- **Suite 04 — Rate limit**: 5 deploys/hour/environment. Resets on the hour. Test env may have hits from previous TCs — check current count before rate limit TCs.

- **Suite 05 — Release Management editor is Quill.js (NOT CKEditor)**: Editor selector is `.ql-editor`. No `.ck-editor__editable` present. All note editing interactions must target `.ql-editor` contenteditable div.
- **Suite 05 — Release Blockers**: "Open blockers" counter detects blockers via (a) priority = "Urgent" or higher, OR (b) "Release Blocker" boolean custom field = Yes. Both mechanisms confirmed working (TC-RDV-215, TC-RDV-216). A custom boolean custom field named "Release Blocker" was created during Suite 05 testing (custom field ID 6, for all projects, all trackers).
- **Suite 05 — Compliance checks**: 4 auto items: "All prs merged" (checks linked PRs), "Approvals in place" (checks approval records), "No open criticals" (checks issue priorities), "Tests last green" (checks latest build status). In test env all show FAIL except "No open criticals" (all linked issues are Low priority). 1 manual item: "Manual qa signoff" — was marked PASS by admin (Redmine Admin, 05/25/2026 10:59 AM).
- **Suite 05 — Status→released blocked by compliance**: Only checked at the PATCH/PUT release update endpoint. Error message lists failing checks: "Compliance checklist incomplete: All prs merged, Approvals in place, Tests last green". Blockers also checked separately: "blocker must be resolved before this release can proceed: #521".
- **Suite 05 — approve_releases permission UI bug (BUG-RDV-041)**: Granting `approve_releases` to QA Engineer role via admin has NO effect on UI. Only `manage_releases` (admin-level) causes Approve/Reject buttons to render. `approve_releases` permission is effectively dead.
- **Suite 05 — HTML export bug (BUG-RDV-040)**: GET /devops_releases/:id/export.html returns raw markdown text with Content-Type: text/html. No markdown-to-HTML conversion. No `<a href>` links for issue references.
- **Suite 05 — No connected git repo**: Phoenix Platform project has no connected GitHub/GitLab repository (message: "No connected GitHub/GitLab repository found for this project"). This blocks TC-RDV-231, 233, 234, 248, 249, 254 (all git-tag and hotfix tests).
- **Suite 05 — qa_user / dev_user passwords**: During Suite 05, both qa_user (ID 13) and dev_user (ID 12) passwords were reset to `Password123!`.
- **Suite 05 — SemverSuggester**: Reads git commits since last tag (v1.9.0 on phoenix-platform). Currently suggests "v1.9.1 PATCH" because only fix/chore commits exist since v1.9.0. feat: commits would trigger MINOR bump; BREAKING CHANGE commits would trigger MAJOR bump.
- **Suite 05 — Release #1**: "v2.0.0 Release" (ID 1, status=ready, tag=v2.0.0, commit=abc1234..., date=2026-06-01). 8 linked issues (#521-524, #527-530 — all "New", "Low" except #521 changed to "Urgent" during testing). #522 has "Release Blocker" custom field = Yes.
- **Suite 05 — Release #2**: "v1.9.0 Release" (ID 2, status=released). 0 linked issues.

- **2026-05-26 fresh instance — BUG-RDV-038, 039, 040 NOT reproduced**: On the fresh Docker instance (project ID=1), environment cards rendered in correct SDLC order (dev→qa→staging→production→uat), the New Release form had a version/milestone dropdown, and the HTML export returned properly formatted HTML with issue links. These three bugs may have been fixed in the current codebase, or the prior session's environment had different setup conditions. Keep bugs open until verified on Forge.
- **2026-05-26 fresh instance — BUG-RDV-044 confirmed**: Environment card relative timestamps produce "8 days days ago" (duplicate word "days"). Reproducible on fresh instance.
- **2026-05-26 fresh instance — BUG-RDV-045 confirmed**: Quill.js editor confirmed via `.ql-editor` and `.ql-toolbar.ql-snow` selectors on fresh instance. CKEditor spec (rfd-112) still not implemented.
- **2026-05-26 fresh instance — BUG-RDV-046 confirmed**: New Release form `#redmineflux_devops_release_name` value="" on page load even when SemverSuggester widget shows suggested version. The "Use this version" button populates the field but auto-population (rfd-084) is not implemented.
- **2026-05-26 fresh instance — TC-RDV-209 PASS (IDOR for deployments)**: Cross-project deployment access via /projects/phoenix-platform/devops_deployments/:id returns 404 for deployments owned by atlas-backend project. Project-scoped lookup confirmed working.
- **Session cookie vs API key precedence**: When navigating with `?key=<api_key>` while an admin session cookie is active, the session cookie takes precedence. Use API-only requests (fetch with credentials:'omit') for accurate permission testing. Cannot use URL ?key= param to test non-admin user access via browser when logged in as admin.

- **Suite 09 — SonarQube webhook drops vulnerability data**: POST /projects/phoenix-platform/devops/sonarqube_webhook returns 202 but zero records are written to devops_vulnerabilities. All 9 SUPPORTED_PROVIDERS vulnerability webhook endpoints only store code quality results (devops_code_quality_results) not vulnerability results — rfd-028 vulnerability ingestion path is absent. TC-RDV-371 FAIL.
- **Suite 09 — Snyk and Trivy NOT in SUPPORTED_PROVIDERS**: Both return HTTP 404. Only sonarqube is accepted for code-quality/vulnerability webhooks. Trivy and Snyk were never added to SUPPORTED_PROVIDERS. TC-RDV-372, TC-RDV-373 FAIL.
- **Suite 09 — FOSSA LicenseRiskMapper broken (BUG-RDV-056)**: All FOSSA webhook dependency records stored with license_name="NOASSERTION" and risk_level="unknown". LicenseRiskMapper cannot extract SPDX identifiers from either `dependencies[].license` or `packages[].licenses[].spdxId` payload formats. Risk filters all return 0 results. Deduplication (unique index on project_id, dependency_name, version, source) WORKS correctly.
- **Suite 09 — SSRF guard absent from sonarqube_host_url (BUG-RDV-057)**: The sonarqube_host_url field in DevOps Settings accepts and saves any URL without IP range validation. AWS metadata URL accepted. No SSRF guard implemented.
- **Suite 09 — XSS in release description (BUG-RDV-058)**: Release description field renders raw HTML/JS on the detail page. `<script>alert('XSS')</script>` executes as stored XSS for all viewers. Not passed through Redmine's HTML sanitizer. To clean up: navigate to release edit, replace description with plain text, save.
- **Suite 09 — Permission gaps on Security tab (BUG-RDV-059)**: `/projects/<id>/devops_vulnerabilities` returns HTTP 200 for Developer role (view_devops only). Should require view_security_scans. Same pattern as BUG-RDV-050 (DORA metrics) and BUG-RDV-011/017 (webhook log, PR dashboard). Multiple controllers use only view_devops as the gate with no secondary permission check.
- **Suite 09 — Compliance checklist filter params**: Correct param names: `audit_action` (not `action_type`), `actor_id`, `from`, `to`, `project_id`. Discovered via form introspection.
- **Suite 09 — Release approval NOT logged in audit trail (TC-RDV-402 FAIL)**: Approving a release via the Approvals section does not create an audit entry. Only environment_lock actions are confirmed to be logged.
- **Suite 09 — devops_engineer login**: ID=13, login=devops_engineer, API key=6c1407ae276e5f449d0e6bd88b3bd9f93c32a852. Role "DevOps" in phoenix-platform.
- **Suite 09 — dev_user update**: ID=5, login=dev_user (NOT dev_user from Forge session — different instance). API key=2309f6ea9f3335476ebc78cf241cc27c8da0e12e. Password on Local Docker remains unknown (reset attempts did not persist; UI showed "Successful update" but login failed).

- **Suite 10 — Incident form field prefix**: Form fields use `redmineflux_devops_incident[field_name]` prefix (e.g., `redmineflux_devops_incident[severity]`). NOT `devops_incident[...]`. Verified via DOM inspection of the New Incident form.
- **Suite 10 — Action buttons are `input[type=submit]`**: All action buttons on the incident detail page (Acknowledge, Resolve, Generate Post-Mortem, Save Correlation) are `<input type="submit" value="...">` elements, NOT `<button>` elements. Use `Array.from(document.querySelectorAll('input[type=submit]')).find(b => b.value === 'X').click()` to interact with them.
- **Suite 10 — Post-mortem wiki naming convention**: Auto-generated post-mortem wiki pages are named `DevOps_Incidents_Post_Mortem_{incident_id}`. Regeneration creates new wiki versions: v1, v2, v3 (all preserved in wiki history). Navigate to `/projects/<id>/wiki/DevOps_Incidents_Post_Mortem_{id}` to view.
- **Suite 10 — Escalation settings in DevOps Settings**: "Incident Escalation Rules" section exists in DevOps Settings admin page. Fields: `escalation_sev1_timeout_minutes` through `escalation_sev4_timeout_minutes`. All save and persist correctly. Cannot auto-trigger escalation via clock simulation in Playwright tests.
- **Suite 10 — Communication templates modal URL**: `/projects/<identifier>/devops_incidents/{id}/comms`. Opens a modal with template selector, preview, and Send Update button. Variable substitution ({{incident_id}}, {{affected_service}}, {{severity}}, {{started_at}}) confirmed working in Preview. TC-RDV-458 PASS.
- **Suite 10 — RBAC gap pattern (manage_incidents)**: Developer role can access `/devops_incidents/new` (HTTP 200) despite not having `manage_incidents` permission. Same pattern as BUG-RDV-050 (DORA), BUG-RDV-059 (Security Vulnerabilities), BUG-RDV-011/017 (webhook log, PR dashboard). Multiple controllers use only `view_devops` as the gate. New bug: BUG-RDV-065.
- **Suite 10 — Incidents created during testing**: #6 (SEV2, Payment API), #7 (SEV1, Checkout Service, linked to Redmine issue #39), #8 (SEV3, Auth Service), #9 (SEV4, Config Service), #10 (blank affected_service → "Unknown Service"). Incident #1 fully resolved (MTTR=1h 27m); post-mortem wiki: DevOps_Incidents_Post_Mortem_1 (versions 1, 2, 3).
- **Suite 10 — Incidents JSON REST API returns 406**: `GET /projects/phoenix-platform/devops_incidents.json` returns HTTP 406 (BUG-RDV-064). The controller's index action has no `respond_to :json` block. Compare to the builds/deployments JSON endpoints which do support JSON format.
- **Suite 10 — Timeline REST API not routed**: All URL patterns for incident timeline entries return 404 (BUG-RDV-063). No `/timeline.json` or `/entries.json` route exists for incidents.
- **Suite 10 — devops_engineer escalation access**: devops_engineer user (ID 13) can access and save escalation timeout settings in DevOps Settings. Can create and manage incidents. Correct behavior — confirmed TC-RDV-455 PASS.

- **Suite 11 — Freeze creation rejects past-date start times (400)**: Server returns HTTP 400 when `starts_at` is in the past. To test active-freeze blocking without clock simulation, create a freeze with starts_at within the next few minutes. Workaround for testing expired-freeze scenarios: delete the active freeze and confirm deploy button re-enables.
- **Suite 11 — Freeze blocking mechanism confirmed**: `DeploymentDispatcher` returns HTTP 423 with JSON `{"error":"Deployment blocked by freeze: <reason>"}`. Deploy button `title` attribute shows the exact freeze reason when disabled. The title attribute is the reliable check for identifying whether a button is disabled due to freeze vs lock vs other.
- **Suite 11 — Lock mechanism confirmed**: `DevopsLock.openModal(envId)` opens the lock form modal (id: `devops-lock-form`). Fields: `lock_reason` and `locked_until`. Lock blocks deploy via HTTP 423 same as freeze. `unexpired_locks` scope auto-expires lock when `locked_until` passes — no manual unlock needed.
- **Suite 11 — Admin deploy modal no override path (BUG-RDV-074)**: The deploy modal (triggered by `DevopsDeploy.openModal(envId)`) shows only Version/Branch and Notes fields. No freeze-related warning or Override Freeze button rendered, even for Admin. API also returns 423 with no override endpoint.
- **Suite 11 — Environment comparison endpoint**: Route is `/projects/<identifier>/devops_environments/compare?a=<id>&b=<id>`. Caching keyed on both environment SHAs (60-second TTL). Same-SHA returns "No differences" empty state. non_member gets 403. Does NOT validate env existence — non-existent IDs cause 302 redirect instead of 404 (BUG-RDV-076).
- **Suite 11 — Environment Request form and tracker**: The form ID is `env-request-form`. Creates a Redmine issue with tracker "Environment Request". Issue #40 was created during Suite 11. Issues of "Environment Request" tracker type accessible only at `/issues/<id>` (global route) — NOT `/projects/<id>/issues/<id>` (returns 404 for this tracker). This is the same quirk noted for "Environment Request" issues (previously documented for this tracker type).
- **Suite 11 — No environment_type field on forms (BUG-RDV-072)**: The Add Environment and Edit Environment forms do not render a `environment_type` / "Type" dropdown. All environments have implicit null type. Auto-ordering (dev→staging→prod→custom) and the `by_type` REST API scope both depend on this field being settable.
- **Suite 11 — Health check UI entirely absent (BUG-RDV-073)**: No Check Now button, no health status light, no last-check timestamp on any environment row. The `EnvironmentHealthChecker` service (rfd-033) is not exposed in the UI. Health check polling and SSRF guard are unverifiable without this feature.
- **Suite 11 — Current environment state after Suite 11**: Freezes: only "Weekly Release Window Freeze" (ID 2, recurring, all envs, Fri 17:00–23:59 UTC) remains. staging-main (env 9) LOCKED, reason "QA is running regression suite", expires 2026-05-29 12:00. production (env 3) LOCKED, reason "Pre-demo freeze: investor presentation…", expires 2026-06-01 16:00. Issue #40: Environment Request issue with HTTP 500 on detail page.

- **Suite 12 — Notification preferences flash text (CORRECTED)**: Flash text after saving `/my/devops_notifications` via PATCH is "Account was successfully updated." — NOT "DevOps preferences updated." (previous note was wrong). Save route must be PATCH; POST to /my/devops_notifications returns 404. Verified TC-RDV-497 PASS.
- **Suite 12 — Plugin settings Apply button quirk (Playwright)**: `browser_click` on `<input type="submit" name="commit" value="Apply">` does NOT trigger form POST in Playwright MCP context. Values typed in form fields are not persisted. Fix: use `Array.from(document.querySelectorAll('form')).find(f => f.action.includes('settings/plugin')).submit()` JavaScript via `browser_evaluate` to correctly POST the form.
- **Suite 12 — SettingsValidator error format**: Plugin settings validator error messages follow format "field_name: must be an integer between X and Y" (e.g., "stale_pr_threshold: must be an integer between 1 and 365"). Confirmed TC-RDV-509.
- **Suite 12 — Plugin settings extra fields**: The plugin settings page at `/settings/plugin/redmineflux_devops` includes additional fields not listed in the TC spec: `sonarqube_webhook_token` (masked text field), `gauge_warn_threshold` (integer), `gauge_critical_threshold` (integer). All three persist correctly.
- **Suite 12 — Notification matrix missing 7 event types (BUG-RDV-078)**: Only 7 event type rows present in `/my/devops_notifications` matrix: code_review_requested, daily_code_review_digest, incident_escalation, build_failed, build_recovered, build_succeeded, pull_request_merged. Missing: deployment_success, deployment_failed, incident_created, incident_resolved, release_published, alert_fired, security_gate_blocked. Per rfd-101 a minimum of 10 event types required.
- **Suite 12 — devops_engineer (ID 13) password**: Reset to `TestPass123!` during Suite 12 testing. Previous password was unknown; reset confirmed via admin edit page.

## Recurring Issues

- HTTP status code semantics are wrong in two places: 401 returned for signature failure (should be 403), and 400 returned for unsupported provider (should be 404) — BUG-RDV-001, BUG-RDV-005
- SonarQube and FOSSA integrations are completely blocked due to missing UI configuration points — BUG-RDV-003, BUG-RDV-004
- Access control gaps at REST API layer: project-scoped authorisation absent (BUG-RDV-008) and wrong permission check on webhook event log (BUG-RDV-011)
- ActivityProvider not registered — all activity stream TCs fail for the same root cause (BUG-RDV-010)
- **Authorization before_action guards missing from multiple controllers (systemic)**: webhook event log (BUG-RDV-011), PR Dashboard + Commits (BUG-RDV-017), DORA Metrics (BUG-RDV-050), Security Vulnerabilities (BUG-RDV-059), New Incident form (BUG-RDV-065). Pattern: controllers use only `view_devops` as the gate — secondary permissions (manage_devops_settings, view_security_scans, manage_incidents, etc.) are not enforced at the controller level. Now confirmed across 5 separate controllers. Priority fix.
- **Export permission misconfigured (BUG-RDV-079)**: Release Notes export actions (`.md` / `.html`) use `manage_releases` guard instead of `view_devops`. Read-only export should be gated at `view_devops` level. Same class of issue as the missing secondary checks above but inverse — a read action is gated on a write permission.
- **Security vulnerabilities in release management**: XSS in release description (BUG-RDV-058), SSRF in sonarqube_host_url (BUG-RDV-057). Neither field is sanitized/validated.

## Environment Notes

- Forge environment credentials: QA_CREDENTIALS_FORGE.md
- Local environment credentials: QA_CREDENTIALS_LOCAL.md
- Never hardcode credentials or base URLs in test files or test case steps
- For webhook tests: the Redmine instance must be reachable from the CI tool's network (or use a local tunnel for local testing)
- Source plugin code location: c:\redmine\plugins\redmineflux_devops\
- Source RFD files: c:\redmine\plugins\redmineflux_devops\backlog\done\ (121 files: 81 features, 33 bugs, 7 tasks)
- Source docs location: c:\redmine\plugins\redmineflux_devops\docs\
- Local Docker environment: http://localhost:3008 (container xenodochial_heisenberg, port 3008→3000)
- Cloudflare tunnel (ephemeral): https://verbal-arms-daisy-vienna.trycloudflare.com → restart command: `& "$env:TEMP\cloudflared.exe" tunnel --url http://localhost:3008`
- Bitbucket auth quirk: plugin uses `Bearer #{api_token}` but Atlassian scoped API tokens require `Basic base64(email:token)` auth. Test Connection always fails for Bitbucket on this environment. Webhook delivery still works via X-Hub-Signature. Bitbucket workspace is `sourabhworkspace` (not `sourabhworkspace-admin`). Repo: sourabhworkspace/devops-plugin-test.
- Local env connection IDs: GitHub ID 1 (connected), GitLab ID 2 (connected), Bitbucket ID 3 (error — expected due to auth mismatch). All on project flux-erp-system.

- **Session 13 retest (2026-05-26) — 22 bugs FIXED since original filing**: BUG-RDV-001–005 (HTTP status codes + SonarQube/FOSSA config), BUG-RDV-007 (commit storage background job now works — RecordInvalid fixed), BUG-RDV-012 (API limit param honoured), BUG-RDV-013 (JSON format supported on all 10 endpoints), BUG-RDV-016 (PR dashboard shows only open PRs, sorted by age), BUG-RDV-018 (commit stats route now exists), BUG-RDV-020 (GitLab SHA links use `/-/commit/`), BUG-RDV-021 (auto-transition per-rule UI present), BUG-RDV-022 (multi-commit push stores all commits), BUG-RDV-024 (custom date pickers in builds filter), BUG-RDV-030 (Slack+Teams columns in notification prefs), BUG-RDV-032 (deploy button disabled for locked env), BUG-RDV-033 (ArgoCD in provider dropdown), BUG-RDV-034 (commit SHA link + deployment URL on detail), BUG-RDV-036 (original deployment shows "Reverted" after rollback), BUG-RDV-038 (env cards in SDLC order), BUG-RDV-039 (version/milestone dropdown in new release form), BUG-RDV-040 (HTML export is proper HTML, not raw markdown). This represents significant plugin quality improvement between the original sessions and the fresh-instance retest.
- **Session 13 — Issue pages return HTTP 500 in fresh env**: ALL issue pages (`/issues/<id>`, `/projects/phoenix-platform/issues/<id>`) return 500 for any issue number in the fresh local Docker instance. Root cause unknown — likely a DevOps ViewHook rendering exception during issue page render. Blocked BUG-RDV-014 (PR reviewer card), BUG-RDV-019 (multi-issue PR linking), BUG-RDV-023 (build badge JS). Must investigate ViewHook error before these bugs can be retested.
- **Session 13 — Rollback API confirmed**: POST to `/projects/<identifier>/devops_deployments/<id>/create_rollback` with body `{"rollback_reason":"<text>"}` and `X-Redmine-API-Key` header creates a new rollback deployment. Rollback_reason is REQUIRED — omitting it returns 422. The original deployment subsequently shows "Reverted" text on its detail page.
- **Session 13 — Fresh env facts**: Deployments at start: IDs 1–14 (webhook+API created). Deployments at end of session: IDs 1–16 (deployment #15 may exist; #16 = rollback of #5 created during BUG-RDV-036 retest). Total commits grew from seeded count to ~49 as push webhooks were sent during the session.
- **Session 13 — dev_user API key on fresh instance**: `2309f6ea9f3335476ebc78cf241cc27c8da0e12e`. dev_user (ID 12) is a Developer on phoenix-platform. Correctly returns 403 on atlas-backend project endpoints — project-scoped protection working on local (IDOR only confirmed on Forge where REST API ignores project membership).
- **Session 13 — BUG-RDV-007 fix mechanism**: On the fresh instance, push webhooks with `repository.html_url` field now correctly store commits. Previously the background job failed with ActiveRecord::RecordInvalid. The fix appears to validate or provide a fallback for the repository lookup so that the commit enrichment step no longer raises. SHA format abc123def456 (12 hex chars) confirmed storable.

- **Session 14 — Forge flux-fujhcd9zj49 retest (2026-05-26) — 15 bugs FIXED**: BUG-RDV-008 (IDOR — project-scoped auth now enforced, daisy.skye gets 403 on non-member projects), BUG-RDV-010 (ActivityProvider registered — 5 DevOps filters in activity sidebar), BUG-RDV-014 (PR reviewer names shown on PR card; approval state still partially absent — closed as partial fix), BUG-RDV-015 (auto-transition journal correct format "[DevOps] Status changed by PR #N merged", system actor), BUG-RDV-019 (multi-issue PR linking works — all referenced issues transitioned), BUG-RDV-023 (badge JS uses numeric project ID — `data-project-id="7"`, badge polls correctly), BUG-RDV-027 (dashboards page loads with Build Queue widget present), BUG-RDV-031 (build duration stored and displayed on detail page), BUG-RDV-037 (concurrent approval guard working — flash "A deployment (#N) is already pending approval for ENV. Approve or reject it before deploying again."), BUG-RDV-041 (approve_releases permission enforced bidirectionally via daisy.skye / Developer role), BUG-RDV-042 (deployments list shows per-environment current-version summary row), BUG-RDV-043 (deployments history has date range filter), BUG-RDV-044 (timestamp duplicate "days" fixed), BUG-RDV-045 (CKEditor now used for release notes, not Quill.js), BUG-RDV-046 (New Release form pre-populated with SemverSuggester output).
- **Session 14 — Forge flux-fujhcd9zj49 environment facts**: Admin API key `5d310678d004cee56dda8597266cf81d32d80c3e`. daisy.skye API key `eabeffbcd64b524ebd1f61f328a0474de071b336` (Developer role, ID 6, member of agileboard). Project agileboard ID 7, identifier "agileboard". Staging environment ID 20 has `approval_required = true`. Developer role ID 7 on Forge.
- **Session 14 — Release #42 "2026.6 Atlas"**: Used for BUG-RDV-041 (approve_releases) testing on Forge. Release is in "ready" or "planning" status on agileboard project.
- **Session 14 — BUG-RDV-037 fix mechanism**: `DeploymentDispatcher.deploy_from_redmine!` (lib/redmineflux_devops/services/deployment_dispatcher.rb, lines 121-130) contains a pre-flight guard that raises `ApprovalAlreadyPending` if any `pending_approval` deployment exists for the environment. The `pending_approval` state is only set via this method, not via direct API create (which always sets `in_progress`). Flash message template: "A deployment (#N) is already pending approval for ENV. Approve or reject it before deploying again."
- **Session 14 — BUG-RDV-023 fix mechanism**: `app/views/hooks/_devops_issue_panel.html.erb` line 64 uses `data-project-id="<%= project.id %>"` (numeric) not the string identifier. Comment on lines 61-63 explains the fix. Both numeric and string identifiers now accepted by the REST API (both return 200 on Forge).
- **Session 14 — Remaining open bugs on Forge (7 total)**: BUG-RDV-009 (DORA 404 — route not implemented), BUG-RDV-011 (webhook log accessible with view_devops only), BUG-RDV-017 (PR/Commits pages return 200 without view_devops), BUG-RDV-026 (JUnit ingestion 422 — rexml missing), BUG-RDV-028 (artifacts not testable — no real CI data), BUG-RDV-029 (no pipeline stages section), BUG-RDV-035 (no issue-deployment linkage on deployment detail or issue pages).
- **Session 14 — Deploy form submission quirk**: Submitting the Deploy modal form via `fetch()` with `redirect:'follow'` follows the redirect but does not fully trigger server-side processing. Use native `form.submit()` to trigger actual browser navigation and server-side form processing. The deployment route is `POST /devops/deployments` (not `/projects/<id>/devops_deployments`) — the latter returns 404.
- **Session 14 — Role edit form selection**: On `/roles/<id>/edit`, `document.querySelector('form')` selects the Redmine search form (first form on page). Use `[...document.querySelectorAll('form')].find(f => f.action?.includes('/roles/<id>'))` to select the correct role-edit form before submitting permission changes.

- **Suite 06 — Test duration monitoring endpoint missing (BUG-RDV-047)**: `GET /projects/<id>/devops/test_duration_stats.json` returns 404 for both authenticated and unauthenticated requests. All URL pattern variations tried (5 patterns, all 404). The TestDurationStats service (rfd-114) and its REST endpoint are not registered in routes. TC-RDV-273, 274, 275, 285 blocked.
- **Suite 06 — Coverage tracking feature not implemented (BUG-RDV-048)**: No dedicated coverage page, no coverage trend widget, and no codecov/coveralls webhook support. `codecov` provider not in SUPPORTED_PROVIDERS. All coverage URL patterns return 404. The rfd-021 feature is entirely absent. TC-RDV-262, 263, 279, 287 blocked.
- **Suite 06 — Traceability page tag format**: The Traceability empty state says tags should be `#<issue_id>`, `[issue-<id>]`, or `@issue(<id>)`. TC-RDV-266 spec used `[#101]` format — this may or may not be recognized. Cannot verify without JUnit ingestion working (BUG-RDV-026).
- **Suite 06 — Traceability CSV endpoint confirmed**: `GET /projects/phoenix-platform/devops_traceability.csv` returns 200 with `Content-Type: text/csv; charset=utf-8`. Header row: `issue_id,issue_visible,test_suite,test_name,status,provider,repository,branch,build_number,duration_ms,latest_at,build_id`. More columns than TC-RDV-268 spec expected (spec: Issue ID, Subject, Test Name, Status). Actual columns include provider/repo/branch context.
- **Suite 06 — Build detail "Failed Tests" section**: The build detail page shows a "Failed Tests" section (not "Test Results"). Without JUnit data it shows "No failed tests for this build." It's unknown if a separate "Test Results" summary section (pass/fail/skip counts) appears once data is ingested — this should be verified when BUG-RDV-026 is fixed.
- **Suite 06 — non_member user (ID 11, login: non_member, API key: 2eb365ddee77713a9fb0fd66d31be0e2cec3e774)**: Confirmed not a member of phoenix-platform. HTTP 403 returned on devops_builds list and build detail. Useful for non-member access control tests in future suites.
- **Suite 06 — SUPPORTED_PROVIDERS confirmed**: [github, gitlab, bitbucket, jenkins, sonarqube, fossa, prometheus, datadog, zabbix, pagerduty]. Coverage providers (codecov, coveralls) not in list. ArgoCD was added per session 14 Forge fix but not verified on local for this session.

- **Suite 07 — DORA metrics UI URL**: The correct Redmine path is `/projects/<identifier>/devops_metrics` (NOT `/devops/metrics`). The JSON API endpoint is `/projects/<identifier>/devops_metrics.json`. The route `/devops/metrics/dora.json` 404s (BUG-RDV-009).
- **Suite 07 — DORA JSON API response structure**: `GET /projects/<id>/devops_metrics.json` returns `{deployment_frequency, lead_time, change_failure_rate, mttr, throughput, cycle_time}` with nested `{value, classification}` per metric. Classification can be `elite`, `high`, `medium`, or `low`.
- **Suite 07 — DORA classification color scheme (CSS rgb values)**: Elite=`rgb(223, 240, 216)` (green), High=`rgb(217, 237, 247)` (blue), Medium=`rgb(252, 248, 227)` (yellow), Low=`rgb(242, 222, 222)` (red). Use `window.getComputedStyle` with `background-color` on the metric card element.
- **Suite 07 — Period filter**: The Apply button is `<input id="metrics_apply_btn" type="submit" value="Apply">` — NOT a `<button>`. Use `#metrics_apply_btn` selector. Filter submits as a form with `?period=7|30|90`.
- **Suite 07 — DORA zero-data state bug (BUG-RDV-049)**: When a project has 0 deployments or 0 incidents, the DORA cards show classification badges (e.g., "Low", "Elite") alongside value "0" instead of "N/A". Expected: N/A when no meaningful data exists (per rfd-022). Affects TC-RDV-302 and TC-RDV-323.
- **Suite 07 — RBAC gap: QA Engineer can access DORA metrics (BUG-RDV-050)**: The plugin uses a single `view_devops` permission check for the metrics page. QA Engineer role has `view_devops`, so they get HTTP 200 on `/devops_metrics`. No separate "view DORA metrics" permission is enforced. Affects TC-RDV-318.
- **Suite 07 — Shared dashboard flag response (BUG-RDV-051)**: PATCH to update `shared=1` by a user without `manage_devops_settings` returns HTTP 200 (correct would be 403). The shared flag is silently NOT saved (data protection is correct). The edit form correctly hides the shared checkbox for unauthorized users. Affects TC-RDV-316.
- **Suite 07 — Dashboard builder URL**: `/projects/<id>/devops/dashboards`. New dashboard form: `redmineflux_devops_dashboard[name]`, `redmineflux_devops_dashboard[shared]`, `redmineflux_devops_dashboard[layout_json]`.
- **Suite 07 — Dashboard widget palette**: 9 widgets available: CICD-BuildQueue, DORA-DeploymentFrequency, DORA-LeadTime, DORA-CFR, DORA-MTTR, FLOW-Throughput, FLOW-CycleTime, QUALITY-FlakyTests, SECURITY-OpenVulnerabilities.
- **Suite 07 — Chart canvas elements**: 3 `<canvas>` elements render on the DORA metrics page: Throughput Trend, DORA Classification Mix (pie/donut), and Cycle Time Distribution. Confirmed by counting canvas elements via Playwright evaluate. Exact data values cannot be verified without sufficient deployment history.
- **Suite 07 — IDOR protection for metrics confirmed**: Querying `/projects/atlantis-api/devops_metrics.json?environment_id=3` (where env 3 belongs to phoenix-platform) returns atlantis-api's own deployment count (0), not phoenix-platform data. Project-scoped queries working. TC-RDV-304 PASS.
- **Suite 07 — Fresh instance user IDs (corrected)**: On the fresh Docker instance (admin key aa7237…, project phoenix-platform ID=1), dev_user = ID 5 (Developer role), qa_user = ID 6 (QA Engineer role). The earlier memory entries referencing ID 12/13 belong to the xenodochial_heisenberg instance. Passwords reset to `TestPass123!` during Suite 07 testing.
- **Suite 07 — CSRF token for dashboard form**: `document.querySelector('form')` picks up the Redmine search form (first form on page). Use `[...document.querySelectorAll('form')].find(f => f.action?.includes('dashboards'))` to target the correct dashboard form for CSRF token extraction.

- **Suite 08 — Monitoring webhook token**: Initially masked in admin plugin settings UI — value was unknown. Reset to `MonitorToken123` via POST to `/settings/plugin/redmineflux_devops` with admin credentials. Use `X-DevOps-Token: MonitorToken123` header for all prometheus, datadog, zabbix, and pagerduty webhook tests on this instance.
- **Suite 08 — Alert feed URL**: `/projects/<identifier>/devops_alerts`. Renders alerts table with severity filter (`name="severity"`) and status filter (`name="status"`). No environment filter control exists (BUG-RDV-053).
- **Suite 08 — Alert ingestion**: Prometheus AlertManager, Zabbix, and Datadog webhook payloads all accepted with HTTP 202. Deduplication by `fingerprint` field works (upsert — second payload with same fingerprint updates, no duplicate row). Confirmed TC-RDV-329, TC-RDV-358.
- **Suite 08 — Alert summary bar**: Present at top of devops_alerts page. Shows "X Firing", "Y Critical", "Z Resolved Today" counts matching live database state. Increments correctly after new webhook (TC-RDV-362 PASS).
- **Suite 08 — Alert resolution webhook**: Sending a resolved webhook (same fingerprint, `status: resolved`) updates alert status to "Resolved" and sets `resolved_at` timestamp. However, no computed duration ("Xh Ym") column is rendered in the alert row. TC-RDV-333 and TC-RDV-363 FAIL.
- **Suite 08 — Alert access control**: Non-member user (ID 11) receives HTTP 403 on `/projects/phoenix-platform/devops_alerts` (TC-RDV-359 PASS). dev_user (ID 5, Developer with view_devops) gets HTTP 200 (TC-RDV-360 PASS). Invalid/tampered X-Hub-Signature-256 returns 403 (TC-RDV-356 PASS). Missing X-DevOps-Token returns 401 (TC-RDV-357 PASS).
- **Suite 08 — AlertIncidentCreator tracker bug (BUG-RDV-052)**: AlertIncidentCreator auto-creates Redmine issues using tracker "Bug" (ID 4), not "Incident" (ID 3). "Incident" tracker exists in the system. Priority "Urgent" and subject format "[Incident] {alertname} on {instance}" are correct. Only tracker ID is wrong. Confirmed on issues #34, #35, #37, #38.
- **Suite 08 — rfd-093 On-Call Schedule not implemented (BUG-RDV-054)**: No on-call widget on any DevOps page. HTML of devops_builds, devops_environments, devops_incidents, devops_alerts contains no reference to "pagerduty", "opsgenie", "on-call", "on_call", or "oncall". TC-RDV-342–345, 366 all BLOCKED.
- **Suite 08 — rfd-118 Infrastructure Resource Usage not implemented (BUG-RDV-055)**: Environment cards show only the `devops-uptime-badge` widget ("UPTIME - 30d / 90d: - / Trend: -"). No CSS classes `gauge`, `cpu`, `memory`, or `resource` exist on the environments page. TC-RDV-350–355, 368, 369 all BLOCKED.
- **Suite 08 — Performance metrics connection endpoint**: POST to `/projects/phoenix-platform/devops/performance/connection` returns HTTP 400 for all URL values tested (public and private, both form-encoded and JSON body). Cannot configure a monitoring connection to drive performance charts. TC-RDV-346–349, 364, 365 all BLOCKED.
- **Suite 08 — Uptime widget data source**: Uptime badge reads from `redmineflux_devops_health_samples` table. Table is empty in fresh Docker instance — widget shows "30d / 90d: - / Trend: -" placeholder. TC-RDV-340, 341, 367 BLOCKED (cannot seed data via Playwright alone).

- **Suite 13 — Security tab visible in sub-nav for Developer (BUG-RDV-059 still open)**: The Security tab link IS present in the DevOps sub-nav even for `dev_user` (Developer role, view_devops only). Navigating to the page returns HTTP 403 (controller now enforces the permission) but the nav link itself is still visible — the view layer has not been updated to hide the link for roles without `view_security_scans`. TC-RDV-536 PARTIAL because the nav item leaks intent even though the page is blocked.
- **Suite 13 — view_devops is the minimal gate for all DevOps pages**: All seven permissions (view_devops, manage_devops_settings, trigger_builds, approve_deployments, manage_releases, manage_incidents, view_security_scans) correctly block non-members and users with no DevOps permissions at all. The recurring bug class is *secondary permission not checked*: the controller checks view_devops first and, in several places, stops there instead of adding a second check for the narrower permission.
- **Suite 13 — Export gated on wrong permission (BUG-RDV-079)**: `GET /devops_releases/:id/export.md` and `GET /devops_releases/:id/export.html` check `manage_releases` instead of `view_devops`. Both dev_user and qa_user (who has approve_releases but not manage_releases) receive 403. Generate notes (POST) correctly requires manage_releases. Only the export (read) action is wrong.
- **Suite 13 — override_security_gate route returns 404**: `POST /projects/phoenix-platform/devops_environments/:id/override_security_gate` returns 404. The feature is not implemented. TC-RDV-542 and TC-RDV-543 (override security gate with and without permission) are both BLOCKED.
- **Suite 13 — qa_user (ID 6) has approve_releases = true**: QA Engineer role (ID 3) has `view_devops: true` and `approve_releases: true`, all other DevOps permissions false. Confirmed via `/roles/3/edit` DOM inspection. qa_user correctly sees the Approve/Reject buttons on the approvals page and can approve a release (TC-RDV-532 PASS). qa_user cannot export release notes (BUG-RDV-079).
- **Suite 13 — Multi-role union confirmed working**: multi_role_user (Manager+Developer roles, ID 26) has the union of both roles' permissions. Gets 200 on all DevOps pages. Can deploy, manage incidents, view security scans, manage settings. TC-RDV-541 PASS.
- **Suite 13 — approve_deployments correctly enforced**: alice (Manager, approve_deployments) can approve pending deployments. dev_user (Developer, no approve_deployments) gets 403 on the approval endpoint. TC-RDV-529/530/531 PASS.
- **Suite 13 — Roles and user IDs confirmed (Session 22, Local)**: alice=ID 14 (Manager), dev_user=ID 5 (Developer), devops_engineer=ID 13 (DevOps), qa_user=ID 6 (QA Engineer), multi_role_user=ID 26 (Manager+Developer). All passwords: TestPass123! (reset during session).
- **Suite 13 — Release v3.0.0 (ID 5)**: Created by alice during TC-RDV-530 testing. Approved by qa_user during TC-RDV-532. Used for export tests TC-RDV-544/545.
- **Suite 13 — Deployment 18**: prod-eu (env 8), approval_required=true, still in pending_approval state at session end. Created during TC-RDV-529 for approve_deployments tests.

- **Suite 14 — E2E workflows blocked by missing GitHub integration**: TC-RDV-546, 547, 556, 559 all BLOCKED — phoenix-platform has no GitHub repo connected in Local Docker. Any TC requiring commit webhooks, PR webhooks, build badges, or git tag creation via GitHub API cannot be executed.
- **Suite 14 — Staging environment has no deploy provider**: `Deploy provider is not configured for this environment` flash message when attempting to deploy to staging (env ID 2). TC-RDV-557 (deployment approval gate) stops at deploy button — approval gate step unreachable. Need to configure a deploy provider for staging to test this flow.
- **Suite 14 — Dashboard builder widget key format is `widget_key` (not `key`)**: When injecting layout JSON into `#devops-dashboard-layout-json`, use `{widget_key: "dora.deployment_frequency", x, y, w, h}` — the server validates for `widget_key` presence and returns validation errors for `key`. `DevopsDashboardBuilder.init()` can be called after setting `data-initial-layout` to force re-render.
- **Suite 14 — Communication template modal uses `input[type=submit]`**: The "Send Update", "Preview", and "Cancel" buttons in the Send incident update dialog are `input[type=submit]` not `<button>` elements. `button:has-text()` Playwright selectors will fail — use `document.querySelectorAll('input[type="submit"]').find(...)` pattern.
- **Suite 14 — Communication template preview correctly substitutes variables**: Preview for "Initial detection" template substitutes `{{affected_service}}` → incident service name, `{{severity}}` → SEV level, `{{status}}` → current status. Variable substitution confirmed working in Local Docker.
- **Suite 14 — Send comms template → no timeline entry (BUG-RDV-069 confirmed)**: After clicking "Send Update" (with valid recipient), the incident timeline does NOT gain a new entry. Timeline only shows the original "Incident created" event. SMTP disabled returns "Email delivery is disabled" error correctly, but no journal/log entry is written for the send attempt.
- **Suite 14 — Freeze window ID 5 deleted during Suite 14**: "Product Launch Freeze" (ID=5) was deleted during TC-RDV-551 testing. Admin re-login was needed after CSRF invalidation from prior fetch() POST calls. Current freeze state: only "Weekly Release Window Freeze" (ID 2) remains active.
- **Suite 14 — Incident #11 (Payment Gateway, SEV2)**: Created by DevOps Engineer. Status: Investigating. Root cause: "Elevated error rate in payment processing - TC-533 test". Used for TC-RDV-558 comms template testing. No communication entries in timeline (BUG-RDV-069).
- **Suite 14 — Dashboard "Sprint Health" (ID 3) created**: Owner: Redmine Admin. 4 DORA widgets: Deployment Frequency (0.03/day), Lead Time (0h), Change Failure Rate (66.7%), MTTR (0h). Shared=Yes. Created 2026-05-28.
- **Suite 14 — Replay prevention confirmed (TC-RDV-555 PASS)**: First Prometheus alert delivery with `groupKey` → HTTP 202; identical replay → HTTP 200 `{status:"ok",duplicate:true}`. Alert feed shows only 1 entry. Deduplication is by `groupKey` field in the Prometheus payload body.
