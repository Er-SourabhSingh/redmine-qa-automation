# QA Status Dashboard

| Plugin | Last Tested | Redmine Version | Open Bugs | Status |
|--------|-------------|-----------------|-----------|--------|
| redmineflux_testcase_management | 2026-09-02 | (fill from environment) | 2 | In Progress — CSV Import feature (external session, folded into repo): 16 TCs (`TESTCASE_MANAGEMENT_CSV_IMPORT.md`), 14 PASS / 2 FAIL. 2 open bugs: BUG-TCM-001 (Medium, whitespace-padded step header drops its value on import), BUG-TCM-002 (Medium, duplicate column header silently discards the second occurrence). Requirements/user-guide docs still empty; rest of plugin surface not yet scoped. |
| redmineflux_advanced_field | 2026-05-21 | 6.0.9.stable | 0 | In Progress (51/51 positive TCs PASS, 2 bugs fixed & closed; workflow TCs TC-RAF-051–054 pending) |
| redmineflux_scarlet | 2026-06-04 | 6.1.2.stable.24650 | 4 | In Progress — Sessions 1+2 complete. 39 TCs total: 37 PASS, 1 FAIL (TC-RSC-005 priority icons), 1 INFO (Gantt on ztflux — module disabled). All core modules covered. 4 open bugs: BUG-RSC-001 activity.css 404 (Medium), BUG-RSC-002 lastJstPreviewed JS (Medium), BUG-RSC-003 priority SVG icons 404 (High), BUG-RSC-004 Tribute double-bind (Low). Remaining optional: issue attachments, saved queries, Wiki/Forum/News CRUD, project archive/delete, admin CRUD flows. |
| redmineflux_mcp | 2026-06-18 | Flux (dev-flux.zehntech.com) | 3 | In Progress — BUG-RFM-002 closed (not a bug — invalid to call team_data on deleted ID). 3 open: BUG-RFM-004 workload_show 500 on deleted ID (Medium), BUG-RFM-009 team author not shown (Low), BUG-RFM-010 Gantt overload clip UI-only (High). BUG-RFM-011 closed (fixed). 3 TCs still blocked (083/085/086). |
| redmineflux_mcp_issuetemplate | 2026-06-16 | Flux (dev-flux.zehntech.com) | 2 | In Progress — Sessions 1+2. 27 TCs: 22 PASS, 3 FAIL, 0 PENDING, 2 N/A. All 5 permissions verified for test1 in ztflux: View✓ Create✓ Edit✓ Apply✓ Delete✓. 2 open bugs: BUG-RIT-001 list_templates 500 on project_id filter (High); BUG-RIT-002 all template ops bypass project-level permission — server uses ANY-project check not per-target-project (High, found via test1 in gdaplt with no template perms). |
| redmineflux_mcp_checklist | — | — | 0 | Not Started |
| redmineflux_crux | — | — | 0 | Not Started — folder scaffolded 2026-09-10; requirements/features-list/user-guide docs still empty, no testing done yet. |
| redmineflux_mcp_knowledgebase | 2026-06-19 | 5.x (localhost:3006) | 0 | **Complete** — Sessions 1+2+3: 39/39 TCs PASS via MCP tools. BUG-RKB-001 (Critical — MCP POST/PUT TypeError) found, fixed, and closed. Fix: added `params` kwarg to `RedmineClient.post()/put()`. All 14 KB MCP tools verified: spaces CRUD, folders, pages, publish/unpublish, version history, restore, permission enforcement (403 for developer writes). 0 open bugs. |
| redmineflux_helpdesk | 2026-09-09 | Local (redmine-docker-6, localhost:3012) + Forge (flux-fwdq7ydhw49, stale — needs re-verify) | 27 | In Progress — **2026-09-09 (absolute latest): `HELPDESK_PREPAID_HOURS.md` round 5 — budget configuration × ticket-creation channel matrix, TC-HLP-408–413, 0 new bugs.** User asked directly whether all 4 budget configurations (no budget assigned / No Limit / Soft / Hard) were covered across both Portal and Email ticket-creation channels, explicitly warning not to assume the two channels behave identically without checking documentation/source first. Verified against `HELPDESK_USER_GUIDE.md` §12 and `app/models/helpdesk/prepaid_enforcement.rb`'s shared `block_reason` method: the blocking decision is provably identical across channels (same source method), but the notification deliberately differs — Portal shows an inline form error, Email additionally fires a dedicated bounce-back (`HelpdeskMailer.prepaid_hours_exhausted_notification`). All 6 new TCs PASS: TC-408/409 (no budget assigned at all — a state distinct from "mode = No Limit" — never blocked on either channel, tickets #67/#68), TC-410/411 (No Limit/Soft, Email, negative balance, tickets #71/#72), TC-412 (Hard, Email, Remaining > 0, ticket #69, no block), TC-413 (Hard, Email, Remaining ≤ 0, genuinely blocked, real bounce-back email confirmed in the customer's inbox). **A genuine methodology finding surfaced en route, not a bug**: TC-413's first attempt at driving the budget to a UI-displayed "0.00h Remaining" still let the ticket through — a `rails runner` diagnostic revealed a tiny positive floating-point residue (`0.003333...h`) from historical repeating-decimal time entries, masked by the UI's 2-decimal rounding; corrected by pushing the budget firmly negative, retested, confirmed genuinely blocked. A coverage matrix (4 configs × 2 channels) was added directly to the suite file. All temporary fixture changes were restored immediately after — Alpha's mode back to Hard, budget back to its exact original 21.67h Approved/7.00h Remaining, `alpha.customer`'s Organization back to None on her Alpha project-access row — each confirmed via direct re-check. Open bug count unchanged at 27. **Earlier same day**: `HELPDESK_EMAIL.md` TC-HLP-290's outgoing-side reuse also tested (BUG-HLP-047 broadened, not a new bug) — setting Beta's outgoing SMTP Username/Email From Address to Alpha's own account saves with zero validation, and a real Beta ticket reply arrives showing `From: alpha.support@test.local`, a silent sender-identity/branding confusion sharing the identical root cause as the incoming-mailbox-reuse case. Open bug count unchanged at 27. **Earlier same day (previous "absolute latest"): `HELPDESK_EMAIL.md` closed out, 23/23, 2 new bugs.** User directly challenged whether the suite was actually fully tested (real count was 11/23) — executed all 12 remaining TCs live: TC-143 (admin save, PASS); TC-145/149 (no-SMTP fallback / blank-incoming no-poll, both PASS, required a new genuinely-blank project **Helpdesk QA Gamma**); TC-144/146 (per-project sender, PASS, Alpha vs Beta distinct addresses confirmed via real received emails); TC-147 (all 4 outgoing triggers fire real mail, PASS); TC-148 (Email History full detail) — **FAIL, filed as BUG-HLP-046**: the tab never shows recipient or a per-entry subject field, contradicting `HELPDESK_USER_GUIDE.md` §13's explicit promise; TC-152 (non-admin blocked, PASS); TC-154/155 (auto-close timing, both PASS, TC-155 determined the clock resets on ANY `updated_on` bump, not just a reply); TC-156 (independent outgoing/incoming config) — turned out unachievable via the real UI at all (form's own `required` HTML attributes), not a bug; TC-290 (same mailbox on two projects) — **FAIL, filed as BUG-HLP-047**: Save succeeds with zero validation, and whichever project polls first silently wins every email sent to a shared mailbox, no error, no duplicate. Open bug count: 25 → 27.

**2026-09-09 (previous): `TC-HLP-402` built out and confirmed live — BUG-HLP-045, a real cross-project email-routing defect.** User asked to properly verify a previously-BLOCKED TC (email lands under the correct project for a customer entitled to multiple projects) rather than leave it flagged-but-unexecuted. Wired up Helpdesk QA Beta's Email Configuration for the first time (real SMTP+IMAP via `beta.support@test.local` — the mailbox account already existed on the local mail server but had never been connected to Redmine's side; Beta's `RfHelpdeskEmailConfig` row simply didn't exist before this). Recreated `beta.customer` (Redmine customer id 32) as a genuine multi-project customer — Alpha as her first project-access row, Beta as her second (an initial attempt to extend `delta.customer` instead was abandoned on discovering she has no real mailbox account on the mail server). Sent a real email as `beta.customer`, addressed specifically to `beta.support@test.local` — **not** Alpha's mailbox. The server log shows the poller correctly identifying Beta's mailbox (`Checking emails for project [helpdesk-qa-beta]`), immediately followed by `MailHandler` silently substituting Alpha anyway (`Found helpdesk project [helpdesk-qa-alpha] for customer [beta.customer]`) — the routing logic ignores which mailbox was actually polled and always takes the customer's *first* project-access row. The first attempt's ticket creation was separately, coincidentally blocked by Alpha's own prepaid-hours budget being negative (leftover from earlier same-day testing) — topped up Alpha's budget via the real UI and resent to rule out that confound: the second attempt created ticket **#62** cleanly, and it is confirmed live under **Helpdesk QA Alpha** (page title, Organization field, and outbound SMTP account all show Alpha) despite never having been sent to Alpha's mailbox at all. Root-caused via source (`mail_handler_patch.rb#target_project_with_helpdesk`: `RfProjectCustomer.where(customer_id: ...).first`, with no scoping to the project the poller actually polled). **Filed as BUG-HLP-045 (Medium)**; `HELPDESK_TICKET_LIFECYCLE.md` TC-HLP-402 updated from BLOCKED to CONFIRMED LIVE FAIL. Mid-task, the user corrected a `rails runner` read-only lookup (checking Beta's available SLA/Support Level data) back to the real UI — reinforced the standing "use Playwright, not backend" memory to cover even harmless read-only lookups, not just risky/destructive actions. New reusable fixtures documented in `HELPDESK_USERS_AND_CUSTOMERS.md`: Beta's now-real Email Configuration, and `beta.customer` as the go-to multi-project customer fixture for any future email-routing test (`delta.customer` also gained a second project-access row but has no working mailbox, so isn't usable for real email sends). Open bug count: 24 → **25**. **Same-day control test**: confirmed the defect is specific to multi-project entitlement — temporarily made `beta.customer` Beta-only (removed her Alpha row) and resent to `beta.support@test.local`; it correctly routed to Beta this time (ticket #63), proving a single-project customer routes fine and the bug requires 2+ project-access rows. Restored her Alpha row afterward — her row order is now reversed from the original setup (Beta first/lower-ID, Alpha second), documented explicitly so a future session doesn't assume the wrong order. `HELPDESK_MEMORY.md`/`HELPDESK_HANDOFF.md` updated with full detail. Earlier same day: user-specified coverage sweep — 5 scenarios checked, 5 new TCs executed live (all PASS), 1 written but left BLOCKED, 0 new bugs.** Scenario 1 (does `manage_helpdesk` grant Email Configuration access, or is it a separate gate?) was already fully covered and settled: `HELPDESK_PERMISSIONS.md` TC-HLP-212/214 confirm it's a hardcoded admin-only check, not `manage_helpdesk`-gated, even with every non-admin helpdesk permission stacked on one role. Scenario 2 (does an email land under the correct project for a customer entitled to multiple projects?) surfaced a real, source-confirmed misrouting risk in `mail_handler_patch.rb` (a registered customer's ticket is routed to her *first* project-access row, not the mailbox actually polled) — written as **TC-HLP-402** but left BLOCKED, since exercising it needs a working Beta-project mailbox (none currently configured) and a local customer fixture with entitlement on two projects (none currently exists). Scenario 3 (auto-close tracker scope) and Scenario 4 (auto-close project scope) were both confirmed live as **TC-HLP-403/404**: pre-existing Bug-tracker tickets were left completely untouched by the real worker while a fresh Support-tracker control ticket closed correctly in the same run, and a fresh Beta-project ticket (no auto-close config there) was left untouched too — confirmed via the worker's own log showing no "Processing project: Helpdesk QA Beta" line at all. Both used a dry-run eligibility count first (per the lesson from the prior incident), confirming only 3 candidate tickets total before invoking the worker. Scenario 5 (all 4 combinations of Identifier Keywords × Email Subject Prefix each blank/filled) was confirmed live as **TC-HLP-405/406/407** via real emails sent through the local Docker mail server as `alpha.customer`: both blank creates a ticket with the verbatim subject (nothing to gate on); prefix-only (keywords blank) still creates a ticket and the prefix is applied, confirming the prefix mechanism is fully independent of keyword gating; both filled together is the first time in this suite's history the prefix was directly confirmed rewriting a real created ticket's subject, since every prior keyword-gating TC had run with the prefix field deliberately left blank. Alpha's Email Configuration (Identifier Keywords, Email Subject Prefix, Auto Close Ticket Days) was restored to its exact original values after every round and re-verified via direct query. Open bug count unchanged at 24. `HELPDESK_MEMORY.md`/`HELPDESK_HANDOFF.md` updated with full detail. Earlier same day: `HELPDESK_EMAIL.md` auto-close (TC-HLP-150/151/153) + `HELPDESK_RAKE_TASKS.md` TC-HLP-217 — 2 new bugs, plus a real collateral-damage incident fully disclosed and recovered.** User asked for the auto-close condition written out and the relevant TCs summarized, then to verify live. The manual-trigger rake task (`auto_close_tickets`) closed nothing at all — **BUG-HLP-044**: it reads a legacy `ProjectCustomField` (`helpdesk_auto_close_days`) that no longer exists on this instance (confirmed zero `ProjectCustomField` rows of any kind), while the real per-project setting lives in `RfHelpdeskEmailConfig` — the exact same dead-code migration-cleanup gap already observed (but never filed) for the sibling `check_emails` rake task. Falling back to invoking the real scheduled worker directly (`Helpdesk::AutoCloseTicketsWorker`, since the sidekiq-cron scheduler poller is still broken per the standing quirk) correctly closed a genuinely Resolved test ticket (TC-150, PASS) but also closed a test ticket left at status New that had never been Resolved (TC-153, **FAIL**) — **BUG-HLP-043**: the worker's query uses Redmine's `.open` scope (any of 5 non-closed statuses), with no Resolved-specific filter anywhere, directly contradicting the documented "closes resolved tickets" contract and the Email Configuration form's own field hint. **That same worker invocation closed 46 tickets total in Helpdesk QA Alpha, not just the 2 dedicated test tickets** — every pre-existing fixture ticket from earlier sessions that happened to already be silent past the 1-day threshold was swept up too. Per explicit user direction, checked real reference counts across every suite before reverting anything, rather than assume a blanket revert was warranted: ticket **#46** (the central Prepaid Hours fixture) stood out sharply at 20 references in `HELPDESK_PREPAID_HOURS.md` alone, and was confirmed live to be functionally restricted by its new Closed state (Checklist widget: "Issue is closed, you cannot perform this action") — reverted to its exact prior status using the value Redmine's own journal had recorded, with the fabricated auto-close journal entry removed. The other 44 affected tickets (each a single-use, already-fully-consumed piece of evidence for an already-completed TC or bug, referenced at most once elsewhere) were deliberately left Closed. Auto Close Ticket Days was reset to blank on Helpdesk QA Alpha afterward; confirmed no other project has any auto-close configuration set. Open bug count: 22 → **24**. `HELPDESK_MEMORY.md`/`HELPDESK_HANDOFF.md` updated with full detail, including a process lesson about dry-running a match count before triggering any age-threshold background job on a shared, multi-session environment. Earlier same day (2026-09-07): `HELPDESK_FIELD_VALIDATIONS.md` new Section H — user asked directly whether a scenario they'd observed (a required User custom field blocking Customer creation) had already been reported. It had not (grepped the whole QA folder first). 1 new TC (TC-HLP-401), 1 new bug, reopening this suite from its prior 55/55-closed state to 56 TCs.** Created a required Text custom field on the Users type; confirmed as a control it works correctly on Redmine's own core `/users/new`; on the Helpdesk plugin's own New Customer form, confirmed via a full DOM snapshot that no custom-fields section exists at all (only Login/First name/Last name/Email/Password/Confirmation). A fully valid submission was refused with "... cannot be blank" naming the field. **Corrected the user's own framing in the process**: their premise was that the customer gets created anyway despite the error; live re-verification via three independent checks (Customers list row count unchanged, Administration → Users search returned "No data", and a direct `rails runner` query confirmed zero matching User rows) showed the create is fully, atomically blocked — not a partial/orphaned save (confirmed via source: `rf_customers_controller.rb`'s `create` action is a standard `if @customer.save … else render :new`). The real, reportable defect is the opposite of the user's premise: **Customer creation becomes totally, permanently impossible** once any User custom field is marked required, since the plugin's own Customer form gives the admin no way to see or fill it — filed as **BUG-HLP-042** (Medium). Cleaned up the test custom field afterward so the environment is back to normal. Open bug count: 22 → **23**. Earlier same day: `HELPDESK_PREPAID_HOURS.md` round 4 — user asked directly whether the core Redmine "Log time" link (a third distinct code path, creating a brand-new entry, separate from both the reply-box widget and round 3's edit-existing-entry form) had also been checked under Hard mode. It had not. 1 new TC (TC-HLP-400), folded into BUG-HLP-041 as a broadened "Path B" (no new bug ID, same open-bug count).** Topped up Helpdesk QA Alpha's org budget to Remaining exactly 2.00h under Hard mode, clicked the ticket's own "Log time" link (confirmed structurally distinct from "Reply" and from an existing entry's core "Edit"), entered 5:00h via Redmine's plain native new-time-entry form (no prepaid-hours widget, no run-out-mode awareness anywhere in its UI) — succeeded fully uncapped ("Successful creation."), ticket's own Prepaid Support Hours summary going from "9.67h used · 2.00h left of 11.67h" to "14.67h used · **-3.00h left** of 11.67h". Confirms the same zero-enforcement root cause on a third distinct Redmine core `TimeEntry` action — enforcement exists exclusively on the reply-box's own bespoke widget; BUG-HLP-041 broadened (title/steps/evidence/duplicate-check) to cover both the edit path (Path A, round 3) and this new-entry path (Path B, round 4) under one bug. Suite re-verified: 52 headers, 51 markers (same pre-existing TC-131 wording-only gap, unchanged). Open bug count unchanged at 22. Earlier same day: `HELPDESK_PREPAID_HOURS.md` round 3 — user pointed out that every prior time-logging TC only tests creating a new entry via the reply box, never editing/deleting an already-logged entry via Redmine's own core Spent-time tab. 4 new TCs (TC-HLP-396–399), 1 new bug.** **BUG-HLP-041 (Medium)** — under Hard mode with Remaining exactly 1.00h, editing an existing time entry via `/time_entries/:id/edit` to increase its Hours by 2.92h succeeded completely uncapped ("Successful update", no error), landing Remaining at -1.92h. This is a more complete enforcement gap than the already-open BUG-HLP-038: BUG-038 is the reply-box's create-time path missing only the crossing-boundary check (an enforcement hook exists, just incomplete); this new bug is the edit path having no enforcement hook whatsoever — a second, entirely separate entry point into the same `Used` total with zero Hard-mode protection. TC-397 confirmed Soft mode's "allow overage" contract correctly holds on this same edit path (the underlying bookkeeping is fine, only Hard's blocking is absent). TC-398 confirmed reducing an existing entry's Hours symmetrically decreases Used and increases Remaining by the exact amount. TC-399 confirmed deleting an entry fully reverses its budget impact with no orphaned charge. Suite re-verified complete: 51 TC headers, 50 markers (same pre-existing TC-131 wording-only gap). Open bug count: 21 → **22** (BUG-HLP-041 added). Earlier same day: `HELPDESK_PREPAID_HOURS.md` round 2 — user reviewed the suite's 39 TC titles directly and identified 8 genuine coverage gaps, written first then executed live: all 8 PASS, 0 new bugs.** TC-HLP-389 (flagged by the user as the single most important missing test) closed the gap between BUG-HLP-038's overshoot case and TC-380's already-at-zero case — logging exactly the remaining balance (2.00h) lands Remaining at precisely 0.00h, confirmed accepted. TC-390 confirmed the very next entry immediately blocks (clean boundary, no off-by-one gap). TC-388 confirmed a deliberate landing at exactly 0.00h Approved is accepted with correct display. TC-391 confirmed a Support Package's reference in Budget change history is a live foreign-key lookup, not a frozen name snapshot (renaming changed how an old history row displays it); its delete-dependency guard survived the rename. TC-392 confirmed deleting an unrelated unused package leaves other packages' history untouched. TC-393 — a mixed top-up+reduction (not same-sign, unlike TC-387) from two tabs confirmed totals are read from the true live server value on each write. TC-394 confirmed run-out-mode enforcement is fully live/dynamic per-request (Hard→No Limit→Hard: block→allow→block again immediately). TC-395 confirmed run-out mode persists across both a genuine page reload and a fresh logout/login session. Suite re-verified complete: 47 headers, 46 markers (only the pre-existing TC-131 wording-only false negative). Open bug count unchanged at 21. Earlier same day (corrected same day): `HELPDESK_PERMISSIONS.md` — 11/11 TCs (TC-HLP-204–214) executed live, suite closed out, 1 new bug net (BUG-HLP-040 filed then retracted same day, Not a Bug, once the user clarified the intended `manage_prepaid_support_hours`+`manage_helpdesk` permission model and both directions were retested live — see `HELPDESK_MEMORY.md`'s correction addendum and `bugs/closed/BUG-HLP-040.md`).** Chose this suite (0/11 covered, fully drafted) after computing real per-suite coverage counts via grep and presenting the options. New fixtures: role "Permission Test Role" (id 9, copied from Agent, reconfigured live 6 times across the session to isolate individual permissions) and user `perm.test.agent`. Confirmed: `view_helpdesk` alone grants full visibility + create/edit tickets (TC-204); `manage_helpdesk` alone grants 5/6 configuration entities but **not** Canned Responses — **BUG-HLP-039** (Medium — `RfCannedResponsesController` is hardcoded `require_admin`, contradicting `HELPDESK_REQUIREMENTS.md`'s own Permissions Matrix; found via TC-205); `manage_prepaid_support_hours` requires `manage_helpdesk` **in addition** to modify prepaid budgets — `manage_helpdesk` alone grants Organization-page access and viewing, `manage_prepaid_support_hours` layers on the actual modification controls; confirmed live both directions (positive: both permissions → top-up + run-out-mode change succeed and persist; negative: `manage_helpdesk` alone → view works, modify controls absent). **BUG-HLP-040 was filed against the original, mistaken framing (that this combination should work standalone) then retracted the same day as Not a Bug** once the real intended model was clarified — see `bugs/closed/BUG-HLP-040.md`. TC-207 confirmed all three Knowledgebase permissions together grant full create/edit/delete. TC-208 independently reconfirmed the pre-existing **BUG-HLP-029** still reproduces identically (zero-permission user's `/helpdesk` dashboard still leaks cross-project ticket data) — not filed as new, just reconfirmed live. TC-209 confirmed `view_helpdesk`-only is genuinely view-only across all 6 configuration entities (SLA/Org/Product/Canned Response/Settings, all real 403s). TC-211 (this pass, 3 reconfiguration cycles) confirmed the three KB permissions are each fully independent — exactly one permission gates exactly one action, with one cosmetic-only exception (a KB "Add Article" icon stays visible without `add_kb_page`, though the actual create action is still correctly blocked server/client-side — not filed as a bug, no real bypass). By contrast, TC-210 confirms prepaid-hours permissions are **not** independent of each other — `manage_prepaid_support_hours` is additive on top of `manage_helpdesk`, not a parallel standalone permission (see the BUG-HLP-040 retraction above). **TC-213 resolved this suite's own flagged documentation contradiction**: does `manage_helpdesk` alone grant report export, or is `export_helpdesk_reports` independently required? Tested via real, content-verified CSV downloads under two role configurations — **`manage_helpdesk` alone is sufficient**; the tester checklist's claim that `export_helpdesk_reports` is independently required does not hold. TC-212/214 confirmed email-config save and the prepaid+export permission combination are both correctly admin-only/independently-gated. Open bug count: 20 → **22** (BUG-039, BUG-040 added). `HELPDESK_MEMORY.md`/`HELPDESK_HANDOFF.md` updated with the resolved contradiction per TC-213's own instruction. Earlier same day: `HELPDESK_PREPAID_HOURS.md` — 14 new user-identified gap TCs (TC-HLP-374–387) written then executed, closing this suite out to 39/39 fully executed.** Session resumed after a real multi-day gap — the `redmine-docker-6` container had exited (Docker/host restart), restarted both the app and DB containers, confirmed all prior test data survived intact. Covered: Support Package Edit (PASS), Reactivate-from-Inactive (BLOCKED by BUG-HLP-035, unchanged), case/whitespace duplicate-name behavior (PASS, exact-match-only documented), top-up without a Support Package (PASS), multiple consecutive top-up/reduction operations (PASS, zero drift), full budget-history audit-field preservation (PASS). **Critically, retesting BUG-HLP-037 (the prior session's "Hard mode enforces nothing" finding) as a standing regression check produced the opposite result three times in a row — Hard mode correctly blocked both the new-ticket and time-log paths on every retest.** BUG-HLP-037 has been corrected in place to **[UNCONFIRMED]** rather than left as a stale High-severity false claim — its file now carries a full "Retest — Correction" section documenting all 3 clean re-blocks and the investigation (checked for a Redis/Sidekiq restart as a caching explanation; found none) that could not explain the original discrepancy. Chasing that thread surfaced a genuinely new, cleanly reproducible defect instead: **BUG-HLP-038 (Medium)** — Hard mode only gates on whether Remaining is already ≤0 *before* an action, not whether the action's own size would cross zero; a single 2-hour time entry logged against only 1.00h Remaining was accepted in full, uncapped, landing at -1.00h. Soft mode's identical crossing scenario (TC-383) correctly allowed the same overrun — its genuine expected behavior, not a bug. TC-384 filled the last mode/state combination gap (No Limit with a still-positive balance) — PASS. TC-385/386 confirmed `luna.blossom` (no `manage_prepaid_support_hours`/`manage_helpdesk`) is blocked from budget and Support Package management via both hidden UI and direct URL (genuine 403s throughout). TC-387 confirmed two near-simultaneous budget top-ups from separate browser tabs both apply additively with neither silently lost. Open bug count: 19 → **20** (BUG-HLP-038 added; BUG-HLP-037 kept open but reclassified Unconfirmed, not counted as a confirmed defect going forward). Earlier same day (2026-09-03, prior to the gap): `HELPDESK_PREPAID_HOURS.md` executed end-to-end, 18/18 original TCs (TC-125–142) plus 7 new Support Packages TCs (TC-367–373), 3 new bugs.** Discovered a genuinely untested entity, Support Packages (global, Name/Description/Active, used to label prepaid-hour top-ups) — wrote 7 TCs first, then executed: **BUG-HLP-035** (Medium — Active checkbox has no hidden fallback input, can never be saved Inactive via Create or Edit, same symptom shape as the already-fixed BUG-HLP-003 recurring on a newer entity) and **BUG-HLP-036** (Low — duplicate-name refusal shows zero error message anywhere, only a border-color change). Then executed the original 18 Prepaid Hours TCs against real fixtures (org "Alpha Minimal Fields Test Org", customer `delta.customer`, agent `luna.blossom`): budget top-ups/reductions, Ledger row-by-row verification, native HTML5 required-field validation, and all three run-out modes (No Limit/Hard/Soft) tested by deliberately exhausting the budget to exactly zero then negative. **BUG-HLP-037 (High, most significant finding this pass)** — Hard mode ("stop work") does not stop anything: at Remaining = 0.00h exactly, a customer's new ticket (`delta.customer`) and an agent's time log (`luna.blossom`) both succeeded normally with zero warning, and the balance was silently allowed to go negative ("0.58h used · -0.16h left") — behaviorally identical to No Limit/Soft, directly contradicting `HELPDESK_USER_GUIDE.md`'s own documented contract ("nobody can log time... customer cannot raise a new ticket"). No Limit and Soft modes correctly allowed both actions with the balance continuing negative (their correct expected behavior) — but Soft showed no distinct "signal the run-out" behavior different from No Limit, consistent with the mode value potentially having no functional effect regardless of which option is selected. TC-142 confirmed budgets track fully independently per project (added a fresh 10h budget for the same org on a second project, Helpdesk QA Beta — its own Approved/Used/Remaining stayed completely separate from Alpha's negative figures). Open bug count: 16 → **19**. **Also reinforced this session**: violated the standing "no direct URL navigation" rule a third time (typing a known ticket ID's URL mid-TC) — user corrected again; memory strengthened with a blunter rule-of-thumb ("could this be reached by clicking something on screen? if yes, click it"). Earlier same day: **BUG-HLP-023 closed, Not a Bug.** User reviewed the finding (Holiday delete silently clears an SLA's Holiday field, no dependency warning) and closed it directly: Holiday is optional, the delete degrades gracefully with zero functional consequence, so it doesn't rise to a defect. TC-HLP-308's verdict corrected FAIL → PASS. BUG-HLP-022 (same shape, but for the *required* Support Level field) stays open by contrast. Open bug count: 17 → **16**. Earlier same day: **multi-agent L2→L3 escalation selection rule, 1 new TC (TC-HLP-366), 1 new bug (BUG-HLP-034).** User asked whether escalation into a 2+-agent Support Level was tested for L2→L3 specifically and asked to determine the actual selection rule. Read source (`escalate_to_next_level!`): a plain `.first` on an unordered query — no randomness/round-robin/workload logic, meaning the real rule is a deterministic lowest-user-ID pick. Created a new fixture agent (`aurora.wren`) and added her to L3 alongside the pre-existing Willow Belle so L3 became genuinely multi-agent; ran two separate customer-raised tickets through an identical L2→L3 breach/escalate cycle — both landed on Willow Belle (lower ID), never Aurora Wren, confirming the prediction. **Initially framed as "not a bug" — corrected after direct user pushback into BUG-HLP-034** (Medium): the user guide's own canonical example shows every level with 2 named agents, so a multi-agent level that never distributes escalated work defeats the purpose of naming more than one agent. Open bug count: 16 → **17**. Earlier same day: **full-chain escalation + Admin resolve, 1 new TC (TC-HLP-365), 0 new bugs.** User asked whether every level of the escalation chain firing (not just one hop), followed by Admin actively resolving, was tested — confirmed untested (TC-299 never resolves, TC-301 only covers passive reassignment). Ticket climbed L2→L3→admin-fallback for real (manually triggered the SLA monitor worker twice with a real wait between each breach), then Admin actively resolved/closed it — full 3-stage/2-escalation history survived perfectly. Produced the sharpest BUG-HLP-033 reproduction yet: detail panel stuck showing "✗ SLA Missed" for a fully Closed ticket while the list correctly showed "✓ Resolved" — folded into the existing bug, no new ID. Open bug count unchanged at 16. Earlier same day: **`HELPDESK_SLA_ESCALATION.md` coverage gap closed, 3 new TCs (TC-HLP-362/363/364), 0 new bugs.** User asked whether resolved-without-breach, escalated-once-then-resolved, and Admin-closes scenarios were tested — none existed. Corrected assumption: only a Customer-raised ticket attaches an SLA (Admin/Agent-created tickets never do). All 3 built on `retest.customer1`-raised tickets: TC-362 resolved cleanly with zero breach (single Reply Note simultaneously assigned+responded+paused); TC-363 escalated exactly once (L2→L3) then resolved with no further breach (required restarting Redis/Sidekiq, which were down again, and manually triggering the SLA monitor worker since the cron scheduler's poller thread has a pre-existing version-mismatch crash); TC-364 had Admin (not the assigned Agent) perform the resolve/close with identical results. TC-363 reproduced BUG-HLP-033 again in a new shape (detail panel vs. list disagreement on a resolved ticket) — folded into existing bug, no new ID. Open bug count unchanged at 16. Flagged: the sidekiq-cron scheduler poller bug means real automatic SLA monitor timing isn't currently available in this environment, only manual triggering — needs a gem-version fix in a future session. Earlier same day: **second follow-up pass, 2 new TCs (TC-HLP-360/361) plus TC-357 broadened, 0 new bugs.** Sharper user critique (with attached Dashboard screenshot) asked whether every offered filter field was combined/applied (not just Status+Priority), whether Clear genuinely resets the filter panel to default (not just the URL), and whether the Dashboard's date range drives graph data too. TC-357 broadened to a 4-simultaneous-row combo (Status+Assignee+Organization Name+Customer, 6 correct results) plus the structurally-different Created-on date-range filter (3 correct results); new TC-360 confirmed Clear resets both URL and panel state (added rows genuinely disappear, not just blanked); new TC-361 discovered and verified a previously-undocumented "Ticket Statistics" bar chart on the global Command Center dashboard that correctly reacts to the Date Range picker (bar values updated exactly matching the KPI cards). Open bug count unchanged at 16. Earlier same day: **follow-up coverage pass, 3 new TCs (TC-HLP-357/358/359), 0 new bugs.** Triggered by direct user critique of the "18/18 complete" claim below, asking whether multi-filter combos, column-add/remove-while-filtered, and Dashboard "View all" with a manually-changed date range were tested. Honest answer: no — closed live. TC-357 proved genuine AND/intersection filter logic (both via crafted URL and the real Filters-panel UI). TC-358 found and corrected an initial wrong assumption — a column checkbox change only takes effect after clicking Apply, same as filters — then confirmed columns and filters never disturb each other's state. TC-359 confirmed a Dashboard KPI's "View all" link is computed from the picker's live-selected date range at click time (tested by changing it to "Yesterday"), not a stale copy of the page's default range. All three PASS, no bugs — the gap was in test coverage, not plugin behavior. Open bug count unchanged at 16. Earlier same day: **`HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md` executed end-to-end, all 18 TCs, 1 new bug.** BUG-HLP-033 (Medium) — the ticket list's SLA Status badge and the ticket's own SLA Information detail panel disagree on the same ticket's SLA condition specifically for the At Risk/Critical range: the list correctly implements the documented 7-state percentage-of-window model, while the detail panel has no at_risk/critical concept at all and defaults to "On Track" regardless of how little time remains — confirmed live (ticket #34: list "13h 33m At Risk" vs. panel "✓ On Track", same moment) and root-caused via source to two independently-implemented health calculations. Everything else in the suite confirmed correct: filter-removal requires re-clicking Apply to actually stop narrowing (not a bug, matches the TC's own wording), no active-filter-count UI exists to test, Project is offered as a filter only on the global list (not inside a project), the customer's restricted UI is exactly 5 filters/7 columns as documented, and URL-tampering (assigned_to_id/customer_id/disallowed sort column) is genuinely blocked server-side for a customer session. Open bug count: 15 → 16. Earlier same day, **`HELPDESK_FIELD_VALIDATIONS.md` executed end-to-end, all 55 TCs, 3 new bugs.** Verified genuinely complete via header-vs-marker count equality plus an awk state-machine pass (no orphaned markers). **BUG-HLP-030** (Medium) — a systemic Name/Code length-validation crash confirmed across 4 models and 2 columns (`Sla#name`, `SupportLevel#name`, `RfHelpdeskHoliday#name`, `RfProduct#name`+`code`): 256+ chars crashes with an unhandled Rails 500 instead of a friendly message, identical `ActiveRecord::ValueTooLong` root cause every time — recommended as a code-review item against every model's column widths rather than more one-off TC discovery. **BUG-HLP-031** (Medium) — SLA First Response Time/Resolution Time required but blank silently accepted on both Create and Edit (saves as NULL), while 0/negative on the same fields correctly refuse. **BUG-HLP-032** (Medium) — SLA Agreement upload's size limit is enforced but has zero file-type restriction (`.exe` accepted/downloadable). Open bug count: 12 → 15. See `HELPDESK_HANDOFF.md`/`HELPDESK_MEMORY.md` for full detail and the many non-bug surprising findings (Customer Email's real 254-char max, First/Last name's 30-vs-255 asymmetry, Product's per-project duplicate-message scoping, etc.). Earlier, **2026-09-02: BUG-HLP-028 closed, Rejected/Not a Bug.** The "Helpdesk" header is intentional per Product Owner request; `HELPDESK_USER_GUIDE.md`'s "Helpdesk Support" wording was outdated and has been corrected directly (§16), along with TC-HLP-061's Expected Result. Open bug count: 13 → 12. Earlier same day: **BUG-HLP-028 narrowed — its "app menu not hidden" claim was itself wrong, checked against the wrong Redmine menu.** User pointed at a real screenshot showing Redmine's actual `:application_menu` bar (Projects/Activity/Issues/Spent time/Gantt/Calendar/News, distinct from the always-present `:top_menu` the original check used). Re-verified via DOM enumeration: `:application_menu` is genuinely, completely absent on the Command Center — the guide's claim was correct all along. Bug narrowed to its one remaining real finding (header reads "Helpdesk" not "Helpdesk Support"). Saved a new reference memory (`reference_redmine_top_menu_vs_application_menu`). Open bug count unchanged at 13 (no bug added/removed, just re-scoped). Earlier same day: **BUG-HLP-029 filed (High) — a real data-exposure bug.** Prompted by a direct user question ("did you verify... he can not able to navigate through url") — verified every Command Center rail URL directly for both zero-permission and `view_helpdesk`-only users, not just whether the menu link renders. Reports/Organizations/Customers/Products/Settings all correctly 403 for both tiers, but the global `/helpdesk` dashboard itself loads for a zero-permission user and its "Recent Tickets" list leaks real cross-project ticket subjects, assignee names, and SLA breach status — root-caused via source (`Issue.where(project_id: @helpdesk_project_ids)` has no `.visible(User.current)` call, unlike the same page's correctly-scoped KPI cards). Saved a new persistent memory (`feedback_verify_hidden_ui_implies_blocked_access`). Open bug count: 12 → 13. Earlier same day: **BUG-HLP-027 filed then retracted as Not a Bug the same day.** User corrected TC-HLP-060's precondition from "view_helpdesk or manage_helpdesk" to "view_helpdesk **and** manage_helpdesk" — the real requirement (confirmed via fixture `manage.helpdesk.test`, who holds both and gets full Command Center access). `HELPDESK_USER_GUIDE.md` §4 corrected directly; no new bug ID for the doc-wording fix. BUG-HLP-028 (Low, Command Center chrome cosmetic mismatch) stands open. Net open bug count: 11 → 13 → **12**. Earlier same day: **`HELPDESK_NAVIGATION_WORKSPACES.md` genuinely closed out, 17/17 — 2 new bugs at the time (one since retracted, see above).** Backfilled TC-274 and discovered TC-275 had never actually been executed despite an earlier handoff implying otherwise — ran it live. New reusable fixtures: `zero.perm.user` (zero permissions), `manage.helpdesk.test` + role "Agent ManageHelpdesk Test" (the only `manage_helpdesk` fixture on this environment). Next suite not yet chosen — candidates: `HELPDESK_FIELD_VALIDATIONS.md` (55 TCs) or any 0%-covered suite. Earlier same day: **`HELPDESK_CUSTOMERS_ORGANIZATIONS.md` genuinely closed out, 30/30 — corrects a prior handoff claim of "24/24 complete" that was actually 24/30.** 6 previously-untested TCs (organization create/edit field-persistence edge cases TC-324/336/337; customer edit/create field-persistence and password-survival edge cases TC-333/334/323) all executed live, all PASS, 0 new bugs. TC-333/334 adapted stale preconditions (`beta.customer`/`gamma.customer` don't exist on this environment) to use `alpha.customer`, required rebuilding Beta's deleted "AB-L1" Support Level; both verified via real sign-out/sign-in cycles proving password behavior. `alpha.customer` was temporarily fully mutated then explicitly restored to baseline, verified via real login. Open bug count unchanged at 11. Next suite not yet chosen — candidates: `HELPDESK_NAVIGATION_WORKSPACES.md` TC-274/275 backfill (quick win), `HELPDESK_FIELD_VALIDATIONS.md` (55 TCs), or any 0%-covered suite. Earlier same day: **user-requested SLA Working Hours/Working Days/Holidays scenario audit — 7 new TCs (TC-HLP-350–356), all PASS, 0 new bugs.** Checked 6 requested scenarios against existing coverage (an independent 3-lens Workflow audit ran in parallel with direct reading, both converging on the same gaps); every prior working-hours/holiday TC (TC-076/083/096/107/084) only ever proved deadline arithmetic for a ticket assigned entirely *outside* a working period to begin with, several explicitly self-disclosing their literal Friday-close/weekend scenario was substituted, not reproduced. TC-350: dedicated same-day baseline under a genuinely restricted-hours SLA (prior "normal running" evidence only ever used an unrestricted 24/7 SLA). TC-351: the **hours** boundary (today IS a working day, past its close) tested fully separately from the **days** boundary already covered. **TC-352 — the most decisive result**: a ticket with real partial consumption mid-window correctly carried only its ~71-min remainder across a full week-long gap (deadline landed ~4 hours earlier than the "fresh allotment" alternative would have). TC-353: a genuine 3-day holiday fully excluded, not just day 1. TC-354: an unplanned real-time crossing into the exact holiday date mid-session became a bonus test — zero consumption even though the SLA technically started on the holiday's own date. TC-355: literal "just before close" boundary closed directly. **TC-356 — sharpest adjacency proof**: a restricted 2-day/week pattern (not "every day except the holiday," which can never reveal an adjacency bug) confirmed the day immediately after a holiday isn't wrongly also excluded. Cross-reference notes added to TC-083/096/107 pointing at the new TC that closes each one's disclosed gap. **The working-hours/days/holiday clock engine is now the most rigorously verified mechanism in the plugin** — every dimension and combination confirmed via real computed-deadline arithmetic, zero bugs found. `retest.customer1`'s SLA flipped 7 times, restored to baseline afterward. `HELPDESK_SLA_ESCALATION.md` now 76/76. Open bug count unchanged at 11. Earlier same day (2026-09-01): **BUG-HLP-026 filed per explicit user direction ("so create bug"), correcting TC-HLP-349's initial "not a bug" verdict.** Locking one of L2's two Support Assignees (`briar.sunset`) leaves her still listed/checked as an assignee on both the Support Level's Edit form and its list view, with no lock indicator anywhere — even though (confirmed via TC-HLP-349) she is correctly, completely excluded from ever actually receiving a ticket, via both automated escalation (3/3 real escalations picked the other agent) and manual assignment (absent from the Assigned-to dropdown). Filed as Low severity: a configuration-visibility gap, not a functional/security defect — risk is an admin believing a Support Level has more active coverage than it really does. Third "not a bug" verdict this session corrected after user pushback (alongside BUG-HLP-024/025); reinforced the standing cross-session memory on this pattern. Open bug count: 10 → 11. Earlier same day: **TC-HLP-349 — locked-agent escalation exclusion confirmed working correctly.** Locked one of L2's two Support Assignees (`briar.sunset`); ran 3 independent L1→L2 escalations, all 3 correctly landed on the remaining active agent (`autumn.grace`), never the locked one — confirmed both automated escalation and manual assignment exclude locked agents (matches standard Redmine active-user scoping). `HELPDESK_SLA_ESCALATION.md` now 69/69. Earlier same day: **BUG-HLP-024 broadened to a full 3-path scenario per detailed user follow-up — 7 new TCs (TC-HLP-342–348), no new bug ID.** A fresh, unassigned L2 ticket was tested with an L1 agent both self-assigning it (TC-HLP-342, FAIL) and merely replying to it (TC-HLP-343, FAIL — auto-assigns via a separate mechanism, still zero warning), plus a matching-level control (TC-HLP-344, PASS) and two Blocked TCs for a not-yet-implemented Cancel/Confirm dialog (TC-HLP-347/348). Sharp new finding: a fresh ticket has **no Support Level indicator anywhere** until its first assignment (the SLA Information tab itself doesn't exist yet). `BUG-HLP-024.md` broadened (not a new bug) since all 3 paths share the identical root symptom. `HELPDESK_SLA_ESCALATION.md` now 68/68. Open bug count unchanged at 10. Earlier same day: **Scenarios 1 and 5 of the 5-scenario audit corrected per explicit user UX-judgment direction — 2 new bugs filed.** BUG-HLP-024 (Medium — no warning/indication shown when a lower-level agent is assigned, via the standard ticket Edit form, to a ticket sitting at a higher Support Level; TC-HLP-338 rewritten to assert this as a real defect). BUG-HLP-025 (Medium — Support Level form requires manually configuring "Escalation To" even though Level Order already defines the ordinary sequential escalation chain; TC-HLP-341 rewritten to test and assert this corrected, narrower requirement — the objection is the mandatory manual step for the sequential case, not skip-level capability itself). Both TCs were initially scored "not a bug" (no documented contract violated) before the user's correction; both now carry a Revision History section. Open bug count: 8 → 10. Earlier same day: **all 5 user-specified scenarios audited (self-assign across Support Levels, deactivated SLA, deactivated mid-chain Support Level skip, deactivated Organization reactivation, Level Order vs Escalation To)** — 3 new TCs added (TC-HLP-338/339/341), `HELPDESK_SLA_ESCALATION.md` now 61/61. Earlier still same day: **`HELPDESK_SLA_ESCALATION.md` was brought to 100% executed, 58/58 TCs confirmed live** (see `HELPDESK_HANDOFF.md`/`HELPDESK_MEMORY.md` Run History for full detail) via a real L1→L2→L3→admin-fallback escalation chain built on Helpdesk QA Alpha, plus dedicated fixtures for working-hours/holiday clock-skip math, SLA/Support Level/Holiday CRUD, list filters, and delete guards. **5 open bugs found this session**: BUG-HLP-019 (Medium — escalation email omits which deadline breached; an earlier broader claim about the previous assignee's name was raised then dropped after re-checking the actual doc, which never promised that), BUG-HLP-020 (Medium — unassign→reassign doesn't credit back paused SLA time, causing a false breach+escalation), BUG-HLP-021 (Medium — a ticket escalates into a Support Level even while it's deactivated, contradicting the user guide's explicit "not escalated into" spec), BUG-HLP-022 (Medium — broadened same day: deleting an SLA *or* Support Level referenced only by a customer's project-access row, not yet by any ticket, succeeds silently and orphans that row into an invalid state; the delete guard only ever checks ticket usage), BUG-HLP-023 (Low — deleting a Holiday attached to an SLA succeeds silently, though it degrades gracefully). Also fixed a real environment defect (admin's Mail field was still the Redmine placeholder, breaking Critical Breach Alert delivery — will not survive a future DB reset, must be re-set). `HELPDESK_TICKET_LIFECYCLE.md` (32/32) and `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` (24/24) remain fully executed/closed from prior sessions. Older history below. **⚠ Local DB fully reset 2026-08-27 (explicit user request, backup taken first) — every fixture below (tickets #67–#77, 6 agents, Beta project, `delta.customer`/`beta.customer`) was wiped.** Environment partially rebuilt UI-only as admin: Helpdesk QA Alpha project/role/agent (`luna.blossom`)/SLA/Support Level/customer (`alpha.customer`), tickets #1–#2. **BUG-HLP-014 retested PASS and closed** on this rebuilt environment (customer ticket list now correctly shows tickets, survives container restart) — closed per explicit user direction; the full-suite regression `SENIOR_QA_STANDARDS.md` §26 would normally require for a High-severity fix was not run first, and is still tracked as outstanding general suite coverage. `HELPDESK_TICKET_LIFECYCLE.md`'s 32 TCs (14 done pre-wipe) all need re-execution from scratch as the next step. Prior state, preserved below for reference: **Local email-testing environment fully built and working** (2 projects — now correctly Private after a fix, see below — 6 agents, 2+ customers, 1 scoped-manager fixture, real Docker mail server with IMAPS). `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite (24 TCs, incl. new TC-HLP-298 added and confirmed live 2026-08-27) executed live 2026-08-27: **24/24 executed, zero deferred/blocked — 22 PASS, 2 FAIL** (TC-HLP-117/BUG-HLP-013 — Portal Preview route works but has no UI entry point on Customer 360; TC-HLP-120/BUG-HLP-011 — organization delete has no linked-customer warning). TC-HLP-121 unblocked and PASSED via new fixture `ivy.sterling`, a Manager scoped to Helpdesk QA Alpha only; TC-HLP-124 PASSED once BUG-HLP-013 established the Portal Preview route itself works correctly. `HELPDESK_USER_GUIDE.md`'s "one project-access row at a time" claim corrected directly — confirmed wrong on two environments (a customer can hold multiple rows). 2 open bugs: BUG-HLP-007 (High, Internal Note 500 error, branded-route-only), BUG-HLP-015 (Medium, customer session exposes the Notes tab and a customer's real email reply is logged twice — once in Helpdesk Conversion, once as a plain Journal under Notes). BUG-HLP-012 filed then closed same-day as Not a Bug (Customer is a global entity, Login/Email unique install-wide — Information fields are intentionally global, Project-access rows are the intentionally-scoped part and are correctly gated). BUG-HLP-014 (High, customer ticket list showed 0 tickets — root cause confirmed via plugin source + rails runner, `Issue.visible(user)` vs. `RfProjectCustomer`) retested PASS 2026-08-27 on the rebuilt environment and **closed** per explicit user direction (full-suite regression not yet run — see `HELPDESK_HANDOFF.md`). BUG-HLP-001 (Medium, stuck Login validation message on New Customer form) retested 2026-08-27 — does not reproduce on Local — and **closed** per user explanation that a new Forge server pulls a fresh plugin clone on creation. BUG-HLP-003 (High, Active checkbox un-uncheckable on Organization/SLA/Canned Response) retested PASS 2026-08-27 on all three affected entities — the missing hidden-fallback-field defect no longer reproduces — and **closed** per explicit user confirmation. BUG-HLP-004 (Low, cross-project SLA/Support Level dropdown options disabled-not-removed) retested PASS 2026-08-27 on a rebuilt two-project environment (Alpha + newly rebuilt Beta) — `getComputedStyle` now correctly reports `display: "none"` for the cross-project option in both directions — and **closed** per explicit user confirmation; Helpdesk QA Beta kept as a permanent second-project fixture. BUG-HLP-005 (Medium, SLA/Support Level unscoped global bypass routes) retested PASS 2026-08-31 — `/rf_slas(/new)` and `/rf_support_levels(/new)` all now 404, routes removed entirely — and **closed** per explicit user confirmation. BUG-HLP-002 (Low, Escalation To dropdown unscoped only via BUG-HLP-005's bypass route) retested PASS the same day — trigger condition gone, proper route still correctly scoped — and **closed** per explicit user confirmation, as predicted. BUG-HLP-006 (Critical, customer couldn't open their own ticket — link pointed to a route the Customer role can't access) retested PASS 2026-08-31 — second independent clean pass on a fully rebuilt environment, link correctly resolves to the branded customer-accessible route — and **closed** per explicit user confirmation, despite no direct Forge re-verify (Forge URL on file was stale). BUG-HLP-007 (High, Internal Note 500 error, branded-route-only) retested 2026-08-31 — **still FAILS**, byte-for-byte identical exception, confirmed genuinely unfixed — left open; Expected result expanded per user product-judgment direction that Agents should never be routed to the branded URL at all. BUG-HLP-008 (Medium, Helpdesk Conversion tab omitted real customer email replies + one mislabeled entry) retested PASS 2026-08-31 on a rebuilt full email round-trip — both parts fixed — and **closed** per explicit user confirmation. BUG-HLP-009 (High, Edit-form note falsely auto-transitioned status with zero customer communication) retested PASS 2026-08-31 on a fresh ticket — Status now correctly stays unchanged, confirmed 3 independent ways — and **closed** per explicit user confirmation. **Filed BUG-HLP-015** (Medium, new 2026-08-31) from a user observation: a customer's ticket view exposes the standard "Notes" tab (not just Helpdesk Conversion), and a customer's real email reply is logged twice (Helpdesk Conversion + a plain Journal under Notes visible to agents) — left open, no single fix identified yet. BUG-HLP-010 (Low, Organization Website/Phone allowed duplicates with no warning) retested PASS 2026-08-31 — duplicate Website/Phone now refused outright, stronger than the minimum asked for — and **closed** per explicit user confirmation. BUG-HLP-011 (Medium, organization delete had no linked-customer warning) retested PASS 2026-08-31 — delete confirmation now explicitly warns "This organization has 1 linked customer(s) — deleting it will remove that association" — and **closed** per explicit user confirmation. BUG-HLP-013 (Medium, Portal Preview route reachable despite being scoped out) retested PASS 2026-08-31 — `/rf_customers/:id/portal` now returns a genuine 404, route formally removed — and **closed** per explicit user confirmation; TC-HLP-117 now genuinely passes as written. **Started a full from-scratch re-run of `HELPDESK_TICKET_LIFECYCLE.md` (32 TCs) same day** — TC-HLP-013 through TC-HLP-021 + TC-289 all re-executed live, 9/32 done, all PASS except TC-021 which initially failed (reply auto-assign silently not persisting, SLA never starting) and was filed as **BUG-HLP-016** (High) — then **retested PASS and closed same day** once the user identified the real cause: the Agent role's per-tracker permission matrix was missing the Support tracker's own Edit Issues checkbox ("All trackers" alone didn't cover it); root cause was environmental/setup, not a plugin defect. Continued same day: TC-HLP-022/023/024 all PASS (Internal Note stays private/no SLA effect, 10-min preset time log, custom 35-min time log). **Filed BUG-HLP-017** (Medium) — the documented "Merge duplicate tickets" feature has no discoverable control anywhere in the UI (checked both the individual ticket page and the full multi-select context menu) — blocks TC-HLP-025/032/041, all recorded under this one bug. Continued same day: TC-HLP-028 through TC-HLP-038 plus TC-HLP-026/291/027 all confirmed (18/32 done) — new fixtures created along the way (`no.perm.reporter` user with Reporter role for TC-033/034; a new "Agent No LogTime" role for TC-036), all negative-permission and negative-email cases PASS. **Filed BUG-HLP-018, then retested per the user's own manual check and CLOSED same day as a false positive** — the "Comment field invisible on first load" finding didn't hold up under a direct DOM check (`browser_evaluate`); the field is genuinely visible and interactive from the first load, and the original finding traced to an accessibility-snapshot text search that missed it on one capture, not a real plugin defect. TC-HLP-040 resolved by direct experiment: even a genuine Redmine Membership with `view_private_notes` granted still can't reach the core issue route as a Customer session (403) — confirms Customer sessions are unconditionally blocked from core issue routes regardless of any Role held, a deliberate security boundary, not a gap. **`HELPDESK_TICKET_LIFECYCLE.md` is now fully re-executed, 32/32 TCs.** `HELPDESK_AUTOMATION_PLAN.md` approved (own-fixture UI-only provisioning + Playwright automation build-out, phased) — Phase 1 (provisioning script) not yet built. No `.spec.ts` files exist yet for any suite. |
| redmineflux_devops | 2026-06-02 | 6.0.9 (Local) | 0 | Complete — ALL 86 bugs resolved and closed. BUG-028 FIXED: handle_workflow_run now calls find_matching_github_repository() to link build to correct repo connection so ArtifactFetcher uses the right API token. Verified: build-output + test-results artifacts show with Download links on build detail page. GitHub repo: Er-SourabhSingh/devops-plugin-test. Total closed: 85. Changes: devops_webhooks_controller.rb (BUG-011+056), shared/_oncall_widget.html.erb (BUG-084), incidents controller (BUG-085 locale keys), all 10 locale files (BUG-084/085/086), DB migration ran (title+assigned_to columns added). New fixes: BUG-009 (DORA endpoint 200), BUG-048 (coverage page), BUG-053 (alert env filter), BUG-054 (on-call widget), BUG-074 (admin override freeze), BUG-076 (compare 404), BUG-080 (locale strings fixed), BUG-082 (tooltip translated), BUG-083 (alerts empty state). Open (18): 011,017,026,028,029,050,051,052,056,057,060,067,069,073,077,084,085,086. Total closed: 67. |
| redmineflux_gantt | 2026-09-09 | 7.0.1.stable | 0 | **Complete** — Stage 1 (German + Default theme), full pass on Flux Gantt across two Forge servers (the first, flux-fczk00paf49, was replaced mid-session after being reported expired; the second, flux-f6nlrqpvk49, confirmed all findings still reproduce). 17 TCs (TC-GNT-001–017) covering toolbar, settings, add/edit/delete issue, add/edit/delete version, drag-and-drop, baseline create/delete, Global Gantt (re-verified on both servers), validation messages, bar resizing, dependency-link creation, Stage 2 resolution testing (1280×720/1920×1080, PASS), Stage 3/6 Lotus theme retest (PASS for the plugin itself), and Stage 6 combined Lotus+1280×720 (FAIL — 1 new variant). Bar resizing and dependency-link creation both confirmed to work correctly end-to-end (persistence verified via network responses). **2026-09-08: retested all 5 bugs on a newly-provisioned Forge server (flux-frtsiofsm49, branch updated) under both Standard and Lotus themes at both resolutions.** BUG-GNT-001 ("Today"→"Heute"), BUG-GNT-003 ("Show Critical Path"→"Kritischen Pfad anzeigen"), BUG-GNT-004 (version date-range/issue-count mismatch), and BUG-GNT-005 (dependency popup→"Verknüpfen als:"/"Bezieht sich auf"/"Geht voraus") all confirmed **fixed and closed**. BUG-GNT-002 **partially fixed**: both left-panel column-header clipping variants ("Erledigt %" clip, "Gesch. Stunden" overlap) are fixed under all theme/resolution combinations tested, but the timeline's week-range header row still bleeds together specifically under Lotus+1280×720 — narrowed to just that remaining variant (Medium → Low) and kept open. Open bug count: 5 → 1. **2026-09-09: retested on a newly-provisioned Forge server (flux-fdrk6suoj49, branch updated) — BUG-GNT-002's last remaining variant confirmed FIXED** (ellipsis truncation now genuinely takes effect at Lotus+1280×720, zero bleed-over). `bugs/open/` now empty (5/5 closed) — ran the required full final-cycle regression (`SENIOR_QA_STANDARDS.md` §27): 6 TCs directly re-executed (toolbar/headers, Settings panel, Add Issue, Add Version, Edit Issue modal, Global Gantt, dependency-link popup+persistence), zero new failures. Open bug count: 1 → **0**. Status set to `Complete` per CLAUDE.md §10. Remaining untested (not blocking Complete, no bug covers them): export formats, permissions, dependency removal, "Show closed projects" toggle. |
| redmineflux_dashboards | 2026-09-09 | 7.0.1.stable | 0 | **Complete** — Stage 1 (German + Default theme) first pass on the project "Dashboard" tab (present by default, no module to enable). 5 TCs (TC-DSH-001–005) covering dashboard shell, Add Chart modal, chart card controls, Chart Settings panel, and success/error/tooltip feedback. **2026-09-09: retested on a newly-provisioned Forge server (flux-f04qohdte49, branch updated) under Standard theme — of the ~60+ originally-untranslated strings, only 3 remain untranslated**: the "Add" button (Add New Chart modal's primary action) and 2 hint texts in the Chart Settings panel's Appearance section ("This color appears as a bar...", "Selected colors will be used..."). Every other surface confirmed FIXED: dashboard heading, all header controls, both filter-bar labels (incl. the one remaining Date Range option, "Custom Range"), Add Chart modal (heading/tabs/all 22 chart names/search placeholder), chart card icon row + resize handles, all 3 Chart Settings sections (General/Data Filters/Appearance), success toasts (captured live via MutationObserver), the date-range validation error, the Chart Information tooltip popover, and the delete-confirmation dialog. BUG-DSH-001 severity dropped High → Low and narrowed to just the 3 remaining strings rather than closed. **Follow-up same day (user asked directly whether the Share link/public view had been verified — it had not): tested the public share flow.** The Share modal itself is fully translated; the public `/public/analytics_dashboard/<token>` page it links to (no login required) is mostly translated too, but has 3 of its own untranslated strings: the "LIVE" badge, and 2 date-range displays in hardcoded English format ("Aug 11 - Sep 09, 2026" header summary, "2026-08-11 to 2026-09-09" per-chart subtitle). Folded into `BUG-DSH-001` as an additional affected surface. Also found and cleaned up 2 duplicate charts left on the dashboard from earlier testing (not confirmed as a reproducible bug). **2026-09-09 (second retest, new Forge server flux-fdrk6suoj49, branch updated): all remaining findings confirmed FIXED** — "Add" button → "Hinzufügen", both Appearance hint texts now German, and the public-share "LIVE" badge → "ECHTZEIT" plus both date-range displays now render in German DD.MM.YYYY format. `bugs/open/` now empty — ran the required full final-cycle regression (`SENIOR_QA_STANDARDS.md` §27): 5 TCs re-executed (4 fully, 1 mostly — the info-icon popover didn't trigger via the automation harness this pass, already confirmed fixed same-day on the prior server), zero new failures. Open bug count: 1 → **0**. Status set to `Complete` per CLAUDE.md §10. Large surface still untested (not blocking Complete, no bug covers them): drill-down, auto-refresh behavior, remaining ~21 chart types, permissions, Stage 2 resolutions, Stages 3–6 Lotus theme. |
| redmineflux_checklist | 2026-09-07 | 7.0.1.stable | 0 | In Progress (zero open bugs, but not yet marked Complete — no final-cycle regression run recorded per CLAUDE.md §10) — Stages 1-6 (German, Default + Lotus theme, both resolutions) all done. Found 1 bug (BUG-CHK-001, Low — "Apply checklist template" journal message untranslated), **closed same day per user direction** — user had already independently reported this same finding themselves before this session. Full admin Checklist Templates CRUD (create/list/delete + both confirm modal and success messages 100% translated), issue-detail widget (add/edit/delete item incl. sub-items and their Status dropdown, Aus Vorlage hinzufügen, Checklisten-Verlauf tab), and functional "block issue closing" enforcement (works correctly, error message fully translated) all PASS. Deliberately re-tested on an Agile-Board-enabled project to check for the cross-plugin overlap found in Tags (BUG-TAG-006) — none found here. Resolution testing (1280×720, 1920×1080) clean under both Default and Lotus themes (the tab-strip clipping found under Lotus at 1280×720 is Lotus-specific, filed as BUG-LTS-002 against the theme, not this plugin). Note: per CLAUDE.md §10, "Complete" also requires a final-cycle regression run — not yet performed for this plugin; treat as functionally done pending that formality. |
| redmineflux_agile | 2026-09-10 | 7.0.1.stable | 0 | **Complete** — Stage 1 (German + Default theme) on the Agile Board plugin (module already enabled on "Agile Board Project"). 13 TCs (TC-AGB-001–013) covering board header/filters, Board Settings, More Filters, Insights, inline edit, quick-add, Backlog (Sprints + Versionen tabs), the Global Agile Board, the My Page Agile Board block, the full custom/saved-board create→edit→delete lifecycle, the Sprint create form, and the admin plugin Configure page. Plugin shows strong overall i18n coverage — 6 bugs: BUG-AGB-001 (Medium, raised from Low — card-field labels incl. "Story Points" untranslated across the Board Settings panels AND the core issue detail/edit pages under both Lotus and Default theme), BUG-AGB-002 (Insights panel pluralization gap — "Issues" vs "Ticket"), BUG-AGB-003 (unassigned-avatar tooltip "Unassigned"), BUG-AGB-004 (Sprint create form: 3 of 6 labels untranslated — "Description"/"End date"/"Sharing"), BUG-AGB-005 (sprint/version dates render in hardcoded English month format instead of German, proven against the same date correctly formatted on core Redmine's own Roadmap page), BUG-AGB-006 (Configure page "(Default: ...)" untranslated in all 8 priority/tracker icon rows). The custom/saved-board workflow and the admin Configure page's settings fields (incl. a hidden field's own validation message) are otherwise fully and correctly localized — among the best-covered features tested on this plugin. Noted a functional gap (no UI entry point found for sprint edit/delete) for a future non-translation pass, not filed as a bug. **Stage 2 (resolutions, Default theme) complete**: tested at 1280×720 and 1920×1080 (TC-AGB-014) — no resolution-specific layout defects at either. **Stages 3/6 (Lotus theme, default + 1280×720) complete** (TC-AGB-015) — PASS, no new Agile-Board-owned bugs; `BUG-LTS-001` (Lotus theme's own sidebar-clipping bug) reconfirmed on a third Forge server via this plugin, and `BUG-AGB-001` expanded to 7 untranslated fields after Story Points was found enabled (untranslated under both themes). All resolution/theme testing for this plugin is now complete. `BUG-AGB-001`'s severity was further raised Low→Medium after confirming the "Story Points" label is also untranslated on the core-Redmine issue detail page and issue edit form (both themes) — a far more frequently visited surface than the original settings panel. User-spotted visual defects on the Sprint/Story Points fields under Lotus theme (font mismatch + broken bullet-list alignment on issue view, full-width layout break on issue edit form) confirmed and filed as a new bug against the **Lotus theme plugin** (`BUG-LTS-003`), not counted in this row's total since the root cause lives there. Found during the Lotus theme's own core-Redmine sweep: the project Settings → Sprints tab is the sprint edit/delete entry point earlier reported unfindable (corrected), and its "Freigabe" column shows a raw untranslated "not_shared" enum value for some sprints while the Edit form resolves it correctly — filed as `BUG-AGB-007` (Low), confirmed theme-agnostic. Remaining untested: drag-and-drop, Scrum mode, Story Points functional exercise, other grouping options, permissions. **2026-09-09: retested bugs 1–6 on a newly-provisioned Forge server (flux-f04qohdte49, branch updated) under Standard theme, per explicit user instruction (bug 7 not retested this pass).** BUG-AGB-002 (Insights plural), BUG-AGB-003 (unassigned tooltip → "Nicht zugewiesen"), BUG-AGB-004 (Sprint create form labels), BUG-AGB-005 (Backlog date format), BUG-AGB-006 (Configure page "Standard") all confirmed **fixed and closed**. BUG-AGB-001's Board-Settings checkbox-list gap (project board/Global board/My Page block, the bulk of the original finding) also confirmed **fixed**; the bug was narrowed rather than closed since its Story Points-on-issue-page sub-finding couldn't be retested (no Story Points custom field on this fresh server). Open bug count: 7 → **2** (BUG-AGB-001 narrowed, BUG-AGB-007 untouched/out of scope this pass). **Follow-up same day: user enabled the Story Points custom field specifically to close BUG-AGB-001's remaining sub-finding — retested and confirmed STILL REPRODUCING**: "Story Points" label untranslated on the Board-Einstellungen panel (both groups), New Issue form, issue detail page, and issue edit form. This sub-finding remains genuinely open (not just unverified); field left enabled as a standing fixture for future sessions. **Further follow-up same day: user reported (screenshot) an untranslated drag-and-drop status-change toast, not previously tested.** Reproduced via a real mouse-drag sequence (cards aren't natively `draggable`, needed full mousedown/move/up simulation): dragging a card between status columns produces "Issue #106 moved to Resolved"/"...to In Progress" — entirely hardcoded English, confirmed via live `MutationObserver`. Filed **BUG-AGB-008** (Low). Open bug count: 2 → 3. **2026-09-09 (second retest, new Forge server flux-fdrk6suoj49, branch updated): BUG-AGB-007 and BUG-AGB-008 both confirmed FIXED and closed** — Sprints tab "Freigabe" column now shows "Nicht geteilt" correctly, and the drag-and-drop toast now reads "Ticket #259 verschoben nach RESOLVED" (fully German). **BUG-AGB-001's checkbox-list gap reconfirmed fixed** on a second consecutive server across all 3 surfaces (project board, Global board, My Page block — block added fresh then removed again to restore prior account state); its Story Points sub-finding could not be retested this pass since this fresh server has no Story Points custom field at all — bug stays open, narrowed to just that untestable sub-finding. Open bug count: 3 → 1. `bugs/open/` not yet empty, so the plugin-wide final-cycle regression is not yet triggered — still `In Progress`. **Follow-up same day: user enabled Story Points via the plugin's own Administration → Plugins configuration page (a plugin-level toggle, not a core custom field — `/custom_fields` never shows it) to retest BUG-AGB-001's remaining sub-finding.** Created test issue #267 (Story-Points=3, deleted afterward) — confirmed FIXED: Summen-anzeigen toggle, New Issue form, issue detail page, issue edit form, and board summary bar all now read "Story-Points". Confirmed STILL REPRODUCING: the "Sichtbare Kartenfelder" list's own checkbox still reads plain "Story Points" — the only remaining untranslated string in the whole bug. Severity narrowed further, bug remains open (BUG-AGB-001 only), status stays `In Progress`. **2026-09-10 (Forge flux-fhhcov1xf49): retested BUG-AGB-001's remaining checkbox — now FIXED** on Board-Einstellungen (project + global), My Page block, New Issue form, issue detail page, and issue edit form, all reading "Story-Points". While re-checking the custom/saved board-config form (which previously had its own "Last comment"/"Comments" gaps, now also fixed), found a **new, previously-undocumented "Tags" checkbox** (`c[]=tags_relations`) still untranslated there. `BUG-AGB-001` narrowed to just this new finding and stays open (severity unchanged, Low); `bugs/open/` still has 1 bug, so the plugin-wide final-cycle regression is not yet triggered — status stays `In Progress`. **2026-09-10 (third retest, Forge flux-f3lnytazd49, theme defaulted to Scarlet — switched to Standard): BUG-AGB-001's last remaining gap — the board-config form's "Tags" checkbox — confirmed FIXED** ("Markierungen"), closing the bug. `bugs/open/` now empty. **Final cycle regression run: all 18 TCs (TC-AGB-001–018) re-executed, all PASS** — header/filters, Board Settings, Insights, inline edit modal, quick-add, Backlog tabs, Global Board, My Page block, the full custom-board create→edit→delete lifecycle (flash messages + delete-confirmation modal), Sprint create form, admin Configure page, 1280×720 resolution testing, Sprints settings tab, and drag-and-drop (real mouse-drag, German toast confirmed) all reconfirmed clean, zero new failures. Open bug count: 0. Marked **Complete**. |
| redmineflux_tags | 2026-09-09 | 7.0.1.stable | 0 | **Complete** — Stage 1 (German + Default theme) complete: full sweep across New/Edit Project, New/Edit Issue, Issue Detail, New/Edit Spent Time, list filters/columns, and admin Manage Tags pages (all 3 tabs: Issue/Time Entry/Project Tags, each independently verified with real data). 6 TCs, 5 FAIL / 1 PASS initially. Separate functional observation flagged (not filed as a bug): the admin tag table's right-click context menu never appears despite `hascontextmenu` markup being present. **Stage 2 (resolutions) complete**: tested at 1280×720 and 1920×1080 — no new layout defects at either resolution. **2026-09-08: all 6 original bugs retested and CLOSED (fixed) on a newly-provisioned Forge server (flux-frtsiofsm49, branch updated) under both Standard and Lotus themes, per explicit user instruction to test only those two themes.** BUG-TAG-001 (tag-widget placeholder), BUG-TAG-002 ("Tag list" field, 6 forms), BUG-TAG-003 (admin page title raw hash literal), BUG-TAG-004 (admin Manage Tags UI + delete modal), and BUG-TAG-005 (Critical — delete-wrong-tag data-loss bug) all confirmed fixed under both themes. **BUG-TAG-006** ("Add Tags" widget overlapping Agile Board's Sprint/Story Points row) was retested once the user enabled the missing Story Points custom field: fixed under Standard theme, but still reproduces under Lotus — root-caused to the same layout gap as the Lotus theme's own `BUG-LTS-003` (Sprint/Story Points rendered outside Lotus's attributes grid), so folded into that bug instead and closed here as a Tags-plugin issue. **BUG-TAG-007 (Medium)** — found via direct user observation/screenshots: under Standard theme, the Markierungen (Tags) field's "Hinzufügen" trigger and edit widget don't align inline with the label like every sibling field (`Sprint:`, `Priorität:`, etc.) — confirmed via DOM the field is missing the standard `.attribute` label+value wrapper markup; does NOT reproduce under Lotus (its own stylesheet independently keeps it inline). **2026-09-09: retested on a newly-provisioned Forge server (flux-fdrk6suoj49, branch updated) under German + Standard theme — BUG-TAG-007 confirmed FIXED** (field now uses the standard `.attribute` wrapper, renders inline in view and edit mode). `bugs/open/` now empty (7/7 closed) — ran the required full final-cycle regression (`SENIOR_QA_STANDARDS.md` §27): all 9 TCs in `TAGS_GERMAN_LANGUAGE.md` re-executed on this server, **zero new failures**, including a network-request-level reconfirmation that the Critical delete-scoping fix (BUG-TAG-005) holds. Open bug count: 1 → **0**. Status set to `Complete` per CLAUDE.md §10 (empty `bugs/open/` + passed final-cycle regression on record). |
| redmineflux_inline_editor | 2026-09-10 | 7.0.1.stable | 0 | **Complete** — Stage 1 (German + Default theme). 3 bugs: BUG-INE-001 (Medium, Assigned-to searchable-dropdown "Search…" placeholder + "— None —" option untranslated, while its own "<<ich>>" option is correctly translated — reproduces on both the issue detail sidebar and the Issues list view, reconfirmed on a new Forge server after the original expired), BUG-INE-002 (Medium, found via cross-plugin testing with Checklist's "block issue closing" — the inline-edit save-rejection error toast wraps an otherwise correctly-translated German message in a hardcoded English "Could not save:" prefix), BUG-INE-003 (Medium, new — the shared post-save "Saved successfully." toast is hardcoded English on every inline-edit field, confirmed on Status/Priority/Description; Description's own CKEditor also has hardcoded English "Cancel"/"Save" buttons, while its tab labels/toolbar tooltips are correctly German). Also confirmed Priority's inline edit uses a plain native `<select>` (not the shared `rf-ss` widget Assignee uses), so it's immune to BUG-INE-001's pattern. Issues list view otherwise clean (header/filter/export links translated, Subject inline edit fine, sidebar "saved queries" confirmed as data not a translation gap). Also tested the Projects card/list view (Name inline-editable, PASS, no bugs) and confirmed a single project's Overview page + the admin Projects table are both out of this plugin's scope (zero inline-edit icons on either). **Stages 3/6 (Lotus theme, incl. 1280×720) complete**: `BUG-INE-001`/`BUG-INE-003` both reconfirmed theme-agnostic, no new plugin bugs (unlike Agile Board's `BUG-LTS-003`, since this plugin mostly attaches to core fields Lotus already styles). Discovered Lotus adds a richer Projects card design plus a card/list toggle not present under Default — the list view is the plugin's own "project table" view, tested clean. Found the Description CKEditor's "Save" button renders oversized (50px vs Cancel's 34px, both 28px under Default) specifically under Lotus theme — filed as `BUG-LTS-004` (Low) against the Lotus theme plugin, not counted in this row's total. Remaining untested: Start/End date, % complete, custom fields, and a dedicated Stage 2 (Default theme resolutions) pass. **2026-09-09: retested all 3 open bugs on a newly-provisioned Forge server (flux-f04qohdte49, branch updated) under Standard theme — all 3 STILL REPRODUCE, byte-for-byte identical, no fix has landed for this plugin.** New screenshot evidence captured for all 3 (including, for the first time, a static image of BUG-INE-002's fast-fading toast via a tight polling loop). Open bug count unchanged at 3. **2026-09-09 (second retest, new Forge server flux-fdrk6suoj49, branch updated): all 3 open bugs confirmed FIXED and closed** — BUG-INE-001 ("Search…"/"— None —" → "Suche"/"Keine"), BUG-INE-002 ("Could not save:" → "Konnte nicht gespeichert werden:"), BUG-INE-003 ("Saved successfully."/"Cancel"/"Save" → "Erfolgreich gespeichert."/"Abbrechen"/"Speichern"). Found and filed new **BUG-INE-004** (Low) — a previously-undocumented interim "Saving…" loading indicator shown during every inline-edit save, still hardcoded English, discovered incidentally while retesting BUG-INE-002/003. Open bug count: 3 → 1 (new bug). **2026-09-10 (Forge flux-fhhcov1xf49): BUG-INE-004 retested and confirmed FIXED** ("Saving…" → "Wird gespeichert…", confirmed on both Priority and Status changes) — closed, `bugs/open/` now empty. **Final cycle regression run: all 9 TCs (TC-INE-001–009) re-executed, all PASS**, zero new failures — Assignee widget, error/success toasts, Description CKEditor, Issues list view, Projects card view, and Lotus-theme retest (incl. `BUG-LTS-004`'s button-height fix) all reconfirmed clean. Open bug count: 0. Marked **Complete**. |
| redmineflux_lotus | 2026-09-10 | 7.0.1.stable | 1 | In Progress — **2026-09-09 retest pass**: retested all 5 open Lotus bugs on Forge server `flux-fhggkobjh49` (theme selected first per user instruction; the originally-used retest server `flux-fdrk6suoj49` began returning HTTP 403 on every route mid-pass, so the user provided this replacement). BUG-LTS-001 (admin+project sidebar label clipping), BUG-LTS-004 (Save/Cancel button height), BUG-LTS-005 (Configure page 404) confirmed FIXED and closed. **BUG-LTS-003 was also initially closed as fixed, then reopened same session**: the first check only compared outer `.attribute` container rects at 1920×1080 and missed that the Sprint value text (an anomalously narrow, wrapping span) overlaps the Story-Points label, and the Tags-widget editor overlaps the Sprint value, specifically at **1280×720** — the user caught this from their own screenshots. Re-verification with element-level bounding boxes confirmed 2 of 4 sub-findings still reproduce at 1280×720 (fixed at 1920×1080), confirmed Lotus-specific (clean under Default theme); the other 2 sub-findings (font/bullet mismatch, edit-form full-width break) are genuinely fixed at both resolutions. **BUG-LTS-002 (Medium) remains open** — issue detail tab strip still clips tab labels at 1280×720, now worse on this server (a 4th tab, "Eigenschaftsänderungen", appears, widening the shortfall from ~23px to ~174px). **BUG-LTS-006 (Medium, new) filed at user's explicit direction**: the subtask/related-tickets tables on the issue detail page render at ~2001px inside a ~548px container at 1280×720 (~32 columns, many contributed by different installed plugins), overlapping the Historie/Notizen sidebar column — confirmed theme-agnostic (reproduces under Default theme too), filed against Lotus anyway since no single plugin uniquely owns the column set. `bugs/open/` has 3 remaining bugs (BUG-LTS-002, BUG-LTS-003, BUG-LTS-006) — no final-cycle regression triggered; Dashboard plugin's own Lotus retest still pending as the last remaining plugin-level item for this cycle. **2026-09-10: BUG-LTS-002 invalidated.** User pushed back with a screenshot showing the "<"/">" buttons next to the tab strip are a scroll control, not evidence of broken clipping. Verified by clicking through the strip on a fresh server (`flux-fhhcov1xf49`): confirmed it's a working paginated "peek of next tab" affordance — earlier tabs are hidden as you page forward, and each tab renders fully once paged to (residual overflow ~5px in the final state, down from the ~23–174px originally described as "clipped"). Moved to `bugs/closed/` as not a bug. `bugs/open/` now has 2 bugs (BUG-LTS-003, BUG-LTS-006). **2026-09-10 (continued, Forge flux-f3lnytazd49): BUG-LTS-006 confirmed FIXED and closed** — a new `div.rf_issue_section_row` wrapper (`overflow-x: auto`) now scrolls the subtask/related-tickets table within its own ~703px bounds instead of spilling into the Historie/Notizen sidebar (confirmed via bounding-box check, clean ~37px gap, no overlap). **BUG-LTS-003 narrowed further**: the Sprint/Story-Points value overlap is now fixed too (Sprint value renders single-line at full width, no longer wrapping) — but the Tags-widget "Hinzufügen" editor still overlaps, now against the Story-Points **label** specifically (49.4×19.5px confirmed) rather than the Sprint value. 3 of BUG-LTS-003's original 4 sub-findings are now fixed; only this one overlap remains. `bugs/open/` now has just 1 bug (BUG-LTS-003) — still `In Progress`, final-cycle regression not yet triggered. |

## How to update

Update this file after every test run. One row per plugin.

**Status values:** `Not Started` / `In Progress` / `Complete`
