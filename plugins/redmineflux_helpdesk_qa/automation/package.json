{
  "name": "redmineflux-helpdesk-qa-automation",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright + TypeScript regression suite for redmineflux_helpdesk (Page Object Model)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "codegen": "playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^26.3.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.0"
  }
}
