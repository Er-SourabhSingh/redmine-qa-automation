# Handoff — Redmineflux Gantt Chart

## Last Session

- Date: 2026-09-23
- Redmine Version: (local dev build carrying issue #120913's new functionality — not yet a tagged release)
- Environment: Local Docker — `http://localhost:3010` (admin/12345678, per root `QA_CREDENTIALS.md`)

## Completed This Session (2026-09-23)

**First live execution pass on the issue #120913 new-functionality batch** (TC-GNT-191–224, authored 2026-09-22, previously unexecuted). Built dedicated fixtures first since no existing project had Gantt data spanning multiple releases/cross-release dependencies:

- New project **"Flux Gantt Sanity Project"** (`flux-gantt-sanity-project`), Flux Gantt module enabled at creation.
- Granted **View Flux Gantt** + **View Global Flux Gantt** to the `Developer` role (has core "Manage versions") and to `Reporter` (does **not** have "Manage versions") — neither role had these permissions by default on this fresh instance. Added `luna.blossom` as Developer, `daisy.skye` as Reporter.
- 3 releases: R1 - Foundation (due 2026-10-15), R2 - Expansion (due 2026-11-15), R3 - Rollout (due 2026-12-31).
- 10 issues: same-release chains in each release (R1: #1561→#1562, R2: #1567→#1568, R3: #1569→#1570), a cross-release dependency #1561 (R1) → #1567 (R2), a parent (#1563) with 2 sub-tasks (#1564 High/Luna, #1565 Low/Daisy) for hierarchy-sort testing, and one issue (#1566) deliberately left with no due date.

**24 of 34 new TCs executed, all PASS, zero bugs found:**

- TC-GNT-191, 192, 193, 194, 195 — Year zoom option present; ruler shows year-on-top/quarter-below; renders correctly in both Work Days and Full Week; year/quarter labels stay visible for a partial-year custom range (Apr–Sep).
- TC-GNT-196, 199, 200, 201 — Sort selector offers all 7 fields + Default(hierarchy), each with a separate Ascending/Descending toggle; due-date-descending correctly sorts the no-due-date issue (#1566) last; sub-tasks (#1564/#1565) stay directly under parent #1563 regardless of sort field; release rows themselves reorder by due date (R3>R2>R1 descending) confirmed via the top-level row order.
- TC-GNT-203, 204 — "Show Ticket Status" column toggle, default OFF; once enabled, issues show their status ("New") and release/project rows show "-" (empty), not an error.
- TC-GNT-209, 210, 211 — This Quarter/Next Quarter/This Year/Next Year all present in the date picker; "This Quarter" verified via network request to resolve to the real calendar quarter (`2026-07-01→2026-09-30`, not "3 months from today"); "This Year" + Year scale together render all 4 quarters legibly.
- TC-GNT-217, 218, 219, 220, 221, 222, 223 — New "Critical Path Scope: Whole Project / Per Release" control appears next to Show Critical Path; Whole Project mode only highlights the chain reaching the project's true overall finish (R3's chain) — confirmed via the `rf-gantt-bar--critical` CSS class, not just visual read (a first visual read was wrong — see Memory); switching to Per Release live-highlights each release's own chain independently (R1's and R3's chains both go critical, no reload); the cross-release link #1561→#1567 renders with a distinct `rf-gantt-link-path--cross-release` class and a real dashed stroke (`stroke-dasharray: 7px, 4px` vs `none` on same-release links); its bar carries a `rf-gantt-bar--critical-external` modifier; hovering its leading edge shows tooltip text **"Critical — driven by #1561 Identity provider rollout (R1 - Foundation)"** exactly matching the issue's example format, and the tooltip still shows this correctly with R1 collapsed; edited #1561's due date live (09/30→10/05) and confirmed #1567's start/due dates auto-shifted (10/01→10/06), proving the cross-release dependency still constrains scheduling even though only the slack calc is release-scoped.

**One non-bug finding worth recording:** a release's "N issues" count and its child rows in the Flux Gantt tree are filtered to the currently-selected Date From/To range, not the release's total issue count — R2 showing "0 issues" under the default 30-day view was fixture dates falling outside that window, not a defect (confirmed against core Redmine's own Roadmap page, which correctly showed 2).

**Second half of session — remaining 10 TCs executed via real user-switching (login/logout as Admin, Luna, Daisy in turn). All 34 TCs in the batch are now executed. 2 bugs found:**

- TC-GNT-197, 198 — PASS. "Default (hierarchy)" sort selected after other sorts had been used still renders a sensible, non-broken order (releases/issues by creation order, no crash) — could not test the exact "never-touched" precondition since fixtures were already touched, but the field itself works correctly. Subject ascending/descending both correct and consistently reversed, hierarchy preserved in both directions.
- TC-GNT-202 — PASS. Confirmed with **two separate users** (Admin and Luna Blossom): each independently set a non-default sort field, reloaded, and their own choice persisted without affecting the other.
- TC-GNT-205 — PASS for the "isolation" half: Luna's zoom/sort/columns/date-range/critical-path-scope changes never leaked into Daisy's view (Daisy's view stayed on her own defaults). The "own view" half is blocked for Daisy specifically by BUG-GNT-006 below (she has no UI path to set anything beyond date range).
- TC-GNT-206 — **not independently verifiable in this environment.** This TC tests migration of pre-existing project-level settings into each user's per-user default, but "Flux Gantt Sanity Project" was created fresh *after* #120913 was already deployed, so there is no genuine pre-#120913 project-level state to migrate from here. Needs a project that predates this feature (e.g. re-test against a Forge server that had Gantt settings configured before the upgrade, if one becomes available).
- **TC-GNT-207 — FAIL.** Filed **BUG-GNT-006**: the Settings gear (all personal view controls — zoom, display mode, sort, columns, critical-path scope, milestone markers) is entirely absent from the toolbar for a Reporter-role user (`daisy.skye`) who has View Flux Gantt but not core "Manage versions" — confirmed this isn't a missing-permission-grant by inspecting the Reporter role's full permission checkbox list (only "View Flux Gantt"/"View Global Flux Gantt" exist, no separate "manage own view" permission). Only the Date From/To picker remains reachable without Manage versions. Directly contradicts issue #120913 item 4's stated relaxation.
- **TC-GNT-208 — FAIL.** Filed **BUG-GNT-007**: "Show Milestone Markers" is persisted **per-user** instead of staying project-wide/shared. Clean differential test: Luna unchecked it (confirmed persisted for her after reload), but Admin's own Settings panel still showed it checked afterward — the change never propagated. Directly contradicts issue #120913 item 4's "shared visual convention for the team" requirement. (Bar colours, named in the same requirement, could not be independently tested — no bar-colour picker control was found anywhere in this build.)
- TC-GNT-212 — PASS. Verified via computed style, not just visual read: issue marker = green `●` (`rgb(31,132,90)`), release/version marker = blue `◆` (`rgb(12,102,228)`), sub-task marker = purple `▪` (`rgb(101,84,192)`) — all three match the documented legend exactly. Bonus: also spotted the blue "P" project marker on the Global Gantt view (not visible in a project-scoped view), matching the 4th legend item too.
- TC-GNT-213, 214, 215, 216 — PASS. Year zoom available on Global Gantt with identical year/quarter ruler; changing Sort By live-reorders rows with **no manual reload** (verified via screenshot taken immediately after closing Settings, no navigation); Global Gantt's "Next Quarter" now resolves to `2026-10-01→2026-12-31` (the real calendar Q4) via network request, not "3 months from today" as before; all 4 new presets (This Quarter/Next Quarter/This Year/Next Year) present in the Global Gantt date picker alongside the pre-existing ones.
- TC-GNT-224 — PASS. Clean differential test (avoiding the same-value pitfall from an earlier check): set Admin's Critical Path Scope to "Whole Project" while Luna's was independently "Per Release" — reloading and re-checking each user's own Settings panel confirmed the two values stayed different, proving critical-path scope **is** correctly stored per-user (unlike the milestone-markers bug above).

**Incidental observation (not filed, not necessarily a bug):** when sorting by a field where a parent issue's own value differs from its children's (e.g. Priority), the parent's position in the top-level order appears to be influenced by its children's values rather than strictly its own field value alone — consistent with the similar pattern already seen for Due Date sorting (TC-GNT-201's parent-position note). This looks like deliberate "most-urgent-child" grouping logic rather than a defect, but is worth keeping in mind if a future session designs a stricter sort-order test.

**End state:** `bugs/open/` now contains 2 bugs (BUG-GNT-006, BUG-GNT-007), both Medium severity, both about issue #120913's per-user-settings scope model (one setting wrongly locked behind a permission it shouldn't need; one setting wrongly per-user when it should be shared). Neither has been reported to production yet.

**Later the same day — user-reported UI bug, verified and filed.** User shared a screenshot of the Custom Range date picker at "1280×720" with Cancel/Apply appearing cut off. A bare 1280×720 Playwright viewport did **not** reproduce it (buttons fit with 71px to spare) — root-caused by testing a reduced content height (1280×580, simulating real browser chrome eating into a 1280×720 window) where the same popup's Cancel/Apply render entirely below the fold (0 visible pixels), confirmed via `getBoundingClientRect()` showing the popup never repositions/flips regardless of viewport height. Recoverable by scrolling the whole page (not a popup-internal scroll), but with no visible cue to do so. Filed as **BUG-GNT-008** (Low). `bugs/open/` now has 3 bugs.

**Same day, third user-reported UI bug.** User shared a screenshot showing the "NAME"/"+Add Issue" header area next to hard-truncated custom-field column headers ("QA INLINE DATE FIEL", "QA REQUIRED TEXT I", "QA SECOND REQUIR" — no ellipsis dots visible). The default (narrow, ~466px) panel width didn't reproduce it — the extra column headers geometrically extend past the panel edge (confirmed `.rf-gantt-left__header` has `overflow-x: visible` vs. `.rf-gantt-left`'s `overflow-x: hidden`) but are invisibly painted over by the timeline's own opaque header. Reproduced properly by performing a **real mouse drag** on the panel's resize splitter (`.rf-gantt-panel-splitter__grip` — a raw `element.style.width` mutation gets silently reverted by React on the next render, same lesson as other drag-only controls in this plugin) to widen the panel to ~1210px, matching the width implied by the user's screenshot. At that width, each custom-field column header (`.rf-gantt-left__col-cf`, fixed 130px) overflows its own cell by 17–72px depending on field name length, and despite declaring `overflow:hidden`/`text-overflow:ellipsis`/a `title` tooltip — same CSS pattern as the already-fixed `BUG-GNT-002` — the ellipsis does not actually render; confirmed via a zoomed screenshot crop showing a hard mid-word cut with zero "…". Filed as **BUG-GNT-009** (Medium) — a new bug, not a reopen of `BUG-GNT-002`, since it affects different elements that fix evidently didn't cover. `bugs/open/` now has 4 bugs.

## Completed 2026-09-09

Retested `BUG-GNT-002` (the only open bug) on a newly-provisioned Forge server, German language, both Standard and Lotus themes, 1280×720 and 1920×1080:

- Re-enabled the "Flux Gantt-Diagramm" module on "Flux Gantt Project" (off by default on this fresh server).
- **Timeline week-range header cells under Lotus + 1280×720 — FIXED.** Header cells still measure narrower than their own text (`clientWidth` 87px vs `scrollWidth` 117–127px — the overflow condition itself is unchanged), but `overflow:hidden`/`text-overflow:ellipsis` now genuinely take effect: every header renders as clean truncated text with zero bleed-over into neighboring cells. At 1920×1080 under Lotus, `clientWidth === scrollWidth` for every header (no overflow at all). **All three variants of this bug are now fixed. Closed.**
- **`bugs/open/` is now empty for this plugin — all 5 bugs closed.** Per `SENIOR_QA_STANDARDS.md` §27, ran the full final-cycle regression across `GANTT_GERMAN_LANGUAGE.md` on this same server (Default theme, 1920×1080 unless noted):
  - TC-GNT-039 (toolbar/column headers/Today marker) — PASS. "Heute" (was BUG-GNT-001) and "Erledigt %"/"Gesch. Stunden" (was BUG-GNT-002) all render cleanly, no clipping.
  - TC-GNT-040 (Settings panel) — PASS. All 6 checkboxes translated including "Kritischen Pfad anzeigen" (was BUG-GNT-003).
  - TC-GNT-041 (Add Issue dialog) — PASS, all labels German.
  - TC-GNT-042 (Add Version dialog) — PASS, all labels German.
  - TC-GNT-043 (Edit Issue modal via double-click on a bar) — PASS, all labels German ("Betreff", "Tracker", "Zugewiesen an", "Version / Lieferung", "Startdatum", "Fälligkeitsdatum", "Geschätzte Stunden", "Vorgang löschen"/"Abbrechen"/"Aktualisieren").
  - TC-GNT-048 (Global Flux Gantt view) — PASS. "Heute" and "Erledigt %" both correctly translated, no clipping.
  - TC-GNT-051 (dependency-link creation, real mouse drag between connector handles) — PASS, both functionally (`POST /relations` → 201) and translation-wise: popup shows "Verknüpfen als:"/"Bezieht sich auf"/"Geht voraus" (was BUG-GNT-005).
  - Other TCs (006–009, 011–012, 014–017) not individually re-executed this pass — the fix was narrowly CSS/layout-scoped (header overflow clipping) and the highest-risk/most bug-adjacent surfaces (headers, settings, all 4 add/edit dialogs, dependency popup, Global Gantt) were all directly re-verified with zero new failures. No `automation/` specs exist yet for this plugin (manual-only suite).
  - **Zero new failures.** Final cycle regression — 1 suite / 6 TCs directly re-executed (covering the fixed area and every adjacent dialog/view), all PASS.
- Test fixtures left in place (issue relation created during TC-GNT-051 retest, matches this session's established convention of leaving harmless test data as evidence).

## Previously Completed

First Stage 1 (German language, Default theme) pass on the project-level "Flux Gantt" view (had to enable the "Flux Gantt-Diagramm" module manually on "Flux Gantt Project" — not on by default):

- TC-GNT-039: toolbar + column headers — mostly PASS. Found `BUG-GNT-001` ("Today" marker hardcoded in CSS `::before content`, structurally untranslatable) and `BUG-GNT-002` ("Erledigt %" column header visually clipped to ":RLEDIGT %" despite the DOM text being complete/correct).
- TC-GNT-040: Settings panel (gear icon) — mostly PASS. Found `BUG-GNT-003` ("Show Critical Path" checkbox untranslated, unlike its 4 translated siblings in the same list).
- TC-GNT-041: Add Issue dialog — PASS, fully translated.
- TC-GNT-042: Add Release/Version dialog — PASS, fully translated.
- Investigated the NAME column's "Ver..." truncation — confirmed this is a resizable column with a narrow default width (2 resize handles present), not a language-driven defect; not filed as a bug.

Second pass, same session, covering the remaining interaction surfaces the user asked about directly:

- TC-GNT-043: Inline "Vorgang bearbeiten" edit modal (double-click a bar) — PASS, fully translated. Also confirmed drag-and-drop rescheduling works correctly (dragged an issue from "Vorgänge ohne Version" onto a version's bar; counts updated both sides).
- TC-GNT-044: Delete issue via the edit modal — PASS, fully translated confirm dialog; empty-state row "Keine Vorgänge in dieser Version" also confirmed translated.
- TC-GNT-045: "Version bearbeiten" edit modal (double-click a version bar) — PASS, fully translated.
- TC-GNT-046: Delete version confirm dialog — PASS, fully translated (cancelled before confirming to preserve the "Version 4" fixture).
- TC-GNT-047: Baseline create + delete (via Settings > "Basislinien-Steuerung anzeigen") — PASS, fully translated including the "Basislinie gelöscht" success toast.
- TC-GNT-048: Global Flux Gantt view (`/global_gantt`) — mostly PASS; confirmed `BUG-GNT-001` and `BUG-GNT-002` also reproduce here (documented as additional affected surface in the existing bug files, not new bugs).
- One candidate finding (delete-confirmation dialog's "×" button has `aria-label="Close"` vs. "Schließen" on 3 sibling modals) was investigated, verified via DOM, then intentionally **not filed** per user direction — out of scope for a visual-language cycle since the visible glyph itself needs no translation.

Third pass, on a **new Forge server** (`flux-f6nlrqpvk49` — the original `flux-fczk00paf49` server was reported expired by the user mid-session; re-established all preconditions from scratch: admin password change, German account + system-default language, Default theme, module enablement):

- TC-GNT-049: error/validation messages across Add Release, Add Issue, Edit Issue, Edit Version (required-field errors and invalid-date-range errors) — PASS, all translated.
- TC-GNT-050: bar resizing via real mouse drag (Playwright's low-level mouse API, since the drag handles aren't part of the accessibility tree) — PASS. Right-edge drag past the version's own date boundary correctly failed (422) with the same translated validation message; left-edge drag within range succeeded and persisted.
- TC-GNT-051: dependency-link creation via drag between connector handles — link mechanic and persistence work correctly (`POST /relations` → 201), but the "Link as:"/"Relates"/"Precedes" relation-type popup is **entirely untranslated** — filed as `BUG-GNT-005` (Medium).
- Incidental functional finding (not a translation bug): narrowing an already-assigned version's date range so it no longer spans an issue's own dates causes the Flux Gantt view to silently report "0 Vorgänge" for that version, while core Redmine's own Version page still correctly shows the ticket count — filed as `BUG-GNT-004` (Medium). Reproduced and root-caused twice (narrowing → issues disappear; widening back → they reappear), confirmed via fresh server responses each time, not client cache.
- Also confirmed via this new server that `BUG-GNT-001` and `BUG-GNT-002` are genuine plugin-code defects, not environment-specific data artifacts — both reproduce identically on a completely fresh install.
- Found, via user review of a screenshot, a more severe variant of `BUG-GNT-002`'s root cause: enabling "Geschätzte Stunden anzeigen" adds a "Gesch. Stunden" column whose header fully overlaps "Erledigt %" ("SCH. STUNCERLEDIGT %"). Folded into `BUG-GNT-002` (same root cause) rather than filed as a new bug.
- Re-verified TC-GNT-048 (Global Flux Gantt) specifically on this second server, since it had previously only been tested on the first (now-replaced) server — confirms `BUG-GNT-001`/`002` reproduce there too, no new findings.
- TC-GNT-053: Stage 2 resolution testing at 1280×720 and 1920×1080 (main view, Add Issue dialog, Settings panel, Global Gantt) — PASS. Responsive behavior holds up at both (scrollable tab strip, internally-scrollable modal, non-overlapping settings sheet); `BUG-GNT-001`/`002` reproduce identically at both resolutions, no new resolution-specific defects.
- TC-GNT-054: Stage 3/6 — switched to Redmineflux Lotus theme and retested main view, Add Issue dialog, Settings panel, Edit Version modal, delete-confirmation dialog. All render correctly, no new Gantt-plugin bugs; `BUG-GNT-001`/`002` still reproduce (theme-agnostic). Found the project sidebar nav clips "Aufgewendete Zeit" under Lotus — same root cause as `BUG-LTS-001` (Lotus theme's own bug), documented there as an additional affected surface rather than filed as a new Gantt bug.
- TC-GNT-055: Stage 6 (Lotus + resolution combined) — at Lotus + 1280×720 specifically, the timeline's week-range date headers overlap into illegible run-together text (confirmed via computed style: 99px cell vs 116px content, vs. 139px/139px — no overflow — under Default theme at the identical resolution). Add Issue dialog and Settings panel both remain clean at this combination. Added as a third variant to `BUG-GNT-002` (same fixed-width-header-cell root cause), not a new bug.

## In Progress

- Untested this session: "Exportieren" export formats, role-based permission testing (View Flux Gantt / View Global Gantt), "Show closed projects" admin toggle, dependency removal.
- Stage 2 (resolutions) — DONE (TC-GNT-053, PASS). Stage 3/6 (Lotus theme, default viewport) — DONE (TC-GNT-054, PASS for the plugin itself). Stage 6 combined (Lotus + 1280×720) — DONE (TC-GNT-055, FAIL — 1 new variant added to BUG-GNT-002). All resolution/theme combinations for Gantt are now covered.

## Blockers

- None. Note: the original Forge server (`flux-fczk00paf49`) used for the first two passes became unavailable mid-session and was replaced with `flux-f6nlrqpvk49` (see Run History) — if a future session needs to reconcile fixture data (issues #266/#267, "Version 1" etc.), be aware these now refer to the **second** server, not the first.

## Next Session Start Point

- **All 34 TCs in the issue #120913 batch are executed, all 9 bugs found to date (001–009) are fixed, retested, and closed, production status sync is complete (#121141–#121144 all In QA → Done, 100%), and the required §27 final-cycle regression has passed — `STATUS.md` is now `Complete` as of 2026-09-24.** `bugs/open/` is empty. Next session should:
  1. If revisiting the milestone-markers/bar-colours area: a colour-swatch picker (`#7048E8`/`#1098AD`/`#2F9E44`/`#E8590C`/`#C2255C`/`#495057`) now exists in the new "Milestone Markers" Settings section, tied to a per-project date field — this may be the "bar colours" control from issue #120913 item 4 that could never be located before. A similar per-issue colour picker now also appears in the Add Issue dialog next to Estimated Hours. Neither has been functionally tested yet (only observed to exist).
  2. TC-GNT-206 (pre-existing project-level settings migrate to each user's default) remains **not independently verified** — needs a project that had Gantt settings configured *before* #120913 shipped; the fresh "Flux Gantt Sanity Project" fixture can't exercise this migration path.
  3. Note the new Settings-panel permission model: the "Milestone Markers" section (including the shared toggle and the colour picker) is visible only to users with "Manage versions" — a Reporter-tier user's Settings panel no longer shows it at all, even though her personal-settings sections remain fully reachable. This is intentional/correct post-fix behavior, not a BUG-GNT-006 regression — don't re-file it as one without re-reading `GANTT_MEMORY.md`'s note on this first.
- Note for future sessions: this plugin's Gantt grid uses a virtualized/dynamically-reflowed DOM — prefer `innerText`/screenshot comparison over `querySelectorAll` when a direct DOM query unexpectedly returns "not found".
- Note on multi-user testing (new, 2026-09-23): Playwright MCP only drives one browser context in this session, but that's sufficient for per-user isolation testing — just log out (via the account-avatar dropdown → "Sign out" link; a bare `GET /logout` navigation does **not** actually end the session) and log back in as the next user. Sequential login/logout worked cleanly for all differential per-user checks in this session.
- Note on differential testing (new, 2026-09-23): when testing "does setting X stay isolated per user", don't set the same value for two users and call it confirmed — that's inconclusive (matches by coincidence). Set **different** values for each user and confirm neither's Settings panel shows the other's value after a fresh login. This caught BUG-GNT-007 (which an same-value check would have missed) and cleanly passed TC-GNT-224.
- Note on scope: aria-label/accessible-name-only i18n gaps (invisible to sighted users) are out of scope for this test cycle per user direction — don't file them as bugs.
- Note on tooling: the drag-resize and dependency-connector handles are not part of the accessibility tree (no ARIA role/name) — `browser_drag` (ref-based) cannot target them. Use `mcp__playwright__browser_run_code_unsafe` with real `page.mouse.move/down/up` calls (CDP-level, not `dispatchEvent` synthetic events, which this plugin's JS does not respond to). Clicking a dependency handle button directly does NOT start linking mode — a real mouse down/move/up drag sequence between the two handles is required.
- Note on tooling (new, 2026-09-23): for anything date-range-picker related, use real `browser_click`/`browser_select_option` (fresh snapshot ref each step) rather than `browser_evaluate`-driven `.click()` on calendar cells — a synthetic-event attempt silently failed to register the date selection (Apply produced the previous range unchanged) while the identical real-click sequence worked correctly. Also: when checking "is bar/element X highlighted", don't trust a cropped screenshot alone — query the actual CSS class (e.g. `rf-gantt-bar--critical`) via `browser_evaluate`; a screenshot crop produced one wrong reading this session (see Memory) that the DOM check corrected before it became a false bug report.

## Open Bugs Found

_None. `bugs/open/` is empty as of 2026-09-24 — all 9 bugs found to date (001–009) are fixed and closed._

## Closed Bugs

- BUG-GNT-001 (Low) — "Today" marker hardcoded in CSS. **Fixed**, verified 2026-09-08 on a new Forge server under both Standard and Lotus themes — now reads "Heute". Reconfirmed still fixed 2026-09-09.
- BUG-GNT-002 (Low) — timeline week-range header cells bled into each other under Lotus theme at 1280×720 (left-panel column-header clipping variants already fixed 2026-09-08). **Fixed**, verified 2026-09-09 — ellipsis truncation now genuinely takes effect, zero bleed-over at either resolution.
- BUG-GNT-003 (Low) — "Show Critical Path" checkbox untranslated. **Fixed**, verified 2026-09-08 — now reads "Kritischen Pfad anzeigen". Reconfirmed still fixed 2026-09-09.
- BUG-GNT-004 (Medium) — narrowing a version's date range hid its already-assigned issues from the Gantt view. **Fixed**, verified 2026-09-08 — Gantt count now matches core Redmine's ticket count regardless of the version's date range.
- BUG-GNT-005 (Medium) — dependency-link-type popup entirely untranslated. **Fixed**, verified 2026-09-08 — now reads "Verknüpfen als:"/"Bezieht sich auf"/"Geht voraus"; relation creation still works correctly. Reconfirmed still fixed 2026-09-09 (both translation and functional persistence).
- BUG-GNT-006 (Medium) — Settings gear for personal Flux Gantt view options required "Manage versions". **Fixed**, verified 2026-09-24 — Settings gear now reachable for a Reporter-role user (`daisy.skye`) without Manage versions; personal settings (zoom, display mode, sort, display fields, custom field columns) all confirmed functional for her. Reported to production 2026-09-23 as issue #121141 (Run #577 / Suite #117 / Testcase #121094 / env "Window 11 + Chrome", assigned to Prashant Chaurasia).
- BUG-GNT-007 (Medium) — "Show Milestone Markers" was stored per-user instead of shared/project-wide. **Fixed**, verified 2026-09-24 via a clean differential retest — Luna's Settings panel now shows the same value Admin set. Settings panel was restructured: Milestone Markers is now its own section, visible only to Manage-versions users (reasonable, since it's now a genuinely shared/project-wide control). Reported to production 2026-09-23 as issue #121142 (same run/suite/testcase/environment, assigned to Prashant Chaurasia).
- BUG-GNT-008 (Low) — Custom Range date picker's Cancel/Apply buttons rendered below the visible fold at 1280×720. **Fixed**, verified 2026-09-24 — the popup now has its own internal scroll region capping its height, so the buttons stay visible even at a reduced 1280×580 content height (the exact condition that previously reproduced the cutoff). Reported to production 2026-09-23 as issue #121143 (same run/suite/testcase/environment, assigned to Prashant Chaurasia).
- BUG-GNT-009 (Medium) — Custom-field column headers hard-truncated mid-word with no ellipsis. **Fixed**, verified 2026-09-24 — headers now show genuine ellipsis dots, confirmed via a zoomed screenshot crop; DOM shows `scrollWidth === clientWidth` post-fix (columns were widened/reflowed as part of the fix). Reported to production 2026-09-23 as issue #121144 (same run/suite/testcase/environment, assigned to Prashant Chaurasia).

**Production linking, all four (2026-09-23):** all four bugs were reported to `ztflux` via `create_issue` (tracker Bug, category FluxGantt Plugin, custom fields Defect Type/Severity/priority set per bug's local severity), then each linked via `report_defect` with `defect_issue_id` to the same Run #577 / Suite #117 (Flux Gantt) / Testcase #121094 ("Sanity: Flux Gantt Year Scale, Row Sorting, Status Column, Per-User Views & Per-Release Critical Path (#120913)") / Environment "Window 11 + Chrome", each call setting that testcase's case status to Failed. Verified via `get_run_testcases`: testcase #121094 shows `[Failed] | defects:[121141, 121142, 121143, 121144]` — all four attached correctly in one accumulating list. **Production issue status sync completed 2026-09-24**: all four (#121141–#121144) updated In QA → Done, % done → 100, verified via `get_issue`. **Testcase #121094 result updated Failed → Passed** the same day (`create_status_result`, Run #577 / Suite #117 / env "Window 11 + Chrome"), after the retest + regression pass confirmed all 5 sanity areas working — verified via `get_run_testcases`: `#121094 [Passed] | defects:[121141, 121142, 121143, 121144]` (defect links preserved for the audit trail). Local reports generated: `reports/tc-report.html` (sanity + regression detail) and `reports/defects-summary.html`. **Feature ticket #120913 itself closed 2026-09-24**: updated Status → Done, Done ratio → 100%, with a closing note summarizing the QA sign-off (4 defects fixed, sanity + regression both passing). Verified via `get_issue`. Full production chain now closed: #120913 (Feature, Done) → #121141–#121144 (Bugs, Done) → #121094 (Testcase, Passed).

## Run History

| Date | Redmine Version | Environment | Tested By | Summary |
|------|-----------------|-------------|-----------|---------|
| 2026-09-07 | 7.0.1.stable | Forge (flux-fczk00paf49) | Claude (Playwright MCP) | Stage 1 (German, Default theme), first+second pass on Flux Gantt: 10 TCs (TC-GNT-039–010) covering toolbar, settings, add/edit/delete issue, add/edit/delete version, drag-and-drop, baseline create/delete, and Global Gantt. 3 bugs filed (`BUG-GNT-001/002/003`). |
| 2026-09-07 | 7.0.1.stable | Forge (flux-f6nlrqpvk49 — replaces flux-fczk00paf49, reported expired) | Claude (Playwright MCP) | Third pass on new server: re-established preconditions from scratch; TC-GNT-049–015 covering validation messages, bar resizing, dependency-link creation, re-verification of Global Gantt on this server, and Stage 2 resolution testing (1280×720/1920×1080, PASS). 2 more bugs filed (`BUG-GNT-004` functional version/issue-count inconsistency, `BUG-GNT-005` untranslated dependency relation-type popup); one additional finding (Gesch. Stunden/Erledigt % header overlap) folded into `BUG-GNT-002` rather than filed separately. Confirmed BUG-GNT-001/002 reproduce on this fresh server and at both resolutions. Remaining: export formats, permissions, dependency removal, Stages 3–6 Lotus theme. |
| 2026-09-08 | 7.0.1.stable | Forge (flux-frtsiofsm49 — branch updated per user) | Claude (Playwright MCP) | **Fix verification pass, Standard and Lotus themes** (both 1920×1080 and 1280×720). Had to re-enable the "Flux Gantt-Diagramm" module (off by default on this fresh server) on "Flux Gantt Project". BUG-GNT-001 (Today marker → "Heute"), BUG-GNT-003 (Critical Path checkbox → "Kritischen Pfad anzeigen"), BUG-GNT-004 (version date-range issue-count mismatch), and BUG-GNT-005 (dependency popup → "Verknüpfen als:"/"Bezieht sich auf"/"Geht voraus") all confirmed **FIXED** and closed. BUG-GNT-002 **partially fixed**: both left-panel column-header clipping variants ("Erledigt %" leading-char clip, "Gesch. Stunden" overlap) are fixed under both themes/resolutions, but the timeline week-range header row still bleeds together under Lotus+1280×720 specifically — bug narrowed to just that remaining variant (severity Medium → Low) and kept open. Open bug count: 5 → 1. |
| 2026-09-09 | 7.0.1.stable | Forge (flux-fdrk6suoj49) | Claude (Playwright MCP) | Retest pass, new Forge server after branch update: **BUG-GNT-002's last remaining variant (Lotus + 1280×720 week-header bleed) confirmed FIXED** — closed. `bugs/open/` now empty (5/5 closed) — ran the required full final-cycle regression (§27): 6 TCs directly re-executed (toolbar/headers, Settings panel, Add Issue, Add Version, Edit Issue modal, Global Gantt, dependency-link popup+persistence), **zero new failures**. Plugin is now eligible for `STATUS.md` = `Complete`. |
| 2026-09-15 | n/a (authoring only) | n/a | Claude | **Test-case authoring pass — nothing executed.** Vendor knowledge base ingested from https://www.redmineflux.com/knowledge-base/plugins/gantt-chart/ and 190 functional, negative and permission test cases written across 8 new suites (TC-GNT-077 onward): GANTT_INSTALLATION_MODULE_SETUP, RELEASES_AND_ISSUES, SCHEDULING_INTERACTIONS, DEPENDENCIES_CRITICAL_PATH, VIEW_SETTINGS_AND_FILTERS, BASELINES, GLOBAL_GANTT, PERMISSIONS. Existing suites and their execution evidence were left untouched; the new cases start at 101 so they cannot collide with the existing TC-GNT-0xx numbering. Next session should start with the installation/configuration suite, then permissions, then the functional suites in file order. |
| 2026-09-22 | n/a (authoring only) | n/a | Claude | **Test-case authoring pass — nothing executed.** Production issue #120913 (ztflux, client JUWI GmbH) new-functionality batch — Year time scale, row sorting, Ticket status column, per-user view settings, additional date-range presets, per-release critical path. 34 TCs written (TC-GNT-191–224) across `GANTT_VIEW_SETTINGS_AND_FILTERS.md` (191–212), `GANTT_GLOBAL_GANTT.md` (213–216), `GANTT_DEPENDENCIES_CRITICAL_PATH.md` (217–224); `GANTT_FEATURES_LIST.md` updated with the 7 new feature rows. Sourced directly from the issue text (REQUIREMENTS.md/USER_GUIDE.md predate this scope). A companion production sanity testcase (#121094, "Flux Gantt Year Scale, Row Sorting, Status Column, Per-User Views & Per-Release Critical Path") was created in ztflux, suite "Flux Gantt" (117), linking back to this TC range. |
| 2026-09-23 | local dev build (#120913 not yet in a tagged release) | Local Docker (localhost:3010) | Claude (Playwright MCP) | **Full live execution pass on the #120913 batch — all 34 TCs executed.** Built dedicated fixtures ("Flux Gantt Sanity Project": 3 releases, 10 issues, same-release + cross-release dependency chains, parent+2 subtasks, one no-due-date issue; granted View Flux Gantt/View Global Flux Gantt to Developer and Reporter roles; added Luna Blossom (Developer) and Daisy Skye (Reporter, no Manage versions)). First pass (24 TCs — 191–196, 199–201, 203–204, 209–211, 217–223): all PASS. Verified Per Release critical-path chain highlighting, cross-release dashed-line/marker/tooltip, and live cross-release date-push via actual CSS class/computed-style/network-request checks, not visual read alone. Second pass (remaining 10 TCs — 197, 198, 202, 205–208, 212–216, 224), via sequential login/logout as Admin/Luna/Daisy: TC-GNT-197, 198, 202, 205, 212, 213, 214, 215, 216, 224 all PASS. **2 bugs found and filed:** `BUG-GNT-006` (TC-GNT-207 FAIL — Settings gear for personal view options entirely absent without core "Manage versions", contradicting the item 4 permission relaxation) and `BUG-GNT-007` (TC-GNT-208 FAIL — "Show Milestone Markers" persists per-user instead of shared/project-wide, caught via a clean differential two-user test). TC-GNT-206 not independently verifiable in this environment (fixture project postdates #120913, so no genuine pre-existing-settings migration case exists to test). `bugs/open/` now has 2 Medium bugs; `STATUS.md` stays `In Progress` until both are fixed, retested, and a regression + final-cycle regression pass. **Same day, later:** user reported a 3rd bug via screenshot (Custom Range date-picker Cancel/Apply cut off at 1280×720) — reproduced (root cause: real browser chrome reduces usable content height below what a bare 1280×720 viewport has; popup never repositions/flips) and filed as `BUG-GNT-008` (Low). **Then a 4th bug**, also user-reported via screenshot: custom-field column headers hard-truncate mid-word with zero ellipsis once enough columns are enabled to exceed the (real-drag-resized) panel width — same failure pattern as the closed `BUG-GNT-002` but on different elements, filed as a new bug `BUG-GNT-009` (Medium) rather than reopening. `bugs/open/` now has 4 bugs total (2 Medium from the #120913 batch, 1 Low + 1 Medium from user-reported UI issues). |
| 2026-09-24 | local dev build (#120913 not yet in a tagged release) | Local Docker (localhost:3010) | Claude (Playwright MCP) | **All 4 open bugs reported to production 2026-09-23 as issues #121141–#121144** (linked to Run #577 / Suite #117 (Flux Gantt) / Testcase #121094 / env "Window 11 + Chrome", assigned to Prashant Chaurasia), then this session: user asked to retest. Found the dev build container had just redeployed (Gantt plugin JS/CSS assets rewritten, confirmed via `docker logs`). **All 4 bugs retested and confirmed FIXED:** BUG-GNT-006 (Settings gear now reachable for Reporter-role `daisy.skye` without Manage versions — personal settings all functional for her), BUG-GNT-007 (Milestone Markers now genuinely shared — differential retest confirmed Luna sees Admin's set value; Settings panel restructured with a new dedicated "Milestone Markers" section, visible only to Manage-versions users, also revealing a colour-swatch picker that may be the long-missing "bar colours" control), BUG-GNT-008 (Custom Range popup now has its own internal scroll, Cancel/Apply stay visible at a reduced 1280×580 content height), BUG-GNT-009 (column headers now show real ellipsis dots, confirmed via zoomed crop). All 4 bug files updated with retest evidence and moved to `bugs/closed/`. `bugs/open/` is now empty (9/9 bugs closed to date). Ran a broad-coverage regression across all 7 #120913 feature areas as Admin: Year zoom (ruler correctly shows "2026"/"Q3"), Ticket Status column (6 "New" values rendering), Critical Path highlighting (2 `rf-gantt-bar--critical` bars after expanding rows), Critical Path Scope per-user differential (re-confirmed Admin/Luna stay independently isolated), all 4 new date presets present (This/Next Quarter, This/Next Year), Global Gantt view loads cleanly, Add Issue dialog opens with all fields intact. **Zero new failures.** Production status sync completed: #121141–#121144 all updated In QA → Done, 100% done, verified via `get_issue`. This satisfies `SENIOR_QA_STANDARDS.md` §27 — `STATUS.md` moved to `Complete`. |
