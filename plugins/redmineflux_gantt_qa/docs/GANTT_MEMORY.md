# Plugin Memory — Redmineflux Gantt Chart

> Plugin-specific observations only. Global rules live in root MEMORY.md.

## Known Quirks

- The "Flux Gantt-Diagramm" project module is NOT enabled by default on any fresh Forge server, even on a project seemingly built for it ("Flux Gantt Project") — always check/enable it at Project Settings > Modules before testing.
- Dependency-link connector handles (the hover-revealed "Eingehende/Ausgehende Abhängigkeit erstellen" buttons) look like normal `<button>` elements but clicking one does NOT start linking mode by itself — a real `page.mouse` down/move/up drag sequence from one bar's outgoing handle to another bar's incoming handle is required (same technique as bar-resizing). `dispatchEvent` synthetic events and a plain `.click()` both do nothing.
- The Gantt grid's DOM is virtualized/dynamically reflowed — a `querySelectorAll` text search can return stale/wrong results if the grid just re-rendered; prefer re-querying immediately before acting, and cross-check with a screenshot when a result looks surprising (e.g. a header that "disappears" from one query but is actually just off in a different render pass).
- A release/version row's "N issues" count and its child rows in the Flux Gantt tree are filtered to the currently-selected Date From/To range, not the release's total issue count. A version whose assigned issues fall outside the visible window will show fewer issues (even "0 issues") — this is by design, not `BUG-GNT-004`'s old under-reporting bug (that was root-caused and fixed on 2026-09-08/09). Cross-check against core Redmine's own Roadmap page (`/versions/<id>`, real total) before suspecting a regression.
- Neither the `Developer` nor `Reporter` role has "View Flux Gantt" / "View Global Flux Gantt" checked by default on a fresh instance (confirmed on the `localhost:3010` local dev build, 2026-09-23) — grant them explicitly at Administration > Roles before any permission-based Gantt test needs a non-Manager role.
- The dependency-connector drag technique (`page.mouse.move/down/up`, not `dispatchEvent`) also applies to the date-range picker's calendar cells — a `browser_evaluate`-driven `.click()` on a calendar `<td>` silently fails to register with the picker's internal state (Apply submits the unchanged previous range) even though the cell visually appears selected; use real `browser_click`/`browser_select_option` with a fresh snapshot ref for every step of a custom date-range selection.
- New per-#120913 CSS classes worth knowing for evidence-gathering (avoid relying on a cropped screenshot alone to judge critical-path highlighting — verify via these classes/computed styles instead): `rf-gantt-bar--critical` (issue bar is on the highlighted chain), `rf-gantt-bar--critical-external` (its incoming critical dependency originates in a different release), `rf-gantt-link-path--cross-release` on the SVG `<path>` (computed `stroke-dasharray: 7px, 4px` vs `none` for a same-release critical link). The cross-release tooltip text format is exactly `"Critical — driven by #<id> <subject> (<release name>)"`.
- The Custom Range date-picker popup (opened via Date From/To → Custom Range) does not reposition/flip or cap its own height when there isn't enough vertical room below — its Cancel/Apply buttons sit at a fixed `top:615/bottom:649` regardless of viewport height. A **bare** 1280×720 content viewport (e.g. a raw Playwright resize) has room to spare, but a **real** 1280×720 browser window (tab bar + address bar + OS taskbar chrome eating into that 720px) does not — reproduce this class of "cut off at 1280×720" report by testing at a reduced content height (e.g. 1280×580) rather than a literal 1280×720 Playwright viewport, or the bug won't show up. Recoverable via page-level scroll, but with no visible affordance pointing to it (filed as `BUG-GNT-008`).
- The left panel's resize splitter (`.rf-gantt-panel-splitter__grip`) is React-state-driven — a raw `element.style.width` mutation via `browser_evaluate` gets silently stomped back on the next render. Use a real `page.mouse.move/down/move.../up` drag sequence (via `browser_run_code_unsafe`) on the grip to actually resize the panel, same lesson as the dependency-connector and date-picker-calendar drag techniques already noted here.
- Custom-field column headers (`.rf-gantt-left__col-cf`) declare `overflow:hidden`/`text-overflow:ellipsis`/a `title` tooltip, same as the already-fixed `BUG-GNT-002` headers — but the ellipsis does **not** actually render once enough columns are enabled to exceed the resized panel width; text hard-cuts mid-word with zero "…" (confirmed via a zoomed screenshot crop, not just a compressed read). Also: the header container (`.rf-gantt-left__header`, `overflow-x: visible`) and the body/row container (`.rf-gantt-left`, `overflow-x: hidden`, fixed resizable width) don't match — at the default (narrow) panel width the extra header cells geometrically extend past the panel edge into the timeline's screen region, invisibly, since the timeline's own opaque header currently paints over them. Filed as `BUG-GNT-009`.

## Confirmed Working

- As of 2026-09-09 (Forge `flux-fdrk6suoj49`), all 5 bugs found during this plugin's German-language cycle are fixed and closed, and a final-cycle regression covering the fixed area plus every adjacent dialog/view passed with zero new failures.
- The fix for `BUG-GNT-002` (header/column overflow) made `overflow:hidden`/`text-overflow:ellipsis` genuinely take effect across all 3 previously-affected surfaces (left-panel "Erledigt %" header, "Gesch. Stunden" overlap, Lotus+1280×720 timeline week-headers) — same root-cause fix, confirmed via bounding-box/computed-style checks, not just visual impression.
- As of 2026-09-23 (local `localhost:3010`), most of the issue #120913 new-functionality batch (32/34 TCs — Year scale, row sorting incl. hierarchy-preserving and no-value-sorts-last behavior, Ticket status column, new date presets incl. real calendar-quarter alignment on both project and Global Gantt, live sort reorder on Global Gantt, and the full per-release critical path feature incl. cross-release dashed-line/marker/tooltip and live date-push, and per-user sort/critical-path-scope isolation) passes cleanly. **2 TCs fail** — see Recurring Issues.

## Recurring Issues

- **User-reported: Custom Range date-picker Cancel/Apply buttons cut off at 1280×720** — filed `BUG-GNT-008` (Low) 2026-09-23, reported by the user with a screenshot from a real browser window. Reproduced by testing a reduced content height (1280×580) rather than a literal 1280×720 viewport (see Known Quirks above for why). Buttons are recoverable via page scroll but there's no visible scroll affordance on the popup itself.
- **User-reported: custom-field column headers cut off/overlapping when many columns are enabled** — filed `BUG-GNT-009` (Medium) 2026-09-23, reported by the user with a screenshot. Same failure pattern as the closed `BUG-GNT-002` (ellipsis declared, not rendering) but on different elements (dynamically-rendered custom-field headers), so filed as a new bug rather than reopening. Required a real splitter-drag to reproduce (see Known Quirks above) — the default narrow panel width doesn't visibly show the defect since the timeline's opaque header currently masks it.
- **Personal Gantt view settings (issue #120913 item 4) are not fully implemented as a coherent per-user model** — two distinct failure modes found 2026-09-23, both filed (`BUG-GNT-006`, `BUG-GNT-007`):
  - The Settings gear itself is still gated behind core "Manage versions" (no separate plugin permission exists for it), so a Reporter-tier user has no UI path to any personal setting except Date From/To.
  - "Show Milestone Markers" is stored per-user when the issue explicitly requires it to stay shared/project-wide (unlike Critical Path Scope, which correctly stays per-user — confirmed via a clean two-different-values differential test).
  - When re-testing a fix for either bug, use a **differential** check (set different values for two users, not the same value) — a same-value check is inconclusive and would have missed BUG-GNT-007.
- When sorting by a field where a parent issue's own value differs from its children's (e.g. Priority), the parent's top-level position appears influenced by its children's values, not strictly its own — same pattern already seen for Due Date sorting. Not filed as a bug (looks like deliberate "most-urgent-child" grouping), but worth knowing before designing a stricter sort-order assertion.

## Environment Notes

- This plugin's testing has moved across several Forge servers as they've been provisioned/expired: `flux-fczk00paf49` → `flux-f6nlrqpvk49` → `flux-frtsiofsm49` → `flux-fdrk6suoj49`. Always reconfirm existing (closed) bugs on a new server before assuming they're still fixed there.
- 2026-09-23: a new local Docker environment at `http://localhost:3010` is in use for the #120913 feature work (a local dev build, not a Forge server) — separate from the Forge servers above. Fixture data ("Flux Gantt Sanity Project", issues #1561–1570) lives only on this instance.
