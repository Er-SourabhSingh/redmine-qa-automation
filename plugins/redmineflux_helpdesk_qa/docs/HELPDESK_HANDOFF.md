# Handoff — Redmineflux Helpdesk

## Last Session

- Date: 2026-08-21
- Redmine Version:
- Environment:

## Completed This Session

- Plugin QA folder scaffolded (docs, testcases, bugs, screenshots, reports, logs)
- Playwright + TypeScript automation framework scaffolded under `automation/` (config, POM base, login page object, role-based auth setup, env loader)
- Docs/testcases file naming switched to `HELPDESK_<NAME>.md` (UPPER_SNAKE_CASE, plugin-prefixed) — now the standard for all new plugins, see `CLAUDE.md` §2b
- `HELPDESK_USER_GUIDE.md`, `HELPDESK_FEATURES_LIST.md` (54 features, categories A–K), and `HELPDESK_REQUIREMENTS.md` all populated with real content derived from the plugin's actual documented behavior
- Per-environment test data registry convention added (`automation/testdata/<PREFIX>_TESTDATA_<ENV>.xlsx`), `HELPDESK_TESTDATA_LOCAL.xlsx` generated (empty — no test execution has happened yet)
- **All 54 features now have written test cases** across 12 suite files in `testcases/`, positive/negative/edge, every case traceable to a specific line in the user guide:
  - `HELPDESK_PLUGIN_INSTALLATION.md` (#1–4, TC-001–012)
  - `HELPDESK_NAVIGATION_WORKSPACES.md` (#5–8, TC-060–074)
  - `HELPDESK_TICKET_LIFECYCLE.md` (#9–17, TC-013–032 + 033–041)
  - `HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md` (#18–21, TC-042–059)
  - `HELPDESK_SLA_ESCALATION.md` (#22–32, TC-075–107)
  - `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` (#33–37, TC-108–124)
  - `HELPDESK_PREPAID_HOURS.md` (#38–40, TC-125–142)
  - `HELPDESK_EMAIL.md` (#41–44, TC-143–156)
  - `HELPDESK_CONTENT_TEMPLATES.md` (#45–47, TC-157–180)
  - `HELPDESK_REPORTING_AUTOMATION.md` (#48–51, TC-181–203)
  - `HELPDESK_PERMISSIONS.md` (#52–53, TC-204–214)
  - `HELPDESK_RAKE_TASKS.md` (#54, TC-215–227)
- Two documentation contradictions in the plugin's own user guide were caught and given dedicated resolver test cases rather than silently picked one way: TC-HLP-122 (does a customer hold one project-access row or many?) and TC-HLP-213 (does `manage_helpdesk` alone grant report export, or is `export_helpdesk_reports` strictly required?)

- **Live testing session on Forge (`flux-fiqll6jza49.forge.zehntech.com`) — first real execution pass:**
  - Enabled the Helpdesk module on "Helpdesk Service Desk"; granted `view_helpdesk` to QA role and `manage_helpdesk` to Team Lead role; reassigned `luna.blossom`→QA, `daisy.skye`→Team Lead, `autumn.grace`→Project Lead on that project (they double as L1/L2/L3 escalation assignees)
  - Confirmed PASS: TC-HLP-097 (holiday CRUD incl. multi-day range), TC-HLP-075/076/078 (SLA creation, working hours/days/holidays saved, field-level History diff), TC-HLP-089 (L1→L2→L3 support-level chain with a pre-existing "test" level as L1), TC-HLP-108 (organization creation), TC-HLP-205 (manage_helpdesk grants config management — done implicitly by daisy.skye performing all the above)
  - Extra pre-existing seed data discovered mid-session (not created by us): a support level named "test" (order 1) and an SLA named "dsfasd" — `HELPDESK_MEMORY.md`'s earlier "zero support levels/SLAs exist" note was wrong and has been corrected
  - **TC-HLP-122 partially informs**: the New Customer form's Project Access section is NOT statically absent as first thought — it renders conditionally once an eligible project/SLA/organization exists. Full resolution still pending — see updated BUG-HLP-001 status below.
  - **TC-HLP-109: PASS on retry** — customer "Retry Test" (`retrytest01`) created successfully (Project access left empty). Several earlier attempts with Project access filled in appeared to fail; not yet re-confirmed whether that combination specifically fails or whether it was the same intermittent issue.

## In Progress

- **2026-08-24: Forge instance rotated** — old URL (`flux-fiqll6jza49`) went offline (`ERR_NAME_NOT_RESOLVED`) mid-session; new URL is `flux-fnwchpuwl49.forge.zehntech.com`, `QA_CREDENTIALS_FORGE.md` updated. All prior session's data (Acme Corp, SLA "Standard", holidays, support levels, customer retrytest01) is gone — fresh instance from the same seed template. Re-did the module-enable + role-grant + user-reassignment setup identically (same role IDs 11/12 and membership IDs 18/27/36 — same template). BUG-HLP-001 stays open/tracked regardless of environment since it's a plugin bug, not environment-specific.
- **CRUD-first verification pass COMPLETE** (per explicit direction: do full CRUD on Organization, Customer, SLA, Holiday, Support Levels BEFORE creating seed data). All five entities now have confirmed Create/Read/Search-match/Search-no-match/Update; Delete was actually executed end-to-end for Organization/SLA/Support Level (throwaway entities), and markup-confirmed (same JS modal) for Holiday/Customer without deleting their kept real records. See `HELPDESK_MEMORY.md` for full detail on each entity and on the two bugs found along the way (BUG-HLP-001 update, BUG-HLP-002 new).
- **TC-HLP-110/122 RESOLVED** — multiple project-access rows on one customer DO persist. Required enabling the Helpdesk module on a second project ("Agile Board Project") and creating a support level for it (AB-L1), since Support Level is a hard-required field per project-access row. See `HELPDESK_MEMORY.md`.

## Blockers

- **BUG-HLP-003 is a complete blocker for deactivating a Canned Response specifically** (TC-HLP-177) — no workaround exists for that entity. For Organization/SLA/Support Level/Product, the same Edit-form bug exists but each has a working list-level toggle workaround, so those are not hard blocks. See Open Bugs below.

## Open Bugs Found

- **BUG-HLP-001 (Medium, open):** New Customer form's Login field shows a permanently stuck "Login must be at least 2 characters long" message that never clears and doesn't reflect actual outcome. Has not reproduced across any of the last three sessions' customer creations — still open since the underlying cause is unconfirmed, just not currently observed.
- **BUG-HLP-002 (downgraded to Low, open) — RE-SCOPED 2026-08-24: a symptom of BUG-HLP-005, not independent.** Escalation To is correctly scoped when the Support Level form is reached via the proper project link (`?project_id=X`, Project locked as hidden input); it's completely unfiltered only when reached via BUG-HLP-005's bypass route (bare `/rf_support_levels/new`, Project as a live dropdown with no re-filter wired to it). Fixing BUG-HLP-005 should resolve this as a side effect — retest together, not separately.
- **BUG-HLP-003 (High, open) — scope CORRECTED and finalized 2026-08-24 on `flux-fudbk2hlu49`:** The Active checkbox cannot be unchecked via Edit on **Organization, SLA, and Canned Response only**. A working list-level AJAX toggle exists as a workaround for Organization and SLA; Canned Response has no workaround at all (complete block). **Product and Support Level are confirmed NOT affected** — both correctly persist a deactivation via their Edit forms. (A same-session finding claiming Support Level had a different, worse defect was a testing artifact from a direct-URL navigation and was retracted after a clean re-test via the real UI link showed correct behavior — see `bugs/open/BUG-HLP-003.md`'s CORRECTION section.) This scope is now final — no further entities need checking.
- **BUG-HLP-004 (Low, open, filed 2026-08-24):** Customer form's SLA Name and Support Level dropdowns (Project Access row) are functionally scoped to the selected project — the wrong-project option can't be selected — but that option is only `disabled`, not removed from the visible list, even though the plugin's own markup sets `hidden=""` on it (a CSS conflict defeats that). Reproduced on both Create and Edit, both fields, both directions. See `bugs/open/BUG-HLP-004.md`.
- **BUG-HLP-006 (Critical, open, filed 2026-08-24, new Forge instance `flux-forgyim9c49`):** A customer cannot open a ticket they created — the Ticket # and Subject links in their own restricted ticket list both point to `/issues/:id` (Redmine's standard issue route), which the Customer role isn't authorized to access. Reproduced 3 times (both links + direct URL). The ticket list itself correctly scopes to the customer's project — only the link target is wrong. Breaks the core "follow an existing ticket" self-service loop the portal explicitly promises. See `bugs/open/BUG-HLP-006.md`.
- **BUG-HLP-005 (Medium, open, filed 2026-08-24) — root cause of BUG-HLP-002.** SLA and Support Level both have global CRUD routes (`/rf_slas(/new)`, `/rf_support_levels(/new)`) unlinked from any discoverable navigation, yet each has its own working "New" button that bypasses the intended project-scoped flow (Project → Helpdesk → SLA tab; Project → Helpdesk → Settings → Support Level tab). For SLA this produces a genuinely orphaned record — no `project_id` field exists on that form at all, so it's invisible from every project's Helpdesk SLA tab forever, yet still fully functional and selectable in Customer creation. For Support Level the required Project dropdown prevents an orphan, but lists every project on the instance including ones without Helpdesk enabled, and (confirmed 2026-08-24) leaves the Escalation To dropdown completely unscoped as a knock-on effect — see BUG-HLP-002, which fixing this bug should resolve as a side effect. See `bugs/open/BUG-HLP-005.md`.

## Test Case Coverage Gap Closed — 2026-08-24: Search/Filter/Apply/Clear across Settings entities and Dashboard KPI links

User flagged that filter functionality (search box, Status dropdown, Apply Filters, Clear Filters, and Dashboard KPI "View all" auto-applying a filter) was visibly present on many screens during manual testing but had no corresponding written test cases outside the Ticket List suite. Audited every testcase file and added the missing coverage — 9 new TCs, none yet executed (writing-only pass, no live testing done for these this session):

- `HELPDESK_NAVIGATION_WORKSPACES.md`: **TC-HLP-274/275** — Command Center and Project Dashboard KPI "View all" links (Unassigned/Open/On Hold/SLA Breached/Resolved) auto-applying the matching filter to the ticket list.
- `HELPDESK_SLA_ESCALATION.md`: **TC-HLP-276/277/278** — SLA, Support Level, and Holiday list search + Status filter (Active/Inactive) + Apply/Clear.
- `HELPDESK_CUSTOMERS_ORGANIZATIONS.md`: **TC-HLP-279** — Organization list search + Status filter + Apply/Clear (previously only Customer's list had a filter TC, TC-HLP-118); **TC-HLP-280** — strengthens Customer list coverage to specifically exercise Apply/Clear buttons and a combined search+organization filter, not just each condition in isolation.
- `HELPDESK_CONTENT_TEMPLATES.md`: **TC-HLP-281/282** — Product list search + Project filter + Status filter + Apply/Clear; Canned Response list search (Name and Content) + Apply/Clear (noted: no Status filter exists there, consistent with BUG-HLP-003's finding that Canned Response has no working deactivate mechanism at all).

**Confirmed not applicable:** "Add Column" (a column picker) only exists on the Ticket list (already covered by TC-HLP-049) — no Settings entity list (Organization/SLA/Support Level/Product/Customer/Holiday/Canned Response) has a column picker, only a fixed table. Not a gap, just doesn't exist elsewhere.

**Next session should execute these 9 TCs live** and record PASS/FAIL, since they are currently written-but-unverified.

**Follow-up same session — dropdown project-scoping gap:** user asked whether the project-scoping *dropdowns* (not list filters) on Organization/SLA/Product/Customer were covered. Found TC-HLP-111 already tested Support Level's dropdown scoping in the Customer form (and fixed a title typo there — it said "members", body was about support levels), but no sibling TC existed for the SLA dropdown on the same form, despite that exact behavior being heavily live-investigated this session (BUG-HLP-004). Added:
- **TC-HLP-283** (`HELPDESK_CUSTOMERS_ORGANIZATIONS.md`) — SLA dropdown project-scoping in the Customer form, mirroring TC-HLP-111, with the live-confirmed 2026-08-24 result folded in.
- **TC-HLP-284** (same file) — Organization dropdown in the Customer form is explicitly documented as NOT project-scoped, by design (Organization has no project association at creation time at all) — written so a future tester doesn't mistake this for the same defect class as BUG-HLP-004.
- **TC-HLP-285** (`HELPDESK_CONTENT_TEMPLATES.md`) — Product dropdown on the ticket form only offers the current project's own products; this specific scoping requirement had never been tested before (TC-HLP-161 only tested that picking a product saves, not that the dropdown is scoped).

## Customer Creation + Login Functionality Verified — 2026-08-24, new Forge instance `flux-forgyim9c49`

User asked to verify customer creation and customer login functionality end-to-end. Full pass: created customer "Login TestCustomer" (login `logintest.customer`, id=30) with an explicit password and Project Access to "Agile Board Project" (Org "tes", Support Level "sdaf"). Confirmed working: login via username, restricted nav (My account/Sign out/My Helpdesk only), "My Helpdesk" portal correctly scoped to the one assigned project, project-level restricted Helpdesk dashboard with real Prepaid Support Hours data, and raising a ticket via the simplified customer "New issue" form (ticket #271, correctly attributed and visible from the admin side).

**Found BUG-HLP-006 (Critical) in this same pass**, only surfaced because the user explicitly asked "is the ticket visible to him, is he able to open it" — I had initially verified creation + admin-side visibility but not confirmed the customer could re-open their own ticket. When tested, both the Ticket # and Subject links in the customer's own ticket list point to `/issues/:id` and are rejected with "You are not authorized to access this page." See Open Bugs below.

## Navigation-Locator Refactor Completed — 2026-08-25

User flagged (correctly) that every page object navigated exclusively via `page.goto(deepUrl)` — confirmed by audit: zero clicks through real nav links/tabs/menus anywhere in the 15-file `automation/tests/` suite. Fixed all of them:

- **`BasePage.ts`**: added real click-through nav primitives — `clickTopNav()`, `clickProjectTab()`, `clickHelpdeskSubNav()`, `clickSettingsTab()` — documented with which label set applies in which context. `clickHelpdeskSubNav()` is deliberately scoped to the `<ul>` containing "Helpdesk Dashboard" rather than a bare page-wide link lookup, because a project's Helpdesk tab renders the standard Redmine project menu's own "Settings" link *and* the Helpdesk sub-nav's different "Settings" link on the same page simultaneously — an unscoped lookup throws a Playwright strict-mode ambiguity error. A similar latent collision ("Products", Command Center rail vs. global Settings tab) is documented but not currently hit by any method.
- **All 15 page objects** rewired to reach their target screen by clicking through real nav — `HelpdeskOrganizationPage`, `HelpdeskProductPage`, `HelpdeskCannedResponsePage`, `HelpdeskHolidayPage`, `HelpdeskCustomerPage` (Command Center rail); `HelpdeskTicketListPage`, `HelpdeskKnowledgebasePage` (project's Helpdesk tab sub-nav); `ProjectMembersPage`, `ProjectSettingsPage` (project's own Settings tab); `RolesPermissionsPage`, `AdminUsersPage` (Administration sidebar).
- **`HelpdeskSlaPage` / `HelpdeskSupportLevelPage`** needed special handling: their real create-flow now goes through `openProjectList()` → click "New SLA"/"New Support Level" (correctly project-scoped by construction), while the BUG-HLP-005 bypass routes are kept as deliberately-named `openGlobalListViaDirectUrl()` / `openNewViaDirectUrl()` methods so that bug stays independently testable rather than silently disappearing because the suite stopped using the broken path. Also fixed a real latent bug in `HelpdeskSupportLevelPage.create()`: when reached via the proper nav, `support_level[project_id]` renders as a locked hidden input, not a selectable `<select>` — the old code's `projectSelect.selectOption()` call would have thrown.
- Most `openEdit()`/`openDetail()` methods changed signature from an opaque ID to the entity's visible name (e.g. `openEdit(organizationName: string)` instead of `openEdit(organizationId: number)`), since that's how a real user actually finds the row to click — IDs aren't visible/guessable in the UI. Safe to change: **zero `.spec.ts` files exist yet**, so nothing outside this refactor called the old signatures.
- **Fixed a real, unrelated bug found in passing**: `LoginPage.ts` targeted `#username`/`#password`/`input[type=submit][name=login]`/`#loggedas` — standard Redmine core login markup. The actual live form (confirmed every session this engagement, most recently on `flux-fwdq7ydhw49`) is a completely different custom Redmineflux-skinned form. `auth.setup.ts` would have failed against any current Forge instance; never actually exercised before since every login this engagement was driven directly via Playwright MCP, not this page object.
- **Fixed pre-existing project infra gaps** (unrelated to the navigation work, but blocking any type-check from running at all): `automation/` had never had `npm install` run (no `node_modules`), and `tsconfig.json` was missing `@types/node` and the `DOM` lib entry. Installed dependencies, added `@types/node`, added `"DOM"` to `lib`. **Full suite now compiles clean (`npx tsc --noEmit`, zero errors).**

**Still outstanding** (deferred, per explicit user direction, to a later session): real test data prep (multi-SLA-per-project, multi-agent support levels, multi-project products, multi-org plan, in-project agent rosters, canned responses, permission-test user/role combinations) — none of this exists yet on the current instance. No `.spec.ts` files exist yet either — writing the first real specs against these now-fixed page objects is the natural next step once test data exists.

## Comprehensive Test Data Prep for Automation — 2026-08-25, Forge `flux-fwdq7ydhw49`

User asked to prepare real, comprehensive test data so a future automation script "will not face any issue whether data exists or not" — i.e. build out the full matrix that was deferred at the end of the navigation-locator refactor session. All done via real click-through UI (SLA/Support Level forms via `?project_id=X` direct URL for expediency during bulk seeding — same mechanism the page objects' proper nav path produces, not the BUG-HLP-005 bypass route since Project renders as a locked hidden input either way at that URL):

- **Permissions/roles** (carried over from earlier in this same Forge rotation, confirmed still in place): QA role → `view_helpdesk`; Team Lead role → all 8 helpdesk permissions; Developer role → none (negative-permission-test role, deliberately left blank so an agent with only Developer can be used to confirm Helpdesk URLs are blocked without permission).
- **Project memberships reassigned**: Helpdesk Service Desk — daisy.skye/luna.blossom/autumn.grace/harmony.rose/willow.belle → Team Lead, selene.frost/sage.willow → QA. Agile Board Project — aurora.wren/briar.sunset/celeste.dawn → Team Lead, sage.willow → QA.
- **Organizations (2)**: Acme Corp, Globex Industries — both used across different customers/projects/prepaid budgets below, so org-scoping test cases have two genuinely distinct organizations to compare, not just one.
- **SLAs (4)**: Standard SLA + Priority SLA on Helpdesk Service Desk; Agile Standard SLA + Agile Priority SLA on Agile Board Project — every project now has two SLAs with different response/resolution targets (4h/24h vs 1h/8h), so SLA-selection and SLA-breach test cases have real variety per project.
- **Support Levels (5), two full multi-agent escalation chains**: Helpdesk Service Desk — L1 (Daisy Skye + Luna Blossom) → L2 (Autumn Grace + Harmony Rose) → L3 (Willow Belle); Agile Board Project — AB-L1 (Aurora Wren + Briar Sunset) → AB-L2 (Celeste Dawn). Confirmed live (again) that the Escalation To dropdown correctly only offers higher-order levels once they exist — had to create L1/L2/L3 in order and go back to edit L1→L2 and L2→L3 escalation afterward, since a level can't point at one that doesn't exist yet.
- **Products (4)**: Helpdesk Portal (HDP) + Ticketing API (TAPI) on Helpdesk Service Desk; Agile Board Web (ABW) + Agile Board Mobile (ABM) on Agile Board Project — two per project as planned, different categories (Web Application/Software/Mobile Application) for product-dropdown-on-ticket-form variety.
- **Canned Responses (4, global/install-wide)**: "Greeting - Ticket Received", "Escalation Notice", "Request for More Information", "Closing - Ticket Resolved" — covers the four canonical points in a ticket's lifecycle where an agent would reach for a template, each using a different subset of the 9 available macros.
- **Customers (4)**, deliberately varied combinations so no automation script can assume a single fixed shape:
  - `acme.hd.contact` — single project (Helpdesk Service Desk), Standard SLA, L1, Acme Corp
  - `globex.agile.contact` — single project (Agile Board Project), Agile Priority SLA, AB-L1, Globex Industries
  - `globex.hd.contact` — single project (Helpdesk Service Desk), Priority SLA, L2, Globex Industries
  - `multiproject.contact` — **two** project-access rows on one customer (Helpdesk Service Desk/Standard SLA/L1/Acme Corp **and** Agile Board Project/Agile Standard SLA/AB-L2/Globex Industries) — re-exercises the TC-HLP-110/122 multi-project-access pattern with a fresh fixture on this rotation, confirmed both rows persisted and both show correctly on the customer list ("Projects: 2").
  - All four use password `Test@12345`.
- **New feature discovered this session — Support Packages + Prepaid Support Hours budgets**: Command Center → Helpdesk Settings has a 5th tab, "Support Packages" (label-only entities for prepaid-hour top-ups/time-log categorization — Name/Description/Active, no other fields), not previously documented in any doc file. Created "Premium Support" and "Standard Support". Then used each Organization detail page's "Prepaid Support Hours" tab → "Add / top up hours" dialog (Project*/Hours*/Support Package/Comment* fields) to give **Acme Corp 40h on Helpdesk Service Desk** (Premium Support, run-out mode set to "Hard — stop work") and **Globex Industries 20h on Agile Board Project** (Standard Support, run-out mode set to "Soft — allow overage") — deliberately different run-out modes so both enforcement paths have real fixture data. Each top-up is logged in a real "Budget change history" table (0.00h → 40.00h / 20.00h, with comment and approver). This whole area (Support Packages tab, the top-up dialog, the 3 run-out modes, the ledger link per project row) is undocumented in `HELPDESK_REQUIREMENTS.md`/`HELPDESK_FEATURES_LIST.md`/`HELPDESK_USER_GUIDE.md` and in `HELPDESK_PREPAID_HOURS.md`'s existing TC-125–142 — worth a dedicated pass to write test cases against the real UI next time prepaid-hours testing is picked up, since the existing TCs were written from user-guide assumptions that undersell what the feature actually has.
- **`automation/testdata/HELPDESK_TESTDATA_FORGE.xlsx` fully repopulated for this rotation** — all sheets (Organizations, SLAs, Support Levels, Products, Customers, Canned Responses, Prepaid Budgets) cleared of the previous rotation's now-nonexistent rows and rewritten with this session's real data; a new **Support Packages** sheet was added (both to this workbook and to `scripts/gen_testdata_xlsx.py`'s `SHEETS` dict for future regenerations) since it's a newly-discovered entity type.
- Still not created this pass: prepaid-budget usage (all fixtures start at 0h Used — no ticket time has been logged against them yet), Holidays (empty on this rotation), Email Configuration.

## SLA Lifecycle Deep-Dive — 2026-08-25, ticket #272

User asked to manually test one specific flow: customer creates a ticket, an agent replies — does it auto-assign, does the SLA start? Confirmed both, plus found a potentially serious combined issue. Full detail in `HELPDESK_MEMORY.md`'s "Recurring Issues (addendum, 2026-08-25)" section — summary:

- Reply auto-assigns to the replying agent and auto-transitions Status → "Waiting for Customer Response". Confirmed via ticket History.
- SLA does not exist on a ticket at all until an agent's first reply — that reply both starts the SLA clock AND counts as the First Response, with deadlines computed working-hours-aware (hand-verified against Standard SLA's 09:00–18:00 Mon–Fri window).
- The same reply immediately **pauses** the SLA (since Status → Waiting for Customer Response). Manually changing Status away from that (via Edit, to "In Progress") did **NOT** resume it — same pause timestamp, no new Activity Log entry, no resume control found anywhere in the UI (Actions menu, Helpdesk Conversion tab).
- Since the real customer is separately blocked from ever replying at all (BUG-HLP-006, re-confirmed this session even in this exact status), the combination means: **if resuming requires a genuine customer reply, the Resolution SLA may be permanently frozen for every ticket that gets a first response, for the life of this plugin** — a much bigger consequence than BUG-HLP-006's original framing.
- **Not yet tested**: whether an inbound email reply (Helpdesk Settings → Email Configuration, never exercised end-to-end this engagement) is the actual intended resume path. This is the key open question.
- **Not filed as a bug** — user chose to just document and decide later, pending the email-reply-path answer.
- Ticket #272 kept as a real, deliberately-unresolved fixture (In Progress status, permanently-paused Standard SLA) — see `HELPDESK_TESTDATA_FORGE.xlsx` Tickets (fixtures) sheet. Ready-made repro case for next session.
- Also discovered, previously undocumented anywhere: a full "SLA Information" tab per ticket (SLA Overview table, SLA Journey per-escalation-stage table, Activity Log table) — none of this is reflected in `HELPDESK_SLA_ESCALATION.md`'s existing test cases.

## Local Email Testing Session — 2026-08-25/26, `redmine-docker-6` (`localhost:3012`)

User asked to build out a full local environment specifically to test real email flows — Forge structurally cannot receive/send email at all (no route to any real inbox). Full session detail lives in `HELPDESK_EMAIL_TEST_PLAN.md` (preconditions, test data, execution results) — summary here:

- **Environment built from scratch**: local instance reset to a clean slate (0 projects/users/custom fields), then rebuilt: 2 projects (Helpdesk QA Alpha id 3, Helpdesk QA Beta id 4), 2 Organizations, 2 SLAs, a dedicated **Agent** role (Developer baseline + `view_helpdesk`/`view_email_history`/KB perms, workflow copied via `WorkflowRule.copy`), 6 Agent Users (3 per project), 2 Customer records, 14 mail server mailboxes (2 support inboxes, 6 agents, 2 customers, 4 spares from earlier).
- **Core infra fixed along the way**: Redis + Sidekiq started for the Helpdesk plugin's 3 scheduled jobs (must be redone after any container restart — does not persist); the local mail server's IMAPS (993) was never enabled (`SSL_TYPE=` blank) — generated a self-signed cert, enabled `SSL_TYPE=self-signed`, installed the cert into the Redmine container's trust store, and fixed Roundcube's IMAP/SMTP config to match (see `HELPDESK_EMAIL_TEST_PLAN.md` §3 and the `reference_docker_mail_server` memory for the exact steps — none of this survives a mail-server container rebuild).
- **Full email round-trip confirmed working, ticket #67 (Helpdesk QA Beta)**: customer emails support inbox with a ticket keyword → real ticket created (correct subject prefix, correct Organization auto-populated from the Customer record) → agent replies via UI (auto-assign, Status → Waiting for Customer Response, correctly threaded outbound email) → customer replies by real threaded email → correctly becomes a note (not a duplicate ticket), Status auto-transitions to In Progress.
- **Resolved the open question left from the 2026-08-25 Forge session** (`HELPDESK_MEMORY.md`'s SLA lifecycle addendum): an inbound email reply from the customer IS a real, working SLA resume/keep-alive path — ticket #67's SLA Information tab showed "✓ On Track" with a live countdown after the customer's email reply, not paused. The earlier "may be permanently frozen forever" fear does not apply to the email channel (only the portal channel, still blocked by BUG-HLP-006).
- **New bug found and filed: BUG-HLP-007 (High, open)** — Internal Note submission crashes with HTTP 500 (`journals.private_notes` NOT NULL violation). First time this path was exercised on Local; the equivalent Forge-side finding (addendum #5) reported it working there, so worth a Forge retest to see if this is a genuine regression or local-build-specific.
- **BUG-HLP-007 retested 2026-08-26 via real click-through navigation (not `browser_navigate`), scope corrected**: the bug is NOT unconditional across "any role" as first reported — it reproduces only via the **branded** `/projects/:id/helpdesk/issues/:id` route (`RfProjectHelpdeskIssuesController`); via the **core** `/issues/:id` route (`IssuesController`, reached by real click-through from the agent's in-app ticket list) Internal Note submission succeeds. Still High severity, not downgraded: confirmed by opening two real outbound notification emails in Roundcube that **both** "View Ticket"/"View Ticket Details" links point to the branded (buggy) route — so this is exactly what any real user clicks. See `bugs/open/BUG-HLP-007.md`'s "Scope correction" section.
- **BUG-HLP-006 retested on Local, 2026-08-26 (originally filed on Forge only)**: does **NOT** reproduce here. Customer `beta.customer` opened ticket #67 successfully via real click-through nav (My Helpdesk → project → Helpdesk Tickets → ticket row); also controlled for ticket origin by creating a fresh ticket (#69) via the customer's own portal "New issue" form (matching the original Forge repro method) — same clean result. Root cause: Local's customer ticket list links to the branded route (which customers CAN access), while the original Forge repro showed the core `/issues/:id` route (which customers CANNOT access) — the mirror image of BUG-HLP-007's finding. **Does not close the bug** — needs a real Forge retest first. See `bugs/open/BUG-HLP-006.md`'s "Retest — 2026-08-26" section and `HELPDESK_MEMORY.md`'s dual-route addendum for the full pattern writeup tying both bugs together.
- **A real but low-severity plugin bug found and ruled out as a data-loss risk**: `Helpdesk::EmailPollerWorker` logs a spurious `MailHandler returned unexpected result: Journal ...` / `could not be processed` warning for every reply-to-existing-ticket email (doesn't recognize a `Journal` return type as success) — confirmed cosmetic-only by waiting for the next real cron tick and confirming the email was correctly marked read and NOT reprocessed (no duplicate notes). Worth filing as a low-severity bug for the misleading log noise alone, not yet filed.
- **Negative-keyword gating — PASS.** First attempt accidentally self-invalidated (my own test email's explanatory body text contained the word "ticket"), correctly creating ticket #68 — a real confirmation that keyword-matching scans the full email content, not just the subject. Redone with genuinely keyword-free text (subject "Good morning", body "Hope you have a wonderful day...") — poller correctly logged `Email does not contain required keywords ["ticket", "issue", "request"] - ticket creation skipped`, and Alpha's ticket list stayed at 2 tickets (no new one created).
- Also confirmed live: BUG-HLP-004's cross-project-dropdown pattern reproduces identically on the Customer form here (SLA/Support Level options correctly functionally scoped but not visually hidden) — same root cause as documented on Forge, not a new finding, just re-confirmed on a second environment.

## Next Session Start Point

- **`HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite is 24/24 executed, zero deferred/blocked** — 22 PASS, 2 FAIL (TC-HLP-117/BUG-HLP-013, TC-HLP-120/BUG-HLP-011). TC-HLP-298 added 2026-08-27.
- **`HELPDESK_TICKET_LIFECYCLE.md` (32 TCs) execution IN PROGRESS as of 2026-08-27** — user picked this suite next. Done so far: TC-HLP-013/014/015/016/017/018/019/020/021/022/023/037/289/295 (14 of 32). **BUG-HLP-014 (High)** found via TC-015: a customer's own Tickets tab shows "0 tickets" even when tickets genuinely exist and are individually reachable. **Root cause confirmed by reading the plugin source** (`docker exec` into `redmine-docker-6-redmine-1`, `/usr/src/redmine/plugins/redmineflux_helpdesk/`): `TicketFilter#base_scope` starts from Redmine's native `Issue.visible(user)`, which requires real project Membership — but customers are (by design) linked only via `RfProjectCustomer`, never a Redmine Member, confirmed via `rails runner` (`alpha.customer.memberships.count == 0`). Every other customer check in the plugin correctly uses `RfProjectCustomer` instead; only this list fell back to Redmine's native scope. **Confirmed systemic, not fixture-specific**: reproduced identically with a brand-new customer (`delta.customer`, zero prior history, ticket #77) at the user's request. Full writeup + suggested fix direction in `bugs/open/BUG-HLP-014.md`. Separately found (not yet its own bug): the `check_emails` **rake task** is dead code — it still reads the legacy `ProjectCustomField` config the UI stopped writing to after a migration to `RfHelpdeskEmailConfig`, while the real scheduled Sidekiq `EmailPollerWorker` correctly uses the new model (confirmed by triggering it directly via `rails runner`, which worked — connected via IMAP fine, just found 0 unread mail waiting). Also fixed a real environment-integrity issue found via TC-013: both Helpdesk QA Alpha and Beta had **"Is public" checked** (Non-member role has Add/View issues), which let an Alpha-only Manager create a ticket in Beta — root-caused as standard Redmine behavior, not a plugin bug, but unchecked "Is public" on both projects to restore proper scoping (re-verified: 403 now). **Two navigation-methodology corrections mid-suite** (see `HELPDESK_MEMORY.md` and the global `feedback_no_direct_url_navigation` memory): (1) don't type a ticket's branded-route URL directly — click through the list, which for an agent lands on the *core* `/issues/:id` route, materially different per BUG-HLP-007; (2) when following a link meant to simulate a specific role's journey (e.g. a real notification-email "View Ticket" link), first explicitly switch the browser's Redmine session to that role — a legitimately-clicked link still renders the wrong view if the tab's session is still someone else's. **Next session should resume this suite at TC-HLP-024** (custom reply time value) and continue through TC-HLP-041 — remaining ~18 TCs, several need real fixtures/waits (merge, time-logging permission variants, all-7-statuses check).
- **BUG-HLP-013 (Medium, open, revised same-day)** — **Portal Preview is now scoped OUT of this plugin's intended functionality** per explicit user product-judgment direction (added to `HELPDESK_SCOPE.md` Out of Scope). The defect: `/rf_customers/:id/portal` is fully reachable and functional (read-only, correct scoping, 403 on out-of-entitlement project) despite having no UI entry point and not being in-scope functionality — the route existing at all is now the problem, not a missing button. Needs a product/dev decision: remove the route, or reverse the scope call and add the entry point (worth noting: the route's internal logic is real, deliberate, correct engineering — not leftover code — which is evidence for that decision either way). TC-HLP-117 → FAIL (route should 404, doesn't), TC-HLP-124 → PASS but now secondary/moot. See `bugs/open/BUG-HLP-013.md` (has a Revision History section documenting the original vs. revised framing).
- **BUG-HLP-011 is open** — organization delete has no linked-customer warning; see `bugs/open/BUG-HLP-011.md`.
- **BUG-HLP-012 was filed then closed same-day as Not a Bug** — Customer Edit form's Information fields (Login/Name/Email/Password) being writable by a project-scoped Manager for a customer outside her scope is, per user product-judgment review, the correct behavior of a global customer directory (Login/Email unique install-wide; "Add project" on the same form was independently confirmed correctly scoped to the Manager's own project). See `bugs/closed/BUG-HLP-012.md` for the full reasoning.
- **New fixture available for future scoped-permission testing**: `ivy.sterling` / `Test@12345`, Manager role, Member of Helpdesk QA Alpha only — see `HELPDESK_USERS_AND_CUSTOMERS.md` "Scoped Manager fixture" section and `helpdesk.local.fixtures.ts`'s `MANAGERS` export.
- **Filed BUG-HLP-008** (Medium) for the `EmailPollerWorker`/Helpdesk Conversion tab finding — see below. No separate low-severity bug needed for the warning alone; it's documented as a likely-shared root cause inside BUG-HLP-008 instead. **Confirmed the full 3-way matrix live** (agent web-UI reply → logged correctly; customer portal reply → logged correctly, tested fresh on ticket #69; customer real-email reply → NOT logged, ticket #67) and wrote it up as formal test cases TC-HLP-286/287/288 in `HELPDESK_EMAIL.md` — the omission is specific to the email channel only, not customer replies in general. BUG-HLP-008's Part B (duplicate/mislabeled entry) did not reproduce on #69's clean single reply — its trigger condition isn't isolated yet, possibly specific to the first reply after an email-originated ticket.
- **Retest both BUG-HLP-006 and BUG-HLP-007 on the current Forge instance, driving BOTH routes deliberately (branded AND core) for each**, now that the dual-route pattern is understood — this is the definitive way to resolve both bugs' open questions in one pass: does Forge's current build show the same split-brain behavior (branded=customer-accessible-but-Internal-Note-crashes, core=Internal-Note-works-but-customer-blocked), or has one/both routes changed since these bugs were originally filed on earlier Forge rotations.
- **Clean up ticket #67**: it has accumulated several throwaway Internal/Reply Notes ("test", "dsfgsdf", "dsaf", "46556") left over from BUG-HLP-007 route-retesting — harmless as evidence but clutters the fixture; either leave as-is with this note or trim before the next demo/handoff.
- **Filed BUG-HLP-009** (High) from a user-reported observation, verified live on a clean fixture (ticket #70), then re-scoped per the user's correction: the bug is NOT "note invisible to customer" (Notes tab is correctly never customer-facing) — it's that the status auto-transitions to "Waiting for Customer Response" via the standard **Edit** form even though **zero** customer-facing communication is produced (no Helpdesk Conversion entry at all, no email). Likely the same branded-vs-core root-cause family as BUG-HLP-007/008 — worth investigating together when this gets fixed. Not yet tested: whether Admin role shares the same gap.
- **Redis/Sidekiq and the mail server's IMAPS config do not survive container restarts** — re-verify both before trusting any new email test result (`ps aux | grep -E 'redis|sidekiq'` inside `redmine-docker-6-redmine-1`; `ss -tlnp | grep 993` inside `local-mail-server`).
- **Forge instance is `flux-fwdq7ydhw49`** as of the 2026-08-25 session — re-verify this is still current before resuming any Forge-side work (per `QA_CREDENTIALS_FORGE.md`'s URL Rule, it rotates per run).
- **Test data matrix is real and comprehensive on Forge.** Writing the first `.spec.ts` files against the now-fixed page objects (`automation/tests/`) remains the natural next automation step — start with SLA/Support Level/Product/Customer suites.
- Separately still pending from earlier sessions: execute the 9 written-but-unverified filter/dropdown TCs (TC-HLP-274–285), recreate Holidays on Forge, `HELPDESK_TICKET_LIFECYCLE.md` suite execution, and a fresh pass to write real test cases for the SLA Information tab (now well understood — see `HELPDESK_MEMORY.md`) and Support Packages/Prepaid Hours top-up UI.
- Resolve TC-HLP-213 (manage_helpdesk vs export_helpdesk_reports) — not yet attempted.

## Run History

> Replaces the old changelog.md — one row per test run / regression pass (see `CLAUDE.md` §2b, §12; `SENIOR_QA_STANDARDS.md` §26/§27).

| Date | Redmine Version | Environment | Tested By | Summary |
|------|-----------------|-------------|-----------|---------|
| 2026-08-21 | — | — | — | Plugin QA folder + Playwright automation framework scaffolded; docs renamed to HELPDESK_*.md; HELPDESK_USER_GUIDE.md populated with real content |
| 2026-08-21 | — | — | — | All docs (requirements/features-list/user-guide) complete; all 54 features written into 12 test-case suites (227 TCs total, positive/negative/edge); test data registry convention established; no execution yet — next session starts actual testing |
| 2026-08-21 | Forge (flux-fiqll6jza49) | Forge | Claude (Playwright MCP), roles set up for daisy.skye/luna.blossom/autumn.grace | First live execution pass. PASS: TC-HLP-097, 075, 076, 078, 089, 108, 109, 205. Found BUG-HLP-001, filed as Critical (customer creation appeared blocked), then downgraded to Medium after a retest succeeded despite the same stale error — root cause of the earlier failures still open. Corrected a wrong HELPDESK_MEMORY.md claim (support level "test" and SLA "dsfasd" pre-existed, not zero as noted). |
| 2026-08-24 | Forge (flux-fnwchpuwl49, rotated mid-session from flux-fiqll6jza49) | Forge | Claude (Playwright MCP), daisy.skye + admin | CRUD-first verification pass complete for Organization/SLA/Support Level/Holiday/Customer (Create/Read/Search/Update all confirmed; Delete executed for Organization/SLA/Support Level via throwaway entities, markup-confirmed for Holiday/Customer). Resolved TC-HLP-110/122 (multi-project customer access genuinely persists — required enabling Helpdesk module on a 2nd project + creating a support level for it). Found BUG-HLP-002 (Escalation To dropdown not project-scoped). BUG-HLP-001 did not reproduce this session (still open, cause unconfirmed). Fixed a real bug in `HelpdeskCustomerPage.ts` itself (wrong submit-button locator, never previously exercised). Generated and populated `HELPDESK_TESTDATA_FORGE.xlsx` (did not exist before). |
| 2026-08-24 | Forge (flux-fw2qhf0ux49, rotated again since prior row) | Forge | Claude (Playwright MCP), admin | Canned Response CRUD+field-validation+macro session. Full CRUD+search+delete executed (throwaway entity), all field validations executed with real confirmed values (Name required, max length exactly 255 chars, duplicate-name exact wording "Name has already been taken", Author read-only). All 9 reply macros confirmed substituting correctly on real ticket #236 in one pass; macro-shortcut-link click-to-insert confirmed. Found BUG-HLP-003 (High) — Active checkbox cannot be unchecked via Edit, fully reproduced end-to-end on Canned Response (missing Rails hidden fallback field); Organization/SLA share the identical DOM markup but the failure itself wasn't independently reproduced on them; blocks TC-HLP-177. Updated `HelpdeskCannedResponsePage.ts` with full CRUD/macro/delete methods. Reset `HELPDESK_TESTDATA_FORGE.xlsx` to match the new server (prior instance's data no longer exists). |
| 2026-08-24 | Forge (flux-fxd0g3s4049, rotated again since prior row) | Forge | Claude (Playwright MCP), admin | Full CRUD verification round for Organization/SLA/Support Level/Product/Customer (Create/Read/Search-match/Search-no-match/Update/Delete + duplicate-field validation, all with real confirmed error wording). Product was a previously-untested gap — added edit/delete to `HelpdeskProductPage.ts`. Confirmed BUG-HLP-003's full scope: Organization/SLA/Support Level/Product all share the broken Edit-form Active checkbox AND a working list-level AJAX toggle workaround; Canned Response is the sole entity with no workaround. Confirmed a real SLA cross-field validation ("Resolution Time must be greater than or equal to the Response Time") and Product's per-project (not install-wide) name/code uniqueness. Fixed a real bug in `HelpdeskSupportLevelPage.ts` (Support Assignees modeled as native select[multiple], actually a custom checkbox-dropdown widget). Rebuilt L1→L2→L3 escalation chain and reset `HELPDESK_TESTDATA_FORGE.xlsx` for the new instance. |
| 2026-08-24 | Forge (flux-fudbk2hlu49, rotated again since prior row) | Forge | Claude (Playwright MCP), admin | Investigated SLA vs. Organization project-scoping mechanisms in depth (see `HELPDESK_MEMORY.md` Known Quirks): SLA has a real hidden `project_id` set only via a specific `?project_id=X` URL query param with no visible nav path to it; Organization has no creation-time scoping at all — its project tab is a derived view based on customer/prepaid-budget linkage (an earlier draft bug on this was filed then retracted per direction, underlying technical finding kept). Set up two properly-scoped SLAs/Support Levels (Standard/L1 on Helpdesk Service Desk, AgileSLA/AB-L1 on Agile Board Project) to definitively test whether the Customer form's SLA/Support Level dropdowns exclude cross-project options like the Support Assignees widget does. Result: functionally scoped (wrong-project option can't be selected) on both Create and Edit, but the option is only `disabled` not removed — DOM shows the plugin sets `hidden=""` on it, which CSS is defeating. Filed as **BUG-HLP-004** (Low). Follow-up investigation confirmed the global `/rf_slas` and `/rf_support_levels` list pages (unlinked from any discoverable nav) each have their own working "New" button pointing at the bare, unscoped `/new` route — for SLA this creates a genuinely orphaned record with no project association at all; for Support Level the required Project dropdown prevents an orphan but lists every project including ones without Helpdesk enabled. Filed as **BUG-HLP-005** (Medium). **Follow-up same session:** a controlled comparison (proper `?project_id=X` link vs. the bypass route) proved BUG-HLP-002 (Escalation To dropdown unscoped) only reproduces via BUG-HLP-005's bypass route — via the proper link, Escalation To is correctly scoped (Project renders as a locked hidden input there, not a live dropdown). Re-scoped BUG-HLP-002 as a symptom of BUG-HLP-005 (downgraded to Low), cross-linked both bug files so a fix to BUG-HLP-005 gets BUG-HLP-002 retested/closed alongside it rather than fixed independently. **Also this session:** re-verified BUG-HLP-003 (Organization, Canned Response reproduced identically), then went further and actually executed the real Edit-form test for SLA/Support Level/Product (previously inferred/list-toggle-only) — found Product and Support Level are NOT affected, correcting the bug's scope down to Organization/SLA/Canned Response. A same-session false-positive finding for Support Level (appeared to silently deactivate on any Save) was caught, investigated (raw server HTML fetch confirmed correct rendering), and retracted after a clean re-test via the real UI link showed correct behavior both times. |
| 2026-08-25 | Forge (flux-fwdq7ydhw49) | Forge | Claude (Playwright MCP), admin | Rewired all 15 `automation/tests/` page objects from `page.goto()`-only navigation to real click-through nav (BasePage nav primitives added, BUG-HLP-005 bypass routes kept as explicit `...ViaDirectUrl()` methods), fixed a real bug in `LoginPage.ts` (stale core-Redmine locators vs. the actual custom login form) and pre-existing infra gaps (`npm install`, `@types/node`, DOM lib) — suite now compiles clean. Then built out a comprehensive test-data matrix for automation: 2 Organizations, 4 SLAs (2 per project), 5 Support Levels across two full multi-agent escalation chains (L1→L2→L3 and AB-L1→AB-L2, 8 distinct agents total), 4 Products (2 per project), 4 Canned Responses, and 4 Customers with deliberately varied project/SLA/support-level/organization combinations (including one genuine multi-project customer). Discovered and populated a previously-undocumented feature, Support Packages + per-organization Prepaid Support Hours budgets (2 packages, 2 budget top-ups with different run-out enforcement modes). Fully repopulated `HELPDESK_TESTDATA_FORGE.xlsx` for this rotation and added a new Support Packages sheet (workbook + generator script). No `.spec.ts` files exist yet — writing the first real specs against this data is the next step. |
| 2026-08-26 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + 2 real agents (aurora.wren, and Alpha via admin) | Built a complete local email-testing environment from a clean slate: 2 projects, 2 orgs, 2 SLAs, dedicated Agent role + copied workflow, 6 agent Users, 2 Customer records, 14 mail server mailboxes. Fixed a real infra gap (IMAPS never enabled on the local mail server — self-signed cert + trust-store install + Roundcube config). Confirmed the full email round-trip working end-to-end on ticket #67 (customer email → ticket → agent reply → customer email reply → note + auto status transition). **Resolved the open SLA-resume question from 2026-08-25**: email reply is a real, working resume path — SLA showed "On Track", not permanently frozen. Found and filed **BUG-HLP-007** (High) — Internal Note submission crashes with a 500 error (`private_notes` NOT NULL violation). Ruled out a suspected duplicate-note risk from a separate cosmetic poller warning (confirmed via a real cron-tick wait, not reprocessed). Negative-keyword gating test in progress at session end (first attempt self-invalidated by accident, redone cleanly). |
| 2026-08-26 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), aurora.wren + beta.customer | Retested BUG-HLP-007 and BUG-HLP-006 using real click-through navigation (corrected away from `browser_navigate` deep-linking per explicit user direction). **BUG-HLP-007 rescoped**: reproduces only via the branded `/projects/:id/helpdesk/issues/:id` route, not the core `/issues/:id` route — confirmed still High severity since both real outbound notification email templates link to the branded route. **BUG-HLP-006 does not reproduce on Local** (customer opened both an email-originated ticket #67 and a fresh portal-originated ticket #69 successfully) — root cause traced to the same dual-route split: Local's customer ticket list links to the branded route (customer-accessible), the original Forge repro showed the core route (customer-blocked). Documented the mirror-image relationship between both bugs in `HELPDESK_MEMORY.md`. Neither bug closed — both need a Forge retest driving both routes deliberately before any status change. **Found and filed BUG-HLP-008** (Medium) while explaining the Helpdesk Conversion vs. Notes tab split to the user: a customer's real inbound-email reply to an existing ticket is never logged in the Helpdesk Conversion tab (only visible under Notes), and a separate duplicate entry there mislabels the agent's own outbound reply as an inbound "Customer replied to Support" message. Upgraded the earlier "cosmetic-only" verdict on the `EmailPollerWorker` Journal-type warning — now understood as a likely shared root cause with BUG-HLP-008's omission, not just log noise. Confirmed the full 3-way reply-origin matrix live and wrote it up as TC-HLP-286/287/288 in `HELPDESK_EMAIL.md`. **Also filed BUG-HLP-009** (High) from a user-reported observation: an agent's non-private note added via the standard Edit form auto-transitions the ticket to "Waiting for Customer Response" but never renders on the customer's own Notes tab — verified live on a clean fixture (ticket #70). |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | Continued coverage-gap sweep before starting full suite execution: confirmed TC-HLP-240/244 (Customer Login/Email uniqueness) already existed; added and confirmed live **TC-HLP-292** (Organization Website/Phone have no uniqueness constraint, matches spec) — filed **BUG-HLP-010** (Low) anyway per explicit user product-judgment direction, since real orgs shouldn't share contact info even though the current spec doesn't require uniqueness. Added and confirmed live **TC-HLP-295** (customer's ticket Edit is Subject-only — real field `#issue_subject`, nothing else becomes editable). Cross-referenced already-gathered Helpdesk Conversion evidence into TC-HLP-016. Drafted and got approval for `HELPDESK_AUTOMATION_PLAN.md` (own-fixture UI-only provisioning + Playwright automation build-out, phased). **Then executed the full `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite live, 21 of 23 TCs** (108–110, 112–120, 122–124, 279–280, 283–284, 293–294 — 111 already confirmed prior session): all real data, real fixtures (Organization "Gamma Corp" created then deleted, Customer `gamma.customer` created with multi-project access then reduced to one). Found **BUG-HLP-011** (Medium): deleting an organization with a linked customer succeeds with only a generic confirmation, no warning — the customer's Organization reference silently clears to "—". Re-confirmed TC-HLP-122 (multi-project customer access) on a second environment and **corrected `HELPDESK_USER_GUIDE.md`'s wrong "one row at a time" prose directly** (both occurrences), since it's now conclusively wrong on two environments. Found TC-HLP-117 (Portal Preview) has **no corresponding UI control at all** on this Local build (full DOM scan confirmed) — blocks TC-HLP-124 too, needs a Forge comparison. Deferred TC-HLP-121 (needs a dedicated project-scoped manager fixture that doesn't exist yet — natural fit for the automation plan's Phase 1 provisioning). |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + new fixture `ivy.sterling` | **Unblocked and executed TC-HLP-121.** Built the missing project-scoped-manager fixture: added `manage_helpdesk`/`export_helpdesk_reports`/`manage_prepaid_support_hours`/`view_helpdesk`/`view_email_history` permissions to the pre-existing but Helpdesk-blank "Manager" role, created user `ivy.sterling`, added her as a project Member of **Helpdesk QA Alpha only** (verified not a Member of Beta). Restored `gamma.customer`'s second (Alpha) project-access row so it held both an Alpha and Beta row, then confirmed as `ivy.sterling` that her Edit form for that customer shows **only** the Alpha row (Beta row completely absent, not disabled); edited and saved the Alpha row's Organization, then verified as admin the Beta row was byte-for-byte unchanged. **TC-HLP-121 PASSES.** **Found BUG-HLP-012 (High)** as a side effect on the same form: the customer's Information section (Login/Name/Email/Password) is fully editable by `ivy.sterling` even for `beta.customer`, a customer with zero Alpha access — proved exploitable (changed and reverted `beta.customer`'s First name from her session). Project-access-row scoping and Information-field scoping are evidently separate, inconsistently-enforced code paths. Updated `HELPDESK_USERS_AND_CUSTOMERS.md`, `helpdesk.local.fixtures.ts` (new `MANAGERS` export, `tsc --noEmit` clean), and `HELPDESK_TESTDATA_LOCAL.xlsx`'s Customers sheet (also fixed a pre-existing column-shift data-quality issue found on `gamma.customer`'s row while updating it) with the new fixture/state. **`HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite is now 22/23** — only TC-HLP-117/124 (Portal Preview, blocked pending Forge comparison) remain open. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), review of prior session's finding | **Reviewed and closed BUG-HLP-012 as Not a Bug**, per user's product-judgment pushback: Customers are a global entity (Login/Email unique install-wide, TC-HLP-240/244), and the same session's own testing had already shown the "Add project" control on the Customer Edit form is correctly scoped to the Manager's own project — decisive evidence that Information fields are the intentionally-global part of the record and Project-access rows are the intentionally-scoped part, not an inconsistency. Moved `bugs/open/BUG-HLP-012.md` to `bugs/closed/BUG-HLP-012.md` with the full resolution writeup (including a residual, not-re-opened note about the narrower Password-only-no-project-touch edge case for future reference). Updated TC-HLP-121's note, `HELPDESK_MEMORY.md`, `bugs/_index.md`, and `STATUS.md` to match. Open bug count: 12 → 11. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Resolved TC-HLP-117/124, closing out the suite at 23/23.** User pushed back on the earlier "Portal Preview doesn't exist" theory with an alternative reading (customer login itself being the portal). Checked `HELPDESK_USER_GUIDE.md` §11 directly instead of theorizing further — it names a real "View portal" button and documents an actual route, `/rf_customers/:id/portal`. Tested it directly: works exactly as documented (read-only banner, Exit preview link, correct per-project listing for `beta.customer`). Tested scoping too: forcing an out-of-entitlement project via `?project_id=3` correctly 403s. Re-ran the full DOM scan on Customer 360 fresh — still zero portal-related controls, confirming the entry point really is missing even though the destination works. Filed **BUG-HLP-013** (Medium): feature is fully functional, only its UI entry point on Customer 360 is missing — same defect shape as BUG-HLP-005 (working feature, no discoverable link) but a distinct root cause. Rescored TC-HLP-117 → **FAIL** (behavior is correct but its own "click View portal" step can't be performed) and TC-HLP-124 → **PASS** (403 confirmed live). `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite is now **23/23 executed, zero deferred/blocked**: 21 PASS, 2 FAIL (TC-117/BUG-HLP-013, TC-120/BUG-HLP-011). Updated `HELPDESK_MEMORY.md`, `bugs/_index.md`, and both TC entries to match. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), review of same session's finding | **Same-day revision of BUG-HLP-013, per user product-judgment direction.** User stated Portal Preview should not be considered part of this plugin's intended functionality at all, regardless of it being documented in `HELPDESK_USER_GUIDE.md`/`HELPDESK_FEATURES_LIST.md`, and directed that the Expected Result should be that the route itself must not exist if it's genuinely out of requirements/functionality. Added Portal Preview to `HELPDESK_SCOPE.md`'s **Out of Scope** list as an explicit, durable decision. Rewrote TC-HLP-117's Expected Result (route should 404, not just "should have a button") — still FAILs, but now for the corrected reason. TC-HLP-124 stays PASS for its own narrow entitlement-scoping check but is now flagged as secondary/moot once the whole route shouldn't exist. Rewrote BUG-HLP-013's Title/Expected/Actual result to match, kept a **Revision History** section in the bug file preserving the original framing, and added a **Recommendation**: needs a product/dev decision to either remove the route entirely or reverse the scope call and add the missing entry point — noted that the route's internal logic (correct read-only rendering, correct entitlement scoping/403) is real, deliberate engineering, not leftover code, which is relevant evidence for that decision either way. Updated `bugs/_index.md`, `HELPDESK_MEMORY.md`, `HELPDESK_HANDOFF.md`. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Coverage-integrity check, prompted by "did you test all testcases in this suite?"** Counted `CONFIRMED LIVE` markers against the suite's 23 TC headers directly rather than answering from memory — found 22, not 23. TC-HLP-284 was the gap: its Expected Result only cited an already-known fact from the BUG-HLP-004 investigation (2026-08-24), never its own dedicated execution. Ran it properly on `beta.customer`'s Edit form (Add project → Project=Alpha → SLA/Support Level correctly re-scope, Organization shows the full unfiltered list → switch Project=Beta → SLA/Support Level correctly flip, Organization list stays exactly the same) — PASS, matches expected result, discarded the unsaved row via Cancel. `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` is now genuinely 23/23 with a real live execution note on every single TC. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **New coverage: TC-HLP-298.** User stated a precise claim — an organization created from within a project's own Helpdesk tab shows in the global Organizations list but not that project's own Organization view, not until a customer's project-access row on that project actually selects it. Checked for existing coverage first (none — TC-HLP-284 only covers the Customer form's dropdown, not the project-scoped `/projects/:id/helpdesk/organization` view or the creation-origin angle), then verified live before writing anything: created "Delta Test Org" via Helpdesk QA Alpha's own "New Organization" button (`project_id=3` in the URL) — appeared in the global list immediately, did NOT appear on Alpha's own Organization tab; only appeared there once selected on `alpha.customer`'s Alpha project-access row. Confirms the claim exactly. Wrote **TC-HLP-298** in `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` (Edge Cases), enriched the `HELPDESK_REQUIREMENTS.md` Known Constraints Organization note with the "creation origin doesn't matter" nuance, updated the suite's Evidence Map, and cleaned up the throwaway org + reverted `alpha.customer`'s Organization back to "Alpha Org" afterward. Suite is now 24 TCs, all confirmed live. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom + ivy.sterling + alpha.customer + admin | **Started `HELPDESK_TICKET_LIFECYCLE.md` (32 TCs), user's chosen next suite.** Environment health-checked first (Docker containers up, Redis+Sidekiq running, IMAPS listening). TC-HLP-013/014/018 confirmed live: project-scoped ticket creation works (ticket #71 as `luna.blossom`); the *global* Command Center Tickets screen needs `manage_helpdesk` specifically, not just `view_helpdesk` as the TC's precondition claims (confirmed via `ivy.sterling`, ticket #72) — not filed as a bug, precondition wording just needs correcting. **Found and fixed a real environment-integrity issue**: both Helpdesk QA Alpha and Beta had "Is public" checked (Non-member role has Add/View issues) — this let `ivy.sterling` (Alpha-only) create a ticket in Beta (#73) via the global New Issue form's unscoped Project dropdown. Root-caused as standard Redmine public-project behavior, not a Helpdesk-plugin bug — but it silently broke the project-scoping assumptions this whole engagement has been testing on, so unchecked "Is public" on both projects and re-verified the boundary now correctly 403s. Deleted the probe ticket. **Found BUG-HLP-014 (High)** via TC-HLP-015: `alpha.customer` successfully created ticket #74 (confirmed real via admin — 5 tickets total on the project), but her own Tickets tab immediately showed "0 tickets" / "No tickets yet," including her older pre-existing ticket — reload-persistent, individual ticket still reachable directly, same URL shows all 5 correctly as admin. Isolated to the Customer role's list view. See `bugs/open/BUG-HLP-014.md`. 7 of 32 TCs done (013/014/015/016/018/289/295); resuming at TC-HLP-017 next session. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom + alpha.customer + admin | **Continued `HELPDESK_TICKET_LIFECYCLE.md`: TC-HLP-017/019/020/021/037 confirmed.** TC-017 (email reply lands as a note, not a new ticket) confirmed by reusing already-gathered `HELPDESK_EMAIL.md` TC-288 evidence on the same ticket (#67) rather than re-running the round-trip — PASS on "no new ticket + lands as a Note," but the "recorded in Email History" half FAILs per already-tracked BUG-HLP-008 Part A. **Two navigation-methodology corrections mid-execution** (both reinforced in `HELPDESK_MEMORY.md` and the global `feedback_no_direct_url_navigation` memory): first, typed a ticket's branded-route URL directly instead of clicking through the list — corrected by redoing TC-019/021 via genuine click-through, which for an agent lands on the **core** `/issues/:id` route (materially different per BUG-HLP-007); confirmed both PASS there (ticket #71: Status → Waiting for Customer Response; fresh unassigned ticket #75: auto-assigned to replying agent, status transition, SLA paused). Second, followed `alpha.customer`'s real notification-email "View Ticket" link while the Redmine browser tab was still under an agent's session — the link click was legitimate but the rendered page wasn't hers; corrected by explicitly signing the tab out and into `alpha.customer` first, then re-clicking. That properly-authenticated path confirmed **TC-020** (customer reply → Status back to In Progress, SLA resumed to "On Track") and, as a side observation, **TC-037** (customer's reply box has only a plain textbox + Save — no Canned Response/Internal Note/time-log controls at all). 12 of 32 TCs done; resuming at TC-HLP-022 next. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom + admin | **TC-HLP-022/023 confirmed, 14/32 done.** TC-022 (Internal Note): via genuine click-through on ticket #74 (core `/issues/74` route), Internal Note saved without crashing (confirms BUG-HLP-007's crash is branded-route-specific), Status stayed In Progress, SLA stayed "On Track" (not paused), and the Helpdesk Conversion tab's content was checked directly — the note text doesn't appear there, confirming exclusion from the customer channel, not just role-based hiding. TC-023 (10-minute time log): real finding — the "Time spent" control is a plain `<select>` dropdown (`-- None --/5/10/15/20 min/Custom`), not clickable chips as the TC's steps describe (relevant to TC-HLP-030 too). Selected "10 min" + Activity, saved — time entry correctly shows "0:10" (ten minutes) on both the ticket and its own Edit page, not ten hours. Resuming at TC-HLP-024 next. |
