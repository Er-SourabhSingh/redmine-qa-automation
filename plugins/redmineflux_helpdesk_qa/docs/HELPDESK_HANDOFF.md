# Handoff — Redmineflux Helpdesk

## Last Session

- Date: 2026-09-01
- Redmine Version: 6 (local Docker, `redmine-docker-6`)
- Environment: Local (`http://localhost:3012`)

## Completed This Session

- Plugin QA folder scaffolded (docs, testcases, bugs, screenshots, reports, logs)
- Playwright + TypeScript automation framework scaffolded under `automation/` (config, POM base, login page object, role-based auth setup, env loader)
- Docs/testcases file naming switched to `HELPDESK_<NAME>.md` (UPPER_SNAKE_CASE, plugin-prefixed) — now the standard for all new plugins, see `CLAUDE.md` §2b
- `HELPDESK_USER_GUIDE.md`, `HELPDESK_FEATURES_LIST.md` (54 features, categories A–K), and `HELPDESK_REQUIREMENTS.md` all populated with real content derived from the plugin's actual documented behavior
- Per-environment test data registry convention added (`automation/testdata/<PREFIX>_TESTDATA_<ENV>.xlsx`), `HELPDESK_TESTDATA_LOCAL.xlsx` generated (empty — no test execution has happened yet)
- **All 54 features now have written test cases** across 12 suite files in `testcases/`, positive/negative/edge, every case traceable to a specific line in the user guide:
  - `HELPDESK_PLUGIN_INSTALLATION.md` (#1–4, TC-001–012)
  - `HELPDESK_NAVIGATION_WORKSPACES.md` (#5–8, TC-060–074)
  - `HELPDESK_TICKET_LIFECYCLE.md` (#9–17, TC-013–032 + 033–041)
  - `HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md` (#18–21, TC-042–059)
  - `HELPDESK_SLA_ESCALATION.md` (#22–32, TC-075–107)
  - `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` (#33–37, TC-108–124)
  - `HELPDESK_PREPAID_HOURS.md` (#38–40, TC-125–142)
  - `HELPDESK_EMAIL.md` (#41–44, TC-143–156)
  - `HELPDESK_CONTENT_TEMPLATES.md` (#45–47, TC-157–180)
  - `HELPDESK_REPORTING_AUTOMATION.md` (#48–51, TC-181–203)
  - `HELPDESK_PERMISSIONS.md` (#52–53, TC-204–214)
  - `HELPDESK_RAKE_TASKS.md` (#54, TC-215–227)
- Two documentation contradictions in the plugin's own user guide were caught and given dedicated resolver test cases rather than silently picked one way: TC-HLP-063 (does a customer hold one project-access row or many?) and TC-HLP-165 (does `manage_helpdesk` alone grant report export, or is `export_helpdesk_reports` strictly required?)

- **Live testing session on Forge (`flux-fiqll6jza49.forge.zehntech.com`) — first real execution pass:**
  - Enabled the Helpdesk module on "Helpdesk Service Desk"; granted `view_helpdesk` to QA role and `manage_helpdesk` to Team Lead role; reassigned `luna.blossom`→QA, `daisy.skye`→Team Lead, `autumn.grace`→Project Lead on that project (they double as L1/L2/L3 escalation assignees)
  - Confirmed PASS: TC-HLP-334 (holiday CRUD incl. multi-day range), TC-HLP-284/076/078 (SLA creation, working hours/days/holidays saved, field-level History diff), TC-HLP-301 (L1→L2→L3 support-level chain with a pre-existing "test" level as L1), TC-HLP-037 (organization creation), TC-HLP-167 (manage_helpdesk grants config management — done implicitly by daisy.skye performing all the above)
  - Extra pre-existing seed data discovered mid-session (not created by us): a support level named "test" (order 1) and an SLA named "dsfasd" — `HELPDESK_MEMORY.md`'s earlier "zero support levels/SLAs exist" note was wrong and has been corrected
  - **TC-HLP-063 partially informs**: the New Customer form's Project Access section is NOT statically absent as first thought — it renders conditionally once an eligible project/SLA/organization exists. Full resolution still pending — see updated BUG-HLP-001 status below.
  - **TC-HLP-041: PASS on retry** — customer "Retry Test" (`retrytest01`) created successfully (Project access left empty). Several earlier attempts with Project access filled in appeared to fail; not yet re-confirmed whether that combination specifically fails or whether it was the same intermittent issue.

## In Progress

- **2026-08-24: Forge instance rotated** — old URL (`flux-fiqll6jza49`) went offline (`ERR_NAME_NOT_RESOLVED`) mid-session; new URL is `flux-fnwchpuwl49.forge.zehntech.com`, `QA_CREDENTIALS_FORGE.md` updated. All prior session's data (Acme Corp, SLA "Standard", holidays, support levels, customer retrytest01) is gone — fresh instance from the same seed template. Re-did the module-enable + role-grant + user-reassignment setup identically (same role IDs 11/12 and membership IDs 18/27/36 — same template). BUG-HLP-001 stays open/tracked regardless of environment since it's a plugin bug, not environment-specific.
- **CRUD-first verification pass COMPLETE** (per explicit direction: do full CRUD on Organization, Customer, SLA, Holiday, Support Levels BEFORE creating seed data). All five entities now have confirmed Create/Read/Search-match/Search-no-match/Update; Delete was actually executed end-to-end for Organization/SLA/Support Level (throwaway entities), and markup-confirmed (same JS modal) for Holiday/Customer without deleting their kept real records. See `HELPDESK_MEMORY.md` for full detail on each entity and on the two bugs found along the way (BUG-HLP-001 update, BUG-HLP-002 new).
- **TC-HLP-045/122 RESOLVED** — multiple project-access rows on one customer DO persist. Required enabling the Helpdesk module on a second project ("Agile Board Project") and creating a support level for it (AB-L1), since Support Level is a hard-required field per project-access row. See `HELPDESK_MEMORY.md`.

## Blockers

- ~~BUG-HLP-003 is a complete blocker for deactivating a Canned Response specifically (TC-HLP-033)~~ — **RESOLVED and closed 2026-08-27**, see Open Bugs below. TC-HLP-033 should now be re-executed as part of the pending `HELPDESK_CONTENT_TEMPLATES.md` regression, since Step 1 (deactivate) can now actually succeed.

## Open Bugs Found

- **BUG-HLP-001 (Medium, CLOSED 2026-08-27):** New Customer form's Login field shows a permanently stuck "Login must be at least 2 characters long" message that never clears and doesn't reflect actual outcome. Never reproduced across the last several sessions' customer creations. Closed per user explanation: a new Forge server pulls a fresh plugin clone on creation, so the original instance's stuck-validation state doesn't carry forward. See `bugs/closed/BUG-HLP-001.md`.
- **BUG-HLP-002 (Low, CLOSED 2026-08-31) — was a symptom of BUG-HLP-005, not independent.** Escalation To was correctly scoped when the Support Level form was reached via the proper project link; it was unfiltered only when reached via BUG-HLP-005's bypass route. Retested 2026-08-31 immediately after BUG-HLP-005: the bypass route no longer exists (404), and the proper route's Escalation To is still correctly scoped. Closed per explicit user confirmation. See `bugs/closed/BUG-HLP-002.md`.
- **BUG-HLP-003 (High, CLOSED 2026-08-27):** The Active checkbox could not be unchecked via Edit on **Organization, SLA, and Canned Response**. A working list-level AJAX toggle existed as a workaround for Organization and SLA; Canned Response had no workaround at all (complete block). **Product and Support Level were confirmed NOT affected.** Retested 2026-08-27 on the rebuilt Local environment: all three affected entities now correctly persist a deactivation through the Edit form. Closed per explicit user confirmation. See `bugs/closed/BUG-HLP-003.md`.
- **BUG-HLP-004 (Low, CLOSED 2026-08-27):** Customer form's SLA Name and Support Level dropdowns (Project Access row) were functionally scoped to the selected project — the wrong-project option couldn't be selected — but that option was only `disabled`, not removed from the visible list, even though the plugin's own markup set `hidden=""` on it (a CSS conflict defeated that). Retested on a rebuilt two-project Local environment (Alpha + a newly rebuilt Beta): the cross-project option now correctly computes `display: "none"` in both directions, both fields. Closed per explicit user confirmation. See `bugs/closed/BUG-HLP-004.md`.
- **BUG-HLP-006 (Critical, CLOSED 2026-08-31):** A customer could not open a ticket they created — the Ticket # and Subject links in their own restricted ticket list pointed to `/issues/:id` (Redmine's standard issue route), which the Customer role isn't authorized to access. Retested twice on Local (2026-08-26 and 2026-08-31, two different environment rebuilds) — both times the link correctly resolves to the branded, customer-accessible route instead. Closed per explicit user confirmation despite no direct Forge re-verify (Forge URL on file was stale) — see `bugs/closed/BUG-HLP-006.md`'s Closed section for the process caveat.
- **BUG-HLP-005 (Medium, CLOSED 2026-08-31) — was the root cause of BUG-HLP-002.** SLA and Support Level both had global CRUD routes (`/rf_slas(/new)`, `/rf_support_levels(/new)`) unlinked from any discoverable navigation, each with its own working "New" button bypassing the intended project-scoped flow. Retested 2026-08-31: all four routes now return a genuine HTTP 404 (removed entirely, not just gated) — confirmed the app was up and the admin session valid at the same time. Proper project-scoped flow still works correctly, now via a cleaner path-based URL with no `project_id` form field needed. BUG-HLP-002 confirmed resolved as the predicted side effect. Both closed per explicit user confirmation. See `bugs/closed/BUG-HLP-005.md`.
- **BUG-HLP-008 (Medium, CLOSED 2026-08-31):** Helpdesk Conversion tab omitted a customer's real inbound-email reply and separately duplicate-logged an agent's own reply mislabeled as an inbound customer message. Retested on a fully rebuilt email round-trip: both parts fixed — the real reply is now correctly logged, no duplicate/mislabel. Closed per explicit user confirmation. See `bugs/closed/BUG-HLP-008.md`.
- **BUG-HLP-009 (High, CLOSED 2026-08-31):** A plain Edit-form note falsely auto-transitioned a ticket's Status to "Waiting for Customer Response" with zero real customer communication. Retested on a fresh ticket: Status now correctly stays unchanged, confirmed 3 independent ways. Closed per explicit user confirmation. See `bugs/closed/BUG-HLP-009.md`.
- **BUG-HLP-015 (Medium, open, filed 2026-08-31):** Two related findings from a user observation, verified live: (1) a customer's ticket view exposes the standard "Notes" tab, not just "Helpdesk Conversion" — per explicit user direction, only Helpdesk Conversion should ever be customer-facing. (2) a customer's real email reply is logged twice — correctly in Helpdesk Conversion, but also as a plain Journal under Notes visible to agents, unlike an agent's own Reply Note which is correctly excluded from Notes entirely. See `bugs/open/BUG-HLP-015.md`.
- **BUG-HLP-018 (CLOSED 2026-08-31, does not reproduce):** Originally filed as "Comment field invisible on first load when Required." User reported it didn't reproduce on their own manual check; retested immediately with a direct DOM inspection (`browser_evaluate`) rather than relying on the accessibility-snapshot text search the original finding used — the Comment field is genuinely visible, on-screen (real bounding rect, `display: block`, `opacity: 1`), and interactive from the very first load. Completed the full reply+time-log flow in one clean attempt, no retry needed. **Closed as a false positive from this session's own testing methodology, not a plugin defect.** See `bugs/closed/BUG-HLP-018.md`.
- **BUG-HLP-017 (Medium, open, filed 2026-08-31):** Documented "Merge duplicate tickets" feature (Feature #17, `HELPDESK_USER_GUIDE.md` §7.4) has no discoverable control anywhere — checked both an individual ticket's own Actions menu (only "Copy link") and the full multi-select context menu for two selected tickets (13 items total, no Merge). Blocks TC-HLP-379, TC-HLP-393, and TC-HLP-397 in `HELPDESK_TICKET_LIFECYCLE.md`, all filed under this one bug. See `bugs/open/BUG-HLP-017.md`.
- **BUG-HLP-019 (Medium, open, filed 2026-09-01, narrowed same day per user pushback):** The real escalation notification email (verified via the actual recipient's Roundcube mailbox on two consecutive real escalations) never states which deadline (Response vs Resolution) breached, even though `HELPDESK_USER_GUIDE.md` documents this as required content. An earlier, broader claim (email should also name the previous assignee) was dropped after re-checking the actual guide — it only promises the previous/new *level* (confirmed present), not the assignee's name. Found executing `HELPDESK_SLA_ESCALATION.md` TC-HLP-322. See `bugs/open/BUG-HLP-019.md`.
- **BUG-HLP-020 (Medium, open, filed 2026-09-01):** Reassigning a ticket after unassigning it resumes the SLA clock's display but does not credit back the paused duration to the deadline — unlike the Waiting-for-Customer-Response pause/resume, which correctly extends the deadline by the exact paused amount. A ticket left briefly unassigned can become instantly "overdue" the moment it's reassigned and get escalated as a real side effect (reproduced: ticket #15 auto-escalated L2→L3 from a ~40-second unassignment). Found executing `HELPDESK_SLA_ESCALATION.md` TC-HLP-298. See `bugs/open/BUG-HLP-020.md`.
- **BUG-HLP-021 (Medium, open, filed 2026-09-01):** A ticket escalates into a Support Level even while that level is deactivated — directly contradicts `HELPDESK_USER_GUIDE.md`'s own spec ("Inactive levels are not offered and are not escalated into"). Found executing TC-HLP-349. See `bugs/open/BUG-HLP-021.md`.
- **BUG-HLP-022 (Medium, open, filed 2026-09-01):** Deleting a Support Level currently selected on a customer's project-access row succeeds silently with no dependency warning (unlike SLA delete, correctly blocked) — orphans the customer's row into an invalid state (SLA still set, Support Level silently reset to None) the form's own validation would never allow directly. Found executing TC-HLP-362. See `bugs/open/BUG-HLP-022.md`.
- **BUG-HLP-023 (Low, open, filed 2026-09-01):** Same defect shape as BUG-HLP-022 but for Holiday↔SLA — deleting an attached Holiday succeeds silently, though it degrades gracefully (SLA's Holiday field clears to "-", not a broken reference). Found executing TC-HLP-364. See `bugs/open/BUG-HLP-023.md`.
- **BUG-HLP-030 (Medium, open, filed 2026-09-02, broadened 2026-09-03):** Creating an SLA, Support Level, Holiday, or Product (Name or Code) over 255 characters crashes with an unhandled Rails 500 (`ActiveRecord::ValueTooLong`) instead of a validation message — confirmed across 4 models, 2 columns, identical root cause every time. See `bugs/open/BUG-HLP-030.md`.
- **BUG-HLP-031 (Medium, open, filed 2026-09-02, broadened 2026-09-02 to cover Edit too):** SLA First Response Time/Resolution Time are required but a blank value is silently accepted on both Create and Edit, saving as NULL — `0`/negative on the same fields are correctly refused. See `bugs/open/BUG-HLP-031.md`.
- **BUG-HLP-032 (Medium, open, filed 2026-09-02):** SLA Agreement upload has no file-type restriction — a `.exe` file uploads and stores successfully despite the 5 MB size limit being genuinely enforced. See `bugs/open/BUG-HLP-032.md`.
- **BUG-HLP-033 (Medium, open, filed 2026-09-03):** The ticket list's SLA Status badge and the ticket's own SLA Information detail panel disagree on the same ticket's SLA condition — root-caused to two independent health-computation implementations; the detail panel has no At Risk/Critical concept at all and defaults to "On Track" for both. See `bugs/open/BUG-HLP-033.md`.
- **BUG-HLP-016 (High, CLOSED 2026-08-31):** Found while re-executing `HELPDESK_TICKET_LIFECYCLE.md` from scratch — TC-HLP-375's reply-auto-assign appeared to silently fail (server log claimed success but `assigned_to_id` never persisted, so the SLA clock never started either), reproduced on 3 tickets (#7, #8, #9). Root cause was environmental, not a plugin defect: the Agent role's per-tracker permission matrix was missing Support tracker's own Edit Issues checkbox. Retested on ticket #10 after the user corrected the role's permissions: PASS — Assignee auto-set correctly, SLA Information tab shows "Paused", matching the original 2026-08-27 evidence. Closed per explicit user confirmation. See `bugs/closed/BUG-HLP-016.md`.

## Test Case Coverage Gap Closed — 2026-08-24: Search/Filter/Apply/Clear across Settings entities and Dashboard KPI links

User flagged that filter functionality (search box, Status dropdown, Apply Filters, Clear Filters, and Dashboard KPI "View all" auto-applying a filter) was visibly present on many screens during manual testing but had no corresponding written test cases outside the Ticket List suite. Audited every testcase file and added the missing coverage — 9 new TCs, none yet executed (writing-only pass, no live testing done for these this session):

- `HELPDESK_NAVIGATION_WORKSPACES.md`: **TC-HLP-155/275** — Command Center and Project Dashboard KPI "View all" links (Unassigned/Open/On Hold/SLA Breached/Resolved) auto-applying the matching filter to the ticket list.
- `HELPDESK_SLA_ESCALATION.md`: **TC-HLP-339/277/278** — SLA, Support Level, and Holiday list search + Status filter (Active/Inactive) + Apply/Clear.
- `HELPDESK_CUSTOMERS_ORGANIZATIONS.md`: **TC-HLP-058** — Organization list search + Status filter + Apply/Clear (previously only Customer's list had a filter TC, TC-HLP-057); **TC-HLP-059** — strengthens Customer list coverage to specifically exercise Apply/Clear buttons and a combined search+organization filter, not just each condition in isolation.
- `HELPDESK_CONTENT_TEMPLATES.md`: **TC-HLP-015/282** — Product list search + Project filter + Status filter + Apply/Clear; Canned Response list search (Name and Content) + Apply/Clear (noted: no Status filter exists there, consistent with BUG-HLP-003's finding that Canned Response has no working deactivate mechanism at all).

**Confirmed not applicable:** "Add Column" (a column picker) only exists on the Ticket list (already covered by TC-HLP-405) — no Settings entity list (Organization/SLA/Support Level/Product/Customer/Holiday/Canned Response) has a column picker, only a fixed table. Not a gap, just doesn't exist elsewhere.

**Next session should execute these 9 TCs live** and record PASS/FAIL, since they are currently written-but-unverified.

**Follow-up same session — dropdown project-scoping gap:** user asked whether the project-scoping *dropdowns* (not list filters) on Organization/SLA/Product/Customer were covered. Found TC-HLP-048 already tested Support Level's dropdown scoping in the Customer form (and fixed a title typo there — it said "members", body was about support levels), but no sibling TC existed for the SLA dropdown on the same form, despite that exact behavior being heavily live-investigated this session (BUG-HLP-004). Added:
- **TC-HLP-049** (`HELPDESK_CUSTOMERS_ORGANIZATIONS.md`) — SLA dropdown project-scoping in the Customer form, mirroring TC-HLP-048, with the live-confirmed 2026-08-24 result folded in.
- **TC-HLP-050** (same file) — Organization dropdown in the Customer form is explicitly documented as NOT project-scoped, by design (Organization has no project association at creation time at all) — written so a future tester doesn't mistake this for the same defect class as BUG-HLP-004.
- **TC-HLP-017** (`HELPDESK_CONTENT_TEMPLATES.md`) — Product dropdown on the ticket form only offers the current project's own products; this specific scoping requirement had never been tested before (TC-HLP-014 only tested that picking a product saves, not that the dropdown is scoped).

## Customer Creation + Login Functionality Verified — 2026-08-24, new Forge instance `flux-forgyim9c49`

User asked to verify customer creation and customer login functionality end-to-end. Full pass: created customer "Login TestCustomer" (login `logintest.customer`, id=30) with an explicit password and Project Access to "Agile Board Project" (Org "tes", Support Level "sdaf"). Confirmed working: login via username, restricted nav (My account/Sign out/My Helpdesk only), "My Helpdesk" portal correctly scoped to the one assigned project, project-level restricted Helpdesk dashboard with real Prepaid Support Hours data, and raising a ticket via the simplified customer "New issue" form (ticket #271, correctly attributed and visible from the admin side).

**Found BUG-HLP-006 (Critical) in this same pass**, only surfaced because the user explicitly asked "is the ticket visible to him, is he able to open it" — I had initially verified creation + admin-side visibility but not confirmed the customer could re-open their own ticket. When tested, both the Ticket # and Subject links in the customer's own ticket list point to `/issues/:id` and are rejected with "You are not authorized to access this page." See Open Bugs below.

## Navigation-Locator Refactor Completed — 2026-08-25

User flagged (correctly) that every page object navigated exclusively via `page.goto(deepUrl)` — confirmed by audit: zero clicks through real nav links/tabs/menus anywhere in the 15-file `automation/tests/` suite. Fixed all of them:

- **`BasePage.ts`**: added real click-through nav primitives — `clickTopNav()`, `clickProjectTab()`, `clickHelpdeskSubNav()`, `clickSettingsTab()` — documented with which label set applies in which context. `clickHelpdeskSubNav()` is deliberately scoped to the `<ul>` containing "Helpdesk Dashboard" rather than a bare page-wide link lookup, because a project's Helpdesk tab renders the standard Redmine project menu's own "Settings" link *and* the Helpdesk sub-nav's different "Settings" link on the same page simultaneously — an unscoped lookup throws a Playwright strict-mode ambiguity error. A similar latent collision ("Products", Command Center rail vs. global Settings tab) is documented but not currently hit by any method.
- **All 15 page objects** rewired to reach their target screen by clicking through real nav — `HelpdeskOrganizationPage`, `HelpdeskProductPage`, `HelpdeskCannedResponsePage`, `HelpdeskHolidayPage`, `HelpdeskCustomerPage` (Command Center rail); `HelpdeskTicketListPage`, `HelpdeskKnowledgebasePage` (project's Helpdesk tab sub-nav); `ProjectMembersPage`, `ProjectSettingsPage` (project's own Settings tab); `RolesPermissionsPage`, `AdminUsersPage` (Administration sidebar).
- **`HelpdeskSlaPage` / `HelpdeskSupportLevelPage`** needed special handling: their real create-flow now goes through `openProjectList()` → click "New SLA"/"New Support Level" (correctly project-scoped by construction), while the BUG-HLP-005 bypass routes are kept as deliberately-named `openGlobalListViaDirectUrl()` / `openNewViaDirectUrl()` methods so that bug stays independently testable rather than silently disappearing because the suite stopped using the broken path. Also fixed a real latent bug in `HelpdeskSupportLevelPage.create()`: when reached via the proper nav, `support_level[project_id]` renders as a locked hidden input, not a selectable `<select>` — the old code's `projectSelect.selectOption()` call would have thrown.
- Most `openEdit()`/`openDetail()` methods changed signature from an opaque ID to the entity's visible name (e.g. `openEdit(organizationName: string)` instead of `openEdit(organizationId: number)`), since that's how a real user actually finds the row to click — IDs aren't visible/guessable in the UI. Safe to change: **zero `.spec.ts` files exist yet**, so nothing outside this refactor called the old signatures.
- **Fixed a real, unrelated bug found in passing**: `LoginPage.ts` targeted `#username`/`#password`/`input[type=submit][name=login]`/`#loggedas` — standard Redmine core login markup. The actual live form (confirmed every session this engagement, most recently on `flux-fwdq7ydhw49`) is a completely different custom Redmineflux-skinned form. `auth.setup.ts` would have failed against any current Forge instance; never actually exercised before since every login this engagement was driven directly via Playwright MCP, not this page object.
- **Fixed pre-existing project infra gaps** (unrelated to the navigation work, but blocking any type-check from running at all): `automation/` had never had `npm install` run (no `node_modules`), and `tsconfig.json` was missing `@types/node` and the `DOM` lib entry. Installed dependencies, added `@types/node`, added `"DOM"` to `lib`. **Full suite now compiles clean (`npx tsc --noEmit`, zero errors).**

**Still outstanding** (deferred, per explicit user direction, to a later session): real test data prep (multi-SLA-per-project, multi-agent support levels, multi-project products, multi-org plan, in-project agent rosters, canned responses, permission-test user/role combinations) — none of this exists yet on the current instance. No `.spec.ts` files exist yet either — writing the first real specs against these now-fixed page objects is the natural next step once test data exists.

## Comprehensive Test Data Prep for Automation — 2026-08-25, Forge `flux-fwdq7ydhw49`

User asked to prepare real, comprehensive test data so a future automation script "will not face any issue whether data exists or not" — i.e. build out the full matrix that was deferred at the end of the navigation-locator refactor session. All done via real click-through UI (SLA/Support Level forms via `?project_id=X` direct URL for expediency during bulk seeding — same mechanism the page objects' proper nav path produces, not the BUG-HLP-005 bypass route since Project renders as a locked hidden input either way at that URL):

- **Permissions/roles** (carried over from earlier in this same Forge rotation, confirmed still in place): QA role → `view_helpdesk`; Team Lead role → all 8 helpdesk permissions; Developer role → none (negative-permission-test role, deliberately left blank so an agent with only Developer can be used to confirm Helpdesk URLs are blocked without permission).
- **Project memberships reassigned**: Helpdesk Service Desk — daisy.skye/luna.blossom/autumn.grace/harmony.rose/willow.belle → Team Lead, selene.frost/sage.willow → QA. Agile Board Project — aurora.wren/briar.sunset/celeste.dawn → Team Lead, sage.willow → QA.
- **Organizations (2)**: Acme Corp, Globex Industries — both used across different customers/projects/prepaid budgets below, so org-scoping test cases have two genuinely distinct organizations to compare, not just one.
- **SLAs (4)**: Standard SLA + Priority SLA on Helpdesk Service Desk; Agile Standard SLA + Agile Priority SLA on Agile Board Project — every project now has two SLAs with different response/resolution targets (4h/24h vs 1h/8h), so SLA-selection and SLA-breach test cases have real variety per project.
- **Support Levels (5), two full multi-agent escalation chains**: Helpdesk Service Desk — L1 (Daisy Skye + Luna Blossom) → L2 (Autumn Grace + Harmony Rose) → L3 (Willow Belle); Agile Board Project — AB-L1 (Aurora Wren + Briar Sunset) → AB-L2 (Celeste Dawn). Confirmed live (again) that the Escalation To dropdown correctly only offers higher-order levels once they exist — had to create L1/L2/L3 in order and go back to edit L1→L2 and L2→L3 escalation afterward, since a level can't point at one that doesn't exist yet.
- **Products (4)**: Helpdesk Portal (HDP) + Ticketing API (TAPI) on Helpdesk Service Desk; Agile Board Web (ABW) + Agile Board Mobile (ABM) on Agile Board Project — two per project as planned, different categories (Web Application/Software/Mobile Application) for product-dropdown-on-ticket-form variety.
- **Canned Responses (4, global/install-wide)**: "Greeting - Ticket Received", "Escalation Notice", "Request for More Information", "Closing - Ticket Resolved" — covers the four canonical points in a ticket's lifecycle where an agent would reach for a template, each using a different subset of the 9 available macros.
- **Customers (4)**, deliberately varied combinations so no automation script can assume a single fixed shape:
  - `acme.hd.contact` — single project (Helpdesk Service Desk), Standard SLA, L1, Acme Corp
  - `globex.agile.contact` — single project (Agile Board Project), Agile Priority SLA, AB-L1, Globex Industries
  - `globex.hd.contact` — single project (Helpdesk Service Desk), Priority SLA, L2, Globex Industries
  - `multiproject.contact` — **two** project-access rows on one customer (Helpdesk Service Desk/Standard SLA/L1/Acme Corp **and** Agile Board Project/Agile Standard SLA/AB-L2/Globex Industries) — re-exercises the TC-HLP-045/122 multi-project-access pattern with a fresh fixture on this rotation, confirmed both rows persisted and both show correctly on the customer list ("Projects: 2").
  - All four use password `Test@12345`.
- **New feature discovered this session — Support Packages + Prepaid Support Hours budgets**: Command Center → Helpdesk Settings has a 5th tab, "Support Packages" (label-only entities for prepaid-hour top-ups/time-log categorization — Name/Description/Active, no other fields), not previously documented in any doc file. Created "Premium Support" and "Standard Support". Then used each Organization detail page's "Prepaid Support Hours" tab → "Add / top up hours" dialog (Project*/Hours*/Support Package/Comment* fields) to give **Acme Corp 40h on Helpdesk Service Desk** (Premium Support, run-out mode set to "Hard — stop work") and **Globex Industries 20h on Agile Board Project** (Standard Support, run-out mode set to "Soft — allow overage") — deliberately different run-out modes so both enforcement paths have real fixture data. Each top-up is logged in a real "Budget change history" table (0.00h → 40.00h / 20.00h, with comment and approver). This whole area (Support Packages tab, the top-up dialog, the 3 run-out modes, the ledger link per project row) is undocumented in `HELPDESK_REQUIREMENTS.md`/`HELPDESK_FEATURES_LIST.md`/`HELPDESK_USER_GUIDE.md` and in `HELPDESK_PREPAID_HOURS.md`'s existing TC-125–142 — worth a dedicated pass to write test cases against the real UI next time prepaid-hours testing is picked up, since the existing TCs were written from user-guide assumptions that undersell what the feature actually has.
- **`automation/testdata/HELPDESK_TESTDATA_FORGE.xlsx` fully repopulated for this rotation** — all sheets (Organizations, SLAs, Support Levels, Products, Customers, Canned Responses, Prepaid Budgets) cleared of the previous rotation's now-nonexistent rows and rewritten with this session's real data; a new **Support Packages** sheet was added (both to this workbook and to `scripts/gen_testdata_xlsx.py`'s `SHEETS` dict for future regenerations) since it's a newly-discovered entity type.
- Still not created this pass: prepaid-budget usage (all fixtures start at 0h Used — no ticket time has been logged against them yet), Holidays (empty on this rotation), Email Configuration.

## SLA Lifecycle Deep-Dive — 2026-08-25, ticket #272

User asked to manually test one specific flow: customer creates a ticket, an agent replies — does it auto-assign, does the SLA start? Confirmed both, plus found a potentially serious combined issue. Full detail in `HELPDESK_MEMORY.md`'s "Recurring Issues (addendum, 2026-08-25)" section — summary:

- Reply auto-assigns to the replying agent and auto-transitions Status → "Waiting for Customer Response". Confirmed via ticket History.
- SLA does not exist on a ticket at all until an agent's first reply — that reply both starts the SLA clock AND counts as the First Response, with deadlines computed working-hours-aware (hand-verified against Standard SLA's 09:00–18:00 Mon–Fri window).
- The same reply immediately **pauses** the SLA (since Status → Waiting for Customer Response). Manually changing Status away from that (via Edit, to "In Progress") did **NOT** resume it — same pause timestamp, no new Activity Log entry, no resume control found anywhere in the UI (Actions menu, Helpdesk Conversion tab).
- Since the real customer is separately blocked from ever replying at all (BUG-HLP-006, re-confirmed this session even in this exact status), the combination means: **if resuming requires a genuine customer reply, the Resolution SLA may be permanently frozen for every ticket that gets a first response, for the life of this plugin** — a much bigger consequence than BUG-HLP-006's original framing.
- **Not yet tested**: whether an inbound email reply (Helpdesk Settings → Email Configuration, never exercised end-to-end this engagement) is the actual intended resume path. This is the key open question.
- **Not filed as a bug** — user chose to just document and decide later, pending the email-reply-path answer.
- Ticket #272 kept as a real, deliberately-unresolved fixture (In Progress status, permanently-paused Standard SLA) — see `HELPDESK_TESTDATA_FORGE.xlsx` Tickets (fixtures) sheet. Ready-made repro case for next session.
- Also discovered, previously undocumented anywhere: a full "SLA Information" tab per ticket (SLA Overview table, SLA Journey per-escalation-stage table, Activity Log table) — none of this is reflected in `HELPDESK_SLA_ESCALATION.md`'s existing test cases.

## Local Email Testing Session — 2026-08-25/26, `redmine-docker-6` (`localhost:3012`)

User asked to build out a full local environment specifically to test real email flows — Forge structurally cannot receive/send email at all (no route to any real inbox). Full session detail lives in `HELPDESK_EMAIL_TEST_PLAN.md` (preconditions, test data, execution results) — summary here:

- **Environment built from scratch**: local instance reset to a clean slate (0 projects/users/custom fields), then rebuilt: 2 projects (Helpdesk QA Alpha id 3, Helpdesk QA Beta id 4), 2 Organizations, 2 SLAs, a dedicated **Agent** role (Developer baseline + `view_helpdesk`/`view_email_history`/KB perms, workflow copied via `WorkflowRule.copy`), 6 Agent Users (3 per project), 2 Customer records, 14 mail server mailboxes (2 support inboxes, 6 agents, 2 customers, 4 spares from earlier).
- **Core infra fixed along the way**: Redis + Sidekiq started for the Helpdesk plugin's 3 scheduled jobs (must be redone after any container restart — does not persist); the local mail server's IMAPS (993) was never enabled (`SSL_TYPE=` blank) — generated a self-signed cert, enabled `SSL_TYPE=self-signed`, installed the cert into the Redmine container's trust store, and fixed Roundcube's IMAP/SMTP config to match (see `HELPDESK_EMAIL_TEST_PLAN.md` §3 and the `reference_docker_mail_server` memory for the exact steps — none of this survives a mail-server container rebuild).
- **Full email round-trip confirmed working, ticket #67 (Helpdesk QA Beta)**: customer emails support inbox with a ticket keyword → real ticket created (correct subject prefix, correct Organization auto-populated from the Customer record) → agent replies via UI (auto-assign, Status → Waiting for Customer Response, correctly threaded outbound email) → customer replies by real threaded email → correctly becomes a note (not a duplicate ticket), Status auto-transitions to In Progress.
- **Resolved the open question left from the 2026-08-25 Forge session** (`HELPDESK_MEMORY.md`'s SLA lifecycle addendum): an inbound email reply from the customer IS a real, working SLA resume/keep-alive path — ticket #67's SLA Information tab showed "✓ On Track" with a live countdown after the customer's email reply, not paused. The earlier "may be permanently frozen forever" fear does not apply to the email channel (only the portal channel, still blocked by BUG-HLP-006).
- **New bug found and filed: BUG-HLP-007 (High, open)** — Internal Note submission crashes with HTTP 500 (`journals.private_notes` NOT NULL violation). First time this path was exercised on Local; the equivalent Forge-side finding (addendum #5) reported it working there, so worth a Forge retest to see if this is a genuine regression or local-build-specific.
- **BUG-HLP-007 retested 2026-08-26 via real click-through navigation (not `browser_navigate`), scope corrected**: the bug is NOT unconditional across "any role" as first reported — it reproduces only via the **branded** `/projects/:id/helpdesk/issues/:id` route (`RfProjectHelpdeskIssuesController`); via the **core** `/issues/:id` route (`IssuesController`, reached by real click-through from the agent's in-app ticket list) Internal Note submission succeeds. Still High severity, not downgraded: confirmed by opening two real outbound notification emails in Roundcube that **both** "View Ticket"/"View Ticket Details" links point to the branded (buggy) route — so this is exactly what any real user clicks. See `bugs/open/BUG-HLP-007.md`'s "Scope correction" section.
- **BUG-HLP-006 retested on Local, 2026-08-26 (originally filed on Forge only)**: does **NOT** reproduce here. Customer `beta.customer` opened ticket #67 successfully via real click-through nav (My Helpdesk → project → Helpdesk Tickets → ticket row); also controlled for ticket origin by creating a fresh ticket (#69) via the customer's own portal "New issue" form (matching the original Forge repro method) — same clean result. Root cause: Local's customer ticket list links to the branded route (which customers CAN access), while the original Forge repro showed the core `/issues/:id` route (which customers CANNOT access) — the mirror image of BUG-HLP-007's finding. **Does not close the bug** — needs a real Forge retest first. See `bugs/open/BUG-HLP-006.md`'s "Retest — 2026-08-26" section and `HELPDESK_MEMORY.md`'s dual-route addendum for the full pattern writeup tying both bugs together.
- **A real but low-severity plugin bug found and ruled out as a data-loss risk**: `Helpdesk::EmailPollerWorker` logs a spurious `MailHandler returned unexpected result: Journal ...` / `could not be processed` warning for every reply-to-existing-ticket email (doesn't recognize a `Journal` return type as success) — confirmed cosmetic-only by waiting for the next real cron tick and confirming the email was correctly marked read and NOT reprocessed (no duplicate notes). Worth filing as a low-severity bug for the misleading log noise alone, not yet filed.
- **Negative-keyword gating — PASS.** First attempt accidentally self-invalidated (my own test email's explanatory body text contained the word "ticket"), correctly creating ticket #68 — a real confirmation that keyword-matching scans the full email content, not just the subject. Redone with genuinely keyword-free text (subject "Good morning", body "Hope you have a wonderful day...") — poller correctly logged `Email does not contain required keywords ["ticket", "issue", "request"] - ticket creation skipped`, and Alpha's ticket list stayed at 2 tickets (no new one created).
- Also confirmed live: BUG-HLP-004's cross-project-dropdown pattern reproduces identically on the Customer form here (SLA/Support Level options correctly functionally scoped but not visually hidden) — same root cause as documented on Forge, not a new finding, just re-confirmed on a second environment.

## Next Session Start Point

- **⚠ CURRENT AS OF 2026-09-22, LATEST UPDATE (supersedes all bullets below it) — read this bullet first, everything below it in this section is historical/superseded.** Same-day continuation: per explicit user instruction ("okay create it on production"), **BUG-HLP-015 was reported to production for the first time** as **#121073** (`ztflux`), assigned to Vaishnavi Bhawsar (user's explicit choice via AskUserQuestion — she was the recommended default since every other Helpdesk production bug this session used her), Priority Medium / Defect Severity Medium-severity / Defect priority Medium / Defect Type Functional / Category Helpdesk Plugin. Description covers both Part A (Notes tab exposed to customer sessions) and Part B (duplicate journal for a customer's real email reply), including the 2026-09-22 retest finding that Part B is now visible from the customer's own Notes tab too. Local `BUG-HLP-015.md` header (`Production Redmine Issue ID` field was missing from this file entirely, pre-dating the template convention — added it) and `bugs/_index.md` both updated. Then, per a further user instruction, **#121073 was linked to Test Case #119389 / Run #541 / Environment "Win 10 + Chrome"** (the same run/testcase every other Helpdesk production bug this session is linked to) via `report_defect` — now shows in the run's defect list alongside the rest. Literal `bugs/open/` count unchanged at **2** (BUG-HLP-015, BUG-HLP-037), but BUG-HLP-015 now has a Production Redmine Issue ID for the first time. **Next session: BUG-HLP-015 (#121073) awaits a real fix decision/dev pickup; BUG-HLP-037 (re-scoped to new-ticket-creation only, prod #120076) also still needs a fix. Worth considering full-plugin final-cycle regression (CLAUDE.md §10/§12, SENIOR_QA_STANDARDS.md §27) once both are resolved, toward marking `redmineflux_helpdesk` Complete in STATUS.md.**

- **⚠ 2026-09-22 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** Same-day continuation: production sync for #121011 and #120543 (both → Done/100%) and #120076 (→ Reopen, re-scoped) was executed after explicit approval — see the 2026-09-22 Run History row. Then, per a further user request, **BUG-HLP-015 was also retested — NOT FIXED, both parts still reproduce.** No Production Redmine Issue ID existed for this bug at the time (never reported), so this was a fresh local retest. Source grep for "BUG-HLP-015" found zero fix attempts anywhere. Part A (Notes tab exposed to a customer session, alongside Helpdesk Conversion) still reproduces exactly as originally found. Part B (a customer's real email reply duplicated as a plain Journal) still reproduces, and is now *worse*: the original finding's one partial mitigation — the customer's own Notes tab rendering empty for her — is gone; she can now see the same duplicate journal an agent sees. Literal `bugs/open/` count unchanged at **2** (BUG-HLP-015 stays open with updated evidence; BUG-HLP-037 unchanged).

- **⚠ 2026-09-22 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User restarted `redmine-docker-6` + Sidekiq and asked to retest 4 production issues: #121011 (BUG-HLP-061), #120543 (BUG-HLP-059), #120076 (BUG-HLP-037), and #119589 — the last of which is an **Inline Editor Plugin** bug, not Helpdesk, and was correctly skipped as out of scope. **BUG-HLP-061 CONFIRMED FIXED and closed** — a real, distinct "Add sub-article" "+" icon now renders per row (visually unambiguous vs. the delete "×"), correctly nesting under the exact row clicked via a new `treeAddParentId` mechanism. **BUG-HLP-059 CONFIRMED FIXED and closed** — the one remaining gap from the 2026-09-21 retest (ticket-list endpoint returning 0 for real customers) is now resolved; all 4 sub-issues found across this bug's history are fixed. **BUG-HLP-037 RECONFIRMED and re-scoped, stays open** — this bug had been marked "Unconfirmed" after 3 clean retests, but this session reproduced the original symptom again for new-ticket creation specifically (org reduced to exactly 0.00h under Hard mode, customer's new ticket still succeeded), while the time-log path on the same org/budget state was correctly blocked. Tally across all retests: new-ticket-creation now fails 2-of-3 independent attempts, time-log blocks 4-of-5 — verdict changed from Unconfirmed to a real defect, re-scoped to the new-ticket-creation path only. Literal `bugs/open/` count: 4 → **2** (BUG-HLP-015, BUG-HLP-037 remain). Production sync for #121011/#120543 (→ Done/100%) and a decision on #120076 (re-scoped, likely stays Reopen/In QA with updated notes) are both still pending explicit approval as of this bullet.

- **⚠ 2026-09-21, LATEST UPDATE (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** Continuing the same-day session: production sync for the 4 bugs closed earlier (#120545/476/471/465 → Done/100%) was executed after explicit approval. **BUG-HLP-061** (KB sidebar delete-icon-looks-like-a-plus) was reported fresh to production as **#121011** and linked to Test Case #119389 / Run #541 / Environment "Win 10 + Chrome" (an environment-name mismatch — "Windows 11 + Chrome" vs. the run's actual "Win 10 + Chrome" — was caught and corrected before linking). Then, per a further user request, the container and Sidekiq were restarted again and 2 more production bugs retested: **BUG-HLP-022** (#119755, SLA half) is now **CONFIRMED FIXED and closed** — both halves (Support Level + SLA) correctly block deletion when only referenced by a customer's project-access row. **BUG-HLP-059** (#120543) was retested again — the `restrict_helpdesk_customer_access` allowlist gap is now genuinely fixed, and single-ticket lookup (`show`) via key-only auth now works (a real improvement from the original 404) — but the ticket **list** endpoint (`GET /tickets`) still returns zero tickets for a real customer, confirmed extensively (2 customers, both auth methods, admin baseline correctly returns 392, caching/cookies ruled out) without the exact internal root cause being pinned down. Kept open, not resynced to production (no status change proposed this time). Literal `bugs/open/` count is now **4**: BUG-HLP-015, BUG-HLP-037 (unconfirmed), BUG-HLP-059 (reopened), BUG-HLP-061 (new). **Next session: consider a focused source-level debugging pass on `Api::V1::TicketsController#index`'s customer scoping** (the mystery from this session — source reads correctly, manual query replication returns the right count, but the live endpoint doesn't) **before anything else touches BUG-HLP-059 again**; otherwise, with only 4 open bugs left, confirm with the user whether to keep chasing production `In QA` bugs, resolve BUG-HLP-015/037 locally, or move to full-plugin final-cycle regression (CLAUDE.md §10/§12, SENIOR_QA_STANDARDS.md §27) toward marking this plugin Complete in STATUS.md.

- **⚠ 2026-09-21 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** This session was a pure production-retest batch, not new suite execution. Retested and closed 4 more production-`In QA` bugs (BUG-HLP-060/052/049/048, prod #120545/476/471/465) — see the 2026-09-21 Run History row for full detail. Also reopened BUG-HLP-059 on production (#120543 → Reopen) per explicit user instruction, since its underlying customer-API-access problem is still not resolved even though both originally-cited causes were fixed. **Critical environment lesson for next session or anyone touching `redmine-docker-6` after a restart**: a container restart auto-runs `rake db:migrate` (core) but NOT `rake redmine:plugins:migrate` (plugin migrations) — if the plugin shipped a new migration, run it manually (`bundle exec rake redmine:plugins:migrate RAILS_ENV=production` inside the container) and restart the container again afterward if any request already hit the live process first. Also remember Sidekiq needs `-C plugins/redmineflux_helpdesk/config/sidekiq.yml` on every manual start, or it silently only listens on the `default` queue.

- **⚠ 2026-09-14 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_PLUGIN_INSTALLATION.md` closed out — the **last** untested suite for this plugin — **11/12 executed live, 1 explicitly skipped per user decision, 0 new bugs.** This suite tests the plugin's install/migration *process* itself (fresh install, upgrades, folder renaming, wrong-order rake invocations), not application behavior, so most of its TCs can't safely run against the shared `redmine-docker-6` (10+ other suites depend on its accumulated fixture data). Built a genuinely fresh, disposable Docker stack instead — `redmine-install-test:6` (official `redmine:6` image + this plugin's own source), at `C:\redmine-docker-install-test\`, port 3099, project `helpdesk-install-test` — with the container's default command overridden to `tail -f /dev/null` so the official image's own auto `bundle install`/`db:migrate` entrypoint logic never fires, giving full manual step-by-step control matching this suite's own literal Steps almost exactly. **All 11 executed TCs PASS**: full fresh-install sequence end-to-end (TC-001); status/tracker artifacts confirmed genuinely created by that exact migration, resolving TC-003's own noted ambiguity — Support is one of Redmine's own 3 default trackers, not plugin-created (TC-002/003); re-running `load_default_data`/`plugins:migrate` both clean no-ops, no duplication (TC-006/010); migrating before `bundle install` failed with a real `Bundler::GemNotFound` (TC-008); migrating before `load_default_data` on a truly virgin DB left the documented broken state + refusal (TC-011); renaming the plugin folder caused a **hard Redmine boot failure** (`Redmine::PluginNotFound`) — more severe than the TC's own expectation — with full recovery confirmed after renaming back (TC-007); a simulated upgrade (no real second version existed, so the same source was re-copied and re-migrated) preserved an existing SLA/customer/ticket fixture unchanged (TC-005); stopping Redis/Sidekiq entirely and raising a real ticket confirmed the SLA genuinely breaches (UI shows "SLA Missed", deadline passed) but never auto-escalates (`response_breached: 0` at the DB level) while all helpdesk UI screens kept working normally (TC-009, the most substantively-built TC this session — required creating a fresh project/SLA/Support Level/Organization/Customer via the real UI on the bare disposable instance). TC-012 (cross-version parity, 5.0.x vs 6.1.x) was explicitly skipped by direct user decision rather than building a second disposable instance (partially scaffolded at `C:\redmine-docker-install-test-5x\`, never built — Dockerfile/compose only). **Notable side finding, folded into BUG-HLP-060, not a new bug**: the Sidekiq scheduler gem-incompatibility crash reproduced byte-for-byte identically on this completely fresh, from-scratch install (`sidekiq 7.3.9`/`connection_pool 3.0.2`, the exact pair Bundler resolves with zero manual intervention) — proving it's deterministic, not `redmine-docker-6`-specific drift. Open bug count unchanged at **40**.

  **Every one of this plugin's 12 suite files is now either fully closed or has an explicit, deliberate decision recorded for what's left (TC-012 skipped) — nothing remains silently untested.** The first disposable container (`helpdesk-install-test`, port 3099) is left running per explicit user instruction — it holds no data anything else in this engagement depends on, safe to tear down (`docker compose -p helpdesk-install-test down -v` from `C:\redmine-docker-install-test\`) whenever convenient, or reuse for future ad-hoc install/migration testing. **Next session: this plugin has no remaining untested suite** — confirm with the user whether to move to a different plugin, do a full-plugin regression pass, or revisit TC-012 with a real second Redmine version.

- **⚠ 2026-09-11 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** Filed **BUG-HLP-053 through 056 to production** (`ztflux`, per explicit user instruction) — each linked to testcase #119389/Run #541/Environment "Win 10 + Chrome" (same triple as the prior batch), assignee Vaishnavi Bhawsar, structured Textile descriptions, PDF-only attachments. **Discovered and fixed a real MCP tool limitation**: `upload_file` silently truncates any file whose base64 encoding exceeds ~20-22K characters (~15KB raw) with no error — the old Playwright/Chromium PDF generator always embedded 4 font files even for plain text, producing 70-104KB PDFs. Rewrote `scripts/gen_bug_pdf.js` to use `pdfkit`'s non-embedded Base-14 fonts instead — same reports now render at 6-8KB. Resulting production issues: #120504/#120507/#120508/#120510.

  Then `HELPDESK_REPORTING_AUTOMATION.md` closed out **23/23** (was 0/23), chosen by the user via AskUserQuestion — **4 new bugs (BUG-HLP-057 through 060)**. Full REST API section explored live against the Swagger spec (Tickets CRUD+merge, Conversations, SLA status show/pause/resume/escalate, Organizations/SLAs CRUD+toggle+history, `process_macros`, `sla_analytics`) — all genuinely functional once request shapes matched the spec exactly (several of my own initial request-shape guesses were wrong, not plugin bugs: nested-vs-flat body params differ per endpoint, and `pause`/`resume` are `PATCH` not `POST`). **BUG-HLP-057 (Medium)**: `export_helpdesk_reports` is a fully vestigial permission — Reports view+export is gated purely by `manage_helpdesk`, confirmed with two contrasting live role configs. **BUG-HLP-058 (Medium)**: the REST API's `conversations` reply endpoint correctly triggers the SLA first-response side effect but the reply itself lands in Notes instead of Helpdesk Conversion — backwards from the established BUG-HLP-015 rule. **BUG-HLP-059 (Medium)**: the Swagger spec's own documented "existing browser session" auth method is completely non-functional for every role including a full admin session (confirmed via a real `401` on an active admin session with no key at all) — combined with Customer accounts having no self-service way to ever obtain an API key, the spec's own "customers restricted to their own tickets" clause is unreachable by any real customer. **BUG-HLP-060 (High, the session's biggest finding)**: per the standing pre-check, Redis + Sidekiq were confirmed down and freshly restarted — Sidekiq's own boot log showed its internal `Scheduled::Poller` thread (the mechanism that fires ALL 3 documented cron jobs: `email_checker`/`sla_monitor`/`auto_close_tickets`) crash immediately with a real `ArgumentError` (a `sidekiq 7.3.9`/`connection_pool 3.0.2` gem incompatibility, confirmed via `bundle list` + full stack trace), silently and permanently disabling all scheduled background automation while the main Sidekiq process itself stays alive and shows no visible symptom. Live-confirmed the real-world consequence on a manufactured 1-minute-SLA ticket: the breach was never auto-detected despite waiting past both the deadline and a full monitor cycle. This is a **regression** — `HELPDESK_MEMORY.md` has this exact cron mechanism on record working correctly earlier in this engagement. `/helpdesk/swagger`'s "Try it out" confirmed to genuinely execute real requests (not just docs); confirmed refused with a real `403` for a non-admin `manage_helpdesk` user, matching the Email Config admin-only rule. SLA Analytics/report figures cross-verified against direct API calls; REST API list counts matched UI counts exactly under an identical filter (83=83). Open bug count: 36 → **40**. **New operational gotcha discovered**: `docker exec <container> sh -c "cmd &"` from a separate exec call does not reliably keep the process alive — start/wait/verify all inside one `docker exec` invocation instead.

  **Next session: pick the next untested suite** — only `HELPDESK_PLUGIN_INSTALLATION.md` (0/12) remains with zero live execution. All other 11 suites are now fully closed out.

- **⚠ 2026-09-11 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_RAKE_TASKS.md` closed out **13/13** (was 1/13, only TC-217/BUG-HLP-044 done before this session) — **4 new bugs found**: **BUG-HLP-053** (`seed_demo_data`'s legacy-project-identifier match silently renames/repurposes any unrelated project via `update_columns`, no sanity check it's actually old demo data — source-verified only, not reproduced live since the primary-identifier project already exists on this instance); **BUG-HLP-054** (`seed_demo_data` crashes on large `TICKETS` values once Sakura Mobility KK's own hard-enforced prepaid budget is exhausted — confirmed **permanent**: re-ran the identical `TICKETS=300` command and it crashed at the exact same point again, 233/300 tickets both times — High severity); **BUG-HLP-055** (`check_emails` is 100% dead code, identical shape to BUG-HLP-044 — reads 7 legacy `ProjectCustomField` keys that don't exist; confirmed live even Alpha/Beta, which have real working mail config, are reported "disabled"); **BUG-HLP-056** (`check_sla` correctly detects/marks breaches but every escalation attempt fails — passes a human-readable reason string that fails the model's inclusion validation, while the real scheduled worker passes the correct value). Also: TC-HLP-255 (`seed_reports`) confirmed via `rake -T` to **not exist at all** — documentation gap, not a bug; TC-HLP-251's own premise corrected (the real project is named "Redmineflux Helpdesk", not "Helpdesk Support" — `helpdesk-support` is only a legacy fallback identifier). **Standing methodology reinforced by direct user correction this session** ("are you using backend for testing why you are not using mcp palywright"): actually *invoking* a rake task has no UI equivalent and is legitimately done via `docker exec`/`bundle exec rake`, but *verifying results/state* must always go through the real UI via Playwright — never a `rails runner` query, even for a simple "does this project exist" check. Open bug count: 32 → 36 (BUG-HLP-053/054/055/056). **Next session: pick the next untested suite** — `HELPDESK_REPORTING_AUTOMATION.md` (0/23) and `HELPDESK_PLUGIN_INSTALLATION.md` (0/12) are the two remaining suites with zero live execution; confirm which with the user before starting.

- **⚠ 2026-09-11 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User asked directly: "have you tested the editor and its options in Knowledgebase pages?" Honest answer at the time: no — TC-HLP-018's PASS only ever confirmed the 12 block-type toolbar buttons exist and that plain paragraph text saves, never that formatted content actually works, despite TC-162's own Steps explicitly calling for "bold, list, link." Built a dedicated fixture article (page id 7, "TC-HLP editor block types test") and genuinely tested everything: all 5 inline formats (Bold/Italic/Underline/Strikethrough/Link, via the floating inline toolbar) and all 12 block types (Text/Heading/List/Code/Quote/Delimiter/Table/Image/Attachment/Raw HTML/Warning/Checklist, via the "+" block popover). **Every single one confirmed correct via direct DOM inspection after a real Publish + full page reload** — real `<b>/<i>/<u>/<s>/<a href>` tags in the paragraph, a real image upload with a working caption, both table cells persisting independently, checklist checked-state surviving the reload. **Zero new bugs** — this was a coverage-completeness pass on already-shipped functionality, not a new feature area. Two methodology notes worth keeping: (1) both the inline toolbar and the block-type popover leave a stale, non-functional second instance in the DOM after their first open — always re-query for the one with a nonzero `getBoundingClientRect()`; (2) a genuine `beforeunload` confirm dialog appears on any navigation away from an edited article, even right after clicking Publish — harmless, but don't mistake it for a hang. `HELPDESK_CONTENT_TEMPLATES.md` TC-HLP-018 updated with the full follow-up. Fixture article (page 7) and test image (`automation/uploads/tc-editor-blocks-test-image.png`) left in place. Open bug count unchanged at 32.

- **⚠ 2026-09-11 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User followed up directly on BUG-HLP-051: "did you notice the custom field's label is visible to the customer but their selected value isn't?" Tested it deliberately (fresh ticket #91, `alpha.customer`, real values on a list-type custom field "Issue Category" = "Report a Bug" and a required string-type field "test" = "custom field value test") — **confirmed, and it's a distinct bug from BUG-HLP-051**, not the same mechanism. Filed as **BUG-HLP-052**: the customer's own ticket view renders the custom field's label but leaves `.value` completely empty (`<div class="value"></div>`), while Admin viewing the identical ticket #91 sees the real saved value (`Report a Bug` / `custom field value test`) — confirmed via direct DOM comparison across both sessions. Unlike BUG-HLP-051 (whole block `display:none`), here the block is fully visible and only the value is missing — worse in user-facing impact since it looks exactly like the customer's own submitted answer was silently lost, not an intentionally-hidden field. Every other attribute on the same template (Status/Priority/Product/Organization/Prepaid Support Hours) renders correctly for the customer — only genuine Redmine custom fields are affected, confirmed across both a list-type and a string-type field. Open bug count: 31 → **32**.

- **⚠ 2026-09-11 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User asked directly: does a Customer select Product when raising a ticket, and can they edit it afterward? Tested live as `alpha.customer`: **selection at creation works correctly** (real Product dropdown on the Customer's New issue form, choice saves server-side — ticket #88, "Falcon Suite"); **post-creation edit correctly absent** (Customer role has no field-editing surface at all — only inline Subject rename + Reply Note — matching the plugin's documented reduced-customer-model, not a defect). But a genuine, previously-untested gap surfaced: **the saved Product value is then permanently invisible to that same customer** — `.product.attribute` is `display:none` specifically for the Customer role (confirmed via `getComputedStyle`, not an inherited collapse), even though `HELPDESK_USER_GUIDE.md` §7 documents "the product" as one of the things shown when a ticket is opened, in the same sentence as Organization — which IS shown to the customer. Filed as **BUG-HLP-051**. `HELPDESK_CONTENT_TEMPLATES.md` TC-HLP-014 updated with a follow-up note (its existing PASS only ever covered Admin/Agent). Open bug count: 30 → **31**.

- **⚠ 2026-09-11 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User asked which suites are genuinely fully tested — checked all 13 suite files directly (each TC header vs. real evidence, not prior handoff claims): **8 confirmed complete**, **4 barely started** (`HELPDESK_CONTENT_TEMPLATES.md` 8/36, `HELPDESK_RAKE_TASKS.md` 1/13, `HELPDESK_REPORTING_AUTOMATION.md` 0/23, `HELPDESK_PLUGIN_INSTALLATION.md` 0/12), and **`HELPDESK_EMAIL.md`'s TC-HLP-078 found to have only source-review evidence, not live** — closed it for real (see Run History), confirming `HELPDESK_EMAIL.md` is now genuinely 23/23. User then picked **`HELPDESK_CONTENT_TEMPLATES.md`**, now closed out **36/36, 3 new bugs**. Products (12 TCs) and the rest of Canned Responses (3 TCs) all PASS, no findings beyond confirming Product's Active checkbox has a real Rails hidden-fallback input (persists correctly, unlike BUG-HLP-003/BUG-HLP-035's checkbox bug on Canned Response/Support Package). Knowledgebase (10 TCs) surfaced **BUG-HLP-048** (creating an article while a parent is open never nests it — root-caused to the create-page request never sending a `parent_id`, only a cosmetic path string), **BUG-HLP-049** (Page History can only view an old version read-only — no compare/restore action exists anywhere in the KB JS bundle), and **BUG-HLP-050** (an uploaded attachment can never be removed — a real `DELETE rf_kb_delete_attachment` endpoint exists and is wired to Editor.js's `block-removed` event, but nothing in the UI ever fires it). Version retention, title+body search, the share-link auth gate plus its project-access enforcement, PDF export, and both customer-role hard restrictions (no CRUD; drafts invisible) all independently PASS. TC-173/174/175 (individual KB permission gates) closed via cross-reference to `HELPDESK_PERMISSIONS.md` TC-HLP-173, which already proved the identical scenario live. Open bug count: 27 → **30**. `HELPDESK_MEMORY.md` addendum added with full detail. Remaining untouched suites for a future session: `HELPDESK_RAKE_TASKS.md`, `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PLUGIN_INSTALLATION.md` — ask the user which to pick next, don't assume.

- **⚠ 2026-09-09 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_PREPAID_HOURS.md` gained a new "round 5" section — **TC-HLP-240–413**, a direct budget-configuration × ticket-creation-channel coverage sweep requested by the user, who explicitly warned not to assume Portal and Email behave identically without checking documentation/source first. Verified against `HELPDESK_USER_GUIDE.md` §12 and `app/models/helpdesk/prepaid_enforcement.rb`'s shared `block_reason` method before writing any TC: the **blocking decision** is provably identical across both channels (same source method), but the **notification** is deliberately different — Portal shows an inline form error, Email additionally fires a dedicated bounce-back (`HelpdeskMailer.prepaid_hours_exhausted_notification`), since MailHandler would otherwise silently swallow the failure. **All 6 new TCs PASS**: TC-408/409 (no budget assigned at all — a state distinct from "mode = No Limit" — never blocked on either channel, tickets #67/#68), TC-410/411 (No Limit/Soft, Email, negative balance, tickets #71/#72), TC-412 (Hard, Email, Remaining > 0, ticket #69, no block), TC-413 (Hard, Email, Remaining ≤ 0, genuinely blocked with a real bounce-back email confirmed in the customer's inbox). **A genuine methodology finding surfaced along the way, not a bug**: TC-413's first attempt at driving the budget to a UI-displayed "0.00h Remaining" still let the ticket through, because the true stored value carried a tiny positive floating-point residue (`0.003333...h`) from historical repeating-decimal time entries — confirmed via a read-only `rails runner` diagnostic, corrected by pushing the budget firmly negative, then retested and confirmed genuinely blocked. **Lesson for future sessions: the UI's 2-decimal "Remaining: 0.00h" can mask a residual positive balance — verify the underlying value directly when a test needs exactly-zero/negative, don't trust the rounded display.** A coverage matrix (4 configs × 2 channels) was added directly to the suite file. All temporary fixture changes were restored immediately after: Alpha's mode back to Hard, budget back to its exact original 21.67h Approved/7.00h Remaining, and `alpha.customer`'s Organization back to None on her Alpha project-access row — all confirmed via direct re-check. No new bugs; open bug count unchanged at 27. `HELPDESK_MEMORY.md` addendum added with full detail.

- **⚠ 2026-09-09 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_EMAIL.md` is now **fully closed out, 23/23 TCs executed, 2 new bugs.** User directly challenged whether the suite had actually been tested completely (real count was 11/23 at that point) — executed all 12 remaining TCs live rather than leaving any flagged: TC-143 (admin save, PASS), TC-145/149 (no-SMTP fallback / blank-incoming no-poll, both PASS — required building a brand-new third project, **Helpdesk QA Gamma**, as a genuinely-blank fixture), TC-144/146 (per-project sender, PASS — Alpha's `alpha.support@test.local` vs Beta's `beta.support@test.local`, both confirmed via real received emails), TC-147 (all 4 outgoing triggers fire real mail, PASS), TC-148 (Email History full detail, **FAIL — filed as BUG-HLP-046**: the tab never shows recipient or a per-entry subject field, direct contradiction of `HELPDESK_USER_GUIDE.md` §13's explicit promise), TC-152 (non-admin blocked, PASS via cross-reference to this session's fresh TC-212 re-test), TC-154/155 (auto-close timing edges, both PASS — TC-155 determined real behavior: the clock resets on ANY `updated_on` bump, not specifically a reply), TC-156 (independent outgoing/incoming config — turned out **unachievable via the real UI at all**, not a bug: the form's HTML5 `required` attributes make it all-or-nothing, TC rewritten to record this), TC-290 (same mailbox on two projects, **FAIL — filed as BUG-HLP-047**: Save succeeds with zero validation, and whichever project happens to poll first silently wins every email sent to the shared mailbox — no duplicate, but a real, deterministic, silent misrouting risk with no warning to the admin). Open bug count: 25 → **27**. New reusable fixtures: **Helpdesk QA Gamma** (project id 4, Helpdesk module + Support tracker, no email config — a clean "blank" fixture for any future no-config test) and mailbox `gamma.support@test.local` on the local mail server. `HELPDESK_MEMORY.md` addendum added with full detail.

- **⚠ 2026-09-09 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User asked to properly verify the BLOCKED `TC-HLP-370` (email-to-project routing for a multi-project customer) rather than leave it flagged-but-unexecuted — **built the missing infrastructure and confirmed a real bug: BUG-HLP-045.** Wired up Helpdesk QA Beta's Email Configuration for the first time (SMTP+IMAP via `beta.support@test.local` — the mailbox existed on the mail server but was never connected to Redmine). Recreated `beta.customer` (id 32) as a genuine multi-project customer: Alpha as her first project-access row, Beta as her second (first tried extending `delta.customer` instead, but she has no real mailbox account — pivoted to `beta.customer`, whose mailbox does exist). Sent a real email as her, addressed specifically to `beta.support@test.local` — the server log shows the poller correctly identifying Beta's mailbox, immediately followed by `MailHandler` silently substituting Alpha (her first row) instead. First attempt's ticket creation was separately blocked by Alpha's own prepaid-hours budget happening to be negative (unrelated, leftover from earlier same-day testing); topped up Alpha's budget via the real UI and resent to rule that out — the second attempt created ticket **#62 cleanly under Helpdesk QA Alpha**, confirmed via page title, Organization field, and outbound SMTP account, despite never being sent to Alpha's mailbox at all. Root-caused via source (`mail_handler_patch.rb#target_project_with_helpdesk`, an unconditional `.first` on the customer's project-access rows with no scoping to the actually-polled project). TC-HLP-370 updated BLOCKED → CONFIRMED LIVE FAIL. **Mid-task correction from the user**: a read-only `rails runner` lookup of Beta's available SLA/Support Level data was stopped in favor of checking it through the real UI instead — reinforced the standing "use Playwright, not backend" memory to cover even harmless read-only lookups, not just risky actions. Open bug count: 24 → **25**. `HELPDESK_MEMORY.md`/`HELPDESK_USERS_AND_CUSTOMERS.md` updated with full detail and the new reusable fixtures (Beta email config, `beta.customer`). **Same-day control test**: confirmed the bug is specific to multi-project entitlement — temporarily made `beta.customer` Beta-only (removed her Alpha row) and resent to `beta.support@test.local`; it correctly routed to Beta this time (ticket #63), proving single-project customers route fine and the bug requires 2+ project-access rows. Restored her Alpha row afterward — her row order is now reversed from the original setup (Beta first/lower-ID, Alpha second), documented explicitly in `HELPDESK_USERS_AND_CUSTOMERS.md` so a future session doesn't assume the wrong order.

- **⚠ 2026-09-09 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User asked to confirm coverage of 5 specific scenarios (email-config permission gate, email-to-project routing, auto-close tracker scope, auto-close project scope, Identifier Keywords × Email Subject Prefix combinations) and add any missing TCs. **5 new TCs executed live (all PASS), 1 new TC written but left BLOCKED, 0 new bugs.** Scenario 1 (permission gate) was already fully covered — `HELPDESK_PERMISSIONS.md` TC-HLP-174/214 confirm Email Configuration access is hardcoded admin-only, not `manage_helpdesk`-gated, settling the user's own stated uncertainty. Scenario 2 (email-to-project routing): source review of `mail_handler_patch.rb` found a customer entitled to two projects gets routed to her **first** project-access row, not the mailbox actually polled — a real misrouting risk, written as **TC-HLP-370** in `HELPDESK_TICKET_LIFECYCLE.md` but left BLOCKED since it needs a working Beta mailbox (Beta currently has zero email config) and a local multi-project customer fixture (none exists — Forge's is a different environment). Scenarios 3/4 (auto-close tracker/project scope) confirmed live as **TC-HLP-082/404** in `HELPDESK_EMAIL.md`: pre-existing Bug-tracker tickets were left untouched while a fresh Support-tracker ticket closed correctly (tracker scope), and a fresh Beta ticket was left untouched with no "Processing project: Helpdesk QA Beta" line in the worker's own log at all (project scope) — both confirmed using a dry-run eligibility count first, per the lesson from the round-3 incident, keeping this run to exactly 3 candidate tickets total. Scenario 5 (all 4 Identifier-Keywords/Email-Subject-Prefix combinations) confirmed live as **TC-HLP-084/406/407** via real emails sent through the local Docker mail server: both blank (ticket created verbatim, no prefix), prefix-only (ticket created with prefix despite zero keyword gating — confirms prefix is independent of gating), and both filled (first-ever direct confirmation in this suite that the prefix genuinely rewrites a real created ticket's subject, since every prior keyword TC ran with the prefix field blank). Alpha's Email Configuration was restored to its exact original state after every config change, re-verified via direct query. `HELPDESK_MEMORY.md` addendum added with full detail. Open bug count unchanged at 24.

- **⚠ 2026-09-09 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_EMAIL.md` auto-close (TC-HLP-077/151/153) + `HELPDESK_RAKE_TASKS.md` TC-HLP-250: user asked for the auto-close condition to be written out and the relevant TCs summarized, then to verify TC-153 live. **2 new bugs, plus a real collateral-damage incident that was fully disclosed and recovered before proceeding.** Set Auto Close Ticket Days = 1 on Helpdesk QA Alpha; created two dedicated test tickets (one Resolved, one left New), backdated both past the threshold. First tried the rake task (`auto_close_tickets`) — it closed nothing at all. Root-caused: **BUG-HLP-044** — the rake task reads a legacy `ProjectCustomField` (`helpdesk_auto_close_days`) that no longer exists on this instance (zero `ProjectCustomField` rows exist at all); the real config lives in `RfHelpdeskEmailConfig`, read correctly only by the actual scheduled worker. Same dead-code pattern already noted but never filed for the sibling `check_emails` rake task. Falling back to invoking the real worker class directly (`Helpdesk::AutoCloseTicketsWorker.new.perform`, since the sidekiq-cron scheduler poller is still broken) correctly closed the Resolved test ticket (TC-150 PASS) but also closed the New-status one (TC-153 FAIL) — **BUG-HLP-043**: the worker's query uses Redmine's `.open` scope (any non-closed status, 5 statuses total), not a Resolved-only filter, contradicting the documented contract and the Email Configuration form's own field hint. **That same worker invocation closed 46 tickets in Helpdesk QA Alpha, not just the 2 test tickets** — every pre-existing fixture ticket from earlier sessions that happened to already be silent past 1 day was swept up too. User explicitly asked to confirm which of the 44 collaterally-closed tickets were genuinely still-relied-upon shared fixtures before any revert. Checked reference counts per ticket: **ticket #46** (central Prepaid Hours fixture, 20 references in `HELPDESK_PREPAID_HOURS.md` alone, and confirmed live to be functionally restricted — Checklist showed "Issue is closed, you cannot perform this action") was reverted to its exact prior status using the journal's own recorded value, with the fabricated auto-close journal entry removed. The other 44 tickets (each a single-use, already-fully-consumed piece of evidence for an already-completed TC/bug, referenced at most once elsewhere) were deliberately left Closed. Auto Close Ticket Days reset to blank on Helpdesk QA Alpha afterward; confirmed no other project has any auto-close config set. Open bug count: 22 → **24**. `HELPDESK_MEMORY.md` addendum added with full detail, including a process lesson about dry-running a count before triggering any age-threshold background job on a shared environment.

- **⚠ 2026-09-07 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_FIELD_VALIDATIONS.md` new Section H: user asked directly whether a scenario they'd observed — a required **User** custom field blocking Customer creation — had already been reported. Grepped the whole QA folder first: genuinely never tested. Wrote **TC-HLP-145** first, then verified live: created a required Text custom field on the Users type; confirmed as a control it works correctly on Redmine's own core `/users/new`; then on the Helpdesk plugin's own New Customer form (`/rf_customers/new`) confirmed via full DOM snapshot **no custom-fields section exists at all** on this form. A fully valid submission was refused with "... cannot be blank" naming the field. **Corrected the user's own framing in the process**: their premise was the customer gets created anyway despite the error — live re-verification (Customers list unchanged, Administration → Users search "No data", and a direct `rails runner` query all confirmed zero records created) showed the opposite: the create is fully, atomically blocked (confirmed via source — a standard `if @customer.save … else render :new`, no partial-commit path). The real, reportable defect is that **Customer creation becomes totally, permanently impossible** the moment any User custom field is required, since the form has no way to satisfy it — not a data-integrity gap. **Filed as BUG-HLP-042 (Medium).** Cleaned up the test custom field afterward (deleted) so the environment is back to normal for future Customer-creation testing. `HELPDESK_FIELD_VALIDATIONS.md` reopens from its prior 55/55-closed state to 56 TCs. Open bug count: 22 → **23**. `HELPDESK_MEMORY.md` addendum added with full detail.

- **⚠ 2026-09-07 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_PREPAID_HOURS.md` round 4: user asked a direct, sharp follow-up on round 3's finding — "did you also check the core spent time form / user add time log when hard stop work is selected" — pointing at a **third**, still-untested code path: Redmine's own core **"Log time"** link (`/issues/:id/time_entries/new`, creates a brand-new entry, distinct from both the reply-box widget and round 3's edit-existing-entry form). Wrote **TC-HLP-239** first, then executed live: **FAIL, folded into BUG-HLP-041 (broadened, not a new bug ID) as "Path B."** Setup: topped up Helpdesk QA Alpha's org budget to Remaining exactly 2.00h under Hard mode (confirmed via DOM read). As `luna.blossom`, clicked the ticket's own **"Log time"** link (confirmed structurally distinct from "Reply" and from an existing entry's "Edit") — landed on Redmine's plain native new-time-entry form, no prepaid-hours widget or run-out-mode awareness anywhere in it. Entered 5:00h (2.5× the 2.00h available), Create. Result: **"Successful creation."**, fully uncapped — ticket's own Prepaid Support Hours summary went from "9.67h used · 2.00h left of 11.67h" to "**14.67h used · -3.00h left of 11.67h**". This confirms the identical root cause (zero enforcement) on a *third* distinct Redmine core `TimeEntry` action, alongside round 3's edit-form finding (Path A) — enforcement exists exclusively on the reply-box's own bespoke widget; BUG-HLP-041 was broadened (title, steps, evidence, duplicate-check) to cover both paths under one bug rather than filing a new ID for the same architectural gap. Suite completeness re-verified: 52 headers, 51 markers (same pre-existing TC-131 wording-only gap, unchanged). Open bug count unchanged at **22** (BUG-041 broadened, not newly created). `HELPDESK_MEMORY.md` addendum added with full detail.

- **⚠ 2026-09-07 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_PREPAID_HOURS.md` round 3: user pointed out a genuinely distinct gap — every prior time-logging TC only tests *creating* a new entry via the reply box, none test **editing or deleting an already-logged entry** via Redmine's own core Spent-time tab, a structurally different code path. 4 new TCs (TC-HLP-235–399), written first then executed live, **1 new bug**: **TC-396 — FAIL, filed as BUG-HLP-041 (Medium)**: under Hard mode with Remaining exactly 1.00h, editing an existing time entry via `/time_entries/:id/edit` to increase its Hours by 2.92h succeeded completely uncapped ("Successful update", no error), landing Remaining at -1.92h. This is a **more complete gap than BUG-HLP-038** (open) — BUG-038 is the reply-box's create path missing only the crossing-boundary check (an enforcement hook exists, just incomplete); this is the edit path having **no enforcement hook at all**. TC-397 (control) confirmed Soft mode's "allow overage" contract correctly holds on this same edit path — only Hard's enforcement is absent, the bookkeeping itself is fine. TC-398 confirmed reducing an entry's Hours symmetrically decreases Used/increases Remaining by the exact amount. TC-399 confirmed deleting an entry fully reverses its budget impact, no orphaned charge. Suite completeness re-verified: 51 headers, 50 markers (same pre-existing TC-131 gap). Open bug count: 21 → **22**. `HELPDESK_MEMORY.md` addendum added with full detail.

- **⚠ 2026-09-07 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_PREPAID_HOURS.md` round 2: user reviewed the suite's 39 TC titles directly and identified **8 genuine coverage gaps**, written first then executed live — **all 8 PASS, 0 new bugs**: TC-HLP-227 (budget landing at exactly 0.00h, fresh dedicated test on Beta), **TC-HLP-228** (the exact positive→exactly-zero boundary — Remaining=2.00h, log exactly 2h — the gap between BUG-HLP-038's overshoot case and TC-380's already-at-zero case, flagged by the user as the single most important missing test), TC-HLP-229 (the very next 1-min entry immediately after landing at zero correctly blocks, no off-by-one gap), TC-HLP-230 (editing a Support Package with real budget-history references confirms the reference is a **live foreign-key lookup, not a frozen name snapshot** — renaming immediately changed how an old history row displays it; delete guard survived), TC-HLP-231 (deleting an unrelated unused package leaves other packages' history untouched), TC-HLP-232 (a **mixed** top-up+reduction from two tabs, +3 then -1, confirms totals are read live from the server on each write, not stale-client-cached — TC-387 only ever tested same-sign additive ops), TC-HLP-233 (switching run-out mode while already exhausted — Hard→No Limit→Hard — confirms enforcement is fully live/dynamic per-request, not cached from whichever mode was active when the budget first hit zero), TC-HLP-234 (run-out mode persists across both a genuine page reload and a fresh logout/login session, confirmed via direct DOM re-check both times). Suite completeness re-verified: 47 TC headers, 46 markers (only the pre-existing TC-131 wording-only false negative, unchanged). Open bug count unchanged at 21. `HELPDESK_MEMORY.md` addendum added with full detail.

- **⚠ 2026-09-07 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_PERMISSIONS.md` is now **fully closed out, 11/11 TCs executed (TC-HLP-166–214)**, chosen after computing real per-suite coverage counts via grep and presenting the options to the user. New fixtures: role "Permission Test Role" (id 9, reconfigured 7 times to isolate individual permission combinations) and user `perm.test.agent`. **1 new bug, net**: **BUG-HLP-039** (Medium — Canned Responses hardcode `require_admin`, ignoring `manage_helpdesk` entirely, unlike every sibling configuration entity; contradicts `HELPDESK_REQUIREMENTS.md`'s own matrix; found via TC-205). **BUG-HLP-040 was filed then retracted the same day, Not a Bug**: originally found `manage_prepaid_support_hours` without `manage_helpdesk` has no reachable UI path to budget management, but when asked to cite the exact requirement backing that as a defect, no such requirement existed in `HELPDESK_USER_GUIDE.md` — the bug's premise was an unverified inference from the permissions table's structure. The user then clarified the actual intended model: `manage_helpdesk` grants access to the Organization area, `manage_prepaid_support_hours` is required *in addition* to modify prepaid hours — the originally-observed 403 is exactly this by-design behavior. Retested both directions live to confirm: positive case (both permissions) — top-up and run-out-mode change both succeed and persist after a genuine page reload; negative case (`manage_helpdesk` alone) — access/view works, modification correctly blocked. TC-HLP-168 rewritten to test the positive case; TC-HLP-172 reworded to make the access-vs-modify distinction explicit. `HELPDESK_REQUIREMENTS.md` and `HELPDESK_USER_GUIDE.md` §20/§26 updated to state the two-permission dependency directly. TC-208 independently reconfirmed **BUG-HLP-029 still reproduces** (not re-filed, just re-verified live). **TC-213 resolved this suite's own flagged documentation contradiction**: tested via real, content-verified CSV downloads under two role configs — **`manage_helpdesk` alone is sufficient** for report export; `export_helpdesk_reports` is not independently required. TC-207/211 confirmed the KB permission set is fully independent (one cosmetic-only DOM inconsistency noted, not a bug — see `HELPDESK_MEMORY.md`). Open bug count: 20 → 22 → **21 net** (BUG-039 added, BUG-040 added then closed same day). `HELPDESK_MEMORY.md` addendum + correction addendum added with full detail. **Next suite still not chosen by the user** — remaining candidates: `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority. Redis/Sidekiq were not needed for this suite (no async/SLA-timing TCs); still down from the prior session's restart per the standing quirk — check before any suite touching SLA/escalation timing.

- **⚠ 2026-09-07 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_PREPAID_HOURS.md` is now **fully closed out, 39/39 TCs executed** — 14 new user-identified gap TCs (TC-HLP-213–387) written then executed. Session resumed after a real multi-day gap (the `redmine-docker-6` container had exited from a Docker/host restart) — restarted both app and DB containers, confirmed all prior test data survived. **Most important finding: retesting BUG-HLP-037 as a "standing regression check" (TC-380/381) produced the opposite result three times in a row** — Hard mode correctly blocked both the new-ticket and time-log paths on every retest, directly contradicting the prior session's "Hard mode enforces nothing" claim. Investigated for a root cause (checked Redis/Sidekiq uptime for a stale-cache theory — none found; tested single-operation vs. multi-operation paths to exact zero — both blocked correctly on retest) but could not explain the original discrepancy. **Corrected BUG-HLP-037 in place** — its file now carries a full "Retest — Correction" section; kept open but reclassified **Unconfirmed** rather than left as a stale High-severity claim or unilaterally closed. That investigation surfaced a new, cleanly reproducible defect instead: **BUG-HLP-038 (Medium)** — Hard mode only gates on whether Remaining is already ≤0 *before* an action, not whether the action's own size would cross zero; a single 2h time entry against only 1.00h Remaining was accepted in full, landing at -1.00h uncapped. Soft mode's identical crossing case (TC-383) correctly allowed the same overrun — that's its genuine expected behavior, not a bug — a useful contrast. Also confirmed: Support Package edit/duplicate-naming/audit-trail behaviors all PASS (TC-374/376–379); TC-375 still BLOCKED by BUG-HLP-035; TC-384 filled the last mode/state gap (No Limit with positive Remaining, PASS); TC-385/386 confirmed `luna.blossom` is blocked from budget/Support-Package management via both hidden UI and direct URL (genuine 403s); TC-387 confirmed two near-simultaneous budget top-ups from separate tabs both apply additively, neither lost. Open bug count: 19 → **20**. `HELPDESK_MEMORY.md` addendum added with full detail. **Next suite still not chosen by the user** — remaining candidates: `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority. Redis/Sidekiq are down again post-restart (standing quirk, not needed for Prepaid Hours) — will need restarting if the next suite touches SLA/escalation timing.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_PREPAID_HOURS.md` executed end-to-end: 18/18 original TCs (TC-125–142) plus 7 new Support Packages TCs (TC-367–373, a genuinely untested entity discovered mid-session), 3 new bugs. **BUG-HLP-035** (Medium — Support Package Active checkbox has no hidden fallback input, can never be saved Inactive via Create or Edit, same symptom shape as the already-fixed BUG-HLP-003 recurring on a newer entity). **BUG-HLP-036** (Low — duplicate Support Package name correctly refused but shows zero error message anywhere). **BUG-HLP-037 (High, most significant)** — deliberately exhausted the budget to exactly Remaining = 0.00h under Hard mode and tested both of its documented effects (`HELPDESK_USER_GUIDE.md` line 827/1463): a customer's new ticket (#48) was created with "Successful creation.", zero block; an agent's 10-min Reply time-log also saved normally, balance going to "0.58h used · -0.16h left" — Hard mode's "stop work" enforcement has no effect at all on either code path, behaviorally identical to No Limit/Soft. No Limit (TC-133) and Soft (TC-135) then correctly allowed both actions with the balance continuing negative — their genuine expected behavior — though Soft showed no distinct "signal the run-out" behavior different from No Limit in this build (worth a follow-up, not filed as a bug). TC-142 confirmed budgets track fully independently per project (added a fresh 10h budget for the same org on Helpdesk QA Beta — completely separate figures from Alpha's negative state). Open bug count: 16 → **19**. **Also reinforced this session**: violated the standing no-direct-URL-navigation rule a third time (typed a known ticket ID mid-TC); memory strengthened with a blunter litmus test. `HELPDESK_MEMORY.md` addendum added with full detail. **Next suite still not chosen by the user** — remaining candidates: `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority. Redis/Sidekiq were up throughout this session (not needed for Prepaid Hours testing) but will need restarting again next session per the standing environment quirk if the next suite touches SLA/escalation timing; the sidekiq-cron scheduler poller bug remains unresolved.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User reviewed **BUG-HLP-023** (Holiday delete silently clears an SLA's Holiday field, no dependency warning) and closed it directly: Holiday is not a required SLA field, the delete degrades gracefully (clears to "-", no broken reference, no invalid state), so a silently-cleared optional field with zero functional consequence doesn't warrant being treated as a defect. Moved to `bugs/closed/BUG-HLP-023.md`, `bugs/_index.md` updated, TC-HLP-364's verdict corrected FAIL → PASS. **Contrast kept explicit**: BUG-HLP-022 (same missing-warning shape, but for Support Level — a *required* field whose silent deletion produces a genuinely invalid customer project-access row) stays open; the distinguishing factor is required-vs-optional plus graceful-vs-broken outcome. Open bug count: 17 → **16**. **Next suite still not chosen by the user** — same remaining candidates: `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User asked whether escalation into a Support Level with **2+ eligible agents** was tested for the **L2→L3** pairing specifically, and asked to actually *determine* the selection rule (random/round-robin/workload/priority-order), not just accept "any pick is fine." TC-HLP-323 already proved one-agent-selected-notification-follows-that-agent, but only on an L1→L2 hop, and explicitly left the rule itself undetermined. Wrote **TC-HLP-333** first, then closed it two ways:
  - **Source-level ground truth**: `rf_issue_sla_status.rb#escalate_to_next_level!` line 222 does a plain `.first` on `RfSupportLevel#support_assignees` (`User.where(id: support_assignee_array)`, **no `.order()`**) — no randomness, no round-robin, no workload lookup anywhere. In practice this returns ascending user-ID order, so the real rule is: **always the same agent, whichever configured assignee has the lowest user ID**.
  - **Live confirmation**: L3 was single-agent (Willow Belle only) — no other agent was free to pair with her (one-level-per-project rule), so created a new fixture `aurora.wren` (User ID 30, higher than Willow Belle's 11) and added her to L3. Ran two separate customer-raised tickets (#44, #45) through an identical L2→L3 breach/escalate cycle — **both landed on Willow Belle, never Aurora Wren**, exactly matching the source prediction. Not random, not round-robin.
  - **Filed as BUG-HLP-034 (Medium)** — corrected from an initial too-lenient "not a bug, just a fairness consideration" framing after the user directly pushed back that this sounds like a real gap. `HELPDESK_USER_GUIDE.md` §10's own canonical worked example shows **every** level of a chain with **two** named agents — multi-agent levels are the documented normal case, not an edge case — so a level with 2+ agents that never actually distributes escalated work across them defeats the visible purpose of naming more than one assignee. Since the pick is a stable lowest-ID selection with zero randomness/round-robin/workload logic, a multi-agent level will always route every escalation to the same one agent in practice, never involving the others unless that agent is later locked/removed.
  - Open bug count: 16 → **17**. New reusable fixture: L3 is now genuinely 2-agent (Willow Belle + Aurora Wren) — see `HELPDESK_USERS_AND_CUSTOMERS.md`'s 2026-09-03 fixture block. **Next suite still not chosen by the user** — same remaining candidates: `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority. Redis/Sidekiq still up from earlier this session but will need restarting again next session (standing quirk); the sidekiq-cron scheduler poller bug is still unresolved.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User asked one more, sharper follow-up: is there a TC covering **every** level of the escalation chain firing (not just one hop), followed by Admin actively resolving the ticket, with the final SLA state checked? Confirmed genuinely untested — TC-299 (full chain) never resolves afterward, TC-301 (admin-fallback) only covers the passive automatic reassignment (nobody ever actively closes it), TC-363 only covers one hop. Wrote **TC-HLP-332** first, then executed live, PASS on the mechanics:
  - Ticket #42 (`retest.customer1`, L2) assigned via Edit-form only (no reply) so its clock genuinely ran. Manually triggered the SLA monitor worker twice with a real wait between each — first breach escalated L2→L3 (Escalation #1), second breach (L3's own recalculated deadline also left untouched) correctly hit `PATH B` admin-fallback (per TC-094/301) — reassigned to Admin, real Critical Breach Alert delivered. Confirmed the genuine full climb via the pre-resolution SLA panel: "↑ Escalated 2×", 3 stages.
  - Admin then **actively** resolved and closed the ticket (distinct from TC-301's passive reassignment). Full history survived perfectly — still 3 stages / 2 escalations / all 6 Activity Log events intact, nothing overwritten by the resolution.
  - **Sharpest BUG-HLP-033 reproduction yet**: the detail panel's overall badge still read "✗ SLA Missed" / "Needs Immediate Attention" for this fully, deliberately Closed ticket, while the list correctly showed "✓ Resolved" at the same moment — because once `response_breached` has ever been set, the panel's priority order checks that flag before ever reaching its `done` branch, so the badge stays permanently "stuck" even after a real resolution surviving a full 2-escalation chain. Not a new bug — third independent reproduction folded into BUG-HLP-033's existing evidence.
  - No new bugs. Open bug count unchanged at 16. `HELPDESK_MEMORY.md` addendum added. **Next suite still not chosen by the user** — same remaining candidates: `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority. Redis/Sidekiq are still up from this session (started ~08:20 AM) but will need restarting again next session per the standing environment quirk; the sidekiq-cron scheduler poller bug (real 2-minute auto-cadence not running, manual triggering used instead) is still unresolved and worth a dedicated fix pass.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User asked a different, sharper question about `HELPDESK_SLA_ESCALATION.md`: did any TC cover (1) a ticket resolved/closed cleanly with zero SLA breach ever occurring, (2) a ticket that breaches once, escalates, and is resolved at the new level with no further breach, and (3) Admin specifically performing the close in either case. Confirmed genuinely untested — none existed anywhere. **User then corrected a wrong assumption before any execution began**: a ticket created directly by Admin/Agent never attaches an SLA/Support Level at all — only a real Customer-raised ticket does, via that customer's project-access row (saved as a new persistent memory, `feedback_sla_requires_customer_raised_ticket`). User also directed the process explicitly: write the test cases first, then execute (saved as `feedback_write_testcases_before_executing`). Wrote **TC-HLP-329/363/364** into `HELPDESK_SLA_ESCALATION.md` first, then executed all three live, all PASS:
  - **TC-HLP-329** (resolved, zero breaches): ticket #39 raised by `retest.customer1`; `autumn.grace`'s single Reply Note simultaneously assigned+responded+paused with zero elapsed time, then resolved/closed with no time pressure. SLA panel "✓ Completed", no breach anywhere.
  - **TC-HLP-330** (breach once → escalate → resolved at new level, no further breach): ticket #40, assigned via Edit-form only (no reply, so response deadline genuinely ran and breached). **Found Redis/Sidekiq down again** (standing environment quirk) — restarted both, but the sidekiq-cron scheduler's own poller thread still crashes on boot with an unrelated `ArgumentError` (pre-existing version-mismatch bug, not introduced this session), so the periodic auto-fire genuinely doesn't run — manually triggered `Helpdesk::SlaMonitorWorker.new.perform` once (same established practice as TC-092/095/299) to get the real breach/escalation code to run. Correctly escalated L2→L3 exactly once; resolved by `willow.belle` at L3 before a second breach could process. **Reproduced BUG-HLP-033 again in a new shape**: the detail panel still showed "⚠ Needs Attention" for this genuinely-Resolved ticket while the list correctly showed "✓ Resolved" — folded into BUG-HLP-033's existing evidence, no new bug filed.
  - **TC-HLP-331** (Admin closes): ticket #41, same no-breach setup as TC-362, but Admin (not the assigned Agent) performed the actual Resolved→Closed transition — identical "✓ Completed" result, no permission difference. One harmless quirk noted: the Activity Log's "Ticket Resolved" event attributes to the ticket's Assignee, not literally who clicked Submit.
  - No new bugs. Open bug count unchanged at 16. `HELPDESK_MEMORY.md` addendum added with full detail. **Next suite still not chosen by the user** — same remaining candidates: `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority. **Also worth a follow-up session**: Redis/Sidekiq restarted this session but the sidekiq-cron scheduler poller's `ArgumentError` was not fixed (only worked around via manual worker triggering) — the real 2-minute auto-cadence is not currently running in this environment at all; flag to the dev team or fix the gem version mismatch before relying on real (non-manually-triggered) SLA monitor timing again.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User pushed further on the same theme with a sharper, more specific ask: every offered filter field combined (not just Status+Priority from the bullet below), Clear's actual reset behavior, and whether the Dashboard's date range also drives graph/chart data — plus attached a screenshot of the Dashboard's KPI cards, listing out every filter field name from the Add-filter menu by hand. Closed live, 2 new TCs plus one existing TC broadened, 0 new bugs:
  - **TC-HLP-416 broadened**: added Organization Name + Assignee as 2 more simultaneous filter rows (4 total), set Status=New + Assignee=Redmine Admin, left Organization Name untouched — exactly 6 tickets, correctly excluding a Redmine-Admin ticket that's In Progress not New. Separately verified the **Created on** date-range filter (From/To text inputs, structurally different from the dropdown fields) — `2026-09-02`–`2026-09-03` correctly narrowed to exactly 3 tickets. Logged an honest scope note in the TC itself: this samples 3 distinct filter-field mechanisms plus a 4-row combination, not all 13 offered fields individually.
  - **New TC-HLP-417**: Clear (distinct from TC-058's per-row Delete) genuinely resets both the URL (bare, no params) AND the filter panel itself — re-opening after Clear shows only the 2 default rows (Status/Customer), the 2 added rows (Organization Name/Assignee) are gone, not just blanked.
  - **New TC-HLP-418 — a genuinely new discovery**: the **global** Command Center Dashboard (`/helpdesk`) has a "Ticket Statistics" bar chart never once noticed in this entire engagement — absent from `HELPDESK_FEATURES_LIST.md`/`HELPDESK_USER_GUIDE.md` and absent from the project-scoped dashboard entirely (confirmed by direct comparison, screenshots taken of both). Confirmed it genuinely reacts to the Date Range picker: switching to "Yesterday" updated its own date-range label and recomputed all 5 bars to exactly match the KPI cards under the same range (Open 34→3, On Hold 35→3, etc.), with SLA Breached correctly staying at 8 on both surfaces (not date-scoped by design, not a bug). Worth a follow-up doc pass to add this chart to the features list — not done this session, wasn't asked for.
  - All three PASS, no bugs. Open bug count unchanged at **16**. `HELPDESK_MEMORY.md` Addendum 2 added with full detail. **Next suite still not chosen by the user** — same remaining candidates: `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** User pushed back hard on the "18/18 complete" claim for `HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md`, asking pointedly whether multi-filter combos, column-add/remove-while-filtered, and Dashboard "View all" with a manually-changed date range were ever actually tested. Honest answer given: no, they weren't — marker-complete is not the same as every real interaction pattern exercised (see `feedback_verify_evidence_content_not_just_marker_presence`). Closed the gap live with 3 new TCs, all genuinely executed (not written first and backfilled):
  - **TC-HLP-416** (`HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md`): multi-filter AND logic, proved via both direct URL (Status=New+Priority=High → 1 ticket #6; Status=Closed+Priority=High, a deliberately non-overlapping combo → 0 tickets) and the real Filters-panel UI (Status dropdown, Add filter → Priority, Apply) landing on the identical result. PASS, no bug.
  - **TC-HLP-419** (same file): columns vs. filters independence. First assumption (column toggle applies client-side/instantly) was **wrong** and corrected after live testing — a column checkbox change only takes effect after clicking **Apply**, same button as filters. Confirmed adding/removing a column via Apply never disturbs an already-applied filter's URL params or result count, and vice versa. PASS, no bug.
  - **TC-HLP-157** (`HELPDESK_NAVIGATION_WORKSPACES.md`, inserted after TC-HLP-156): Dashboard KPI "View all" with a manually-changed Date Range (TC-HLP-155/275 had only ever exercised the page's default range). Changed the picker to "Yesterday" — KPI counts live-updated (Open 34→3), and the "View all" redirect carried the just-picked range (`created_from=2026-09-02&created_to=2026-09-02`), not the stale default; resulting list showed exactly 3 tickets, matching. One KPI (SLA Breached) carries no date params at all regardless of picker state — confirmed not a bug, just never date-scoped by design. PASS, no bug.
  - **No new bugs found in this follow-up pass** — all three gaps closed with confirmed-working results. Open bug count unchanged at **16**. `HELPDESK_MEMORY.md` addendum added with full detail. **Next suite still not chosen by the user** — same remaining candidates as the bullet below: `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md` is now genuinely **18/18** (TC-HLP-398–059), executed as `luna.blossom` (Agent) for the 15 agent-facing cases and `alpha.customer` for the 3 customer-facing cases (TC-051/055/057). **1 new bug: BUG-HLP-033** (Medium) — the ticket list's SLA Status badge and the ticket's own SLA Information detail panel disagree on the same ticket's real-time SLA condition specifically in the At Risk/Critical range (list correctly shows amber/red per the documented 7-state percentage model, panel unconditionally shows "On Track" since it only has a 5-state binary model with no at_risk/critical concept at all) — root-caused via source (`RfIssueSlaStatus#sla_health_status` vs. `_sla_information.html.erb`'s `overall_health`), confirmed live on ticket #34. Everything else in the suite confirmed working correctly (filter panel Delete-then-reApply mechanic, customer's exact 5-filter/7-column restriction, server-side URL-tampering enforcement, pixel-consistent Apply/Clear/Add-filter/Columns controls) — see today's newest Run History row and `HELPDESK_MEMORY.md`'s matching addendum for full detail. Open bug count: 15 → **16** (007, 015, 017, 019-026, 029-033). **Next suite not yet chosen by the user** — remaining fully-or-partially-untouched suites: `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority.

- **⚠ 2026-09-03 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_FIELD_VALIDATIONS.md` is now genuinely **55/55** — every TC carries a real `CONFIRMED LIVE` marker, verified via header-count-equals-marker-count AND an awk state-machine pass (no orphaned markers). **3 new bugs, all still open**: **BUG-HLP-030** (Medium) — the headline finding, a systemic Name/Code length-validation crash confirmed across **four models and two columns** (`Sla#name`, `SupportLevel#name`, `RfHelpdeskHoliday#name`, `RfProduct#name`, `RfProduct#code`): a 256-char value crashes with an unhandled Rails 500 (`ActiveRecord::ValueTooLong`, MySQL column-width violation) instead of a friendly validation message, in every case discovered by first confirming 255 chars saves cleanly. Recommended as a code-review item (audit every model's `validates` blocks against migration column widths) rather than continuing to find these one TC at a time. **BUG-HLP-031** (Medium) — SLA's First Response Time / Resolution Time are marked required but a blank value is silently accepted on both Create and Edit (saves as NULL), while `0`/negative on the same fields ARE correctly refused — classic `numericality` without `presence` gotcha. **BUG-HLP-032** (Medium) — SLA Agreement upload correctly enforces its 5 MB size limit but has zero file-type restriction (`.exe` genuinely stored/downloadable). Everything else confirmed either a working validation or a documented-as-probe observed behavior — see `HELPDESK_MEMORY.md`'s new addendum for the full list of non-bug surprising findings (Customer Email's real 254-char max, First/Last name's differing 30-vs-255 limits, the recurring "address address" doubled-word message quirk, Product's per-project-scoped duplicate wording, etc.). Open bug count: 12 → **15** (007, 015, 017, 019-026, 029, 030, 031, 032). No local Canned Response or "Phoenix Core"/"Falcon Suite" Product fixtures existed on `redmine-docker-6` at session start — created fresh as this suite's Local fixtures. **Next suite not yet chosen by the user** — remaining fully-or-partially-untouched suites: `HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md`, `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority.

- **⚠ 2026-09-02 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_NAVIGATION_WORKSPACES.md` is now genuinely **17/17**, its negative/permission-boundary cases verified at the URL level (not just the UI level), and both of its chrome bugs have been fully resolved. **Net bug activity this session: BUG-HLP-027 filed then retracted (Not a Bug), BUG-HLP-028 filed → narrowed (its "app menu not hidden" half was itself wrong, checked against the wrong Redmine menu) → closed (Rejected, Not a Bug — "Helpdesk" header is intentional per Product Owner request, the guide's "Helpdesk Support" wording was outdated), BUG-HLP-029 stands (High — a real data-exposure bug in the global dashboard's Recent Tickets list, zero visibility scoping, root-caused via source).** `HELPDESK_USER_GUIDE.md` §4 (Agent permission requirement) and §16 (Command Center header text) both corrected directly to match confirmed-intentional behavior. Saved two new persistent memories: `feedback_verify_hidden_ui_implies_blocked_access` (test URLs directly, don't infer from menu visibility) and `reference_redmine_top_menu_vs_application_menu` (Redmine has two separate named menus that can coexist on one page). Open bug count: 11 → 13 → 12 → 13 → **12** net (007, 015, 017, 019-026, 029 — BUG-HLP-027 and BUG-HLP-028 both closed same day they were filed). New reusable fixtures for any future permission-tier testing: `zero.perm.user` (zero permissions anywhere), `manage.helpdesk.test` + role "Agent ManageHelpdesk Test" (the only `manage_helpdesk` fixture on this environment). See today's five newest Run History rows for full detail. **Next suite not yet chosen by the user** — candidates: `HELPDESK_FIELD_VALIDATIONS.md` (55 TCs, 4/55 done, largest remaining suite), or any of the fully-untouched suites — `HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md`, `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority.

- **⚠ 2026-09-02 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` is now genuinely **30/30** (was actually 24/30 despite the bullet below claiming "24/24 complete" — that claim was wrong; verified via the same structural + verdict-classification coverage check used on `HELPDESK_SLA_ESCALATION.md`). The 6 closed-out TCs (324/336/337/333/334/323) all PASS, zero new bugs — see today's Run History row for full detail. Open bug count unchanged at **11** (BUG-HLP-007, 015, 017, 019, 020, 021, 022, 023, 024, 025, 026). **Next suite not yet chosen by the user** — candidates, in rough priority order: (1) backfill `HELPDESK_NAVIGATION_WORKSPACES.md` TC-274/275 (evidence already exists in `HELPDESK_MEMORY.md` addendum #4, just needs copying into the TC file — quick win, closes a known gap first); (2) `HELPDESK_FIELD_VALIDATIONS.md` (55 TCs, 4/55 done, largest remaining suite); (3) any of the fully-untouched suites — `HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md`, `HELPDESK_PREPAID_HOURS.md` (1/18), `HELPDESK_EMAIL.md` (3/18), `HELPDESK_CONTENT_TEMPLATES.md` (7/36), `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`. Ask the user before starting the next suite — don't assume priority.

- **⚠ 2026-09-02 (superseded by the bullet above) — read this bullet first, everything below it in this section is historical/superseded.** `HELPDESK_TICKET_LIFECYCLE.md` (32/32), `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` (24/24), and `HELPDESK_SLA_ESCALATION.md` (**76/76**, TC-HLP-312/339/341–356 added this session) are all complete. **Latest work: a user-requested audit of 6 Working Hours/Working Days/Holiday scenarios, closing every genuine gap with 7 new TCs (TC-HLP-354–356), all PASS, zero new bugs** — the working-hours/days/holiday clock engine is now the most rigorously verified mechanism in the plugin (every dimension — hours, days, holidays — and combination — fresh vs. partial-consumption, single vs. multi-day holiday, generic vs. adjacency-sensitive — confirmed via real computed-deadline arithmetic). Cross-reference notes added to TC-083/096/107 pointing at the new TC that closes each one's previously-disclosed substitution gap. Also earlier this session, per a direct user completeness question, this suite had a genuine content-level audit (not just a marker-presence check): every marker's verdict was classified and the 15 shortest evidence blocks manually re-read, catching and fixing one real gap (TC-HLP-341's evidence didn't match its own Expected Result — rewritten). **TC-HLP-353**: confirmed a locked agent's account is correctly excluded from ever being assigned a ticket, via both manual assignment and automated escalation (3/3 escalations in a row picked the other, active L2 assignee) — but locking does not automatically remove the agent from a Support Level's own assignee configuration, and nothing on-screen flags the stale entry. Initially logged as low-impact/not filed, then **filed as BUG-HLP-026 (Low)** per explicit user direction ("so create bug") — the third "not a bug" verdict this session corrected after user pushback (alongside BUG-HLP-024/025). All **5 user-specified scenarios** have been live-tested and written up; **Scenarios 1 and 5 were then corrected** after the user pushed back on their initial "not a bug" verdicts, resulting in **BUG-HLP-024** (no warning/indication before a lower-level agent takes ownership of a higher-Support-Level ticket) **and BUG-HLP-025** (Support Level form requires manually configuring "Escalation To" even though Level Order already defines the ordinary sequential chain). **BUG-HLP-024 was then broadened further** per a detailed user follow-up: confirmed the same zero-warning defect reproduces via 2 additional, genuinely distinct paths — an L1 agent self-assigning a fresh unassigned L2 ticket (TC-HLP-315), and an L1 agent merely *replying* to one, which auto-assigns them via a separate mechanism with no warning either (TC-HLP-316) — plus a sharp related finding that a fresh/unassigned ticket has **no Support Level indicator anywhere** (the SLA Information tab itself doesn't exist until first assignment). A matching-level control (TC-HLP-317, PASS) and two Blocked acceptance TCs for a not-yet-implemented Cancel/Confirm dialog (TC-HLP-318/348) round out the set. All of BUG-HLP-024/025's TCs carry Revision History sections; see the two newest 2026-09-01 Run History rows and `HELPDESK_MEMORY.md`'s matching addenda for full detail. Scenarios 2, 3, 4 stand as originally resolved (documented/intentional behavior, or already covered by BUG-HLP-021 for Scenario 3). A real 3-level escalation chain exists on Helpdesk QA Alpha (L1 Luna Blossom → L2 Autumn Grace+Briar Sunset → L3 Willow Belle → admin fallback, SLA "Alpha Escalation Test SLA", 1-min targets) — reusable for future breach/escalation work without rebuilding. Also reusable: "Alpha Working-Hours-Skip Test SLA", "Alpha Holiday-Skip Test SLA", "Alpha Deactivation Escalation Test SLA" (deactivated, unreferenced), and tickets #24/#25/#26 (fresh L2 fixtures now assigned to Luna/Luna/Autumn respectively — reusable for any further cross-level-warning work once BUG-HLP-024 is fixed, to re-run TC-HLP-318/348). **11 bugs remain open** (BUG-HLP-007, 015, 017, 019, 020, 021, 022, 023, 024, 025, 026) — BUG-HLP-024 was broadened, not newly created (no count change on that pass); BUG-HLP-026 (locked agent still listed as a Support Level assignee, no lock indicator anywhere) is the newest, filed from TC-HLP-353. The Redmine admin user's Mail field was fixed from the placeholder `admin@example.net` to the real `admin@test.local` — **re-check this after any future DB reset, it will not survive one**. **`beta.customer` does not currently exist on this environment** (never recreated after the 2026-08-27 DB reset) — don't assume any TC precondition naming her is still valid; check first. `HELPDESK_USERS_AND_CUSTOMERS.md` still needs updating with the new agent fixtures (`autumn.grace`/`briar.sunset`/`willow.belle`) and the admin-email fix — not yet done. **Next suite not yet chosen** — remaining untouched suites: `HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md`, `HELPDESK_PREPAID_HOURS.md`, `HELPDESK_EMAIL.md`, `HELPDESK_CONTENT_TEMPLATES.md`, `HELPDESK_REPORTING_AUTOMATION.md`, `HELPDESK_PERMISSIONS.md`, `HELPDESK_RAKE_TASKS.md`, `HELPDESK_PLUGIN_INSTALLATION.md`, `HELPDESK_NAVIGATION_WORKSPACES.md` (some already partially covered by earlier sessions — check each file's own CONFIRMED LIVE markers before assuming zero coverage).

- **⚠ 2026-08-27: Local DB was fully reset mid-cycle.** All fixture data referenced below (tickets #67–#77, `alpha.customer`/`delta.customer`/`beta.customer`, 6 agents, Beta project, etc.) from sessions before this point no longer exists. The environment has been partially rebuilt: **Helpdesk QA Alpha only** (project/role/agent `luna.blossom`/SLA/Support Level/customer `alpha.customer`, tickets #1–#2) — see `HELPDESK_USERS_AND_CUSTOMERS.md`'s "CURRENT STATE" banner for the exact current fixture set. **BUG-HLP-014 was retested PASS and closed** on this rebuilt environment (see Run History above and `bugs/closed/BUG-HLP-014.md` Retest + Closed sections) — closed per explicit user direction, ahead of the full `HELPDESK_TICKET_LIFECYCLE.md` suite (32 TCs) + adjacent-feature regression `SENIOR_QA_STANDARDS.md` §26 would normally require for a High-severity fix. That regression is **still outstanding** (not required to keep BUG-HLP-014 closed anymore, but still valuable as general suite coverage — the DB wipe invalidated every prior TC result in that suite, so it's effectively a from-scratch re-run of all 32 TCs, not just the ones already done pre-wipe: 013–023/037/289/295). **This is the natural next step.**

- **`HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite — historical note, corrected 2026-09-02: this "24/24" claim was wrong, the suite actually had 6 untested TCs at the time (TC-324/336/337/333/334/323). See the current-as-of-2026-09-02 bullet above — now genuinely 30/30.** Of the original 24: 22 PASS, 2 FAIL (TC-HLP-056/BUG-HLP-013, TC-HLP-061/BUG-HLP-011). TC-HLP-065 added 2026-08-27.
- **`HELPDESK_TICKET_LIFECYCLE.md` (32 TCs) execution IN PROGRESS as of 2026-08-27** — user picked this suite next. Done so far: TC-HLP-365/014/015/016/017/018/019/020/021/022/023/037/289/295 (14 of 32). **BUG-HLP-014 (High)** found via TC-015: a customer's own Tickets tab shows "0 tickets" even when tickets genuinely exist and are individually reachable. **Root cause confirmed by reading the plugin source** (`docker exec` into `redmine-docker-6-redmine-1`, `/usr/src/redmine/plugins/redmineflux_helpdesk/`): `TicketFilter#base_scope` starts from Redmine's native `Issue.visible(user)`, which requires real project Membership — but customers are (by design) linked only via `RfProjectCustomer`, never a Redmine Member, confirmed via `rails runner` (`alpha.customer.memberships.count == 0`). Every other customer check in the plugin correctly uses `RfProjectCustomer` instead; only this list fell back to Redmine's native scope. **Confirmed systemic, not fixture-specific**: reproduced identically with a brand-new customer (`delta.customer`, zero prior history, ticket #77) at the user's request. Full writeup + suggested fix direction in `bugs/open/BUG-HLP-014.md`. Separately found (not yet its own bug): the `check_emails` **rake task** is dead code — it still reads the legacy `ProjectCustomField` config the UI stopped writing to after a migration to `RfHelpdeskEmailConfig`, while the real scheduled Sidekiq `EmailPollerWorker` correctly uses the new model (confirmed by triggering it directly via `rails runner`, which worked — connected via IMAP fine, just found 0 unread mail waiting). Also fixed a real environment-integrity issue found via TC-013: both Helpdesk QA Alpha and Beta had **"Is public" checked** (Non-member role has Add/View issues), which let an Alpha-only Manager create a ticket in Beta — root-caused as standard Redmine behavior, not a plugin bug, but unchecked "Is public" on both projects to restore proper scoping (re-verified: 403 now). **Two navigation-methodology corrections mid-suite** (see `HELPDESK_MEMORY.md` and the global `feedback_no_direct_url_navigation` memory): (1) don't type a ticket's branded-route URL directly — click through the list, which for an agent lands on the *core* `/issues/:id` route, materially different per BUG-HLP-007; (2) when following a link meant to simulate a specific role's journey (e.g. a real notification-email "View Ticket" link), first explicitly switch the browser's Redmine session to that role — a legitimately-clicked link still renders the wrong view if the tab's session is still someone else's. **Next session should resume this suite at TC-HLP-378** (custom reply time value) and continue through TC-HLP-397 — remaining ~18 TCs, several need real fixtures/waits (merge, time-logging permission variants, all-7-statuses check).
- **BUG-HLP-013 (Medium, open, revised same-day)** — **Portal Preview is now scoped OUT of this plugin's intended functionality** per explicit user product-judgment direction (added to `HELPDESK_SCOPE.md` Out of Scope). The defect: `/rf_customers/:id/portal` is fully reachable and functional (read-only, correct scoping, 403 on out-of-entitlement project) despite having no UI entry point and not being in-scope functionality — the route existing at all is now the problem, not a missing button. Needs a product/dev decision: remove the route, or reverse the scope call and add the entry point (worth noting: the route's internal logic is real, deliberate, correct engineering — not leftover code — which is evidence for that decision either way). TC-HLP-056 → FAIL (route should 404, doesn't), TC-HLP-066 → PASS but now secondary/moot. See `bugs/open/BUG-HLP-013.md` (has a Revision History section documenting the original vs. revised framing).
- **BUG-HLP-011 (Medium, CLOSED 2026-08-31):** Organization delete previously had no linked-customer warning. Retested: the confirmation modal now explicitly warns "This organization has 1 linked customer(s) — deleting it will remove that association." Closed per explicit user confirmation. See `bugs/closed/BUG-HLP-011.md`.
- **BUG-HLP-012 was filed then closed same-day as Not a Bug** — Customer Edit form's Information fields (Login/Name/Email/Password) being writable by a project-scoped Manager for a customer outside her scope is, per user product-judgment review, the correct behavior of a global customer directory (Login/Email unique install-wide; "Add project" on the same form was independently confirmed correctly scoped to the Manager's own project). See `bugs/closed/BUG-HLP-012.md` for the full reasoning.
- **New fixture available for future scoped-permission testing**: `ivy.sterling` / `Test@12345`, Manager role, Member of Helpdesk QA Alpha only — see `HELPDESK_USERS_AND_CUSTOMERS.md` "Scoped Manager fixture" section and `helpdesk.local.fixtures.ts`'s `MANAGERS` export.
- **Filed BUG-HLP-008** (Medium) for the `EmailPollerWorker`/Helpdesk Conversion tab finding — see below. No separate low-severity bug needed for the warning alone; it's documented as a likely-shared root cause inside BUG-HLP-008 instead. **Confirmed the full 3-way matrix live** (agent web-UI reply → logged correctly; customer portal reply → logged correctly, tested fresh on ticket #69; customer real-email reply → NOT logged, ticket #67) and wrote it up as formal test cases TC-HLP-073/287/288 in `HELPDESK_EMAIL.md` — the omission is specific to the email channel only, not customer replies in general. BUG-HLP-008's Part B (duplicate/mislabeled entry) did not reproduce on #69's clean single reply — its trigger condition isn't isolated yet, possibly specific to the first reply after an email-originated ticket.
- **Retest both BUG-HLP-006 and BUG-HLP-007 on the current Forge instance, driving BOTH routes deliberately (branded AND core) for each**, now that the dual-route pattern is understood — this is the definitive way to resolve both bugs' open questions in one pass: does Forge's current build show the same split-brain behavior (branded=customer-accessible-but-Internal-Note-crashes, core=Internal-Note-works-but-customer-blocked), or has one/both routes changed since these bugs were originally filed on earlier Forge rotations.
- **Clean up ticket #67**: it has accumulated several throwaway Internal/Reply Notes ("test", "dsfgsdf", "dsaf", "46556") left over from BUG-HLP-007 route-retesting — harmless as evidence but clutters the fixture; either leave as-is with this note or trim before the next demo/handoff.
- **Filed BUG-HLP-009** (High) from a user-reported observation, verified live on a clean fixture (ticket #70), then re-scoped per the user's correction: the bug is NOT "note invisible to customer" (Notes tab is correctly never customer-facing) — it's that the status auto-transitions to "Waiting for Customer Response" via the standard **Edit** form even though **zero** customer-facing communication is produced (no Helpdesk Conversion entry at all, no email). Likely the same branded-vs-core root-cause family as BUG-HLP-007/008 — worth investigating together when this gets fixed. Not yet tested: whether Admin role shares the same gap.
- **Redis/Sidekiq and the mail server's IMAPS config do not survive container restarts** — re-verify both before trusting any new email test result (`ps aux | grep -E 'redis|sidekiq'` inside `redmine-docker-6-redmine-1`; `ss -tlnp | grep 993` inside `local-mail-server`).
- **Forge instance is `flux-fwdq7ydhw49`** as of the 2026-08-25 session — re-verify this is still current before resuming any Forge-side work (per `QA_CREDENTIALS_FORGE.md`'s URL Rule, it rotates per run).
- **Test data matrix is real and comprehensive on Forge.** Writing the first `.spec.ts` files against the now-fixed page objects (`automation/tests/`) remains the natural next automation step — start with SLA/Support Level/Product/Customer suites.
- Separately still pending from earlier sessions: execute the 9 written-but-unverified filter/dropdown TCs (TC-HLP-155–285), recreate Holidays on Forge, `HELPDESK_TICKET_LIFECYCLE.md` suite execution, and a fresh pass to write real test cases for the SLA Information tab (now well understood — see `HELPDESK_MEMORY.md`) and Support Packages/Prepaid Hours top-up UI.
- ~~Resolve TC-HLP-165 (manage_helpdesk vs export_helpdesk_reports)~~ — **RESOLVED 2026-09-07**: `manage_helpdesk` alone is sufficient, confirmed via real content-verified CSV downloads. See `HELPDESK_PERMISSIONS.md` and the current-as-of bullet above.

## Run History

> Replaces the old changelog.md — one row per test run / regression pass (see `CLAUDE.md` §2b, §12; `SENIOR_QA_STANDARDS.md` §26/§27).

| Date | Redmine Version | Environment | Tested By | Summary |
|------|-----------------|-------------|-----------|---------|
| 2026-08-21 | — | — | — | Plugin QA folder + Playwright automation framework scaffolded; docs renamed to HELPDESK_*.md; HELPDESK_USER_GUIDE.md populated with real content |
| 2026-08-21 | — | — | — | All docs (requirements/features-list/user-guide) complete; all 54 features written into 12 test-case suites (227 TCs total, positive/negative/edge); test data registry convention established; no execution yet — next session starts actual testing |
| 2026-08-21 | Forge (flux-fiqll6jza49) | Forge | Claude (Playwright MCP), roles set up for daisy.skye/luna.blossom/autumn.grace | First live execution pass. PASS: TC-HLP-334, 075, 076, 078, 089, 108, 109, 205. Found BUG-HLP-001, filed as Critical (customer creation appeared blocked), then downgraded to Medium after a retest succeeded despite the same stale error — root cause of the earlier failures still open. Corrected a wrong HELPDESK_MEMORY.md claim (support level "test" and SLA "dsfasd" pre-existed, not zero as noted). |
| 2026-08-24 | Forge (flux-fnwchpuwl49, rotated mid-session from flux-fiqll6jza49) | Forge | Claude (Playwright MCP), daisy.skye + admin | CRUD-first verification pass complete for Organization/SLA/Support Level/Holiday/Customer (Create/Read/Search/Update all confirmed; Delete executed for Organization/SLA/Support Level via throwaway entities, markup-confirmed for Holiday/Customer). Resolved TC-HLP-045/122 (multi-project customer access genuinely persists — required enabling Helpdesk module on a 2nd project + creating a support level for it). Found BUG-HLP-002 (Escalation To dropdown not project-scoped). BUG-HLP-001 did not reproduce this session (still open, cause unconfirmed). Fixed a real bug in `HelpdeskCustomerPage.ts` itself (wrong submit-button locator, never previously exercised). Generated and populated `HELPDESK_TESTDATA_FORGE.xlsx` (did not exist before). |
| 2026-08-24 | Forge (flux-fw2qhf0ux49, rotated again since prior row) | Forge | Claude (Playwright MCP), admin | Canned Response CRUD+field-validation+macro session. Full CRUD+search+delete executed (throwaway entity), all field validations executed with real confirmed values (Name required, max length exactly 255 chars, duplicate-name exact wording "Name has already been taken", Author read-only). All 9 reply macros confirmed substituting correctly on real ticket #236 in one pass; macro-shortcut-link click-to-insert confirmed. Found BUG-HLP-003 (High) — Active checkbox cannot be unchecked via Edit, fully reproduced end-to-end on Canned Response (missing Rails hidden fallback field); Organization/SLA share the identical DOM markup but the failure itself wasn't independently reproduced on them; blocks TC-HLP-033. Updated `HelpdeskCannedResponsePage.ts` with full CRUD/macro/delete methods. Reset `HELPDESK_TESTDATA_FORGE.xlsx` to match the new server (prior instance's data no longer exists). |
| 2026-08-24 | Forge (flux-fxd0g3s4049, rotated again since prior row) | Forge | Claude (Playwright MCP), admin | Full CRUD verification round for Organization/SLA/Support Level/Product/Customer (Create/Read/Search-match/Search-no-match/Update/Delete + duplicate-field validation, all with real confirmed error wording). Product was a previously-untested gap — added edit/delete to `HelpdeskProductPage.ts`. Confirmed BUG-HLP-003's full scope: Organization/SLA/Support Level/Product all share the broken Edit-form Active checkbox AND a working list-level AJAX toggle workaround; Canned Response is the sole entity with no workaround. Confirmed a real SLA cross-field validation ("Resolution Time must be greater than or equal to the Response Time") and Product's per-project (not install-wide) name/code uniqueness. Fixed a real bug in `HelpdeskSupportLevelPage.ts` (Support Assignees modeled as native select[multiple], actually a custom checkbox-dropdown widget). Rebuilt L1→L2→L3 escalation chain and reset `HELPDESK_TESTDATA_FORGE.xlsx` for the new instance. |
| 2026-08-24 | Forge (flux-fudbk2hlu49, rotated again since prior row) | Forge | Claude (Playwright MCP), admin | Investigated SLA vs. Organization project-scoping mechanisms in depth (see `HELPDESK_MEMORY.md` Known Quirks): SLA has a real hidden `project_id` set only via a specific `?project_id=X` URL query param with no visible nav path to it; Organization has no creation-time scoping at all — its project tab is a derived view based on customer/prepaid-budget linkage (an earlier draft bug on this was filed then retracted per direction, underlying technical finding kept). Set up two properly-scoped SLAs/Support Levels (Standard/L1 on Helpdesk Service Desk, AgileSLA/AB-L1 on Agile Board Project) to definitively test whether the Customer form's SLA/Support Level dropdowns exclude cross-project options like the Support Assignees widget does. Result: functionally scoped (wrong-project option can't be selected) on both Create and Edit, but the option is only `disabled` not removed — DOM shows the plugin sets `hidden=""` on it, which CSS is defeating. Filed as **BUG-HLP-004** (Low). Follow-up investigation confirmed the global `/rf_slas` and `/rf_support_levels` list pages (unlinked from any discoverable nav) each have their own working "New" button pointing at the bare, unscoped `/new` route — for SLA this creates a genuinely orphaned record with no project association at all; for Support Level the required Project dropdown prevents an orphan but lists every project including ones without Helpdesk enabled. Filed as **BUG-HLP-005** (Medium). **Follow-up same session:** a controlled comparison (proper `?project_id=X` link vs. the bypass route) proved BUG-HLP-002 (Escalation To dropdown unscoped) only reproduces via BUG-HLP-005's bypass route — via the proper link, Escalation To is correctly scoped (Project renders as a locked hidden input there, not a live dropdown). Re-scoped BUG-HLP-002 as a symptom of BUG-HLP-005 (downgraded to Low), cross-linked both bug files so a fix to BUG-HLP-005 gets BUG-HLP-002 retested/closed alongside it rather than fixed independently. **Also this session:** re-verified BUG-HLP-003 (Organization, Canned Response reproduced identically), then went further and actually executed the real Edit-form test for SLA/Support Level/Product (previously inferred/list-toggle-only) — found Product and Support Level are NOT affected, correcting the bug's scope down to Organization/SLA/Canned Response. A same-session false-positive finding for Support Level (appeared to silently deactivate on any Save) was caught, investigated (raw server HTML fetch confirmed correct rendering), and retracted after a clean re-test via the real UI link showed correct behavior both times. |
| 2026-08-25 | Forge (flux-fwdq7ydhw49) | Forge | Claude (Playwright MCP), admin | Rewired all 15 `automation/tests/` page objects from `page.goto()`-only navigation to real click-through nav (BasePage nav primitives added, BUG-HLP-005 bypass routes kept as explicit `...ViaDirectUrl()` methods), fixed a real bug in `LoginPage.ts` (stale core-Redmine locators vs. the actual custom login form) and pre-existing infra gaps (`npm install`, `@types/node`, DOM lib) — suite now compiles clean. Then built out a comprehensive test-data matrix for automation: 2 Organizations, 4 SLAs (2 per project), 5 Support Levels across two full multi-agent escalation chains (L1→L2→L3 and AB-L1→AB-L2, 8 distinct agents total), 4 Products (2 per project), 4 Canned Responses, and 4 Customers with deliberately varied project/SLA/support-level/organization combinations (including one genuine multi-project customer). Discovered and populated a previously-undocumented feature, Support Packages + per-organization Prepaid Support Hours budgets (2 packages, 2 budget top-ups with different run-out enforcement modes). Fully repopulated `HELPDESK_TESTDATA_FORGE.xlsx` for this rotation and added a new Support Packages sheet (workbook + generator script). No `.spec.ts` files exist yet — writing the first real specs against this data is the next step. |
| 2026-08-26 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + 2 real agents (aurora.wren, and Alpha via admin) | Built a complete local email-testing environment from a clean slate: 2 projects, 2 orgs, 2 SLAs, dedicated Agent role + copied workflow, 6 agent Users, 2 Customer records, 14 mail server mailboxes. Fixed a real infra gap (IMAPS never enabled on the local mail server — self-signed cert + trust-store install + Roundcube config). Confirmed the full email round-trip working end-to-end on ticket #67 (customer email → ticket → agent reply → customer email reply → note + auto status transition). **Resolved the open SLA-resume question from 2026-08-25**: email reply is a real, working resume path — SLA showed "On Track", not permanently frozen. Found and filed **BUG-HLP-007** (High) — Internal Note submission crashes with a 500 error (`private_notes` NOT NULL violation). Ruled out a suspected duplicate-note risk from a separate cosmetic poller warning (confirmed via a real cron-tick wait, not reprocessed). Negative-keyword gating test in progress at session end (first attempt self-invalidated by accident, redone cleanly). |
| 2026-08-26 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), aurora.wren + beta.customer | Retested BUG-HLP-007 and BUG-HLP-006 using real click-through navigation (corrected away from `browser_navigate` deep-linking per explicit user direction). **BUG-HLP-007 rescoped**: reproduces only via the branded `/projects/:id/helpdesk/issues/:id` route, not the core `/issues/:id` route — confirmed still High severity since both real outbound notification email templates link to the branded route. **BUG-HLP-006 does not reproduce on Local** (customer opened both an email-originated ticket #67 and a fresh portal-originated ticket #69 successfully) — root cause traced to the same dual-route split: Local's customer ticket list links to the branded route (customer-accessible), the original Forge repro showed the core route (customer-blocked). Documented the mirror-image relationship between both bugs in `HELPDESK_MEMORY.md`. Neither bug closed — both need a Forge retest driving both routes deliberately before any status change. **Found and filed BUG-HLP-008** (Medium) while explaining the Helpdesk Conversion vs. Notes tab split to the user: a customer's real inbound-email reply to an existing ticket is never logged in the Helpdesk Conversion tab (only visible under Notes), and a separate duplicate entry there mislabels the agent's own outbound reply as an inbound "Customer replied to Support" message. Upgraded the earlier "cosmetic-only" verdict on the `EmailPollerWorker` Journal-type warning — now understood as a likely shared root cause with BUG-HLP-008's omission, not just log noise. Confirmed the full 3-way reply-origin matrix live and wrote it up as TC-HLP-073/287/288 in `HELPDESK_EMAIL.md`. **Also filed BUG-HLP-009** (High) from a user-reported observation: an agent's non-private note added via the standard Edit form auto-transitions the ticket to "Waiting for Customer Response" but never renders on the customer's own Notes tab — verified live on a clean fixture (ticket #70). |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | Continued coverage-gap sweep before starting full suite execution: confirmed TC-HLP-104/244 (Customer Login/Email uniqueness) already existed; added and confirmed live **TC-HLP-096** (Organization Website/Phone have no uniqueness constraint, matches spec) — filed **BUG-HLP-010** (Low) anyway per explicit user product-judgment direction, since real orgs shouldn't share contact info even though the current spec doesn't require uniqueness. Added and confirmed live **TC-HLP-388** (customer's ticket Edit is Subject-only — real field `#issue_subject`, nothing else becomes editable). Cross-referenced already-gathered Helpdesk Conversion evidence into TC-HLP-368. Drafted and got approval for `HELPDESK_AUTOMATION_PLAN.md` (own-fixture UI-only provisioning + Playwright automation build-out, phased). **Then executed the full `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite live, 21 of 23 TCs** (108–110, 112–120, 122–124, 279–280, 283–284, 293–294 — 111 already confirmed prior session): all real data, real fixtures (Organization "Gamma Corp" created then deleted, Customer `gamma.customer` created with multi-project access then reduced to one). Found **BUG-HLP-011** (Medium): deleting an organization with a linked customer succeeds with only a generic confirmation, no warning — the customer's Organization reference silently clears to "—". Re-confirmed TC-HLP-063 (multi-project customer access) on a second environment and **corrected `HELPDESK_USER_GUIDE.md`'s wrong "one row at a time" prose directly** (both occurrences), since it's now conclusively wrong on two environments. Found TC-HLP-056 (Portal Preview) has **no corresponding UI control at all** on this Local build (full DOM scan confirmed) — blocks TC-HLP-066 too, needs a Forge comparison. Deferred TC-HLP-062 (needs a dedicated project-scoped manager fixture that doesn't exist yet — natural fit for the automation plan's Phase 1 provisioning). |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + new fixture `ivy.sterling` | **Unblocked and executed TC-HLP-062.** Built the missing project-scoped-manager fixture: added `manage_helpdesk`/`export_helpdesk_reports`/`manage_prepaid_support_hours`/`view_helpdesk`/`view_email_history` permissions to the pre-existing but Helpdesk-blank "Manager" role, created user `ivy.sterling`, added her as a project Member of **Helpdesk QA Alpha only** (verified not a Member of Beta). Restored `gamma.customer`'s second (Alpha) project-access row so it held both an Alpha and Beta row, then confirmed as `ivy.sterling` that her Edit form for that customer shows **only** the Alpha row (Beta row completely absent, not disabled); edited and saved the Alpha row's Organization, then verified as admin the Beta row was byte-for-byte unchanged. **TC-HLP-062 PASSES.** **Found BUG-HLP-012 (High)** as a side effect on the same form: the customer's Information section (Login/Name/Email/Password) is fully editable by `ivy.sterling` even for `beta.customer`, a customer with zero Alpha access — proved exploitable (changed and reverted `beta.customer`'s First name from her session). Project-access-row scoping and Information-field scoping are evidently separate, inconsistently-enforced code paths. Updated `HELPDESK_USERS_AND_CUSTOMERS.md`, `helpdesk.local.fixtures.ts` (new `MANAGERS` export, `tsc --noEmit` clean), and `HELPDESK_TESTDATA_LOCAL.xlsx`'s Customers sheet (also fixed a pre-existing column-shift data-quality issue found on `gamma.customer`'s row while updating it) with the new fixture/state. **`HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite is now 22/23** — only TC-HLP-056/124 (Portal Preview, blocked pending Forge comparison) remain open. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), review of prior session's finding | **Reviewed and closed BUG-HLP-012 as Not a Bug**, per user's product-judgment pushback: Customers are a global entity (Login/Email unique install-wide, TC-HLP-104/244), and the same session's own testing had already shown the "Add project" control on the Customer Edit form is correctly scoped to the Manager's own project — decisive evidence that Information fields are the intentionally-global part of the record and Project-access rows are the intentionally-scoped part, not an inconsistency. Moved `bugs/open/BUG-HLP-012.md` to `bugs/closed/BUG-HLP-012.md` with the full resolution writeup (including a residual, not-re-opened note about the narrower Password-only-no-project-touch edge case for future reference). Updated TC-HLP-062's note, `HELPDESK_MEMORY.md`, `bugs/_index.md`, and `STATUS.md` to match. Open bug count: 12 → 11. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Resolved TC-HLP-056/124, closing out the suite at 23/23.** User pushed back on the earlier "Portal Preview doesn't exist" theory with an alternative reading (customer login itself being the portal). Checked `HELPDESK_USER_GUIDE.md` §11 directly instead of theorizing further — it names a real "View portal" button and documents an actual route, `/rf_customers/:id/portal`. Tested it directly: works exactly as documented (read-only banner, Exit preview link, correct per-project listing for `beta.customer`). Tested scoping too: forcing an out-of-entitlement project via `?project_id=3` correctly 403s. Re-ran the full DOM scan on Customer 360 fresh — still zero portal-related controls, confirming the entry point really is missing even though the destination works. Filed **BUG-HLP-013** (Medium): feature is fully functional, only its UI entry point on Customer 360 is missing — same defect shape as BUG-HLP-005 (working feature, no discoverable link) but a distinct root cause. Rescored TC-HLP-056 → **FAIL** (behavior is correct but its own "click View portal" step can't be performed) and TC-HLP-066 → **PASS** (403 confirmed live). `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` suite is now **23/23 executed, zero deferred/blocked**: 21 PASS, 2 FAIL (TC-117/BUG-HLP-013, TC-120/BUG-HLP-011). Updated `HELPDESK_MEMORY.md`, `bugs/_index.md`, and both TC entries to match. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), review of same session's finding | **Same-day revision of BUG-HLP-013, per user product-judgment direction.** User stated Portal Preview should not be considered part of this plugin's intended functionality at all, regardless of it being documented in `HELPDESK_USER_GUIDE.md`/`HELPDESK_FEATURES_LIST.md`, and directed that the Expected Result should be that the route itself must not exist if it's genuinely out of requirements/functionality. Added Portal Preview to `HELPDESK_SCOPE.md`'s **Out of Scope** list as an explicit, durable decision. Rewrote TC-HLP-056's Expected Result (route should 404, not just "should have a button") — still FAILs, but now for the corrected reason. TC-HLP-066 stays PASS for its own narrow entitlement-scoping check but is now flagged as secondary/moot once the whole route shouldn't exist. Rewrote BUG-HLP-013's Title/Expected/Actual result to match, kept a **Revision History** section in the bug file preserving the original framing, and added a **Recommendation**: needs a product/dev decision to either remove the route entirely or reverse the scope call and add the missing entry point — noted that the route's internal logic (correct read-only rendering, correct entitlement scoping/403) is real, deliberate engineering, not leftover code, which is relevant evidence for that decision either way. Updated `bugs/_index.md`, `HELPDESK_MEMORY.md`, `HELPDESK_HANDOFF.md`. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Coverage-integrity check, prompted by "did you test all testcases in this suite?"** Counted `CONFIRMED LIVE` markers against the suite's 23 TC headers directly rather than answering from memory — found 22, not 23. TC-HLP-050 was the gap: its Expected Result only cited an already-known fact from the BUG-HLP-004 investigation (2026-08-24), never its own dedicated execution. Ran it properly on `beta.customer`'s Edit form (Add project → Project=Alpha → SLA/Support Level correctly re-scope, Organization shows the full unfiltered list → switch Project=Beta → SLA/Support Level correctly flip, Organization list stays exactly the same) — PASS, matches expected result, discarded the unsaved row via Cancel. `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` is now genuinely 23/23 with a real live execution note on every single TC. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **New coverage: TC-HLP-065.** User stated a precise claim — an organization created from within a project's own Helpdesk tab shows in the global Organizations list but not that project's own Organization view, not until a customer's project-access row on that project actually selects it. Checked for existing coverage first (none — TC-HLP-050 only covers the Customer form's dropdown, not the project-scoped `/projects/:id/helpdesk/organization` view or the creation-origin angle), then verified live before writing anything: created "Delta Test Org" via Helpdesk QA Alpha's own "New Organization" button (`project_id=3` in the URL) — appeared in the global list immediately, did NOT appear on Alpha's own Organization tab; only appeared there once selected on `alpha.customer`'s Alpha project-access row. Confirms the claim exactly. Wrote **TC-HLP-065** in `HELPDESK_CUSTOMERS_ORGANIZATIONS.md` (Edge Cases), enriched the `HELPDESK_REQUIREMENTS.md` Known Constraints Organization note with the "creation origin doesn't matter" nuance, updated the suite's Evidence Map, and cleaned up the throwaway org + reverted `alpha.customer`'s Organization back to "Alpha Org" afterward. Suite is now 24 TCs, all confirmed live. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom + ivy.sterling + alpha.customer + admin | **Started `HELPDESK_TICKET_LIFECYCLE.md` (32 TCs), user's chosen next suite.** Environment health-checked first (Docker containers up, Redis+Sidekiq running, IMAPS listening). TC-HLP-365/014/018 confirmed live: project-scoped ticket creation works (ticket #71 as `luna.blossom`); the *global* Command Center Tickets screen needs `manage_helpdesk` specifically, not just `view_helpdesk` as the TC's precondition claims (confirmed via `ivy.sterling`, ticket #72) — not filed as a bug, precondition wording just needs correcting. **Found and fixed a real environment-integrity issue**: both Helpdesk QA Alpha and Beta had "Is public" checked (Non-member role has Add/View issues) — this let `ivy.sterling` (Alpha-only) create a ticket in Beta (#73) via the global New Issue form's unscoped Project dropdown. Root-caused as standard Redmine public-project behavior, not a Helpdesk-plugin bug — but it silently broke the project-scoping assumptions this whole engagement has been testing on, so unchecked "Is public" on both projects and re-verified the boundary now correctly 403s. Deleted the probe ticket. **Found BUG-HLP-014 (High)** via TC-HLP-367: `alpha.customer` successfully created ticket #74 (confirmed real via admin — 5 tickets total on the project), but her own Tickets tab immediately showed "0 tickets" / "No tickets yet," including her older pre-existing ticket — reload-persistent, individual ticket still reachable directly, same URL shows all 5 correctly as admin. Isolated to the Customer role's list view. See `bugs/open/BUG-HLP-014.md`. 7 of 32 TCs done (013/014/015/016/018/289/295); resuming at TC-HLP-369 next session. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom + alpha.customer + admin | **Continued `HELPDESK_TICKET_LIFECYCLE.md`: TC-HLP-369/019/020/021/037 confirmed.** TC-017 (email reply lands as a note, not a new ticket) confirmed by reusing already-gathered `HELPDESK_EMAIL.md` TC-288 evidence on the same ticket (#67) rather than re-running the round-trip — PASS on "no new ticket + lands as a Note," but the "recorded in Email History" half FAILs per already-tracked BUG-HLP-008 Part A. **Two navigation-methodology corrections mid-execution** (both reinforced in `HELPDESK_MEMORY.md` and the global `feedback_no_direct_url_navigation` memory): first, typed a ticket's branded-route URL directly instead of clicking through the list — corrected by redoing TC-019/021 via genuine click-through, which for an agent lands on the **core** `/issues/:id` route (materially different per BUG-HLP-007); confirmed both PASS there (ticket #71: Status → Waiting for Customer Response; fresh unassigned ticket #75: auto-assigned to replying agent, status transition, SLA paused). Second, followed `alpha.customer`'s real notification-email "View Ticket" link while the Redmine browser tab was still under an agent's session — the link click was legitimate but the rendered page wasn't hers; corrected by explicitly signing the tab out and into `alpha.customer` first, then re-clicking. That properly-authenticated path confirmed **TC-020** (customer reply → Status back to In Progress, SLA resumed to "On Track") and, as a side observation, **TC-037** (customer's reply box has only a plain textbox + Save — no Canned Response/Internal Note/time-log controls at all). 12 of 32 TCs done; resuming at TC-HLP-376 next. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom + admin | **TC-HLP-376/023 confirmed, 14/32 done.** TC-022 (Internal Note): via genuine click-through on ticket #74 (core `/issues/74` route), Internal Note saved without crashing (confirms BUG-HLP-007's crash is branded-route-specific), Status stayed In Progress, SLA stayed "On Track" (not paused), and the Helpdesk Conversion tab's content was checked directly — the note text doesn't appear there, confirming exclusion from the customer channel, not just role-based hiding. TC-023 (10-minute time log): real finding — the "Time spent" control is a plain `<select>` dropdown (`-- None --/5/10/15/20 min/Custom`), not clickable chips as the TC's steps describe (relevant to TC-HLP-391 too). Selected "10 min" + Activity, saved — time entry correctly shows "0:10" (ten minutes) on both the ticket and its own Edit page, not ten hours. Resuming at TC-HLP-378 next. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + alpha.customer | **Full DB reset + environment rebuild + BUG-HLP-014 retest PASS + closed.** At explicit user request, performed a full Local database reset (backup taken first) and rebuilt the entire environment from scratch, UI-only as `admin` (no rails/backend shortcuts, since this doubles as the Forge provisioning template): project Helpdesk QA Alpha, user `luna.blossom`, role Agent (workflow copied from Manager), Alpha project membership, SLA "Alpha Standard SLA", Support Level "L1", customer `alpha.customer`. Logged in as the customer and created ticket #1 — retested **BUG-HLP-014**: Tickets tab correctly showed "1 tickets" (previously would show "0 tickets"). Verified further: survives a full container restart (Redis/Sidekiq manually restarted, cron jobs re-registered), and a second ticket correctly updates the count to "2 tickets" — rules out both caching and a fluke single-row render. **Verdict: RETEST PASS.** Per explicit user direction ("closed the bug"), moved `bugs/open/BUG-HLP-014.md` → `bugs/closed/BUG-HLP-014.md` with a Closed section documenting that the High-severity full-suite regression normally required by `SENIOR_QA_STANDARDS.md` §26 was **not** run first — this is a deliberate, user-directed exception to that process, not an oversight, and the regression is still tracked as outstanding general suite coverage (see Next Session Start Point). Open bug count: 13 → 12. `HELPDESK_USERS_AND_CUSTOMERS.md` updated with a "CURRENT STATE" banner reflecting the rebuilt fixtures; all older fixture references (Beta project, other agents, `delta.customer`, `beta.customer`) are now stale/historical. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Retested BUG-HLP-001, then closed.** Navigated to New Customer via genuine click-through: no stuck "Login must be at least 2 characters long" error on fresh page load (the bug's defining symptom), none while typing a valid login, and the form submitted successfully on the first attempt (no retry needed, unlike the original session). Does not reproduce on Local. User explained a new Forge server pulls a fresh plugin clone on creation, so whatever caused the stuck-validation state on the original `flux-fiqll6jza49` instance doesn't carry forward — moved `bugs/open/BUG-HLP-001.md` → `bugs/closed/BUG-HLP-001.md` on that basis (root cause itself was never confirmed in code, closure rests on consistent non-reproduction + this explanation, not a confirmed code fix). Open bug count: 12 → 11. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Retested BUG-HLP-003, then closed.** Re-ran the full create/edit→uncheck→save→reload cycle for all three affected entities on the rebuilt environment: SLA ("Alpha Standard SLA", real fixture), Organization ("Alpha Org Retest", throwaway), Canned Response ("Retest Response BUG-HLP-003", throwaway). All three now correctly persist a deactivation — the Edit-form checkbox shows unchecked on reload, and each entity's list display (AJAX toggle for SLA/Organization, static badge flipping to "Inactive" for Canned Response) matches. Restored the SLA fixture back to Active, deleted both throwaway entities via the standard confirmation-modal delete flow. Closed per explicit user confirmation. Open bug count: 11 → 10. |
| 2026-08-27 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Retested BUG-HLP-004, then closed.** This bug needs two projects each with their own SLA/Support Level — Local only had Alpha, so rebuilt a second project from scratch: Helpdesk QA Beta (module enabled, Is Public unchecked), SLA "Beta Standard SLA", Support Level "AB-L1" (assignee `luna.blossom`, added as a Beta Member with role Agent first — the assignee picker only offers real project members). Inspected the Customer form's SLA Name/Support Level `<select>` option nodes directly via `getComputedStyle()` in both directions (Project=Alpha and Project=Beta): the cross-project option now correctly reports `display: "none"`, not `"block"` as originally found — the `hidden` attribute is no longer defeated by CSS. Kept Helpdesk QA Beta as a permanent fixture (not throwaway) for future dual-project TCs. Closed per explicit user confirmation. Open bug count: 10 → 9. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Retested BUG-HLP-005, then BUG-HLP-002, both closed.** Checked the bypass routes directly (admin backdoor-route testing, not a customer/agent journey, so direct URL navigation is the correct method here): `/rf_slas`, `/rf_slas/new`, `/rf_support_levels`, `/rf_support_levels/new` all now return a genuine HTTP 404 — the routes were removed from the router entirely, a stronger fix than the bug's own "Expected result" asked for. Confirmed the app itself was up and the admin session valid at the same time (ruled out a broader outage). Proper project-scoped flow re-confirmed working, now via a cleaner path-based URL (`/projects/1/rf_slas/new`, `/projects/1/rf_support_levels/new`) with no `project_id` form field needed at all (the URL path alone carries it). While on the properly-scoped Support Level form, also checked BUG-HLP-002's Escalation To dropdown — correctly scoped to Alpha's own support levels only (Beta's AB-L1 excluded), confirming BUG-HLP-002 is resolved as the side effect its own bug file predicted. Both closed per explicit user confirmation. Open bug count: 9 → 7. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), alpha.customer | **Retested BUG-HLP-006, then closed.** Second independent clean pass on Local, this time on the environment fully rebuilt earlier this session (zero shared history with the 2026-08-26 retest's fixtures). Logged in as `alpha.customer` via genuine click-through, clicked ticket #2's link from her own Helpdesk Tickets list — correctly resolves to the branded route (`/projects/helpdesk-qa-alpha/helpdesk/issues/2`), which the Customer role is authorized for; ticket opened with full content, no authorization error. The bug's own notes recommended a Forge retest before closing (root cause was a Forge-vs-Local route difference), but the Forge URL on file was already flagged stale — closed anyway per explicit user confirmation, resting on two independent clean Local passes rather than a confirmed Forge fix. Open bug count: 7 → 6. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom | **Retested BUG-HLP-007 — still FAILS, left open.** On the rebuilt environment (ticket #2, new agent `luna.blossom`), submitted an Internal Note via the branded route (`/projects/helpdesk-qa-alpha/helpdesk/issues/2`) — crashed with HTTP 500, confirmed via server log to be the byte-for-byte same failure as originally reported (`RfProjectHelpdeskIssuesController#update`, `private_notes => ""`, `ActiveRecord::NotNullViolation`). Core route (`/issues/2`) contrast check same session: Internal Note saved cleanly, correctly marked Private. This is a genuinely unfixed defect — same controller, same param shape, same exception. Bug stays open. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + luna.blossom + alpha.customer | **Retested BUG-HLP-008, then closed.** Required rebuilding the full email round-trip from scratch: Email Configuration for Helpdesk QA Alpha didn't survive the DB reset (created fresh: SMTP/IMAP `mail:587`/`mail:993`, mailbox `alpha.support@test.local`); also caught and fixed a stale precondition (Settings → General → Host name and path was wrongly `localhost:3000`). Mail server mailboxes themselves survived (separate container). Followed the original repro exactly: customer email → ticket #3 → agent Reply Note → customer's real threaded email reply (poller triggered manually via `rails runner` rather than waiting on the cron interval). **Part A (missing entry): PASS** — Helpdesk Conversion count went 3→4, the real reply is now correctly logged, server log shows a clean save instead of the original `MailHandler returned unexpected result: Journal` warning. **Part B (duplicate/mislabel): PASS** — all 4 entries unique and correctly attributed, no fabricated "From: Customer" content. Closed per explicit user confirmation. Open bug count: 6 → 5. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom + alpha.customer | **Retested BUG-HLP-009, then closed.** Created a fresh ticket #4 (Status: New) via `alpha.customer`'s portal, then `luna.blossom` opened it via genuine click-through (core `/issues/4` route), clicked the top-level Edit link (not Reply), added a non-private note, left Status untouched, submitted. Status remained "New" — confirmed 3 ways: the ticket page itself, no "Property changes" tab exists at all (zero field changes), and a clean side-by-side contrast in Luna's Activity feed (ticket #4 shows no "Waiting for Customer Response" tag, while ticket #3's genuine Reply Note from earlier this session correctly does). Noted a secondary, non-bug observation: unlike the original repro, this note is now visible in the customer's own Notes tab — normal Redmine behavior for a non-private note, not a defect, and not part of this bug's scope. Closed per explicit user confirmation. Open bug count: 5 → 4. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom + alpha.customer | **Filed BUG-HLP-015** (Medium) from a user-reported observation, verified live before filing. Two related findings on tickets #3/#4 (reused from the BUG-HLP-008/009 retests): (1) a customer session's ticket view always exposes the standard "Notes" tab, not just "Helpdesk Conversion" — confirmed on a ticket with no email history (Notes is the only tab) and one with full email history (both tabs shown side by side); per explicit user direction, only Helpdesk Conversion should be customer-facing. (2) a customer's real email reply is logged twice — correctly in Helpdesk Conversion, but also as a plain Journal visible under Notes to agents/admins, unlike an agent's own Reply Note which is correctly excluded from Notes entirely (confirmed via BUG-HLP-009's retest). The customer's own Notes tab view doesn't show this duplicate (some suppression exists for her own session), but the tab itself is still exposed and the duplicate is visible to any agent. Not yet fixed — left open for triage on whether to hide the tab, stop the duplicate Journal, or both. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Retested BUG-HLP-010, then closed.** Created "Alpha Org" (Website `https://acme.example.com`, Phone `+1 (555) 123-4567`), then attempted a second, differently-named organization with the exact same Website and Phone. Submission was **refused outright** — "Phone number has already been taken" / "Website has already been taken" — stronger than the bug's own minimum ask (a soft warning). Deleted the throwaway "Alpha Org" afterward. Closed per explicit user confirmation. `HELPDESK_USER_GUIDE.md` §3.4 and TC-HLP-096 still describe the old permissive behavior and need their own updates (tracked separately, not blocking closure). Open bug count: 5 → 4. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Retested BUG-HLP-011, then closed.** Created a fresh Organization "Gamma Corp", linked it to `alpha.customer`'s existing Helpdesk QA Alpha project-access row, confirmed User Count = 1, then clicked Delete. The confirmation modal is no longer generic — it now reads "This organization has 1 linked customer(s) — deleting it will remove that association," exactly the consequence-aware warning the bug asked for. Confirmed the deletion: `alpha.customer`'s Organization reference cleared to "—" gracefully, SLA/Support Level preserved, matching the original repro's "graceful, not a crash" finding — just with the warning shown first now. Closed per explicit user confirmation. Open bug count: 4 → 3. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Retested BUG-HLP-013, then closed.** Checked whether `/rf_customers/:id/portal` (and its `?project_id=` variant) still resolves for a fresh customer (`alpha.customer`, Customer ID 6, zero shared history with the original `beta.customer` repro) — both now return a genuine HTTP 404, confirmed the app itself was up and the admin session valid at the same time. Customer 360 still correctly shows no "View portal" entry point either way, now moot since the destination is gone. Matches the first option in the bug's own Recommendation (formally remove the route). TC-HLP-056 now genuinely passes as written. Closed per explicit user confirmation. Open bug count: 3 → 2. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + luna.blossom + alpha.customer | **Started a full from-scratch re-run of `HELPDESK_TICKET_LIFECYCLE.md`** (per explicit user instruction, since the post-DB-reset environment makes prior "CONFIRMED LIVE" markers stale even though the text remains) — added a suite-wide "Navigation methodology" section and a TC-HLP-366-specific step to the test case file itself, per explicit user instruction, after being caught typing a ticket's URL directly instead of clicking through. **TC-HLP-365/014/015/016/017/018/289/020/021 all re-executed live, 9/32 done**: TC-013/014 (both ticket-creation paths) — PASS, ticket #5/#6. TC-015 (customer sees only her own tickets) — PASS, ticket #7, correctly showed exactly her 4 tickets, no BUG-HLP-014 regression. TC-016 (fresh qualifying email creates a ticket) — PASS, real Roundcube round-trip, ticket #8, Helpdesk Conversion 2-entry shape matches original evidence. TC-017 (threaded email reply lands as a note) — **now PASSes on both halves** (previously only half-passed due to BUG-HLP-008): real threaded reply via the ticket's own confirmation email, no `MailHandler returned unexpected result` warning, Helpdesk Conversion went 2→3. TC-018 — PASS, reused this session's own fresh evidence. TC-289 — PASS, reused this session's own BUG-HLP-009 retest evidence. TC-019 (agent Reply Note → Waiting for Customer Response) — PASS, ticket #8, Helpdesk Conversion (3)→(4). TC-020 (customer reply → In Progress) — PASS, via the portal this time (not email), Helpdesk Conversion (4)→(5). **TC-021 (reply auto-assign + SLA pause on an unassigned ticket) — FAILS**, a genuine regression from its own 2026-08-27 evidence: Status still auto-transitions correctly, but Assignee stays "-" and no SLA Information tab ever appears, even though "Alpha Standard SLA" exists and is Active. Server log shows the assignment logic (`ReplyAssignment`) computing and logging success, but the actual field never persists (`assigned_to_id changed?` false on the same request) — diagnosed via source inspection to most likely be `Issue#safe_attributes=` silently filtering the field. Reproduced on two independent tickets (#7, #9). Filed **BUG-HLP-016** (High). Session-wide finding: zero tickets today log `SLA: true`, meaning no ticket in this environment can currently start an SLA clock at all. Open bug count: 2 → 3 (BUG-HLP-007, 015, 016). Resuming at TC-HLP-376 next. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + luna.blossom | **Retested BUG-HLP-016, then closed.** User identified the actual root cause directly on the Agent role's own permissions page: the per-tracker "Issue tracking" matrix had Support tracker's own **Edit issues** checkbox not granted — this session's earlier from-scratch role setup had only used "All trackers," which turned out not to cover the tracker-specific row. User corrected the permission. Retested on a fresh unassigned ticket **#10**: Reply Note → Save → Assignee correctly changed to **Luna Blossom**, Status → Waiting for Customer Response, and a new **SLA Information** tab appeared showing "⏸ Paused" — matching the original 2026-08-27 evidence exactly. Confirmed this was environmental, not a plugin code defect (corrected the bug file's earlier `safe_attributes=` theory accordingly). Closed per explicit user confirmation. **New standing note for future environment rebuilds: when creating the Agent role, explicitly check both the general Edit Issues permission AND the Support tracker's own Edit Issues checkbox in the per-tracker matrix — "All trackers" alone is not sufficient.** Open bug count: 3 → 2 (BUG-HLP-007, BUG-HLP-015 remain open). |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), luna.blossom | **Continued `HELPDESK_TICKET_LIFECYCLE.md` re-run: TC-HLP-376/023/024 confirmed, TC-HLP-379/032/041 blocked.** TC-022 (Internal Note stays private, no status/SLA effect): PASS — fresh ticket #11 assigned + moved to In Progress via Edit (not Reply) to get a genuinely active/unpaused SLA precondition, then an Internal Note kept Status/SLA unchanged and landed correctly marked Private on the Notes tab, no Helpdesk Conversion entry. TC-023 (10-min preset time log): PASS, `Spent time: 0:10 h`. TC-024 (custom 35-min time log, first-ever live execution): PASS — isolated via the Spent Time report to a distinct `0:35`/Customer Call row, cumulative ticket total correctly `0:45 h`. **Found BUG-HLP-017** (Medium) while attempting TC-025: the documented "Merge duplicate tickets" feature (Feature #17, User Guide §7.4) has no discoverable control anywhere — checked both an individual ticket's Actions menu (only "Copy link") and the complete multi-select context menu for two selected tickets (13 items, no Merge) — created tickets #12/#13 as ready-made duplicate fixtures for a future retest. TC-025, TC-032, and TC-041 all recorded as FAIL/BLOCKED on this one bug rather than filed separately. Open bug count: 2 → 3 (BUG-HLP-007, BUG-HLP-015, BUG-HLP-017). Resuming at TC-HLP-380 next. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + luna.blossom + alpha.customer + beta.customer + qa@test.local | **Continued `HELPDESK_TICKET_LIFECYCLE.md` re-run — TC-028/029/030/031/033/034/035/036/037/038/295 confirmed, TC-026/291/027 confirmed, TC-039 found a new bug (18/32 done, only TC-039's own suite-position and TC-040 remain).** Negative time-log cases (TC-028/029): both PASS — a client-side alert refuses Save when Activity is missing or a custom value is non-numeric, confirmed nothing was submitted either time (Spent time unchanged, no new journal). TC-030 (mutual exclusion): PASS, confirmed via the real single `<select>` control's own behavior. TC-031 (reply with no time selected): PASS, reply posted, no time entry created. **Permission-boundary TCs required new fixtures, all first-ever live executions**: created user `no.perm.reporter` with Redmine's default **Reporter** role (confirmed zero helpdesk permissions) to pass TC-033 (no Helpdesk tab, `/helpdesk/new` 403s) and TC-034 (`alpha.customer` correctly 403s on Helpdesk QA Beta, which she has no entitlement to — "My Helpdesk" doesn't even list it); created a new role **"Agent No LogTime"** (`view_helpdesk` on, Redmine's `log_time` off) and reassigned it to the same fixture user to pass TC-036 (reply form renders fully minus the Time spent block entirely). TC-035 (customer cannot merge) and TC-037 (customer reply-box UI) both re-confirmed on ticket #8. TC-295 (customer Edit = Subject only) re-confirmed on ticket #8, including a real modify-and-revert round-trip. TC-038 (7 statuses + workflow transitions): PASS — walked ticket #11 from Waiting for Customer Response through Resolved → Feedback → Closed as `luna.blossom`, confirmed the dropdown's own available options genuinely change per step (workflow-gated, not display-only). **Negative email cases (TC-026/291/027), all first-ever live executions, sent as real emails and confirmed via server log, zero tickets created for any**: TC-026 (`qa@test.local`, fully unregistered) → "ticket creation blocked" + unregistered-user notification sent. TC-291 (`beta.customer`, registered only for Beta, emailing Alpha) → resolves a previously-open question: the poller's customer lookup is genuinely scoped per-project, so she's treated identically to a total stranger relative to Alpha's mailbox. TC-027 (`alpha.customer`, keyword-free email) → "Email does not contain required keywords... ticket creation skipped," a distinct rejection path from the registration checks. **Found BUG-HLP-018** (Low/Medium) while executing TC-039: with Comment marked Required for time logs, the reply form's Comment field is entirely absent on first load (not just gated behind picking a time preset) — Save throws "A comment is required on time entries" with no way to comply, but the field becomes visible immediately after that failed attempt and a second Save succeeds; not data-destructive, but contradicts the TC's own "field proactively appears" expectation. Setting restored to its original state afterward. Open bug count: 3 → 4 (BUG-HLP-007, 015, 017, 018). TC-HLP-396 (customer granted `view_private_notes`) not yet attempted — flagged as architecturally unclear, since Customers use `RfProjectCustomer` entitlement, not core Redmine Membership/Role, so it's untested whether a Role-based permission grant even applies to a Customer session at all. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + alpha.customer | **`HELPDESK_TICKET_LIFECYCLE.md` re-run complete — all 32 TCs re-executed.** Resolved TC-HLP-396 by direct experiment rather than leaving it unattempted: added `alpha.customer`'s genuine underlying Redmine User (ID 6) as an actual project **Member** of Helpdesk QA Alpha with the "Agent No LogTime" role (after granting it **View private notes**), on top of her existing Customer entitlement. Result: her branded ticket list still doesn't surface tickets outside her own `RfProjectCustomer` scope (Membership grants no extra visibility there), and — more decisively — navigating directly to the **core** `/issues/11` route (the only way to isolate the permission itself) returned **"You are not authorized to access this page"**, flatly denied despite the granted permission. **Confirms `is_helpdesk_customer?` sessions are unconditionally blocked from core issue routes regardless of any Role/permission also held** — TC-040's assumed mechanism doesn't apply to Customer sessions at all; not a defect, a stricter security boundary than the TC assumed. Cleaned up the diagnostic Membership afterward (confirmed back to "No data"). **Final suite tally**: 32/32 TCs executed. Open bugs from this suite: BUG-HLP-007 (pre-existing), BUG-HLP-017 (Merge feature undiscoverable, blocks TC-025/032/041), BUG-HLP-018 (Comment field UX, TC-039). BUG-HLP-015 remains open from earlier in the session (different suite). |
| 2026-09-01 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + retest.customer1 | **`HELPDESK_SLA_ESCALATION.md` completed — all 22 remaining TCs executed, suite now 58/58.** SLA/Support Level CRUD (TC-078/315/316/325) confirmed via a disposable "Alpha CRUD Test SLA(-Full)" pair. Negative/validation cases (TC-099/100/101/103) all confirmed refused with clear messages (duplicate name, zero-assignee). TC-098 found the literal precondition ("customer with no SLA") unconstructable — SLA and Support Level are jointly hard-required on a project-access row, refining the existing Known Quirk. TC-102 (cross-level assignee exclusion) and TC-106 (second-project dropdown independence) both confirmed via direct `<select>` inspection. TC-104 (deactivate SLA) PASS. **TC-105 (deactivate Support Level) FAIL** — a ticket escalated straight into a deactivated L2 exactly as if active; filed **BUG-HLP-021**, directly contradicting the user guide's "Inactive levels are not offered and are not escalated into." TC-276/277 (list filters) both PASS on every condition including combined search+status. Delete guards: TC-296 (in-use SLA) correctly refused server-side ("assigned to one or more issues"); **TC-297 (in-use Support Level) FAIL** — first attempt used a stale precondition (`beta.customer` doesn't exist on this environment anymore, so the original target level had zero real dependents), redone properly with a disposable level genuinely attached to `alpha.customer` — deletion succeeded silently and orphaned her row (SLA still set, Support Level reset to None, an otherwise-unreachable invalid combination); filed **BUG-HLP-022**. TC-304 (unused SLA) PASS. **TC-308 (Holiday delete) half-FAIL** — unattached holiday deleted cleanly, but one attached to an SLA also deleted silently (degrades gracefully to "-", not a broken reference); filed **BUG-HLP-023**. TC-083/084/107 (working-hours/holiday clock math) all confirmed via purpose-built substitute scenarios (real time is a Tuesday, so the literal Friday/Monday case couldn't be produced): "Alpha Working-Hours-Skip Test SLA" (Working Days = Friday only) proved a multi-day skip landing exactly on the next Friday's window open; "Alpha Holiday-Skip Test SLA" (24/7 hours, one attached holiday for the next day) proved the holiday was fully excluded from the deadline math (computed deadline landed a full day later than the naive calculation would). **Suite total: 5 new bugs this pass (BUG-HLP-019 revised, 020, 021, 022, 023), all still open.** Admin's Mail field fix (from the prior session) re-verified still in place. |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (documentation correction, per explicit user direction) | **Rewrote TC-HLP-396 itself**, not just its evidence note — user flagged that the TC's own Precondition/Steps ("grant `view_private_notes` to the customer's role") wrongly implied a customer could/should ever be added as a project Member with a Role; a customer only ever authenticates through the plugin's own separate portal view. Retitled to "A customer's portal session can never see internal or private notes, under any configuration," rewrote the Expected Result to state this as an absolute boundary (no internal note, no privately-flagged note, ever, regardless of any Role permission), and reframed the Membership-grant step as an explicit non-supported boundary check rather than a real configuration. The already-gathered live evidence didn't need to be re-collected — it already showed exactly this outcome — so the CONFIRMED LIVE note was reworded from "FAIL as literally written" to a plain **PASS** against the corrected expectation. |
| 2026-09-01 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + autumn.grace + retest.customer1 + no.perm.reporter | **`HELPDESK_SLA_ESCALATION.md` live execution — real 3-level chain built and driven end-to-end, 2 new bugs, admin-email fix.** Built a genuine L1→L2→L3 escalation chain on Helpdesk QA Alpha using 3 new agent fixtures (`autumn.grace`/`briar.sunset`/`willow.belle`, `Test@12345`) and a fast-breach SLA ("Alpha Escalation Test SLA", 1-min Response/Resolution, no working-hours restriction). Confirmed live via real Sidekiq monitor runs and the previously-undocumented "SLA Information" tab (SLA Overview/Journey/Activity Log): TC-075/076/079/080/081/085/087/088/089/090/091/092/094/095/096(partial)/097/300/301/302/303/305/306/307/317/318/319/320/327/329/299 all PASS, TC-093 FAIL (**BUG-HLP-019**: escalation email never states which deadline breached — response or resolution — per `HELPDESK_USER_GUIDE.md`'s explicit content spec; verified via the real recipient's Roundcube inbox; an initial broader claim that it should also name the previous assignee was raised then dropped same day after the user asked "where does it say this in the docs?" — the guide only promises the previous/new *level*, which is genuinely present), TC-086 FAIL (**BUG-HLP-020**: reassigning after unassigning resumes the SLA clock's display but doesn't credit back the paused duration, so a briefly-unassigned ticket goes instantly overdue and escalates as a real side effect). **Found and fixed a real environment defect along the way**: the Redmine admin user's Mail field was still the out-of-the-box `admin@example.net` placeholder, silently breaking every Critical Breach Alert — fixed to `admin@test.local` and re-verified end-to-end (fresh ticket #17, breach → Sidekiq log shows correct recipient → email confirmed actually present in her real inbox). TC-301's open question (does the top-level-breach fallback reassign to Admin, and does Admin get notified) is now fully answered: yes, on the same monitor cycle, once, with a distinct "Critical SLA Alert" template. `HELPDESK_REQUIREMENTS.md` updated with the SLA/Support-Level-target decoupling finding from TC-300. **Added TC-HLP-310/331** (user-requested, after being asked to explain how a directly-entered mid/last-level ticket's escalation was verified) making explicit and traceable on their own what had only been byproduct evidence before: TC-330 confirms a ticket entering directly at a mid-level (never visited L1) still escalates to the next level exactly like a naturally-arrived one (reusing ticket #15's BUG-HLP-020 log evidence); TC-331 confirms a ticket entering directly at the top level doesn't attempt further escalation and correctly hits admin fallback (reusing ticket #17's evidence). Both PASS. Confirmed a real, sensible (non-bug) Support-Assignees-picker exclusivity rule and a static (non-refreshing) Escalation-To dropdown behavior — both written into `HELPDESK_MEMORY.md`. `HELPDESK_USERS_AND_CUSTOMERS.md` NOT yet updated with the new fixtures — still pending. Open bug count: 3 → 5 (BUG-HLP-007, 015, 017 pre-existing + new 019, 020). |
| 2026-08-31 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Retested BUG-HLP-018 per user's manual check ("not reproduced"), then closed as a false positive.** Re-enabled Comment-required, opened a fresh Reply form on ticket #11 with no prior failed Save attempt — the `Comment *` field was present as a textbox from the very first snapshot. Cross-checked directly against the DOM via `browser_evaluate` (bypassing the accessibility-snapshot text search the original finding relied on): `#rf-reply-time-comment` has `display: block`, `visibility: visible`, a real on-screen bounding rect, `opacity: 1` — genuinely visible and interactive. Completed the full flow in one clean attempt (typed reply, filled Comment, picked Activity, Save) with no alert and no retry — Spent time correctly went 0:55h → 1:05h. **Root cause of the original false positive**: the very first check's `Grep` over a Playwright accessibility-snapshot capture missed this real, rendered label+input pair on that one capture — a snapshot/timing artifact, not a rendering gap; every subsequent snapshot and the direct DOM query show it correctly. Moved `bugs/open/BUG-HLP-018.md` → `bugs/closed/BUG-HLP-018.md` with a Retest + Closed section documenting this. Setting reverted to its original (Comment not required) state afterward. `HELPDESK_TICKET_LIFECYCLE.md` TC-HLP-395 needs no change — its Expected Result was correct all along. Open bug count: 4 → 3 (BUG-HLP-007, BUG-HLP-015, BUG-HLP-017 remain open). **Lesson for future sessions: before filing a "field/control is completely absent" bug from an accessibility-snapshot text search, cross-check via a direct DOM query (`browser_evaluate` + `getComputedStyle`) or at minimum a second fresh snapshot, since a single snapshot capture can occasionally miss a real, visible element.** |
| 2026-09-01 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Scenarios 1 and 5 corrected per explicit user UX-judgment direction — 2 new bugs filed (BUG-HLP-024, BUG-HLP-025).** User pushed back on both "not a bug" verdicts from the same-day 5-scenario audit below. **Scenario 1 / TC-HLP-312**: redone via the ticket's standard Edit form (`/issues/22/edit`, per explicit instruction to stop using the inline quick-editor) — the Assigned-to `<select>` shows every candidate by name only, no Support Level indicator, no confirmation on Submit; selected `luna.blossom` (L1) for the L3 ticket, saved cleanly, SLA Information tab still read "L3" afterward with zero warning anywhere. Filed **BUG-HLP-024** (Medium). TC-HLP-312 rewritten (title + Expected Result now assert the warning requirement directly) with a Revision History section. **Scenario 5 / TC-HLP-351**: user clarified the real objection was narrower than the original analysis assumed — not "is skip-level escalation illegitimate" but "why does Escalation To need manual configuration at all when Level Order already defines the ordinary sequential chain." Re-confirmed live: `Level Order` and `Escalation To` are two fully independent required fields on the Support Level form, Order only restricts Escalation To's valid options, never auto-derives it. Filed **BUG-HLP-025** (Medium). TC-HLP-351 rewritten to test and assert this corrected requirement, also with a Revision History section. Took fresh screenshots for both (`screenshots/BUG-HLP-024/`, `screenshots/BUG-HLP-025/`) since neither had evidence captured at the time of the original "not a bug" pass. Saved a new persistent cross-session memory (`feedback_ux_judgment_overrides_documented_contract_bar`): a "no documented contract violated" verdict is a starting read, not final — once the user asserts their own UX/product judgment, correct the TC and file the bug rather than defending the original framing. Open bug count: 8 → 10. |
| 2026-09-01 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + retest.customer1 + luna.blossom + autumn.grace | **BUG-HLP-024 broadened to a full 3-path scenario per detailed user follow-up request — 7 new TCs (TC-HLP-315 through 348), no new bug ID (broadened existing BUG-HLP-024).** User asked for a fresh, unassigned L2 ticket to be tested with an L1 agent both self-assigning it and replying to it (auto-assign), plus a matching-level control and Cancel/Confirm acceptance checks. Created 3 fresh tickets as `retest.customer1` (L2 baseline, unchanged): #24 (self-assign), #25 (reply-action), #26 (matching-level control). **TC-HLP-313**: confirmed a fresh ticket is unassigned by default (Assignee "-"). **TC-HLP-314**: confirmed a sharp new finding — a fresh/unassigned ticket has **no Support Level indicator anywhere**, and the SLA Information tab itself doesn't exist yet (only appears after first assignment) — an agent has no way to self-check the level beforehand. **TC-HLP-315 (self-assign) and TC-HLP-316 (reply-action) both FAIL** — `luna.blossom` (L1) self-assigned ticket #24 and separately replied to ticket #25 (auto-assigning her via a genuinely separate plugin mechanism), both with zero warning of any kind; both tickets' SLA Information tabs read "L2" once they appeared. **TC-HLP-317 PASS** (control) — `autumn.grace` (L2) self-assigning ticket #26 (also L2) correctly shows no warning, matching level. **TC-HLP-318/348 (Cancel/Confirm on the not-yet-implemented dialog) recorded as Blocked**, not faked as PASS — no confirmation dialog exists in the current build for either button to be tested. Broadened `BUG-HLP-024.md` (title, Steps, Actual result, Evidence, new Revision History section) rather than filing a new bug ID, since all 3 paths share one root symptom (no validation layer checks assignee level vs. ticket level before finalizing any assignment) — same logic as BUG-HLP-022's earlier broadening. Took 2 new screenshots (`bug-hlp-024-ticket24-self-assign-l1-to-l2-no-warning.png`, `bug-hlp-024-ticket25-reply-auto-assign-l1-to-l2-no-warning.png`). `retest.customer1`'s baseline (L2) was not touched this pass — used as-is. Open bug count unchanged at 10 (BUG-HLP-024 broadened, not new). |
| 2026-09-01 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + retest.customer1 + luna.blossom | **User-specified 5-scenario coverage audit — all 5 live-tested and written up, 0 new bugs at the time (2 later corrected to bugs — see the row above, out of date order since the correction was appended after this row was already written).** User asked whether TCs already existed for 5 specific scenarios; none did, so each was live-tested fresh. **Scenario 1 (TC-HLP-312, new)**: `luna.blossom` (L1-only) self-assigned ticket #22 sitting at Support Level L3 (previously Willow Belle) via the plain inline Assignee editor — succeeded instantly, zero warning/block/confirmation, dropdown offered every project member unfiltered by level; the ticket's own Support Level stayed "L3" afterward (didn't follow the new assignee down). Not a bug — the guide never claims Assignee is level-gated; flagged as an open design question rather than filed. **Scenario 2 (TC-HLP-320, new)**: built a disposable deactivated SLA ("Alpha Deactivation Escalation Test SLA") on `retest.customer1`'s project-access row, had her create ticket #23 — it got created successfully but with **zero SLA attached at all** (no SLA Information tab, no deadlines, no auto-assignment), matching `HELPDESK_USER_GUIDE.md`'s own Troubleshooting section verbatim ("SLA Information panel missing" → cause: "the SLA is inactive"). Manually assigned it to `luna.blossom` and ran the SLA monitor directly — ticket #23 never appeared in the log at all (4 other real breaches processed that cycle), confirming manual assignment can't resurrect a clock that was never created; all three of the user's sub-questions collapse to this one root cause. Matches documented behavior — not a bug. **Scenario 3**: already fully answered by existing BUG-HLP-021 + TC-HLP-349 Step 3 — sharpened that TC's evidence wording today to state explicitly: a ticket does **not** skip a deactivated mid-chain Support Level, it lands directly on it. **Scenario 4 (TC-HLP-064, reactivation half added)**: the 2026-08-27 evidence only proved deactivation hides an Organization from the New Customer dropdown; today explicitly re-verified reactivation reappearance too, using a disposable "Alpha Reactivation Test Org" — active → offered, deactivated → excluded, reactivated → reappears, each confirmed via a fresh page load. Deleted the disposable org afterward. **Scenario 5 (TC-HLP-351, new)**: resolved directly from the live product's own UI — L1's real Escalation To dropdown offers both L2 and L3 (not just the next level), with inline help text confirming non-linear/skip-level escalation chains are intentional ("Level Order 1 can escalate to Level Order 2 or 3"). Not redundant with Level Order, not a UX bug — Order is the eligibility/ranking rule, Escalation To is the actual routing pointer. One minor doc-wording gap noted (not filed): the guide's own "Escalates to: the level above" phrasing undersells how permissive the real field is. `retest.customer1`'s Support Level/SLA were temporarily flipped twice this session (for Scenarios 1 and 2) and explicitly restored to her L2/"Alpha Escalation Test SLA" baseline both times. |
| 2026-09-01 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + alpha.customer | **TC-HLP-353 (new): locked-agent escalation exclusion — confirmed working correctly, no bug, one cosmetic gap noted.** User asked whether a locked agent's account is protected from being assigned by escalation, and whether locking removes them from the Support Level's own assignee list. Locked `briar.sunset` (one of L2's two Support Assignees, alongside `autumn.grace`) via Administration → Users → Lock. **L2's Edit form still showed her checked ("2 selected")** — locking has zero automatic effect on Support Level configuration, a real but low-impact config-drift gap. Created 3 fresh L1 tickets (#27/#28/#29) via `alpha.customer`, assigned each to `luna.blossom`, let all three breach, triggered the SLA monitor (2 cycles needed to cross all three deadlines) — **all 3 L1→L2 escalations landed on `autumn.grace`, never once on the locked `briar.sunset`**, despite her still being nominally listed as eligible. Confirmed the same exclusion holds for manual assignment too: ticket #27's Assigned-to dropdown lists only the active agents, `Briar Sunset` is entirely absent from the option list — same active-user scoping Redmine applies everywhere, which the plugin's escalation logic evidently respects rather than naively iterating the Support Level's raw list. Not a bug — the actual functional guarantee holds completely via both paths; only the Support Level's own display is stale. Unlocked `briar.sunset` afterward (confirmed via the action link flipping back to "Lock") to restore the L2 chain's normal two-agent state. |
| 2026-09-01 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **BUG-HLP-026 filed per explicit user direction ("so create bug"), correcting TC-HLP-353's earlier "not a bug" verdict.** Re-locked `briar.sunset` briefly to capture proper screenshot evidence (none had been taken during the original TC-HLP-353 pass): confirmed her account genuinely locked (Administration → Users, action link reads "Unlock"), then screenshotted L2's Edit form still showing "2 selected" with both `Autumn Grace` and `Briar Sunset` checked, and the Support Levels list view's own Support Assignees column still plainly reading "Autumn Grace, Briar Sunset" — no lock indicator anywhere on either screen. Filed **BUG-HLP-026** (Low): not a functional/security defect (the locked agent is genuinely, correctly excluded from ever being assigned, confirmed both ways in TC-HLP-353), but a real configuration-visibility gap — an admin reviewing Support Level coverage has no on-screen way to tell one of the listed assignees can never actually receive a ticket, risking a false sense of redundant coverage. Unlocked `briar.sunset` again afterward to restore the L2 chain's normal state. TC-HLP-353 updated with a Revision History section. Third correction this session of a "not a bug" verdict after explicit user pushback (alongside BUG-HLP-024/025) — reinforced the standing `feedback_ux_judgment_overrides_documented_contract_bar` memory with this data point. Open bug count: 10 → 11. |
| 2026-09-01 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Full-suite coverage audit, prompted by a direct user question ("are you tested this suite completely??").** Re-ran the established coverage script (every TC header has a CONFIRMED LIVE marker) — 69/69 confirmed present, as before. This time went further: classified every marker's actual verdict (53 PASS / 10 FAIL / 2 BLOCKED / 3 legitimate exploratory findings with no PASS/FAIL wording by design — TC-098/346/349, all individually re-read and confirmed genuine) and manually re-checked the 15 shortest evidence blocks in the file. **Found one real gap: TC-HLP-341** ("Holiday list search and Apply/Clear Filters work correctly") had a marker from 2026-08-24 whose evidence never actually tested Search/Apply/Clear — it only recorded an unrelated fact (no Status dropdown exists for Holidays). Confirmed as a genuine outlier by diffing against sibling TCs TC-HLP-339/277 (identical filter pattern on SLA/Support Level), which had real, complete, matching evidence. Re-ran TC-278 properly live: searched "Winter Break" against the 2 real holidays, correctly narrowed to 1; Clear correctly restored both. Rewrote the TC with a Revision History section, keeping the original (still-true) filter-bar observation as a separate preserved note. Saved a new persistent memory (`feedback_verify_evidence_content_not_just_marker_presence`): a marker-presence check proves every TC was touched, not that its evidence answers its own Expected Result — spot-check content (verdict classification + shortest-evidence-block review + sibling-TC diffing) whenever asked directly about completeness. **`HELPDESK_SLA_ESCALATION.md` is now genuinely, thoroughly verified 69/69** — no other gaps found in this pass. |
| 2026-09-02 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **`HELPDESK_CUSTOMERS_ORGANIZATIONS.md` genuinely closed out, 30/30 — corrects a prior handoff claim of "24/24 complete" that was actually 24/30.** A "which suite next" check (prompted by a direct user question) surfaced 6 untested TCs the prior handoff note had missed: TC-HLP-038/336/337 (organization create/edit field-persistence edge cases) and TC-HLP-044/334/323 (customer edit/create field-persistence and password-survival edge cases). All 6 executed live and written up with `CONFIRMED LIVE` evidence — **all PASS, zero new bugs.** TC-333/334 adapted their stale preconditions (naming `beta.customer`/`gamma.customer`, neither of which exist on this environment since the 2026-08-27 DB reset) to use `alpha.customer` instead, and required rebuilding Beta's deleted "AB-L1" Support Level to unblock the cross-project change. TC-334 and TC-333 were both verified via real sign-out/sign-in cycles proving password/login behavior, not just form-level assumptions. New fixtures kept: Organization id 8 "Alpha Minimal Fields Test Org", Organization id 9 "Alpha Full-Fields Test Org (All Changed)", Customer id 14 `delta.customer`. `alpha.customer` was temporarily fully mutated by TC-334 (Login/Email/Password/Project Access) then explicitly restored to her exact original baseline immediately after, verified via a real login, since she's referenced across many other suites. Re-ran the coverage script post-writeup — confirmed genuinely 30/30, no missing evidence. Also re-surfaced (not actioned) the still-open `HELPDESK_NAVIGATION_WORKSPACES.md` TC-274/275 backfill gap (real evidence exists only in `HELPDESK_MEMORY.md`'s addendum #4, never copied into the TC file itself, so that suite still shows 0/17 in-file). Open bug count unchanged at 11. |
| 2026-09-02 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + `luna.blossom` + `zero.perm.user` + `manage.helpdesk.test` | **`HELPDESK_NAVIGATION_WORKSPACES.md` genuinely closed out, 17/17 — 2 new bugs, surfaces that the plugin's real Agent permission tier is split in a way the guide doesn't document.** Backfilled TC-274 (real evidence previously sat only in `HELPDESK_MEMORY.md`) and discovered TC-275 (its project-scoped sibling) had actually never been executed despite an earlier handoff implying both were done — executed it live this session. Central finding, confirmed via reading `init.rb` and `rf_project_helpdesk_controller.rb` source, not just observation: a `view_helpdesk`-only Agent (the real fixtures `luna.blossom`/`autumn.grace`/`briar.sunset`/`willow.belle`) never gets "Helpdesk" in the top application menu at all (menu identical to a genuinely no-permission user, confirmed via new fixture `zero.perm.user`), and only sees 3 of the project Helpdesk tab's 6 sub-tabs (SLA/Organization/Settings all 403, even by direct URL) — both gates are deliberate `manage_helpdesk`-only checks in the source, contradicting the guide's own "manage_helpdesk (or view_helpdesk)" Agent definition. Filed as **BUG-HLP-027** (Medium, framed as a doc-vs-code discrepancy needing a product decision, not presuming which side is wrong — same framing as BUG-HLP-013). Created a new fixture, `manage.helpdesk.test` (role "Agent ManageHelpdesk Test", since old `ivy.sterling` didn't survive the 2026-08-27 reset), to confirm the full-permission path: 7-item Command Center rail, all 6 project sub-tabs, top-menu link all present and working. **Second bug, BUG-HLP-028** (Low): the Command Center's actual chrome doesn't match the guide either — confirmed via direct DOM inspection that the header reads "Helpdesk" not "Helpdesk Support", and Redmine's application menu is NOT hidden (still `display: flex`, real `offsetParent`) — cosmetic only, the icon rail itself works correctly. TC-HLP-150 got a Revision History (not a new bug) — its own steps describe the exact bypass routes BUG-HLP-005 already fixed by removing entirely (both now 404). TC-HLP-154 needed a first-ever prepaid budget on Alpha to actually render its table (added 10h, confirmed the section appears correctly — was a data gap, not a defect). TC-HLP-158/074's paging-specific claims are honestly BLOCKED (all Settings-style lists have ≤3 real rows, no page 2 reachable without fabricating bulk dummy data). New reusable fixtures: `zero.perm.user`, `manage.helpdesk.test` + its custom role, Product id 1 "Nav Test Product", Alpha's new 10h prepaid budget. Open bug count: 11 → 13. |
| 2026-09-02 | Local (redmine-docker-6, localhost:3012) | Local | Claude, per explicit user correction | **BUG-HLP-027 retracted as Not a Bug, same day it was filed.** User corrected TC-HLP-147's own precondition from "`view_helpdesk` **or** `manage_helpdesk`" to "`view_helpdesk` **and** `manage_helpdesk`" — the real, intended requirement for the top-menu link (and the project-level SLA/Organization/Settings tabs) is the combination, not either permission alone. This resolves BUG-HLP-027's own open judgment call ("filing this either as 'expand the code' or 'fix the docs' is a genuine judgment call") in favor of: **the code is correct as written, the documentation was wrong.** Closed and moved to `bugs/closed/BUG-HLP-027.md` after the user picked "Retract as Not a Bug" from 3 offered options. Corrected `HELPDESK_USER_GUIDE.md` §4's Agent definition and its tester checklist (§26 group B) directly to state the real requirement — no new bug ID filed for the doc-wording fix. TC-HLP-147/065/070's evidence in `HELPDESK_NAVIGATION_WORKSPACES.md` rewritten to reflect the corrected precondition: TC-060 now shows PASS (verified via `manage.helpdesk.test`, who holds both permissions), with `luna.blossom`'s view_helpdesk-only behavior kept as an informational note rather than the main verdict. Open bug count: 13 → **12**. A good example of this engagement's standing lesson working in reverse — a TC precondition correction can retract a bug just as validly as it can create one (c.f. BUG-HLP-024/025, which went the opposite direction on 2026-09-01). |
| 2026-09-02 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `zero.perm.user` + `luna.blossom` | **BUG-HLP-029 filed (High) — a genuine data-exposure bug, found by directly answering the user's question "did you verify... he can not able to navigate through url" rather than treating a hidden menu link as proof of a blocked URL.** Tested every Command Center rail URL directly for both `zero.perm.user` (0 permission) and `luna.blossom` (`view_helpdesk` only): Reports/Organizations/Customers/Products/Settings all correctly 403 for both tiers, matching the menu's own hidden state. **One exception**: the global `/helpdesk` dashboard itself loads for `zero.perm.user` (a user with zero project membership or permission anywhere), and its "Recent Tickets" section renders 8 real Helpdesk QA Alpha tickets — real subjects, project name, assignee names, SLA breach status — while the same page's 5 KPI cards correctly read 0 for her. **Root cause confirmed via source** (`rf_helpdesk_controller.rb#index`): the `issues` relation feeding the Recent Tickets list and its breakdowns is built via `Issue.where(project_id: @helpdesk_project_ids)` with no `.visible(User.current)` call at all, unlike `DashboardCards.resolve_all(user: User.current, ...)` which builds the correctly-scoped KPI cards on the same page. Clicking into a leaked ticket correctly 403s — a metadata/list leak, not a full-content breach. Saved a new persistent cross-session memory (`feedback_verify_hidden_ui_implies_blocked_access`): a hidden UI affordance is never sufficient evidence a URL is actually blocked — test the URL directly, per permission tier, always. `HELPDESK_NAVIGATION_WORKSPACES.md` TC-HLP-160 strengthened with the full URL-level verification matrix (table of screen × permission tier × result). Open bug count: 12 → 13. |
| 2026-09-02 | Local (redmine-docker-6, localhost:3012) | Local | Claude, per user pointing at a real screenshot | **BUG-HLP-028 narrowed same day — its original "app menu not hidden" claim was itself wrong, based on checking the wrong Redmine menu.** User showed a real `/projects` screenshot with a second nav bar (Projects/Activity/Issues/Spent time/Gantt/Calendar/News) underneath the always-present top bar, and asked whether that was the element meant. Re-verified via `browser_evaluate` enumerating every `<ul>` on the Command Center page: the original check (`<ul class="scarlet-topmenu-managed">`, Redmine's `:top_menu` — present on every page including login) was the wrong element; Redmine's real `:application_menu` (the bar the user pointed at — becomes a project's own tab bar inside a project) has **no matching `<ul>` anywhere in the Command Center's DOM**, genuinely and completely absent, correctly replaced by the icon rail. So `HELPDESK_USER_GUIDE.md` §16's "application menu is hidden" claim is actually **correct** — no code or doc fix needed for that half. BUG-HLP-028 narrowed to its one remaining real finding: the header reads "Helpdesk" not "Helpdesk Support". TC-HLP-148/064/072/073 all rewritten to reflect this. Saved a new persistent reference memory (`reference_redmine_top_menu_vs_application_menu`) — Redmine core has two separate named menus that can coexist on one page, and a "menu hidden" claim must be checked against the specific one meant, not whichever bar looks like "the top". Open bug count unchanged at 13 (BUG-HLP-028 stays open, just narrower in scope; no bug added or removed). |
| 2026-09-02 | Local (redmine-docker-6, localhost:3012) | Local | Claude, per explicit user direction with Product Owner context | **BUG-HLP-028 closed, Rejected/Not a Bug.** With the application-menu half already confirmed correct, the user closed out the remaining header-text claim directly: "Helpdesk" (not "Helpdesk Support") is intentional per Product Owner request — the plugin is behaving exactly as designed — and `HELPDESK_USER_GUIDE.md`'s "Helpdesk Support" wording is what was outdated. Moved to `bugs/closed/BUG-HLP-028.md` with a Closed section. Corrected `HELPDESK_USER_GUIDE.md` §16 and TC-HLP-148's Expected Result directly to say "Helpdesk". This settles the bug's own open "expand the code or fix the docs" question in favor of the docs, resolved by the actual product owner rather than inferred from source (contrast BUG-HLP-013/027, which stayed open questions or got settled by TC-precondition logic rather than product authority). Open bug count: 13 → **12**. |
| 2026-09-01/02 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + alpha.customer + retest.customer1 | **User-requested SLA Working Hours/Working Days/Holidays scenario audit — 7 new TCs (TC-HLP-354–356) added and executed, all PASS, 0 new bugs.** User asked for 6 specific scenarios (during/outside working hours, non-working day, working day, holiday, boundary/transition cases) checked against existing coverage, missing ones added and executed. Ran an independent 3-lens Workflow audit in parallel with direct reading — both converged on the same real gaps in TC-HLP-285/083/096/107/084 (all previously only proved deadline arithmetic for a ticket assigned entirely **outside** a working period to begin with; several explicitly disclosed their own literal Friday-close/weekend-rollover scenario was substituted, not reproduced). **TC-350**: dedicated same-day baseline under a genuinely restricted-hours SLA (09/01, ticket #30, deadline 03:04 PM same day) — every prior "normal running" TC used an unrestricted 24/7 SLA. **TC-351**: hours-boundary (today IS a working day, but past its close) tested fully separately from the days-boundary TC-083/107 already covered (ticket #31, deadline exactly tomorrow's 09:30 open). **TC-352 — the most decisive result**: a ticket with real partial consumption mid-window correctly carried only its ~71-min remainder across a full week-long gap (ticket #32, deadline 09/08 10:11 AM, not the "fresh allotment" alternative of 09/08 02:00 PM — a ~4hr difference). **TC-353**: a genuine 3-day holiday fully excluded, not just day 1 (ticket #33, deadline landed on the day after the entire range). **TC-354**: an unplanned real-time crossing into the exact holiday date mid-session became a valuable bonus test — a ticket assigned literally ON the holiday showed zero consumption for that day (ticket #34). **TC-355**: literal "just before close" (ticket #35, tiny ~16-min remainder consumed, ~14-min remainder correctly carried to tomorrow). **TC-356 — sharpest adjacency proof**: using a restricted 2-day/week pattern (Wed+Thu only, holiday=Wed) rather than TC-084/353's "every day except the holiday" configs (which can never reveal an adjacency bug) — deadline landed one day later (Thursday), not one week later (ticket #36). Added follow-up cross-reference notes to TC-083/096/107 pointing at the specific new TC that closes each one's previously-disclosed gap. **Net: the working-hours/days/holiday clock engine is now the most rigorously verified mechanism in the plugin — every dimension and combination confirmed correct via real computed-deadline arithmetic, zero bugs found.** `retest.customer1`'s SLA field was flipped 7 times and restored to her L2/"Alpha Escalation Test SLA" baseline afterward. `HELPDESK_SLA_ESCALATION.md` now 76/76. |
| 2026-09-02/03 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **`HELPDESK_FIELD_VALIDATIONS.md` executed end-to-end — all 55 TCs across Sections A-G (Organization, Customer, SLA, Support Level, Holiday, Canned Response, Product), 3 new bugs.** Verified genuinely complete via `grep -c` header-vs-marker equality AND an awk state-machine pass confirming no orphaned/misattributed markers between TC sections. **BUG-HLP-030** (Medium): a Name/Code-length-validation crash discovered incrementally across 4 models — `Sla#name` (TC-250), `SupportLevel#name` (TC-258), `RfHelpdeskHoliday#name` (TC-263), and `RfProduct#name`+`RfProduct#code` (TC-312, the first case of one model failing on two distinct columns) — each confirmed via 255-chars-succeeds/256-chars-crashes, root-caused via server log every time to the identical `ActiveRecord::ValueTooLong (Mysql2::Error: Data too long for column '<col>')`. Organization Name/Customer Login+Last name/Canned Response Name all correctly handle the same boundary with a friendly message, so this is a real, inconsistent gap, not a plugin-wide pattern — recommended as a code-review item against every model's migration column widths. **BUG-HLP-031** (Medium): SLA First Response Time/Resolution Time marked required but blank is silently accepted on both Create (TC-251/252) and Edit (TC-326), saving as NULL, while `0`/negative on the same fields correctly refuse — the cross-field Resolution≥Response check (TC-253, unit-aware) works correctly and is unaffected. **BUG-HLP-032** (Medium): SLA Agreement upload's 5 MB size limit genuinely enforced (client-side alert) but zero file-type restriction — `.exe` accepted and downloadable (TC-256). Every other TC confirmed either a working validation or a non-bug observed behavior — see `HELPDESK_MEMORY.md`'s new addendum for the full list (Customer Email's real 254-char max via TC-245, First/Last name's 30-vs-255 asymmetry resolved via TC-335's Edit-form probe, the recurring "address address" doubled-word message quirk, Product's per-project-scoped "already taken for this project" wording via TC-332, Support Level's Level Order having airtight dual-message validation unlike SLA's numeric-blank gap). No local Canned Response or "Phoenix Core"/"Falcon Suite" Product fixtures existed on this environment — created fresh (Canned Response ids 2-3, Product ids 5-6) and reused across the Edit-form-parity TCs (TC-270/340, TC-332). `automation/uploads/` gained 4 new checked-in boundary-test fixtures (`under-5mb.pdf`, `over-5mb.pdf`, `test-file-type.docx`, `test-file-type.exe`). Open bug count: 12 → **15**. |
| 2026-09-03 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `luna.blossom` + `alpha.customer` | **`HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md` executed end-to-end — all 18 TCs (TC-HLP-398–059), 1 new bug.** Ran the 15 agent-facing cases as `luna.blossom` (Agent, view_helpdesk-only) and 3 customer-facing cases (TC-051/055/057) as `alpha.customer`. **BUG-HLP-033** (Medium): TC-050's badge-vs-detail-panel cross-check found the ticket list's SLA Status badge and the ticket's own SLA Information tab genuinely disagree for the At Risk/Critical range — ticket #34 showed "13h 33m At Risk" (amber) in the list while its own detail tab simultaneously read "✓ On Track" (green), same moment, same ticket. Root-caused via source: the list badge correctly implements the documented 7-state percentage-of-window model (`RfIssueSlaStatus#sla_health_status`), while the SLA Information panel computes an entirely separate 5-state binary model with no at_risk/critical concept — anything not breached/overdue/paused/resolved defaults to "On Track" regardless of remaining time. A related, lower-severity finding: Breached tickets show "Needs Attention" in the panel, not "SLA Missed" (same root disconnect, less severe since both convey urgency). Resolved and Paused states ARE consistent between the two views. Everything else in the suite confirmed correct, several genuinely useful findings: the filter panel's Delete button is visual-only until Apply is re-clicked (TC-043/058, not a bug — TC-058's own wording anticipates this); there is no active-filter-count UI element anywhere to test (TC-056, Blocked/N-A); Project is offered as a filter only on the global list, not inside a project (TC-052, confirms the known 16-vs-17-field quirk); the customer's restricted UI is exactly 5 filters/7 columns as documented (TC-051); URL-level enforcement for a customer session is genuinely server-side — manipulating `assigned_to_id`/`customer_id` params or forcing a disallowed sort column both left her results/sort correctly unaffected (TC-055/057); Apply/Clear/Add filter/Columns are pixel-identical (40px height, 6px radius) via direct measurement (TC-059). Open bug count: 15 → **16**. |
| 2026-09-03 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `luna.blossom` | **Follow-up coverage pass, triggered by direct user critique of the "18/18 complete" claim above — 3 new TCs (TC-HLP-416/358/359), 0 new bugs.** User asked pointedly whether multi-filter combinations, column-add/remove-while-filtered, and Dashboard "View all" with a manually-changed date range were ever tested. Honest answer: no — closed live. **TC-HLP-416** (`HELPDESK_TICKET_LIST_FILTERS_COLUMNS.md`): proved genuine AND/intersection filter logic two ways — direct URL (Status=New+Priority=High → exactly 1 ticket #6; a deliberately non-overlapping Status=Closed+Priority=High → exactly 0 tickets) and the real Filters-panel UI (Status dropdown → Add filter → Priority → Apply) landing on the identical 1-ticket result. **TC-HLP-419** (same file): columns vs. filters independence — initial assumption that a column checkbox applies instantly/client-side was live-tested and found **wrong**; a column change only takes effect after clicking Apply, same button as filters. Confirmed both directions (add then remove the Author column) that Apply-ing a column change never disturbs an already-applied filter's result count or URL params, and vice versa. **TC-HLP-157** (`HELPDESK_NAVIGATION_WORKSPACES.md`, inserted after TC-HLP-156): changed the Dashboard's Date Range picker from its default to "Yesterday" — KPI counts live-updated (Open 34→3), and clicking "View all" carried the just-picked range (`created_from=2026-09-02&created_to=2026-09-02`) into the redirect, not the page's stale original default; resulting ticket list showed exactly 3 rows, matching. Confirmed the SLA Breached KPI's own link (`?sla=breached`) carries no date params at all regardless of picker state — not a bug, that card was simply never date-scoped. All three PASS, no bugs found — the gap was in test coverage, not plugin behavior. Open bug count unchanged at **16**. |
| 2026-09-03 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `luna.blossom` | **Second same-day follow-up, triggered by a sharper user critique with an attached Dashboard screenshot — 2 new TCs (TC-HLP-417/361) plus TC-357 broadened, 0 new bugs.** User asked whether every offered filter field (not just Status+Priority) was combined and applied, whether Clear genuinely removes added filter rows and resets to default (not just clears the URL), and whether the Dashboard's date range also drives graph/chart data. **TC-HLP-416 broadened**: added Organization Name + Assignee as 2 more simultaneous rows (4 total), Status=New + Assignee=Redmine Admin (Organization Name left untouched) → exactly 6 tickets, correctly excluding a Redmine-Admin ticket that's In Progress not New; separately verified the **Created on** date-range filter (From/To text inputs, structurally different from the dropdown fields) narrowed correctly to exactly 3 tickets for `2026-09-02`–`2026-09-03`. Logged an honest scope note: this samples 3 distinct filter mechanisms plus a 4-row combo, not all 13 offered fields individually. **New TC-HLP-417**: Clear (distinct from TC-058's per-row Delete) genuinely resets both the URL and the filter panel itself — re-opened panel shows only the 2 default rows, added rows are gone, not blanked. **New TC-HLP-418 — a genuinely new discovery**: the global Command Center Dashboard (`/helpdesk`) has a "Ticket Statistics" bar chart never previously noticed in this entire engagement, absent from the project-scoped dashboard entirely and undocumented in `HELPDESK_FEATURES_LIST.md`/`HELPDESK_USER_GUIDE.md`. Confirmed it genuinely reacts to the Date Range picker — switching to "Yesterday" updated its date-range label and all 5 bar values to exactly match the KPI cards under the same range, with SLA Breached correctly staying unchanged on both surfaces (not date-scoped by design). All three PASS, no bugs — open bug count unchanged at 16. |
| 2026-09-03 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `retest.customer1` + `autumn.grace` + `willow.belle` + admin | **`HELPDESK_SLA_ESCALATION.md` coverage gap closed — 3 new TCs (TC-HLP-329/363/364), 0 new bugs.** User asked whether resolved-without-breach, escalated-once-then-resolved, and Admin-closes scenarios were tested — confirmed none existed. User corrected a wrong assumption before execution: only a Customer-raised ticket attaches an SLA/Support Level (Admin/Agent-created tickets never do) — saved as `feedback_sla_requires_customer_raised_ticket`. User also directed writing the TCs before executing — saved as `feedback_write_testcases_before_executing`. **TC-362**: ticket #39, a single Reply Note from `autumn.grace` simultaneously assigned+responded+paused with zero elapsed time, then resolved/closed — SLA panel "✓ Completed", zero breaches. **TC-363**: ticket #40, assigned via Edit-form only (no reply) so its response deadline genuinely ran and breached; found Redis/Sidekiq down again (restarted both), but the sidekiq-cron scheduler's poller thread still crashes on boot with a pre-existing `ArgumentError` (version mismatch, not introduced this session) so the real auto-cadence doesn't run — manually triggered `Helpdesk::SlaMonitorWorker.new.perform` once (established practice from TC-092/095/299) to run the real breach/escalation code on demand. Escalated L2→L3 exactly once, resolved by `willow.belle` before any second breach could process — SLA panel showed exactly 1 escalation, full history intact. Reproduced BUG-HLP-033 again in a new shape (detail panel "⚠ Needs Attention" vs. list "✓ Resolved" for the same genuinely-resolved ticket) — folded into BUG-HLP-033's existing evidence, no new bug. **TC-364**: ticket #41, same no-breach setup as TC-362, but Admin performed the actual Resolved→Closed transition instead of the assigned Agent — identical "✓ Completed" result, no permission difference (one harmless quirk: the Activity Log attributes the resolve event to the ticket's Assignee, not to Admin who clicked Submit). Open bug count unchanged at 16. Flagged for a future session: the sidekiq-cron scheduler poller bug means real (non-manually-triggered) SLA monitor timing is not currently available in this environment — needs a gem-version fix before relying on it again. |
| 2026-09-03 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `retest.customer1` + `autumn.grace` + `willow.belle` + admin | **One more same-day follow-up on `HELPDESK_SLA_ESCALATION.md` — 1 new TC (TC-HLP-332), 0 new bugs.** User asked whether a TC covers a ticket climbing every level of its escalation chain (not just one hop) followed by Admin actively resolving it. Confirmed untested — TC-299 (full chain) never resolves, TC-301 (admin-fallback) only covers passive reassignment, TC-363 only covers one hop. Wrote TC-365 first, then executed: ticket #42 (`retest.customer1`, L2) assigned via Edit-form only so its clock genuinely ran; manually triggered the SLA monitor worker twice with a real wait between each — first breach escalated L2→L3, second breach (L3's own recalculated deadline also left untouched) correctly hit admin-fallback (Critical Breach Alert delivered to admin). Confirmed the genuine full climb ("↑ Escalated 2×", 3 stages) via the pre-resolution SLA panel. Admin then actively resolved and closed the ticket — full history survived perfectly, all 3 stages/2 escalations/6 Activity Log events intact, nothing overwritten. **Sharpest BUG-HLP-033 reproduction yet**: the detail panel's overall badge still read "✗ SLA Missed" for this fully, deliberately Closed ticket (the panel's breach-flag check fires before its resolved-status check, permanently "stuck"), while the list correctly showed "✓ Resolved" at the same moment — folded into BUG-HLP-033's existing evidence as a third reproduction, this time surviving a full multi-escalation chain. Open bug count unchanged at 16. |
| 2026-09-03 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `retest.customer1` + `autumn.grace` + admin | **One more same-day follow-up on `HELPDESK_SLA_ESCALATION.md` — 1 new TC (TC-HLP-333), 1 new bug (BUG-HLP-034), 1 new reusable fixture.** User asked whether the multi-agent-escalation scenario was tested specifically for L2→L3 (TC-302 only tested L1→L2), and asked to actually determine the selection rule rather than accept "any pick is fine." Read the source (`rf_issue_sla_status.rb#escalate_to_next_level!`): a plain `.first` on an unordered `User.where(id: [...])` query — no randomness, no round-robin, no workload logic — meaning the real rule is a deterministic lowest-user-ID pick. Since L3 was single-agent (Willow Belle only, ID 11) and no other project agent was free to pair with her, created a new fixture `aurora.wren` (ID 30) and added her to L3. Ran two separate customer-raised tickets (#44, #45) through an identical L2→L3 breach/escalate cycle — both landed on Willow Belle, never Aurora Wren, exactly matching the source prediction. **Initially framed as "not a bug, just a fairness consideration" — corrected after direct user pushback** ("it sound like this is the gap in escalation assignee") into **BUG-HLP-034** (Medium): `HELPDESK_USER_GUIDE.md` §10's own canonical example shows every level with 2 named agents, so a multi-agent level that never distributes escalated work across its agents defeats the visible purpose of naming more than one. Open bug count: 16 → **17**. New reusable fixture: L3 is now genuinely 2-agent. |
| 2026-09-03 | Local (redmine-docker-6, localhost:3012) | Local | Claude, per explicit user direction | **BUG-HLP-023 closed, Not a Bug.** User reviewed the finding (Holiday delete silently clears an SLA's Holiday field, no dependency warning) and closed it directly: Holiday is an optional SLA field, the delete degrades gracefully with zero functional consequence (field clears to "-", no broken reference, no invalid state), so this doesn't rise to a defect worth a warning gate. Moved to `bugs/closed/BUG-HLP-023.md`, TC-HLP-364's verdict corrected FAIL → PASS. BUG-HLP-022 (same missing-warning shape, but for the *required* Support Level field, whose silent deletion does produce an invalid customer row) stays open by contrast. Open bug count: 17 → **16**. |
| 2026-09-03 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `delta.customer` + `luna.blossom` + admin | **`HELPDESK_PREPAID_HOURS.md` executed end-to-end — 18/18 original TCs (TC-125–142) plus 7 new Support Packages TCs (TC-367–373), 3 new bugs.** Discovered a genuinely untested entity (Support Packages, global Name/Description/Active) flagged in an earlier handoff note but never given test cases — wrote 7 TCs first, then executed: BUG-HLP-035 (Medium — Active checkbox has no hidden fallback input, can never save Inactive) and BUG-HLP-036 (Low — duplicate-name refusal shows zero error message). Then executed the original 18 TCs against org "Alpha Minimal Fields Test Org" / `delta.customer` / `luna.blossom`: budget top-ups/reductions, Ledger row-by-row content, native HTML5 required-field validation all PASS. **BUG-HLP-037 (High)** — deliberately exhausted the budget to Remaining = 0.00h under Hard mode: a customer's new ticket and an agent's time log both succeeded normally with the balance silently going negative ("0.58h used · -0.16h left"), directly contradicting the documented "nobody can log time... customer cannot raise a new ticket" contract — Hard mode's enforcement has no effect on either code path. No Limit/Soft correctly allowed the same actions (their genuine expected behavior), though Soft showed no distinct run-out signaling from No Limit. TC-142 confirmed per-project budget independence via a fresh Beta-project budget on the same org. Open bug count: 16 → **19**. Reinforced the standing no-direct-URL-navigation rule after a third violation. |
| 2026-09-07 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `luna.blossom` + `delta.customer` + admin | **`HELPDESK_PREPAID_HOURS.md` closed out to 39/39 — 14 user-identified gap TCs (TC-HLP-213–387) written then executed; BUG-HLP-037 corrected to Unconfirmed, BUG-HLP-038 filed.** Resumed after `redmine-docker-6` had exited (Docker/host restart during the gap) — restarted app+DB containers, all data survived. Support Package Edit/duplicate-naming/audit-trail TCs all PASS; TC-375 (Reactivate) still BLOCKED by BUG-HLP-035. **Retesting Hard-mode-at-zero as a "standing regression check" (TC-380/381) blocked correctly three times in a row — directly contradicting the prior session's BUG-HLP-037 finding.** Investigated (Redis/Sidekiq uptime, single- vs multi-operation paths to exact zero) without finding a root cause for the original discrepancy; corrected BUG-HLP-037's file in place with a full "Retest — Correction" section, kept open but reclassified Unconfirmed. That investigation surfaced **BUG-HLP-038 (Medium)** instead — Hard mode only checks whether Remaining is already ≤0 before an action, not whether the action's own size would cross zero; a 2h entry against 1.00h Remaining was accepted in full, landing at -1.00h. Soft mode's identical crossing case (TC-383) correctly allowed the same overrun (its genuine expected behavior). TC-384 filled the last mode/state gap (No Limit, positive Remaining, PASS). TC-385/386 confirmed `luna.blossom` is blocked from budget/Support-Package management via hidden UI and direct URL alike (genuine 403s). TC-387 confirmed two near-simultaneous budget top-ups from separate tabs both apply additively, neither lost. Open bug count: 19 → **20**. |
| 2026-09-07 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `perm.test.agent` + `luna.blossom` + `zero.perm.user` + admin | **`HELPDESK_PERMISSIONS.md` closed out to 11/11 (TC-HLP-166–214) — 2 new bugs, 1 documentation contradiction resolved, 1 pre-existing bug reconfirmed.** Chosen after computing real per-suite coverage via grep and presenting the options to the user. New fixtures: role "Permission Test Role" (id 9, reconfigured 6 times to isolate each permission combination) and user `perm.test.agent`. **BUG-HLP-039** (Medium) — Canned Responses hardcode `require_admin`, ignoring `manage_helpdesk` entirely unlike every sibling configuration entity (SLA/Support Level/Organization/Product/Holiday), contradicting `HELPDESK_REQUIREMENTS.md`'s own matrix. **BUG-HLP-040** (Medium) — `manage_prepaid_support_hours` without `manage_helpdesk` has no reachable UI path at all, since the prerequisite Organization page's `show` action requires `manage_helpdesk` with no fallback, even though the write action's own permission check is correct per source comments. TC-208 independently reconfirmed **BUG-HLP-029 still reproduces** identically (not re-filed). **TC-213 resolved this suite's own flagged contradiction**: real, content-verified CSV downloads under two role configs confirmed `manage_helpdesk` alone is sufficient for report export — `export_helpdesk_reports` is not independently required. TC-207/210/211 confirmed the KB and prepaid-hours permission sets are each fully independent (one KB "Add Article" icon visibility inconsistency noted as cosmetic-only, not a bug — the create action itself stays blocked). `HELPDESK_REQUIREMENTS.md`'s Permissions Matrix updated directly with the confirmed export answer plus both new bugs' exceptions. Open bug count: 20 → **22**. |
| 2026-09-07 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `perm.test.agent` + `manage.helpdesk.test` + admin | **BUG-HLP-040 retracted the same day, Not a Bug — `manage_prepaid_support_hours` permission model clarified, TC-HLP-168/210 rewritten.** User asked for the exact requirement/documentation citation backing BUG-HLP-040's premise; re-checking `HELPDESK_USER_GUIDE.md` line by line found no such requirement — the bug's premise (that `manage_prepaid_support_hours` must work standalone without `manage_helpdesk`) was an unverified inference from the permissions table's flat structure, not a stated contract. User then supplied the actual intended model: `manage_helpdesk` grants access to the Organization area; `manage_prepaid_support_hours` is required *in addition* to modify prepaid hours. Retested both directions live to confirm the code matches: **positive case** (both permissions, `perm.test.agent`) — Organization page opens, top-up succeeds (Approved 14.75h→15.75h), run-out mode change succeeds (No limit→Soft), both confirmed persisted after a genuine full-page reload; **negative case** (`manage_helpdesk` alone, `manage.helpdesk.test`) — access/view works, top-up control absent, run-out mode renders as plain text, modification correctly blocked. Rewrote `HELPDESK_PERMISSIONS.md` TC-HLP-168 to test the positive case with this evidence, and TC-HLP-172 to make the access-vs-modify distinction explicit. Moved `BUG-HLP-040` to `bugs/closed/` with a full Closed section documenting the retraction and both retests. Updated `HELPDESK_REQUIREMENTS.md`'s Permissions Matrix (split the prepaid-budgets row into an access row and a modify row) and `HELPDESK_USER_GUIDE.md` §20/§26 to state the two-permission dependency directly instead of citing the bug. Open bug count: 22 → **21**. |
| 2026-09-07 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `luna.blossom` + admin | **`HELPDESK_PREPAID_HOURS.md` round 2 — 8 new user-identified gap TCs (TC-HLP-227–395), all PASS, 0 new bugs.** User reviewed the suite's 39 TC titles directly (not asked to, offered proactively) and identified 8 genuine coverage gaps the existing suite never isolated cleanly, written first then executed live. **TC-HLP-228 — flagged by the user as the single most important missing test**: the exact positive→exactly-zero boundary (Remaining=2.00h, log exactly 2h) sits between BUG-HLP-038's overshoot case and TC-380's already-at-zero case, neither of which individually exercises it — confirmed accepted, landing Remaining at precisely 0.00h. TC-390 confirmed the very next entry (1 min) immediately blocks, a clean boundary with no off-by-one gap. TC-388 confirmed a fresh, deliberate top-up-then-reduce-to-exactly-0.00h is accepted with correct display (no crash/divide-by-zero). TC-391 — editing a Support Package with real budget-history references (TC-374 had explicitly avoided this precondition) confirmed the reference is a **live foreign-key lookup, not a frozen name snapshot**: renaming immediately changed how an old history row displays it, and the delete-dependency guard survived intact. TC-392 confirmed deleting an unrelated unused package leaves other packages' history completely untouched. TC-393 — a **mixed** top-up+reduction from two tabs (+3 then -1, TC-387 only ever tested same-sign additive ops) confirmed totals are computed from the true live server value at each write, not a stale client-side read a same-sign test could never expose. TC-394 confirmed run-out-mode enforcement is fully live/dynamic per-request (Hard→No Limit→Hard, block→allow→block again immediately) — not cached from whichever mode was active when the budget first hit zero. TC-395 confirmed run-out mode persists across both a genuine page reload and a fresh logout/login session. Suite re-verified complete: 47 headers, 46 markers (only the pre-existing TC-131 wording-only false negative, unchanged from before). Open bug count unchanged at 21. |
| 2026-09-07 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `luna.blossom` + admin | **`HELPDESK_PREPAID_HOURS.md` round 3 — editing/deleting an existing time entry (TC-HLP-235–399), 1 new bug.** User pointed out every prior time-logging TC only tests *creating* a new entry via the reply box — none test editing/deleting an already-logged entry via Redmine's own core Spent-time tab. **BUG-HLP-041 (Medium)** — under Hard mode with Remaining exactly 1.00h, editing an existing entry via `/time_entries/:id/edit` to increase its Hours by 2.92h succeeded completely uncapped, landing Remaining at -1.92h — a more complete gap than the already-open BUG-HLP-038 (which only misses the crossing case on the reply-box's create path; this path has no enforcement hook at all). TC-397 confirmed Soft mode's contract correctly holds on the same edit path (bookkeeping is fine, only Hard's enforcement is missing). TC-398 confirmed reducing an entry's Hours symmetrically decreases Used/increases Remaining. TC-399 confirmed deleting an entry fully reverses its budget impact. Suite re-verified: 51 headers, 50 markers (same pre-existing TC-131 gap). Open bug count: 21 → **22**. |
| 2026-09-07 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `luna.blossom` + admin | **`HELPDESK_PREPAID_HOURS.md` round 4 — the core "Log time" link, a third distinct code path (TC-HLP-239), folded into BUG-HLP-041 as Path B.** User asked directly whether the core Redmine "Log time" link (`/issues/:id/time_entries/new`, creating a brand-new entry) had also been checked under Hard mode — it had not. Topped up Alpha's budget to Remaining exactly 2.00h under Hard mode, clicked the ticket's own "Log time" link (confirmed distinct from "Reply" and from editing an existing entry), entered 5:00h via Redmine's plain native new-entry form (no prepaid-hours widget, no run-out-mode awareness in its UI), Create — succeeded fully uncapped ("Successful creation"), ticket's own summary going from "9.67h used · 2.00h left of 11.67h" to "14.67h used · -3.00h left of 11.67h". Confirms the identical root cause on a third distinct core `TimeEntry` action; BUG-HLP-041 broadened (not a new ID) to cover both the edit path (Path A) and this new-entry path (Path B). Suite re-verified: 52 headers, 51 markers (same pre-existing TC-131 gap). Open bug count unchanged at **22**. |
| 2026-09-07 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **`HELPDESK_FIELD_VALIDATIONS.md` new Section H — Customer creation vs. a required User custom field (TC-HLP-145), 1 new bug.** User asked directly whether a scenario they'd observed (required User custom field blocking Customer creation) had already been reported — genuinely never tested (grepped the whole QA folder first). Created a required Text custom field on the Users type; confirmed as a control it works correctly on Redmine's own core `/users/new`; on the Helpdesk plugin's own New Customer form, confirmed via full DOM snapshot no custom-fields section exists at all. A fully valid submission was refused with "... cannot be blank." **Corrected the user's own framing**: they believed the customer gets created anyway despite the error; live re-verification (Customers list unchanged, Users search "No data", direct `rails runner` query) confirmed zero records created — the create is fully, atomically blocked (source: standard `if @customer.save … else render :new`). The real defect is that Customer creation becomes totally, permanently impossible once any User custom field is required, since the form has no way to satisfy it. **Filed as BUG-HLP-042 (Medium).** Cleaned up the test custom field afterward. `HELPDESK_FIELD_VALIDATIONS.md` reopens from 55/55-closed to 56 TCs. Open bug count: 22 → **23**. |
| 2026-09-09 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `rails runner`), admin | **`HELPDESK_EMAIL.md` auto-close (TC-HLP-077/151/153) + `HELPDESK_RAKE_TASKS.md` TC-HLP-250 — 2 new bugs, plus a real collateral-damage incident fully disclosed and recovered.** User asked for the auto-close condition written out and the relevant TCs summarized, then to verify live. Rake task (`auto_close_tickets`) closed nothing — **BUG-HLP-044**: it reads a legacy `ProjectCustomField` that no longer exists on this instance (0 rows of any kind), same dead-code pattern as the never-filed `check_emails` gap. The real worker (`Helpdesk::AutoCloseTicketsWorker`, invoked directly since the scheduler poller is still broken) correctly closed a Resolved test ticket (TC-150 PASS) but also closed a New-status one (TC-153 FAIL) — **BUG-HLP-043**: its query uses Redmine's `.open` scope (5 non-closed statuses), not Resolved-only, contradicting the documented contract. That run also closed 46 tickets total in Helpdesk QA Alpha, not just the 2 test tickets — every pre-existing fixture ticket silent past 1 day got swept up. Per explicit user direction, checked reference counts before reverting anything: only ticket **#46** (20 references in `HELPDESK_PREPAID_HOURS.md`, confirmed functionally restricted live) was reverted to its exact prior status (recovered from its own journal) with the fabricated journal entry removed; the other 44 (each single-use, already-fully-consumed TC/bug evidence, ≤1 reference elsewhere) were left Closed. Auto Close Ticket Days reset to blank on Alpha afterward; no other project has any auto-close config. Open bug count: 22 → **24**. |
| 2026-09-09 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `rails runner`), admin + `alpha.customer` (Roundcube) | **User-specified coverage sweep — 5 new TCs executed (all PASS), 1 written but BLOCKED, 0 new bugs.** User asked to confirm coverage of 5 scenarios and add missing TCs: email-config permission gate (already fully covered — `HELPDESK_PERMISSIONS.md` TC-HLP-174/214 confirm hardcoded admin-only, not `manage_helpdesk`-gated); email-to-project routing for a multi-project customer (**TC-HLP-370**, `HELPDESK_TICKET_LIFECYCLE.md`, BLOCKED — source review found a real misrouting risk in `mail_handler_patch.rb`, but exercising it needs a Beta mailbox and a local multi-project customer fixture, neither of which exist yet); auto-close tracker scope (**TC-HLP-082**, PASS — Bug-tracker tickets #37/#38 untouched, Support-tracker #57 closed); auto-close project scope (**TC-HLP-083**, PASS — Beta ticket #58 untouched, confirmed via the worker's own log showing no "Processing project: Helpdesk QA Beta" line); and all 4 Identifier-Keywords/Email-Subject-Prefix combinations (**TC-HLP-084/406/407**, all PASS, via real emails through the local Docker mail server — both-blank creates verbatim, prefix-only confirms the prefix is independent of keyword gating, both-filled is the first direct confirmation in this suite that the prefix genuinely rewrites a real ticket's subject). TC-403/404 applied a dry-run eligibility count before invoking the worker (3 candidates total), per the lesson from the prior incident. Alpha's Email Configuration restored to its exact original state and re-verified via direct query after every change. `HELPDESK_MEMORY.md` addendum added. Open bug count unchanged at 24. |
| 2026-09-09 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + `beta.customer` (Roundcube) | **TC-HLP-370 built out and confirmed live — BUG-HLP-045, a real cross-project email-routing defect.** User asked to properly verify the previously-BLOCKED TC-402 rather than leave it flagged. Wired up Helpdesk QA Beta's Email Configuration for the first time (SMTP+IMAP via `beta.support@test.local` — mailbox existed on the mail server but was never connected to Redmine). Recreated `beta.customer` (id 32) as a genuine multi-project customer, Alpha as her first project-access row and Beta as her second (tried `delta.customer` first, but she has no real mailbox account). Sent a real email as her specifically to `beta.support@test.local` — server log confirmed the poller correctly identified Beta's mailbox, then `MailHandler` silently substituted Alpha (her first row) anyway. Ruled out a confound (Alpha's prepaid budget happened to be negative, blocking the first attempt) by topping up Alpha's budget and resending — ticket **#62** was created cleanly under **Helpdesk QA Alpha** despite never being sent to Alpha's mailbox, confirmed via page title/Organization field/outbound SMTP account. Root-caused via source (`mail_handler_patch.rb#target_project_with_helpdesk`, unconditional `.first` on project-access rows). TC-402 updated BLOCKED → CONFIRMED LIVE FAIL. Mid-task, user corrected a `rails runner` lookup back to the real UI — reinforced the standing "use Playwright, not backend" memory. **Same-day follow-up**: user asked to confirm this is specific to multi-project entitlement — temporarily made `beta.customer` Beta-only (removed her Alpha row), resent to `beta.support@test.local`, and it correctly routed to Beta this time (ticket #63) — confirming single-project customers route fine, the bug requires 2+ rows. Restored her Alpha row afterward (row order is now reversed from the original setup — Beta first, Alpha second — documented in `HELPDESK_USERS_AND_CUSTOMERS.md`). Open bug count: 24 → **25**. |
| 2026-09-09 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `rails runner`), admin + `alpha.customer`/`beta.customer` (Roundcube) | **`HELPDESK_EMAIL.md` closed out, 23/23, 2 new bugs.** User directly challenged whether the suite was actually fully tested (real count was 11/23) — executed all 12 remaining TCs live. TC-143 (admin save, PASS). TC-145/149 (no-SMTP fallback / blank-incoming no-poll, both PASS) required a new genuinely-blank third project, **Helpdesk QA Gamma** — confirmed via Roundcube a reply from Gamma uses Redmine's global `redmine@example.net`, and via poller log Gamma is silently skipped entirely (no incoming config). TC-144/146 (per-project sender, PASS) cross-referenced Alpha's real auto-close email (`alpha.support@test.local`) against a fresh Beta reply (`beta.support@test.local`) — two genuinely distinct senders. TC-147 (all 4 outgoing triggers, PASS) confirmed creation/reply/auto-close live this session plus a real pre-existing SLA breach email from an earlier session. TC-148 (Email History full detail) — **FAIL, filed as BUG-HLP-046**: the tab shows sender/timestamp/body but never recipient or a per-entry subject field, confirmed via a full-page DOM search; directly contradicts `HELPDESK_USER_GUIDE.md` §13's explicit promise. TC-152 (non-admin blocked, PASS via cross-reference to a fresh TC-212 re-test earlier this session). TC-154/155 (auto-close timing edges, both PASS) — TC-155 determined real, previously-undocumented behavior: the clock resets on ANY `updated_on` bump, not specifically a reply. TC-156 (independent outgoing/incoming config) turned out **unachievable via the real UI** — the form's own HTML5 `required` attributes make every field mandatory regardless of section, so no bug was filed, the TC was rewritten to record the real constraint. TC-290 (same mailbox on two projects) — **FAIL, filed as BUG-HLP-047**: Save succeeds with zero validation naming the conflict; sending one email to the shared mailbox showed the first-polled project (Alpha) silently wins via IMAP Seen-flag consumption, the second (Beta) gets nothing, no error, no duplicate — a real, deterministic, silent misrouting risk. New reusable fixture: **Helpdesk QA Gamma** (project id 4, blank email config) plus mailbox `gamma.support@test.local`. Open bug count: 25 → **27**. |
| 2026-09-09 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin + `beta.customer` (Roundcube) | **BUG-HLP-047 broadened — outgoing-side reuse tested too.** User asked directly whether the same-email-configuration test had also been checked for outgoing, not just incoming. Reset Beta's incoming to its own account, then set Beta's outgoing SMTP Username + Email From Address to Alpha's own account (`alpha.support@test.local`) — Save succeeded with zero validation, identically to the incoming case. Replied on a real Beta ticket (#63) — sent successfully with no crash, but the customer's actual received email (`beta.customer@test.local`, Roundcube) shows `From: alpha.support@test.local`, not Beta's own address, despite genuinely being a Beta ticket. Unlike the incoming case this causes no misrouting/data-loss — a silent sender-identity/branding confusion instead, sharing the identical root cause (zero uniqueness validation on account reuse across projects). BUG-HLP-047 broadened in place (title/Actual Result/Recommend) rather than filing a second bug ID. Beta's outgoing SMTP restored immediately after, confirmed via direct query. Open bug count unchanged at 27. |
| 2026-09-09 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `rails runner`), admin + `alpha.customer` (Roundcube) | **`HELPDESK_PREPAID_HOURS.md` round 5 — budget configuration × ticket-creation channel matrix, TC-HLP-240–413, 0 new bugs.** User asked directly whether all 4 budget configurations (no budget assigned / No Limit / Soft / Hard) were covered across both Portal and Email channels, warning not to assume they behave identically. Verified against `HELPDESK_USER_GUIDE.md` §12 and `app/models/helpdesk/prepaid_enforcement.rb`'s shared `block_reason` method first: the blocking decision is provably identical across channels, but the notification deliberately differs (Portal inline error vs. Email's dedicated bounce-back). All 6 new TCs PASS: TC-408/409 (no budget assigned, Portal/Email, tickets #67/#68, never blocked), TC-410/411 (No Limit/Soft, Email, negative balance, tickets #71/#72), TC-412 (Hard, Email, Remaining > 0, ticket #69, no block), TC-413 (Hard, Email, Remaining ≤ 0, genuinely blocked, bounce-back email confirmed in the customer's inbox). **Methodology finding en route, not a bug**: TC-413's first attempt at "exactly 0.00h Remaining" still let the ticket through — a `rails runner` diagnostic revealed a tiny positive floating-point residue (`0.003333...h`) from historical repeating-decimal time entries, masked by the UI's 2-decimal rounding; corrected by pushing the budget firmly negative, then retested and confirmed genuinely blocked. A coverage matrix (4 configs × 2 channels) was added to the suite. All temporary fixture changes restored immediately after (Alpha's mode back to Hard, budget back to its exact original 21.67h/7.00h, `alpha.customer`'s Organization back to None), confirmed via direct re-check. Open bug count unchanged at 27. |
| 2026-09-10 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), admin | **Completeness audit across all 13 suite files — found and closed a real gap: `HELPDESK_EMAIL.md` TC-HLP-078 had only ever been verified via source review, never executed live.** User asked which suites are genuinely fully tested. Checked every suite file directly (each TC header vs. actual live evidence in the section following it, not relying on prior handoff claims) — 8 of 13 confirmed complete (SLA_ESCALATION 81/81, TICKET_LIFECYCLE 33/33, NAVIGATION_WORKSPACES 18/18, CUSTOMERS_ORGANIZATIONS 30/30, TICKET_LIST_FILTERS_COLUMNS 22/22, FIELD_VALIDATIONS 56/56, PREPAID_HOURS 60/60, PERMISSIONS 11/11); 4 barely started (CONTENT_TEMPLATES 8/36, RAKE_TASKS 1/13, REPORTING_AUTOMATION 0/23, PLUGIN_INSTALLATION 0/12). `HELPDESK_EMAIL.md` — previously claimed "23/23 closed out" — turned out to be 22/23: TC-HLP-078 ("Auto-close days blank or 0 disables auto-close") had a "CONFIRMED via source review... not independently re-executed live" note, not real execution. Closed it for real: checked every project's `RfHelpdeskEmailConfig` first (Alpha/Beta both `auto_close_days: nil`, Gamma has no config row at all — a genuine zero-risk dry run), created a dedicated ticket #87, set Resolved, backdated `updated_on` 5 days (test-setup only, `rails runner`, same established methodology as TC-150), invoked the real `Helpdesk::AutoCloseTicketsWorker` directly — log confirmed "Total tickets closed: 0", and ticket #87 was reopened live afterward still Resolved, no auto-close journal entry. `HELPDESK_EMAIL.md` is now genuinely **23/23**. No new bugs. Open bug count unchanged at 27. |
| 2026-09-10 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP), `alpha.customer`/`delta.customer`/`beta.customer`/`luna.blossom`/admin | **`HELPDESK_PREPAID_HOURS.md` round 6 — customer-without-organization time-logging + multi-customer shared-organization budget, TC-HLP-246/415, 0 new bugs.** User asked to investigate two specific budget scenarios directly, verifying against documentation before concluding either way. **Scenario 1 (TC-HLP-246):** `alpha.customer` (Organization: "—") raised ticket #84; `luna.blossom` assigned it to herself and logged 15 min via Reply — succeeded cleanly, no block, no budget warning; the ticket's own detail page shows "Organization: -" and has no "Prepaid Support Hours:" line at all (that line only renders once an organization actually resolves). Confirms a no-organization customer is never subject to budget enforcement, per §12's "resolved through the ticket author's organization" plus §3's "Optional" framing — flagged as a documentation gap (never explicitly stated) rather than a bug. **Scenario 2 (TC-HLP-247):** `delta.customer` and `beta.customer`, both entitled to "Alpha Minimal Fields Test Org" on Helpdesk QA Alpha, each raised a separate ticket (#85, #86). Logging 15 min on #85 moved the org's Helpdesk QA Alpha budget from 14.67h/7.00h used/left to 14.92h/6.75h — and ticket #86 (untouched) immediately reflected the same updated figures before any action was taken on it. Logging 15 min on #86 moved it again to 15.17h/6.50h, the correct combined total of both entries. Confirms budget consumption is keyed by (organization, project), not by individual customer — two unrelated customers under one organization genuinely share and compete for the same pool, exactly matching §12's "A budget belongs to an organization on one project." Both scenarios PASS as designed/documented behavior, not bugs. Fixtures (tickets #84/#85/#86, their logged time, and the org's now-higher Used total) left in place as reusable evidence, per this suite's established convention of not reverting genuine budget-consumption test results. Open bug count unchanged at 27. |
| 2026-09-11 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `docker exec` for rake invocations only), admin | **Customer-view Product/custom-field visibility (BUG-HLP-051/052), full Knowledgebase editor block-type/inline-formatting sweep (0 new bugs), and `HELPDESK_RAKE_TASKS.md` closed out 13/13 — 4 new bugs (BUG-HLP-053–056).** User directly challenged whether Product selection/edit-lock on ticket creation had actually been tested — confirmed selection works and saves, but the saved value is then permanently `display:none` for the Customer role (**BUG-HLP-051**), contradicting `HELPDESK_USER_GUIDE.md` §7. A direct user follow-up ("did you notice the custom field label is visible but the value isn't") surfaced a distinct second bug the same day: genuine Redmine custom fields render their label but leave `.value` empty for the customer, despite the data being genuinely saved (confirmed via Admin) — **BUG-HLP-052**. Then, per a user "test all of them" answer to whether the KB editor's block types/inline formatting had been exhaustively tested, swept all 12 block types and 5 inline formats via genuine Publish+reload+DOM inspection — zero new bugs, pure coverage completion, `HELPDESK_CONTENT_TEMPLATES.md` TC-HLP-018 updated. **Standing methodology corrected by direct user pushback mid-session** ("are you using backend for testing why you are not using mcp palywright") — re-did a project-existence pre-check via real Playwright UI navigation instead of `rails runner`; the rule going forward: invoking a rake task itself (no UI trigger exists) is still legitimately `docker exec`, but verifying any resulting state must always be via the real UI. Then closed `HELPDESK_RAKE_TASKS.md` from 1/13 to genuinely 13/13: **BUG-HLP-053** (seed_demo_data's legacy-project-identifier match renames/repurposes any matching project via `update_columns`, no sanity check — source-verified, not reproduced live since the primary-identifier project already exists here); **BUG-HLP-054** (seed_demo_data crashes on large `TICKETS` values once Sakura Mobility KK's own hard-enforced prepaid budget is exhausted — confirmed **permanent**: re-running the identical command crashed at the exact same point again, 233/300 tickets both times; High severity); **BUG-HLP-055** (check_emails is 100% dead code, identical shape to BUG-HLP-044 — even Alpha/Beta, with real working mail config, are reported "disabled"); **BUG-HLP-056** (check_sla detects/marks breaches correctly but every escalation attempt fails — wrong reason-string literal passed vs. what the model's inclusion validation and the real scheduled worker both expect). Also confirmed TC-HLP-255's `seed_reports` task **does not exist at all** (`rake -T` shows only 5 real tasks) — a documentation gap, not a bug — and corrected TC-HLP-251's stale premise (real project is "Redmineflux Helpdesk", not "Helpdesk Support"). Open bug count: 32 → **36**. |
| 2026-09-11 | Local (redmine-docker-6, localhost:3012) + Production (`ztflux`, flux.zehntech.com) | Local + Production | Claude (Playwright MCP + `fetch`-driven REST API testing + `docker exec` for Sidekiq/Redis restart), admin + `manage.helpdesk.test` + `alpha.customer` | **Filed BUG-HLP-053–056 to production (`ztflux`), fixed a real MCP `upload_file` size-limit bug via a `pdfkit` rewrite of `gen_bug_pdf.js`, then `HELPDESK_REPORTING_AUTOMATION.md` closed out 23/23 (was 0/23) — 4 new bugs (BUG-HLP-057–060).** Explicit user instruction to report BUG-HLP-053–056 to production, linked to testcase #119389/Run #541/Environment "Win 10 + Chrome", assignee Vaishnavi Bhawsar. Discovered the redmineflux MCP's `upload_file` tool silently truncates (or throws "invalid base64") any file whose base64 encoding exceeds ~20-22K characters — the old Chromium-rendered PDFs (70-104KB, always embedding 4 font files even for plain text) were being silently truncated on upload with no error. Rewrote `scripts/gen_bug_pdf.js` around `pdfkit`'s non-embedded Base-14 fonts, shrinking output to 6-8KB — safely under the ceiling; all 4 production issues (#120504/507/508/510) filed successfully with correctly-sized PDF attachments. User then directly challenged whether `HELPDESK_RAKE_TASKS.md` was genuinely fully tested — re-confirmed 13/13 honestly by re-reading the file rather than asserting from memory — then picked `HELPDESK_REPORTING_AUTOMATION.md` via AskUserQuestion. Full REST API section (Tickets/Conversations/SLA status/Organizations/SLAs/`process_macros`/`sla_analytics`) exercised live against the Swagger spec, correcting several of my own initial request-shape guesses (nested-vs-flat body params per endpoint; `pause`/`resume` are `PATCH` not `POST`). **BUG-HLP-057** (Medium): `export_helpdesk_reports` is fully vestigial — Reports view+export gated purely by `manage_helpdesk`. **BUG-HLP-058** (Medium): the REST API's `conversations` reply endpoint correctly triggers the SLA first-response side effect but logs to Notes instead of Helpdesk Conversion. **BUG-HLP-059** (Medium): the Swagger spec's documented "existing browser session" auth method is completely non-functional for every role (confirmed via a real admin session getting `401`), and Customer accounts have no self-service way to ever obtain an API key at all — the spec's own customer-access clause is unreachable in practice. **BUG-HLP-060** (High, biggest finding): after the standing pre-check restarted Redis + Sidekiq (both found down), Sidekiq's own boot log showed its internal `Scheduled::Poller` thread — which fires all 3 documented cron jobs (`email_checker`/`sla_monitor`/`auto_close_tickets`) — crash immediately with a real `ArgumentError` (`sidekiq 7.3.9`/`connection_pool 3.0.2` gem incompatibility, confirmed via `bundle list` + full stack trace), silently disabling all scheduled background automation with the main process staying alive. Live-confirmed on a manufactured 1-minute-SLA ticket: breach never auto-detected despite waiting past the deadline and a full monitor cycle — a genuine **regression**, since this exact mechanism is on record working earlier in this engagement. `/helpdesk/swagger`'s "Try it out" confirmed to genuinely execute real requests; confirmed refused (`403`) for a non-admin `manage_helpdesk` user. REST API list counts matched UI counts exactly under an identical filter (83=83). Open bug count: 36 → **40**. All suites now closed except `HELPDESK_PLUGIN_INSTALLATION.md` (0/12). |
| 2026-09-14 | Disposable Docker instance (`redmine-install-test:6`, `localhost:3099`, isolated from `redmine-docker-6`) | Local (disposable) | Claude (Playwright MCP + `docker exec` for install/migration steps), admin + a fresh customer fixture | **`HELPDESK_PLUGIN_INSTALLATION.md` closed out — the last untested suite, 11/12 executed live, TC-012 explicitly skipped per user decision, 0 new bugs.** This suite tests the install/migration process itself, not application UI behavior, so several TCs are destructive (folder renaming, wrong-order migrations) and unsafe to run against the shared `redmine-docker-6`. Built a fresh disposable stack instead: official `redmine:6` image + this plugin's own source, container command overridden to `tail -f /dev/null` so the image's own auto `bundle install`/`db:migrate` entrypoint logic never fires — full manual step-by-step control. TC-001: complete fresh-install sequence (bundle install → db:migrate → load_default_data → plugins:migrate → install+start Redis/Sidekiq → restart) PASS end-to-end, plugin listed/enabled via real UI. TC-002/003: "Waiting for Customer Response" status and "Support" tracker both confirmed created by this exact migration — Support resolved as one of Redmine's own 3 default trackers, not plugin-created. TC-006/010: re-running `load_default_data`/`plugins:migrate` both clean no-ops, zero duplication. TC-008: migrating before `bundle install` → real `Bundler::GemNotFound`, zero tables created. TC-011: migrating before `load_default_data` on a virgin DB → 1 status created by the plugin alone, then the documented refusal, 0 trackers/priorities left. TC-007: renaming the plugin folder → **hard Redmine boot failure** (`Redmine::PluginNotFound`), more severe than expected; full recovery after renaming back. TC-005: simulated upgrade (no real second version available, so identical source re-copied + re-migrated) preserved an SLA/customer/ticket fixture unchanged. TC-009: stopped Redis/Sidekiq entirely, built a fresh project/SLA/Support Level/Org/Customer via the real UI, raised+assigned a real ticket — SLA genuinely breached (UI: "SLA Missed") but never auto-escalated (`response_breached: 0` at the DB level), all helpdesk UI screens kept working normally. TC-012 (5.0.x vs 6.1.x parity) skipped by explicit user decision rather than building a second disposable instance. **Side finding, folded into BUG-HLP-060, not new**: the Sidekiq scheduler gem-incompatibility crash reproduced byte-for-byte identically on this completely fresh install, confirming it's deterministic (the plugin's own Gemfile resolves `sidekiq 7.3.9`+`connection_pool 3.0.2` with zero manual intervention), not `redmine-docker-6`-specific drift. Open bug count unchanged at 40. **Every one of this plugin's 12 suite files is now either fully closed or has an explicit user decision recorded for what's left.** Disposable container left running per user instruction, holds no data anything else depends on. |
| 2026-09-18 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `rails runner`/rake for worker/rake invocation only), admin | **BUG-HLP-043 and BUG-HLP-044 retested and CONFIRMED FIXED, both closed — the two production-`In QA` bugs for this plugin (#120375/#120376).** User restarted `redmine-docker-6` and asked to find every Helpdesk bug currently `In QA` on production and retest it — `redmineflux_core_list_issues` against `ztflux` (subject "BUG-HLP") returned exactly these two. Redis/Sidekiq were found NOT running after the restart (only Puma auto-restarts on this container, standing quirk) and were started manually first. Rediscovered a real navigation gotcha while re-locating the Email Configuration form: the per-project `/projects/:id/helpdesk/settings` page has only 3 tabs (Holiday/Products/Support Level), no Email Configuration at all — that form lives only at the top-level `/rf_helpdesk/setting?tab=email_configuration&config_project_id=<id>`, reached from "Helpdesk Command Center", not from a project's own Settings. Set Auto Close Ticket Days = 1 on Alpha via that real UI, created a fresh backdated New-status ticket (#336). **BUG-HLP-043**: live-invoked `Helpdesk::AutoCloseTicketsWorker` directly — ticket #336 correctly left untouched (`Closed 0 tickets`); source confirms the query is now `.where(status_id: resolved_status.id)`, Resolved-only, replacing the old unscoped `.open`. **BUG-HLP-044**: ran `bundle exec rake redmineflux_helpdesk:auto_close_tickets` for real — genuinely processed both Alpha and "Redmineflux Helpdesk" and closed 43 real eligible tickets in one run (vs. the original's silent "no project processed" no-op); source confirms it now reads `RfHelpdeskEmailConfig.for_project(project)&.auto_close_days`, replacing the dead legacy `ProjectCustomField` lookup. Both bugs' fix pass corrected the identical scope defect in both files together. Auto Close Ticket Days reset back to blank on Alpha afterward. Both bug files updated with retest evidence and moved to `bugs/closed/`; `bugs/_index.md` updated. **Production sync to In QA → Done / 100% for #120375/#120376 still pending explicit write approval** (not yet executed this session). Per explicit user instruction, also deleted the two disposable install-test containers (`redmine-docker-fresh-test` port 3097, `helpdesk-install-test-5x` port 3098) used for earlier provisioning/TC-012 work — only `redmine-docker-6` remains for this plugin now. Open bug count: 40 → **38**. |
| 2026-09-18 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `rails runner`/rake for source-fix verification only), admin + `alpha.customer` | **5 more production-`In QA` bugs retested and CONFIRMED FIXED, all closed — BUG-HLP-050, 051, 053, 055, 056 (prod #120474/#120475/#120504/#120508/#120510).** User supplied the 5 production issue numbers directly; `redmineflux_core_get_issue` mapped each to its local bug ID. **BUG-HLP-050** (KB attachment removal): a genuinely new delete icon now exists in the Attachments panel's Action column, wired to a real confirm modal and `DELETE rf_kb_delete_attachment`; live-clicked on the original fixture (article 3 / attachment 9) and confirmed via direct DB check both the `Attachment` row and its join record are destroyed (residual nuance noted: the article's saved content JSON still visually shows the old block's filename until the article itself is re-saved — a display artifact, not a functional gap). **BUG-HLP-051** (customer Product field hidden): the offending CSS rule in `customer_menu.css` is removed with a comment explaining the original mistake (it guarded against an already-server-side-prevented duplicate); live-verified via a fresh ticket (#337) as `alpha.customer` — Product now visibly shows. **BUG-HLP-053** (seed_demo_data legacy-identifier hijack): `seed_project` now requires the seeder's own `SEED_DESCRIPTION_MARKER` in a legacy-identifier match's description before reusing/renaming it — source-verified only, same as the original filing (the legacy branch stays unreachable live on this instance). **BUG-HLP-055** (check_emails dead legacy config): now reads `RfHelpdeskEmailConfig`; live run correctly recognized Alpha/Beta as enabled (real protocol/server/username, attempted a genuine IMAP connection — refused due to a separate local mail-server networking issue, not the original defect) while Gamma/Redmineflux Helpdesk stayed correctly skipped. **BUG-HLP-056** (check_sla escalation reason mismatch): now passes the correct machine-readable `'response_breach'`/`'resolution_breach'`; live run went from the original `Escalations: 0, Errors: 118` to `Escalations: 202, Errors: 0`, confirmed via a real `[SLA][ESCALATION]` log line. All 5 local bug files' Production Redmine Issue ID fields were also standardized to the `#NNNNNN (ztflux)` format (were bare numbers). All 5 production issues synced to Status: Done, % Done: 100. Also corrected a real discrepancy found while reconciling counts: `HELPDESK_MEMORY.md`'s narrative "Open bug count" figure had drifted into a cumulative-bugs-ever-filed counter (reached "40") that no longer matched the real `bugs/open/` folder (which held 22 files before this session's closures) — switched to reporting the literal folder count going forward (**15** remaining open: BUG-HLP-015/020/022/037/045/046/047/048/049/052/054/057/058/059/060). |
| 2026-09-18 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + real Roundcube email round-trip + `fetch`-driven REST API testing + `docker exec` for mail-server/Sidekiq fixes), admin + `alpha.customer` + `perm.test.agent` | **6 more production-`In QA` bugs retested — BUG-HLP-045/046/047/057/058 CONFIRMED FIXED and closed (prod #120377/378/379/539/540 synced to Done/100%); BUG-HLP-059 (#120543) only PARTIALLY FIXED, kept OPEN.** User asked to restart Sidekiq (container had been restarted again) and retest the next batch of production-`In QA` bugs; `redmineflux_core_list_issues` returned these 6. **Real environment fix along the way**: the local mail server's Dovecot (IMAP) was stuck in a crash-loop on a stale `/run/dovecot/master.pid` since an earlier restart — only Postfix (SMTP) was up; fixed via `supervisorctl stop dovecot; rm -f /run/dovecot/*.pid; supervisorctl start dovecot`, confirmed port 993 reachable from `redmine-docker-6` afterward. **BUG-HLP-045** (email misrouted to customer's first project row): `target_project_with_helpdesk` now prefers the polled project; live-verified via the real `Helpdesk::EmailPollerWorker` after giving `alpha.customer` a genuine second project-access row and sending a real email to Beta's mailbox — routed correctly. **New rake-task-only defect found while chasing false negatives, not filed (out of scope)**: `check_emails`'s `Redmine::IMAP.check(imap_options) { |message| ... }` block is dead code on this Redmine version — the real `Redmine::IMAP.check(imap_options={}, options={})` never yields, it calls `MailHandler.safe_receive` with its own unset `options` argument — so the rake task's per-project routing/options are silently never applied; the real scheduled worker (own direct `Net::IMAP` handling) is unaffected. **BUG-HLP-046** (Email History missing recipient/subject/direction): `_index.html.erb` now renders explicit Inbound/Outbound badges + Subject + To:, live-verified on ticket #63. **BUG-HLP-047** (duplicate mailbox config): two new model validations refuse Save with a clear conflicting-project message, live-verified both incoming and outgoing reuse. **BUG-HLP-057** (`export_helpdesk_reports` no-op): gate now also accepts that permission; live-verified `perm.test.agent` can view all 5 Report tabs and export CSV. **BUG-HLP-058** (API reply logs to Notes not Conversion): endpoint now sets the same thread-local signal the UI Reply Note uses; live Conversion count went 0→1 with a real Outbound/"via Reply Note" entry (still also visible under plain Notes — noted, not confirmed whether a genuine UI reply differs, not blocking closure). **BUG-HLP-059** (session auth + no customer API key): both originally-cited causes genuinely fixed (session-cookie auth now works for admin; `alpha.customer`'s `/my/api_key` now shows a real key) — but end-to-end a customer still can't reach the API: `restrict_helpdesk_customer_access` (a separate, unrelated filter) redirects any logged-in customer away from `api/v1/*`, never added to its allowlist; and key-only access (no session) reaches the API but returns zero visible tickets for her, including her own (`total_count: 0`, `404` on her own ticket by ID) — likely the same no-real-Membership pattern as BUG-HLP-014. Kept open with the full nuanced writeup rather than closed or reframed unilaterally; flagged to the user for a decision. Literal `bugs/open/` count: 15 → **10**. |
| 2026-09-18 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `docker exec` for rake re-run only), admin + `alpha.customer` | **2 more production bugs retested and CONFIRMED FIXED, both closed — BUG-HLP-054 (#120507) and BUG-HLP-020 (#119752); BUG-HLP-059 (#120543) re-checked, unchanged from earlier this session.** User supplied 3 more production issue numbers. **BUG-HLP-054** (seed_demo_data crashes on large `TICKETS` once Sakura's prepaid budget is exhausted): `log_time_for_ticket` now wraps its `TimeEntry.create!` in a `rescue StandardError` that skips just the one over-budget entry (explicit `BUG-HLP-036 (via #120507)` comment) instead of aborting the whole rake task. Live-verified: re-ran `TICKETS=300` on the project already sitting at 234 tickets from the original crash — hit the identical trigger condition **14 times**, each gracefully skipped with a clear message, and the task completed all the way to a real `Done.` summary (66 new, 300 total across 6 SLA states) instead of crashing again at 233. **BUG-HLP-020** (unassign/reassign resumes the SLA clock without crediting back the paused duration): `rf_issue_sla_status.rb#resume!` now extends both `response_deadline` and `resolution_deadline` by the pause duration for any pause reason — `pause_sla`/`resume_sla` (the methods `issue_patch.rb` calls on unassign/reassign) now delegate to the exact same shared `resume!` the Waiting-for-Customer-Response cycle already used correctly, unifying two previously-separate code paths into one. Live-verified end-to-end on a fresh customer-raised ticket (#404, genuinely SLA-attached): assigned → noted real `response_deadline` (`14:13:55`) → unassigned via the real Edit form (`is_paused: true`, `paused_at` recorded) → waited a real ~2 minutes → reassigned via the real Edit form → `response_deadline` moved to `14:15:55`, `total_paused_minutes: 2` — extended by exactly the pause duration, the precise mechanism whose absence caused the original bug's false L2→L3 escalation. Minor residual, not blocking: the Activity Log still shows no explicit Paused/Resumed entries for this specific cycle (an audit-trail display gap, not a deadline-accuracy one). Both production issues synced to Done/100%. Literal `bugs/open/` count: 10 → **8**. |
| 2026-09-21 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `docker exec`/`rails runner` for source verification, container restart, and plugin migration only), admin + `retest.customer1` | **BUG-HLP-059 reopened on production per explicit user instruction; then 4 more production bugs retested and CONFIRMED FIXED, all closed — BUG-HLP-060 (#120545), BUG-HLP-052 (#120476), BUG-HLP-049 (#120471), BUG-HLP-048 (#120465).** User first instructed: "if bug 59 is not fixed please reopen it write note in 120543 production" — moved #120543 from In QA to **Reopen** with a note detailing both remaining gaps from the prior retest (the `restrict_helpdesk_customer_access` allowlist gap and the zero-ticket-visibility gap), folded into the same bug's scope rather than filed separately; local file/index updated to match. User then asked to restart the container, start Sidekiq, and retest 4 more production-`In QA` issues. **BUG-HLP-060** (Sidekiq scheduler crash): `connection_pool` pin (`~> 2.5`) confirmed picked up on this restart's `bundle install`; live-verified a fresh ticket (#406) auto-breached and auto-escalated with zero manual worker invocation, purely via the scheduled cron mechanism (after correcting my own Sidekiq startup command to include `-C plugins/redmineflux_helpdesk/config/sidekiq.yml` — a bare `sidekiq -e production` silently strands every cron-enqueued job on the wrong queue with no error). **BUG-HLP-052** (customer view: custom field value blank): the customer template now delegates to Redmine core's own `render_half_width_custom_fields_rows`/`render_full_width_custom_fields_rows` helpers; live-verified as the ticket's real author (`retest.customer1`) — the previously-empty `.value` div now shows "Report a Bug". **BUG-HLP-049** (KB Page History no compare/restore): real `version_restore` action + compare modal + per-row Compare/Restore links now exist; live-verified on "Resetting your password" — Compare showed a genuine side-by-side diff, Restore genuinely overwrote current content with an old version's (after snapshotting the pre-restore state first, confirmed via DB). **BUG-HLP-048** (KB nesting never sends parent_id): `createNewPage()` now sends `parent_id`, model has real associations/validations, `buildTree()` actually nests the flat list; live-verified creating a child from an open parent nested it correctly in both breadcrumb and sidebar tree. **Critical operational discovery, recorded to root `MEMORY.md` and this plugin's memory for all future sessions**: this plugin shipped a real migration (`add_parent_id_to_rf_knowledgebase_pages`) that a plain container restart does NOT apply — `redmine-docker-6`'s entrypoint auto-runs `rake db:migrate` (core) but only runs `rake redmine:plugins:migrate` if `REDMINE_PLUGINS_MIGRATE` is set, which it isn't. The first BUG-HLP-049/048 retest attempts both `500`'d for exactly this reason; fixed by running the plugin migration manually and then restarting the container again (a migration applied to a live Puma process leaves stale prepared-statement/schema-cache state that needs a restart to clear). All 4 bug files updated with retest evidence and moved to `bugs/closed/`; `bugs/_index.md` updated. **Production sync to In QA → Done/100% for #120545/476/471/465 still pending explicit write approval** (not yet executed this session). Literal `bugs/open/` count: 8 → **4** (BUG-HLP-015, 022, 037 unconfirmed, 059 reopened remain). **Same session, follow-up**: user pointed out via two screenshots that hovering a sub-page in the KB sidebar tree shows what looks like a "+" icon which, when clicked, opens a delete confirmation — asked directly whether this had been noticed. Investigated and filed **BUG-HLP-061 (Low)**: source-confirmed no per-row "add child" icon exists at all (a `.add-child-icon` CSS rule is dead, never emitted by JS); the Delete icon's own SVG is a genuine "×" (confirmed unambiguous at 200×200px) but renders at only 10×10px in the tree, small enough to be visually indistinguishable from a "+". A real confirm dialog stands between the click and any actual deletion, so not a silent-data-loss risk, but a genuine icon-clarity defect. Literal `bugs/open/` count: 4 → **5**. Production sync executed this session, after explicit approval: #120545/476/471/465 all set to Status: Done, % Done: 100. BUG-HLP-061 reported fresh to production as **#121011** (ztflux), then linked to Test Case #119389 / Run #541 / Environment "Win 10 + Chrome" per explicit user request (an environment mismatch was caught first — Run #541 only carries "Win 10 + Chrome" for that testcase, not "Windows 11 + Chrome" — user confirmed the correct one before the link was made). |
| 2026-09-21 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `docker exec`/`rails runner` for source verification and container/Sidekiq restart only), admin + `retest.customer1` + `alpha.customer` | **2 more production bugs retested — BUG-HLP-022 (#119755) CONFIRMED FIXED and closed; BUG-HLP-059 (#120543) retested again, PARTIALLY FIXED, stays open with refined evidence.** User asked to restart the container, restart Sidekiq, and retest these two. Both restarted cleanly, no pending plugin migrations this time. **Unrelated cosmetic surprise, not pursued further**: the login page now shows a completely different "Redmineflux"-branded demo skin with a "Demo quick login" dropdown of generic roles — confirmed to be purely a login-page skin change; all real fixtures/data were intact post-login. **BUG-HLP-022 (SLA half)**: created a fresh SLA, assigned it to a customer's project-access row with no ticket referencing it, clicked Delete — now correctly blocked ("This SLA cannot be deleted because it is currently assigned to a customer."), confirmed via DB the SLA genuinely still exists. Both halves (Support Level + SLA) now fixed; closed. **BUG-HLP-059**: the `restrict_helpdesk_customer_access` allowlist gap is now genuinely fixed (explicit `api/v1/*` early-return with a `BUG-HLP-059 (reopened)` comment) — live-verified a real customer session can reach the API without being redirected away. A genuine new partial improvement was found: `GET /tickets/:id` (show) now works for a customer's own ticket via key-only auth (was a 404 originally). But the list endpoint (`GET /tickets`) still returns `total_count: 0` for a customer with real authored tickets — confirmed on 2 different customers, via both key-only and session-based auth, against an admin-key baseline correctly returning all 392 tickets unfiltered; extensive black-box testing (cache-busting, `credentials: 'omit'`, ruling out stale code via file mtimes) confirmed this is real and reproducible, but the exact internal root cause was not pinned down this session despite the source appearing to already implement the right fix. Kept open, not resynced to production (already Reopen from before, no status change proposed). Literal `bugs/open/` count: 5 → **4** (BUG-HLP-022 closed). Remaining open: BUG-HLP-015, BUG-HLP-037 (unconfirmed), BUG-HLP-059 (reopened), BUG-HLP-061. |
| 2026-09-22 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `docker exec`/`rails runner` for source verification and container/Sidekiq restart only), admin + `delta.customer` + `luna.blossom` | **3 more production bugs retested (a 4th, #119589, correctly skipped — Inline Editor Plugin, not Helpdesk) — BUG-HLP-061 (#121011) and BUG-HLP-059 (#120543) CONFIRMED FIXED and closed; BUG-HLP-037 (#120076) RECONFIRMED and re-scoped, stays open.** User restarted the container + Sidekiq, both cleanly, no pending plugin migrations. **BUG-HLP-061**: source now emits a real, distinct "Add sub-article" icon (`kb-plus-icon`, a genuine "+" at 12×12px) per row, visually confirmed unambiguous vs. the delete "×" via a real screenshot at actual size; a new `treeAddParentId` mechanism correctly nests a new article under the exact row clicked (verified via a 3-level breadcrumb), not just whichever page happens to be open — a more complete fix than the bug's own minimum recommendation. **BUG-HLP-059**: the last remaining gap from 2026-09-21 (ticket-list endpoint returning `total_count: 0` for real customers) is now resolved — both `retest.customer1` (29 tickets) and `alpha.customer` (2 tickets) get correctly-scoped results via key-only and session auth, admin baseline still correctly unfiltered at 392; all 4 sub-issues found across this bug's full retest history are now fixed, no root cause for the prior day's failure was ever identified (possibly a stale-process artifact from that session's own restart sequence). **BUG-HLP-037**: previously marked "Unconfirmed" after 3 clean retests — this session's retest (org reduced to exactly 0.00h Remaining under Hard mode via a single reduction, matching the original repro's shape) reproduced the original symptom again for new-ticket creation (`delta.customer`'s new ticket succeeded, "Successful creation.", ticket #407 left in place as evidence), while the time-log path on the identical budget state (`luna.blossom` replying to ticket #46 with a 10-min entry) was correctly blocked with a real validation error. Tally across all retests to date: new-ticket-creation fails 2-of-3 independent attempts (majority), time-log blocks 4-of-5 (reliable) — verdict changed from Unconfirmed to a real, reproducible defect, re-scoped to cover only the new-ticket-creation path; time-log enforcement treated as confirmed working. Org budget restored to baseline (21.67h Approved / 6.75h Remaining) afterward. Literal `bugs/open/` count: 4 → **2** (BUG-HLP-061, BUG-HLP-059 closed). Remaining open: BUG-HLP-015, BUG-HLP-037 (reconfirmed, re-scoped). Production sync for #121011/#120543 and a decision on #120076's production status are both still pending explicit approval as of session end. |
| 2026-09-22 | Local (redmine-docker-6, localhost:3012) | Local | Claude (Playwright MCP + `docker exec`/`rails runner` for source grep and read-only checks), admin + `alpha.customer` | **Production sync executed for the 3 bugs above (#121011/#120543 → Done/100%, #120076 → Reopen) after explicit approval; then BUG-HLP-015 also retested at user request — NOT FIXED, both parts still reproduce.** No Production Redmine Issue ID exists for BUG-HLP-015 (never reported), so this was a fresh local retest, not a fix-verification. Source grep for "BUG-HLP-015" across the whole plugin found zero matches — no fix attempted for either part. Part A: `alpha.customer` on ticket #3 still sees a full "Notes" tab alongside "Helpdesk Conversion (4)" — unchanged from the original finding. Part B: her real 2026-08-31 email reply is still duplicated as a plain Journal (`change-3`) alongside its correct Helpdesk Conversion entry — and this is now *worse* than originally found, since the original mitigation (her own Notes tab rendering empty for her) is gone; she can now see the same duplicate journal an agent sees, confirmed identical from both sessions. Bug stays open with updated retest evidence; nothing to sync on production (never reported). |
| 2026-09-22 | Local (redmine-docker-6, localhost:3012) | Local | Claude, admin (via redmineflux MCP to `ztflux`) | **BUG-HLP-015 reported to production for the first time, per explicit user instruction ("okay create it on production").** Created **#121073** on `ztflux`, tracker Bug, assigned to Vaishnavi Bhawsar (user's explicit selection via AskUserQuestion), Priority Medium / Defect Severity Medium-severity / Defect priority Medium / Defect Type Functional / Category Helpdesk Plugin. Description (Textile) covers Preconditions, both Part A and Part B steps/expected/actual, the 2026-09-22 retest finding that Part B is now visible from the customer's own Notes tab too, and a Recommendation section carried over from the local file's Notes. Local `BUG-HLP-015.md` — which had no `Production Redmine Issue ID` header field at all, pre-dating the template convention — updated with the field and a closing note; `bugs/_index.md` row updated to reference #121073. No status change to `bugs/open/` (still open locally, now also open on production). |
