# Bug Index — Redmineflux Helpdesk

| Bug ID | Title | Status | Severity | Redmine Version | File Path |
|--------|-------|--------|----------|-----------------|-----------|
| BUG-HLP-001 | New Customer form shows a permanently stuck Login validation message that doesn't reflect actual submission outcome (misleading, not a hard block — some attempts succeed despite it). **Retest 2026-08-27 (Local, fresh rebuilt environment): does NOT reproduce** — no stuck error on fresh load, customer created successfully on the first attempt (no retry needed). **Closed same day** — per user explanation, a new Forge server pulls a fresh plugin clone on creation, so the original instance's stuck-validation state doesn't carry forward; root cause itself was never confirmed in code. | Closed | Medium | Forge (flux-fiqll6jza49) | bugs/closed/BUG-HLP-001.md |
| BUG-HLP-002 | Support Level "Escalation To" dropdown is unscoped ONLY via the unlinked global route — caused by BUG-HLP-005, not independent. **Retest 2026-08-31: does NOT reproduce** — the bypass route (BUG-HLP-005) no longer exists at all, so this bug's trigger condition is gone; proper-navigation Escalation To confirmed still correctly scoped. **Closed per explicit user confirmation**, as predicted when BUG-HLP-005 was fixed. | Closed | Low (root cause was BUG-HLP-005) | Forge (flux-fnwchpuwl49 / re-verified flux-fudbk2hlu49) | bugs/closed/BUG-HLP-002.md |
| BUG-HLP-003 | Active checkbox cannot be unchecked via Edit on Organization, SLA, and Canned Response only (Product and Support Level confirmed NOT affected); a working list-level toggle exists as a workaround for Organization/SLA, but Canned Response has no workaround (complete block). **Retest 2026-08-27 (Local, fresh rebuilt environment): PASS on all three entities** — SLA, Organization, and Canned Response all now correctly persist an Active-uncheck through the Edit form. **Closed per explicit user confirmation.** | Closed | High | Forge (flux-fw2qhf0ux49 / flux-fxd0g3s4049 / flux-fudbk2hlu49) | bugs/closed/BUG-HLP-003.md |
| BUG-HLP-004 | Customer form's SLA/Support Level dropdowns still list cross-project options (disabled, not removed) — the plugin's own `hidden` attribute on those options is defeated by CSS. **Retest 2026-08-27 (Local, two-project rebuild): does NOT reproduce** — `getComputedStyle(option).display` now correctly reports `"none"` for the cross-project option, both directions, both dropdowns. **Closed per explicit user confirmation.** | Closed | Low | Forge (flux-fudbk2hlu49) | bugs/closed/BUG-HLP-004.md |
| BUG-HLP-005 | SLA and Support Level can be created via unlinked global routes that bypass the intended project-scoped navigation — for SLA this produces a permanently orphaned, unmanageable record. **Retest 2026-08-31: PASS** — `/rf_slas`, `/rf_slas/new`, `/rf_support_levels`, `/rf_support_levels/new` all now return genuine 404s (routes removed entirely, stronger than the requested fix); proper project-scoped flow re-confirmed working with a cleaner path-based URL. **Closed per explicit user confirmation.** | Closed | Medium | Forge (flux-fudbk2hlu49) | bugs/closed/BUG-HLP-005.md |
| BUG-HLP-006 | A customer cannot open a ticket they created — the ticket link in their own restricted ticket list points to the standard `/issues/:id` route, which the Customer role is not authorized to access. **Does NOT reproduce on Local** — confirmed twice independently (2026-08-26 and 2026-08-31, on two different environment rebuilds) — Local's customer ticket list links to the branded `/projects/:id/helpdesk/issues/:id` route instead, which customers can access. **Closed per explicit user confirmation** despite no Forge-side re-verify (current Forge URL was stale) — see Closed section for the process caveat. | Closed | Critical | Forge (flux-forgyim9c49) | bugs/closed/BUG-HLP-006.md |
| BUG-HLP-007 | Submitting an Internal Note crashes with a 500 (`journals.private_notes` NOT NULL violation, submitted as `""`) — reproduces only via the branded `/projects/:id/helpdesk/issues/:id` route (`RfProjectHelpdeskIssuesController`), not via core `/issues/:id`; real-world impactful because every notification email's "View Ticket" link uses the branded route. **Retest 2026-08-31 (Local, fully rebuilt environment): still reproduces, byte-for-byte identical exception.** **Expected result expanded 2026-08-31 per user product-judgment direction: an Agent should never be routed to the branded/customer URL at all — only the core `/issues/:id` route.** Retested 2026-09-10 (prod #119631 checked in) — CONFIRMED FIXED: the branded route now redirects Agents server-side to the core route, and Internal Note submission on the core route succeeds cleanly. | Closed | High | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-007.md |
| BUG-HLP-008 | "Helpdesk Conversion" tab is an incomplete/inaccurate email audit trail — a customer's real inbound-email reply to an existing ticket is never logged there (only visible under Notes), and one outbound agent-reply entry is duplicate-logged mislabeled as an inbound "Customer replied to Support" message; likely shares a root cause with the `EmailPollerWorker` warning. **Retest 2026-08-31 (Local, full email round-trip rebuilt): PASS on both parts** — the real customer reply is now correctly logged (Conversion count 3→4), no duplicate/mislabeled entries; server log shows a clean save instead of the original `MailHandler returned unexpected result: Journal` warning. **Closed per explicit user confirmation.** | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-008.md |
| BUG-HLP-009 | Adding a note via the standard Redmine "Edit" form auto-transitions the ticket's Status to "Waiting for Customer Response" even though nothing is actually communicated to the customer through any channel (no Notes visibility — expected — but also no Helpdesk Conversion entry, no email). **Retest 2026-08-31 (Local, fresh ticket): PASS** — Status stays "New" after a plain Edit-form note, confirmed via the ticket itself, the absence of any Property changes tab, and a clean contrast in the agent's Activity feed against a genuine Reply Note on a different ticket. **Closed per explicit user confirmation.** | Closed | High | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-009.md |
| BUG-HLP-010 | Organization Website and Phone Number allow exact duplicates across different organizations with no warning — matches current documented spec (only Name is unique) but filed per explicit user product-judgment direction as a real-world data-integrity gap. **Retest 2026-08-31 (Local, fresh rebuilt environment): PASS** — duplicate Website/Phone now genuinely refused outright ("Phone number has already been taken" / "Website has already been taken"), stronger than the minimum the bug asked for. **Closed per explicit user confirmation.** `HELPDESK_USER_GUIDE.md` §3.4 and TC-HLP-096 still need their own updates to reflect the new enforced-unique behavior (tracked separately, not blocking this closure). | Closed | Low | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-010.md |
| BUG-HLP-011 | An organization linked to a customer can be deleted with only a generic "are you sure" prompt — no warning that a customer is linked, silently clearing the customer's Organization reference to "—". **Retest 2026-08-31 (Local, fresh rebuilt environment): PASS** — delete confirmation now explicitly reads "This organization has 1 linked customer(s) — deleting it will remove that association"; rest of the flow (graceful clearing to "—", SLA/Support Level preserved) unchanged. **Closed per explicit user confirmation.** | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-011.md |
| BUG-HLP-012 | ~~Customer Edit form's Information section editable by an out-of-project Manager~~ — **Closed: Not a Bug.** Customers are a global entity (Login/Email unique install-wide); the Add-project mechanism on the same form is correctly scoped to the Manager's own project, confirming Information fields are the intentionally-global part of the record, Project-access rows are the intentionally-scoped part | Closed | — | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-012.md |
| BUG-HLP-013 | **Revised 2026-08-27 per user product-judgment direction.** Portal Preview (`/rf_customers/:id/portal`) is not considered part of this plugin's intended functionality (see `HELPDESK_SCOPE.md`), yet the route is fully reachable and functional (read-only, correct scoping, 403 on out-of-entitlement project) despite having no UI entry point anywhere — the defect is now that the route exists at all, not that it lacks a button. Needs a product/dev decision: remove the route, or reverse the scope call and add the missing entry point. TC-HLP-056 → FAIL, TC-HLP-066 → PASS (secondary). **Retest 2026-08-31 (Local, fresh customer, zero shared history): PASS** — `/rf_customers/6/portal` (and its `?project_id=` variant) now return a genuine HTTP 404; the route was formally removed, matching the first option in this bug's own Recommendation. TC-HLP-056 now genuinely passes as written. **Closed per explicit user confirmation.** | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-013.md |
| BUG-HLP-014 | A customer's own Tickets list shows "0 tickets" even when their tickets genuinely exist and are individually reachable. **Root cause confirmed via source + `rails runner`**: `TicketFilter#base_scope` starts from Redmine's native `Issue.visible(user)`, which requires real project Membership — but customers are (by design) linked only via `RfProjectCustomer`, never a Redmine Member, so `Issue.visible` returns zero unconditionally. Every other customer check in the plugin (page access, ticket access, Dashboard count) correctly uses `RfProjectCustomer` instead; only this one list fell back to Redmine's native scope. **Confirmed systemic** — reproduced identically with a brand-new customer (`delta.customer`) with zero prior history, so this affects every customer account, not a fixture-specific artifact. **Retest 2026-08-27: PASS** on a fully rebuilt environment (new project/agent/role/SLA/support level/customer, zero shared history with original repro) — customer ticket list now correctly shows 1 then 2 tickets, survives a container restart. **Closed same day per explicit user direction** — note the §26 full-suite regression this High-severity fix would normally require was not run before closure (environment wipe means it's effectively a from-scratch 32-TC re-run); still tracked as outstanding general suite coverage in `HELPDESK_HANDOFF.md`. | Closed | High | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-014.md |
| BUG-HLP-015 | A customer's ticket view exposes the standard "Notes" tab (not just "Helpdesk Conversion") — and a customer's own real email reply is logged twice: once in Helpdesk Conversion (customer-facing, correct) and again as a plain Journal visible under Notes to agents, unlike an agent's own Reply Note which is correctly excluded from Notes entirely. Found while retesting BUG-HLP-008/009 in the same session. **Retest 2026-09-22: NOT FIXED, both parts still reproduce** — source grep for "BUG-HLP-015" finds zero fix attempts anywhere; the customer session still sees a full "Notes" tab alongside "Helpdesk Conversion" on ticket #3, and the duplicate journal (her real email reply) is now visible on **both** the agent's and the customer's own Notes tab (the original partial mitigation — customer's own view being filtered — is gone). Reported to production 2026-09-22 as **#121073** (ztflux), assigned to Vaishnavi Bhawsar. | Open | Medium | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-015.md |
| BUG-HLP-016 | Reply auto-assign appeared to silently fail (server logged success but `assigned_to_id` never persisted, so the SLA clock never started) — reproduced on tickets #7, #8, #9 while re-executing `HELPDESK_TICKET_LIFECYCLE.md` TC-HLP-375. **Root cause confirmed environmental, not a plugin defect**: the Agent role's per-tracker permission matrix was missing the Support tracker's own Edit Issues checkbox ("All trackers" alone didn't cover it). **Retest 2026-08-31 (ticket #10), after the permission was corrected: PASS** — Assignee auto-set to Luna Blossom, SLA Information tab shows "Paused", matching original evidence exactly. **Closed per explicit user confirmation.** | Closed | High | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-016.md |
| BUG-HLP-017 | Documented "Merge duplicate tickets" feature (Feature #17, `HELPDESK_USER_GUIDE.md` §7.4) has no discoverable control anywhere — checked both an individual ticket's own Actions menu (only "Copy link") and the full multi-select context menu for two selected tickets (13 items, no Merge). Blocks TC-HLP-379, TC-HLP-393, and TC-HLP-397, all filed under this one bug. Retested 2026-09-10 (prod #119713 checked in) — CORRECTED verdict, Not a Bug: developer confirmed a deliberate product decision not to build a Merge control; our own `HELPDESK_USER_GUIDE.md` §7.4 and TC-HLP-379/032/041 updated to match. Closed as resolved-by-design, not a code fix. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-017.md |
| BUG-HLP-018 | Originally filed as "Comment field invisible on first load when marked Required for time logs" — **retested 2026-08-31 per user's manual check: DOES NOT REPRODUCE.** A direct DOM inspection (`browser_evaluate`) confirmed the Comment field is genuinely visible, on-screen, and interactive from the very first load; the original finding was a false positive caused by an accessibility-snapshot text search that missed a real, rendered field on one capture. Completed the full reply+time-log flow cleanly in one attempt with no retry needed. **Closed as not a bug** — a testing-methodology error on this session's part, not a plugin defect. | Closed | — | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-018.md |
| BUG-HLP-019 | The real escalation notification email (verified via the actual recipient's mailbox on two consecutive real L1→L2 and L2→L3 escalations) never states whether the Response or Resolution deadline breached, even though `HELPDESK_USER_GUIDE.md` explicitly documents this as required email content and both are tracked separately by the plugin. **Revised 2026-09-01**: an earlier "never names the previous assignee" claim was dropped — the guide only promises the previous/new *level* (confirmed present), not the assignee's name, so that part was never a documented requirement. Retested 2026-09-10 (prod #119751 checked in) — CONFIRMED FIXED: escalation email now shows a "Breach Type: Response SLA" row, verified via real recipient mailbox. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-019.md |
| BUG-HLP-020 | Reassigning an unassigned ticket resumes the SLA clock without crediting back the paused duration to the deadline — unlike the Waiting-for-Customer-Response pause/resume (TC-HLP-297), which correctly extends the deadline. Real consequence confirmed: the ticket became instantly resolution-breached and escalated (L2→L3) the moment the monitor next ran, purely because it sat unassigned briefly. Found while executing TC-HLP-298. **Retest 2026-09-18 (prod #119752 checked in): CONFIRMED FIXED** — `pause_sla`/`resume_sla` now delegate to the same shared `resume!` that extends both deadlines by the pause duration; live-verified a real 2-minute unassign→reassign cycle moved the response deadline forward by exactly 2 minutes. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-020.md |
| BUG-HLP-021 | A ticket escalates into a Support Level even while that level is deactivated — directly contradicts `HELPDESK_USER_GUIDE.md`'s own documented spec ("Inactive levels are not offered and are not escalated into"). Confirmed live: deactivated L2, let a ticket breach at L1, and the monitor escalated it straight into L2 anyway (reassigned, notified, escalation count incremented) exactly as if L2 were still active. Found while executing TC-HLP-349. Retested 2026-09-10 (prod #119754 checked in) — CONFIRMED FIXED: ticket stayed at L1 and fell back to Admin instead of escalating into deactivated L2. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-021.md |
| BUG-HLP-022 | **Broadened 2026-09-01**: deleting an SLA *or* Support Level that's only referenced by a customer's project-access row (no ticket yet) succeeds silently with no dependency warning — orphans the customer's row into an invalid state the form's own validation would never allow to be saved directly (one field still set, the other silently reset to None). SLA deletion IS correctly blocked when a real ticket uses it (TC-HLP-361) — the check only ever looks at ticket usage, never customer project-access usage, for either entity. Found while executing TC-HLP-362, broadened to SLA per a user follow-up question. Retested 2026-09-10 (prod #119755 checked in) — PARTIALLY FIXED: Support Level deletion is now correctly blocked ("assigned to a customer"), but SLA deletion is still unguarded — "Successful deletion." with the customer row left orphaned exactly as originally reported. **Retest 2026-09-21 (prod #119755 checked in again): CONFIRMED FIXED** — SLA deletion now correctly blocked with the identical message pattern ("currently assigned to a customer"); confirmed via DB that the SLA genuinely still exists. Both halves now fixed. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-022.md |
| BUG-HLP-023 | ~~Deleting a Holiday currently attached to an SLA's Holidays field succeeds with no dependency warning~~ — **Closed 2026-09-03: Not a Bug**, per explicit user product-judgment review. Holiday is an optional SLA field (unlike Support Level in BUG-HLP-022, which stays open) — the delete degrades gracefully (field clears to "-", no broken reference, no invalid state), so a silently-cleared optional field with zero functional consequence doesn't rise to the level of a defect worth a warning gate. Found while executing TC-HLP-364. | Closed | — | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-023.md |
| BUG-HLP-024 | **Broadened 2026-09-01**: no warning or indication is shown before a lower-level agent takes ownership of a higher-Support-Level ticket, via any of 3 confirmed paths — an admin manually reassigning an already-assigned ticket (Edit form), an agent self-assigning a fresh unassigned ticket, or an agent simply replying to a fresh unassigned ticket (which auto-assigns them with zero warning). A fresh ticket also has no Support Level indicator visible anywhere until after its first assignment (no SLA Information tab exists yet), so an agent has no way to self-check the level beforehand either. Found while executing TC-HLP-312/342/343; originally logged as an observed behavior/open design question, then filed as a bug per explicit user UX-judgment direction, then broadened same day to cover the fresh-ticket self-assign and reply-triggered paths per a follow-up user request. Retested 2026-09-10 (prod #119771 checked in) — CONFIRMED FIXED: all 3 paths now show a "Support Level Mismatch" warning (Path C has a tailored reply-specific message) with working Cancel/Continue Anyway. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-024.md |
| BUG-HLP-025 | Support Level create/edit form requires manually configuring "Escalation To" as a separate required field, even though Level Order already defines the escalation sequence for the ordinary case (L1→L2→L3). Order only restricts which options Escalation To may pick from — it never auto-derives the value. Found while executing TC-HLP-351; originally concluded as intentional (non-redundant) design supporting skip-level chains, then re-scoped per explicit user correction: the objection is the mandatory manual step for the common sequential case, not skip-level capability itself. Retested 2026-09-10 (prod #119772 checked in) — CORRECTED verdict, CONFIRMED FIXED: an untouched blank Escalation To now genuinely auto-persists the next level by Order, already-configured levels are left unchanged, and manual override still works. (An earlier same-day retest pass wrongly tested the manual-override path and scored FAIL; superseded.) | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-025.md |
| BUG-HLP-026 | Locking an agent's Redmine account does not remove them from a Support Level's Support Assignees list, and nothing anywhere (Edit form dropdown, list view's Support Assignees column) indicates the account is locked — the configuration silently goes stale even though the locked agent is genuinely, correctly excluded from ever being assigned a ticket in practice (both manual assignment and automated escalation confirmed via TC-HLP-353). Risk: an admin could believe a Support Level has more active coverage than it really does, with no on-screen signal to correct that. Found while executing TC-HLP-353; originally logged as a low-impact observation, then filed as a bug per explicit user direction. Retested 2026-09-10 (prod #119773 checked in) — CONFIRMED FIXED: both the Edit form dropdown and list view now show "Briar Sunset (locked)". | Closed | Low | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-026.md |
| BUG-HLP-027 | ~~An Agent holding only `view_helpdesk` never gets "Helpdesk" in the top menu~~ — **Closed same day: Not a Bug.** User corrected TC-HLP-147's precondition from "view_helpdesk or manage_helpdesk" to "view_helpdesk **and** manage_helpdesk" — the real, intended requirement is the combination. `manage_helpdesk`-gating the top menu and the SLA/Organization/Settings project tabs is deliberate, correct plugin behavior (confirmed via source); the actual defect was in this engagement's own documentation (`HELPDESK_USER_GUIDE.md` §4's Agent definition and the original TC-HLP-147/070 preconditions), corrected directly rather than via a new bug ID. | Closed | — | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-027.md |
| BUG-HLP-028 | ~~The Command Center's header reads "Helpdesk" instead of the documented "Helpdesk Support"~~ — **Closed same day: Rejected, Not a Bug.** Application menu correctly hidden and icon rail correctly displayed (confirmed via DOM inspection after an earlier over-broad claim was corrected). The "Helpdesk" header is intentional per Product Owner request — `HELPDESK_USER_GUIDE.md`'s "Helpdesk Support" text was the outdated side, corrected directly. Found while executing TC-HLP-148. | Closed | — | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-028.md |
| BUG-HLP-029 | The global Command Center Dashboard's "Recent Tickets" list (and its priority/status/organization breakdowns) has zero visibility scoping — a user with no project membership and no helpdesk permission anywhere (`zero.perm.user`) sees real ticket subjects, project name, assignee names, and SLA breach status from Helpdesk QA Alpha via direct URL to `/helpdesk`, even though every other Command Center screen (Reports/Organizations/Customers/Products/Settings, all confirmed 403) and the ticket detail page itself (also 403) correctly block her. Root-caused via source: `Issue.where(project_id: @helpdesk_project_ids)` has no `.visible(User.current)` call, unlike the same dashboard's KPI cards (which correctly read 0 for her). Found while directly verifying, per a user question, whether hiding the top-menu link is backed by real URL-level blocking for the no-permission and `view_helpdesk`-only tiers — it is, everywhere except this one dashboard action. Retested 2026-09-10 (prod #119855 checked in) — CONFIRMED FIXED: `/helpdesk` now returns a clean 403 for `zero.perm.user`, no leaked ticket data. | Closed | High | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-029.md |
| BUG-HLP-030 | Creating an SLA, Support Level, Holiday, or Product (Name or Code) over 255 characters crashes with an unhandled Rails 500 error instead of a validation message — a systemic, likely install-wide gap now confirmed across four models and two distinct columns. Root-caused via server log: identical `ActiveRecord::ValueTooLong (Mysql2::Error: Data too long for column '<col>' at row 1)` on `Sla#name`, `SupportLevel#name`, `RfHelpdeskHoliday#name`, `RfProduct#name`, and `RfProduct#code` — none has a length validation matching its DB column width. Found while executing TC-HLP-115, confirmed also affecting Support Level (TC-HLP-125), Holiday (TC-HLP-131), and Product (TC-HLP-142) — recommend the dev team audit every model's `validates` blocks against column widths rather than continuing to discover these one TC at a time. Retested 2026-09-10 (prod #119958 checked in) — CONFIRMED FIXED across all 4 models/2 columns (SLA verified live; the other 3 verified via source, each carrying an explicit `length: { maximum: 255 }` validation with a BUG-HLP-030 comment). | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-030.md |
| BUG-HLP-031 | SLA First Response Time and Resolution Time are marked required (*) on both the New SLA and Edit SLA forms, but a blank value is silently accepted server-side on either — the SLA shows the field as "-" (NULL), with no first-response or resolution deadline for the SLA clock to compute against. Contrast: `0` and negative values on the same fields ARE correctly refused ("must be greater than 0"), consistent with a classic Rails gotcha where `numericality` validation skips entirely on `nil` with no separate `presence: true`. The cross-field check (Resolution ≥ First Response, unit-aware) works correctly and is not part of this bug. Found while executing TC-HLP-116/252, confirmed also affecting Edit while executing TC-HLP-123. Retested 2026-09-10 (prod #119979 checked in) — CONFIRMED FIXED: both fields now correctly refuse a blank value ("cannot be blank"). | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-031.md |
| BUG-HLP-032 | SLA Agreement file upload has no file-type restriction — a `.exe` file (well under the correctly-enforced 5 MB size limit) uploads and stores successfully, downloadable at `/attachments/download/7/test-file-type.exe`, no client-side or server-side content-type check anywhere. Admin-only screen (`manage_helpdesk`-gated) so kept at Medium, not higher, but still a real arbitrary-file-upload gap. Found while executing TC-HLP-121, per that TC's own explicit guidance to flag this if reproduced. Retested 2026-09-10 (prod #119980 checked in) — CONFIRMED FIXED: the same .exe file is now refused with "file type is not allowed for security reasons". | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-032.md |
| BUG-HLP-033 | The ticket list's SLA Status badge and the ticket's own SLA Information detail panel disagree on the same ticket's SLA condition — root-caused via source to two entirely independent health-computation implementations. The list (`_sla_timer.html.erb` → `RfIssueSlaStatus#sla_health_status`) correctly implements the documented 7-state percentage-of-window model (on_track/at_risk/critical/etc.), while the detail panel (`_sla_information.html.erb`'s `overall_health`) is a coarser 5-state binary model with no at_risk/critical concept at all — anything not breached/overdue/paused/resolved unconditionally shows "✓ On Track", even at 21% of the window remaining. Confirmed live: ticket #34 showed "13h 33m At Risk" (amber) in the list while its own SLA Information tab simultaneously read "✓ On Track" (green) for the identical deadline. Found while executing TC-HLP-406. Retested 2026-09-10 (prod #120022 checked in) — CONFIRMED FIXED: a ticket engineered to exactly 30% window remaining showed "At Risk" on both the list and the (now-rebuilt) SLA Information panel. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-033.md |
| BUG-HLP-034 | SLA escalation into a Support Level with 2+ configured agents always assigns the same one agent — whichever has the lowest Redmine user ID — never any other agent on that level, no matter how many tickets escalate into it. Root-caused via source (`rf_issue_sla_status.rb#escalate_to_next_level!` does a plain `.first` on an unordered `User.where(id: [...])` query — no randomness, round-robin, or workload logic anywhere). Confirmed live: added a second L3 agent (`aurora.wren`, higher ID) alongside the pre-existing `willow.belle` (lower ID); two independent tickets both escalated to Willow Belle, never Aurora Wren. `HELPDESK_USER_GUIDE.md` §10's own canonical example treats multi-agent levels as the normal case, so a level with 2+ named agents that never actually distributes escalated work across them defeats the visible purpose of configuring more than one. Found while executing TC-HLP-333. Retested 2026-09-10 (prod #120025 checked in) — CONFIRMED FIXED: two fresh tickets escalating into L3 landed on two different agents (aurora.wren and willow.belle). | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-034.md |
| BUG-HLP-035 | A Support Package can never be created or edited as Inactive — the Active checkbox has no hidden fallback input (`<input type="checkbox" name="...[active]" value="1">` with no preceding `type="hidden"` sibling), so unchecking it submits no `active` key at all and the record silently saves/stays Active regardless. Reproduced 3 times: at creation (twice, different packages) and via a later Edit on the same package — every attempt to uncheck Active was confirmed via direct DOM `.checked` reads immediately before Save, yet the list always shows a green "Active" badge afterward. Blocks TC-HLP-210 (no way to produce a real Inactive Support Package fixture via the UI). Same symptom shape as the already-fixed BUG-HLP-003 (Organization/SLA/Canned Response), recurring on this newer, never-previously-tested entity. Found while executing TC-HLP-207. Retested 2026-09-10 (prod #120074 checked in) — CONFIRMED FIXED: unchecking Active now correctly persists Inactive. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-035.md |
| BUG-HLP-036 | Creating a Support Package with a duplicate Name is correctly refused (data integrity preserved, confirmed no duplicate row created) but shows zero error message anywhere — only the Name field's border turns orange/red, no text, no flash banner, nothing in the DOM matching any error/alert selector. A real user has no way to know why nothing happened. Found while executing TC-HLP-208. Retested 2026-09-10 (prod #120075 checked in) — CONFIRMED FIXED: clear "Name has already been taken" message now shown. | Closed | Low | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-036.md |
| BUG-HLP-037 | **[RECONFIRMED 2026-09-22, re-scoped]** Hard mode ("stop work") does not reliably block a customer's new ticket at zero remaining hours — 2 of 3 independent attempts across sessions failed to block (original + 2026-09-22 retest), only 1 (a prior correction's Retest 3) blocked correctly. The time-log path, by contrast, is reliably enforced (4 of 5 attempts blocked correctly, only the original finding failed). Latest retest: reduced org "Alpha Minimal Fields Test Org" to exactly 0.00h Remaining under Hard mode via a single reduction, `delta.customer`'s new ticket succeeded ("Successful creation.", ticket #407, left in place as evidence), while `luna.blossom`'s time-log attempt on ticket #46 was correctly blocked with a real validation error. Recommend re-scoping to cover only the new-ticket-creation path going forward. Found while executing TC-HLP-201/139, retested via TC-HLP-219/381 and again 2026-09-22. | Open (Reconfirmed) | Medium | Local (redmine-docker-6, localhost:3012) | bugs/open/BUG-HLP-037.md |
| BUG-HLP-038 | Hard mode only gates on whether Remaining is already ≤0 *before* an action — it does not check whether the action's own size would cross zero. A single 2h time entry logged against only 1.00h Remaining was accepted in full, uncapped, pushing the balance to -1.00h with no warning. Cleanly reproducible (unlike BUG-HLP-037), distinct root cause. Found while executing TC-HLP-221. Retested 2026-09-10 (prod #120077 checked in) — CONFIRMED FIXED: oversized entry now refused with a validation error. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-038.md |
| BUG-HLP-039 | Canned Response management (`RfCannedResponsesController`) is hardcoded `require_admin` for every action — it ignores `manage_helpdesk` entirely, unlike its five sibling configuration entities (SLA/Support Level/Organization/Product/Holiday), all of which correctly accept `manage_helpdesk`. Directly contradicts `HELPDESK_REQUIREMENTS.md` line 40's Permissions Matrix, which explicitly promises `manage_helpdesk` grants canned-response management. Confirmed via source and live 403 on a clean `manage_helpdesk`-only role. Found while executing TC-HLP-167. Retested 2026-09-10 (prod #120082 checked in) — CONFIRMED FIXED: `manage_helpdesk`-only user can now create Canned Responses, no 403. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-039.md |
| BUG-HLP-040 | ~~A role granted only `manage_prepaid_support_hours` (no `manage_helpdesk`) has no reachable UI path to budget management~~ — **Closed same day: Not a Bug.** User clarified the intended permission model directly: `manage_helpdesk` grants access to the Organization area, `manage_prepaid_support_hours` is required *in addition* to modify prepaid hours — the originally-observed 403 for `manage_prepaid_support_hours`-only (no `manage_helpdesk`) is exactly this by-design behavior, not a defect. Retested both directions live: positive case (both permissions) — top-up and run-out-mode change both succeed and persist; negative case (`manage_helpdesk` without `manage_prepaid_support_hours`) — access/view works, modification correctly blocked. TC-HLP-168/210 rewritten to match. Found while executing TC-HLP-168. | Closed | — | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-040.md |
| BUG-HLP-041 | Hard mode ("stop work") has zero enforcement anywhere in Redmine's own core Spent-time paths — neither editing an existing time entry (`/time_entries/:id/edit`) NOR creating a brand-new one via the core "Log time" link (`/issues/:id/time_entries/new`) is enforced; enforcement exists ONLY on the reply-box's own create-time-log widget. Confirmed live on both: (edit) mode `hard`, Remaining 1.00h, `0:05`→`3:00` (+2.92h) saved cleanly, Remaining landed at -1.92h; (core "Log time") mode `hard`, Remaining 2.00h, new 5:00h entry via the native form succeeded uncapped ("Successful creation"), Remaining landed at -3.00h. Distinct from BUG-HLP-038 (open) — that's a partial boundary gap on the reply-box's own create path; this is a complete absence of enforcement on both of Redmine's core `TimeEntry` paths. Found while executing TC-HLP-235 (edit) and TC-HLP-239 (core Log time link). Retested 2026-09-10 (prod #120086 checked in) — CONFIRMED FIXED: both the core Edit form and the core "Log time" new-entry link now correctly block an oversized entry under Hard mode. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-041.md |
| BUG-HLP-042 | Customer creation is unconditionally, permanently blocked once any User custom field is marked required — the Customer create/edit forms never expose a custom-fields section at all (unlike Redmine's own core `/users/new`), so the admin has no way to satisfy the requirement. Confirmed live: created a required "Users" custom field, confirmed it works correctly on core `/users/new`, then a fully valid New Customer submission was refused with "... cannot be blank" and no record of any kind was created (verified via UI, Users search, and a direct `rails runner` query) — a clean, total block, not a partial/orphaned save. Root-caused via source (`rf_customers_controller.rb`, standard atomic `if @customer.save`/`render :new`). User asked directly whether this scenario (which they had observed) was already reported — it was not; first coverage via TC-HLP-145. Retested 2026-09-10 (prod #120087 checked in) — CONFIRMED FIXED: the New Customer form now renders the required User custom field and creation succeeds. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-042.md |
| BUG-HLP-043 | Auto-close closes ANY open-status ticket past the silence window (New/In Progress/Feedback/Waiting for Customer Response/Resolved), not just Resolved ones as documented in `HELPDESK_USER_GUIDE.md` §3.6/§19 and the Email Configuration form's own field hint. Confirmed live: a ticket left at status New, backdated past a 1-day Auto Close Ticket Days threshold, was closed by the real `Helpdesk::AutoCloseTicketsWorker` with a fabricated "automatically closed due to inactivity" note, despite never being Resolved. Root-caused via source — the worker's query uses Redmine's `.open` scope (any `is_closed: false` status) with no Resolved-specific filter. Found while executing TC-HLP-081. **Retest 2026-09-18 (Local, prod #120375 checked in): CONFIRMED FIXED** — worker now filters `.where(status_id: resolved_status.id)`; a fresh New-status backdated ticket (#336) was correctly left untouched on a live run. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-043.md |
| BUG-HLP-044 | `rake redmineflux_helpdesk:auto_close_tickets` is completely non-functional on any project, regardless of configuration — it reads a legacy `ProjectCustomField` (`helpdesk_auto_close_days`) that no longer exists on this instance (zero `ProjectCustomField` rows exist at all), while the real config lives in `RfHelpdeskEmailConfig` (read correctly by the actual scheduled worker). Confirmed live: identical eligible-ticket setup produced zero closures via the rake task but closed correctly when the real worker class was invoked directly. Same dead-code migration-cleanup pattern already noted (but never filed) for the sibling `check_emails` rake task. Found while executing TC-HLP-077/153, formal TC home is TC-HLP-250 in `HELPDESK_RAKE_TASKS.md`. **Retest 2026-09-18 (Local, prod #120376 checked in): CONFIRMED FIXED** — rake task now reads `RfHelpdeskEmailConfig` and genuinely closed 43 real eligible tickets across 2 projects on a live run; `check_emails`'s identical pattern still NOT independently verified fixed. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-044.md |
| BUG-HLP-045 | An email sent to one project's own mailbox is routed to a different project — a customer entitled to **multiple** projects always gets her *first* project-access row's project, never the project whose mailbox actually received the email. Confirmed live: registered a customer (`beta.customer`) with Alpha as her first row and Beta as her second, emailed Beta's own mailbox specifically — the resulting ticket (#62) was created under Helpdesk QA Alpha regardless (confirmed via page title, Organization field, and outbound SMTP account all showing Alpha). Control test confirms the defect is specific to multi-project entitlement: the same customer made Beta-only (Alpha row removed) correctly routed a subsequent email to Beta (ticket #63). Root-caused via source (`mail_handler_patch.rb#target_project_with_helpdesk`): `RfProjectCustomer.where(customer_id: ...).first`, with no scoping to the project the poller actually polled. Found while executing TC-HLP-370 in `HELPDESK_TICKET_LIFECYCLE.md`. **Retest 2026-09-18 (prod #120377 checked in): CONFIRMED FIXED** — `target_project_with_helpdesk` now prefers the polled project; live-verified via the real `EmailPollerWorker`, log reads "Using project [helpdesk-qa-beta] the email's own mailbox was polled from". | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-045.md |
| BUG-HLP-046 | The Helpdesk Conversion / Email History tab never shows recipient, subject, or an explicit direction field for any entry — only sender, timestamp, and body — directly contradicting `HELPDESK_USER_GUIDE.md` §13's explicit promise of "sender, recipient, subject, body and timestamp" per entry. Confirmed live on ticket #63 (3 real entries): full-page DOM search found zero "recipient"/"To:" occurrences and no per-entry "Subject:" field (the 3 "Subject:" matches all traced to one email's own embedded body template, not a structured header). Found while executing TC-HLP-072. **Retest 2026-09-18 (prod #120378 checked in): CONFIRMED FIXED** — the partial now renders explicit Inbound/Outbound badges, Subject, and To: recipient for every entry, live-verified on ticket #63's 5 real entries. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-046.md |
| BUG-HLP-047 | The same email account (incoming and/or outgoing) can be configured on two different projects with zero validation or warning. Incoming reuse: set Beta's incoming Mail Username to Alpha's own mailbox, saved cleanly, sent one real email — Alpha's turn in the poller's project loop ran first and silently claimed it (ticket #66), Beta got nothing. Outgoing reuse: set Beta's outgoing SMTP to Alpha's account, saved cleanly, replied on a real Beta ticket — sent successfully but the customer's actual received email shows `From: alpha.support@test.local`, not Beta's own address (silent sender-identity confusion, no data loss). Found while executing TC-HLP-079. **Retest 2026-09-18 (prod #120379 checked in): CONFIRMED FIXED, both directions** — two new model validations now refuse Save with a clear message naming the conflicting project, live-verified for both incoming Mail Username and outgoing SMTP Username reuse. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-047.md |
| BUG-HLP-050 | An attachment added to a Knowledgebase article (via the Editor.js "Attachment" content block) cannot be removed through any discoverable UI interaction, despite `HELPDESK_USER_GUIDE.md` §17 promising "Add and remove files on an article." Adding is fully confirmed working (real file, real download link, listed correctly). Removing fails via every path tried: the Attachments panel's Action column has only a download link (confirmed via `innerHTML`, no delete icon); the block's settings button only opens a type-conversion popover with no delete tune; multiple keyboard-based block-selection + Backspace/Delete attempts all left the block intact. Root-caused via source: a real `changeHandler()` listens for Editor.js's own `block-removed` event on attachment blocks and correctly calls `DELETE rf_kb_delete_attachment/:id` — the removal mechanism exists and is wired, but nothing in the actual UI ever fires that event. Found while executing TC-HLP-022. **Retest 2026-09-18 (prod #120474 checked in): CONFIRMED FIXED** — a real delete icon now exists in the Attachments panel's Action column; live-clicked, confirmed, and verified via direct DB check that the `Attachment` row and its join record are genuinely destroyed. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-050.md |
| BUG-HLP-049 | Knowledgebase Page History can only view an older version's read-only content — no compare or restore action exists anywhere, despite `HELPDESK_USER_GUIDE.md` §17 explicitly promising both ("Compare versions and restore an older one"). Confirmed via source: the version-history table renders only 3 plain columns (no action column, no per-row controls), `getVersion(id)` only fetches and renders old content into the editor while explicitly hiding both Save and Publish, and a full-bundle text search for "restore"/"compare" returns zero matches anywhere in the plugin's KB JS. Version retention itself works correctly (TC-HLP-020 passes) — this is specifically the two promised actions on top of that being fully absent. Found while executing TC-HLP-021. **Retest 2026-09-21 (prod #120471 checked in): CONFIRMED FIXED** — real `version_restore` controller action, compare modal, and per-row Compare/Restore links now exist; live-verified Compare shows a real side-by-side diff and Restore genuinely overwrites current content (snapshotting the pre-restore state first) on "Resetting your password". | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-049.md |
| BUG-HLP-048 | Documented Knowledgebase "nest a child article from its parent" feature (`HELPDESK_USER_GUIDE.md` §17, `HELPDESK_FEATURES_LIST.md` #47) never actually nests, root-caused via source: `createNode()` correctly reads the open article's ID intending to create a child, but `createNewPage()`'s AJAX payload to `rf_knowledgebase_pages.json` never includes a `parent_id` (only a cosmetic display-string `path`) — so the server has no relationship to record and the new article always renders as a root-level sibling. Confirmed twice independently (synthetic click, then a fully genuine Playwright click) — both landed as root siblings, not children. No other mechanism exists either: the toolbar's 7 controls, the Menu dropdown (Attachments/History), and drag-and-drop (confirmed via source to only reorder same-parent siblings, never re-parent) all lack any nesting path. Found while executing TC-HLP-019. **Retest 2026-09-21 (prod #120465 checked in): CONFIRMED FIXED** — a real `parent_id` migration/column, model associations+validations, permitted param, and JS payload/tree-building fix all now exist; live-verified creating a child from an open parent correctly nests it in both the breadcrumb and sidebar tree. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-048.md |
| BUG-HLP-051 | A customer picks a ticket's Product when raising it (a real dropdown, confirmed working, choice saves correctly server-side) — but that same value is permanently invisible to them afterward: `.product.attribute` is `display:none` specifically for the Customer role, on the same ticket view where Organization (documented in the identical sentence, `HELPDESK_USER_GUIDE.md` §7) IS shown. Confirmed via `getComputedStyle` the hide targets the element itself, not a collapsed ancestor. Customers also have no field-editing surface at all post-creation (inline Subject rename + Reply Note only) — expected, not itself a bug — but that only compounds the gap: the customer is asked to choose Product, then can never review what they chose, before or after any edit. Found while directly answering a user question about customer Product selection/edit-lock behavior. **Retest 2026-09-18 (prod #120475 checked in): CONFIRMED FIXED** — the offending CSS rule is removed; a fresh customer ticket (#337) now shows "Product: Falcon Suite" correctly, `getComputedStyle` confirms `display: block`. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-051.md |
| BUG-HLP-052 | Distinct from BUG-HLP-051: on a customer's own ticket, every genuine Redmine custom field's **label renders but its value is dropped entirely** (`<div class="value"></div>`, empty), even though the value is genuinely saved — confirmed via Admin viewing the identical ticket, both a list-type (Issue Category) and a string-type ("test") custom field showing their real values there. Every other attribute on the same customer-facing template (Status, Priority, Product, Organization, Prepaid Support Hours) correctly shows its value — only custom fields are affected. Worse in user-facing impact than BUG-HLP-051 since the label-with-blank-value looks exactly like the customer's own submitted data was silently lost, not an intentionally-hidden field. Found immediately after BUG-HLP-051 per a direct user follow-up observation. **Retest 2026-09-21 (prod #120476 checked in): CONFIRMED FIXED** — customer template now delegates to Redmine core's own custom-field-rendering helpers; live-verified on a fresh ticket, `.value` div now renders the real saved value ("Report a Bug") instead of empty. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-052.md |
| BUG-HLP-053 | `seed_demo_data`'s legacy-identifier fallback (`Project.find_by(identifier: 'helpdesk-support')`) silently renames and repurposes ANY project matching that identifier — via `update_columns` (bypasses validations/callbacks) — with no check that it's actually old demo data (no Support-tracker check, no marker), then dumps 25 fake tickets into it. Real-world trigger: an admin's unrelated project literally named "Helpdesk Support" gets exactly that identifier from Redmine's own default slugification. Found while executing TC-HLP-257 in `HELPDESK_RAKE_TASKS.md`; confirmed via source review (not reproduced end-to-end live — the primary-identifier project already exists on this instance from TC-HLP-251, making the legacy branch unreachable without destroying those fixtures). **Retest 2026-09-18 (prod #120504 checked in): CONFIRMED FIXED, source-verified** — `seed_project` now only reuses/renames a legacy-identifier match if its description carries the seeder's own `SEED_DESCRIPTION_MARKER`; otherwise a brand-new project is created instead. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-053.md |
| BUG-HLP-054 | `seed_demo_data` crashes with an unhandled `ActiveRecord::RecordInvalid` (unconditional `TimeEntry.create!` in `log_time_for_ticket`, no budget pre-check) once cumulative logged time exceeds Sakura Mobility KK's own hard-enforced 80h prepaid budget — a budget the very same seeder invocation configures. Confirmed live: `TICKETS=300` crashed with only 233 of 300 tickets created (no summary line, no graceful message); re-running the identical command crashed again at the exact same point (still exactly 233 tickets both times) — deterministic RNG (`Random.new(42)`) plus a non-replenishing budget mean this is now a **permanent** regression on this instance, not a transient failure: every future invocation requesting enough Sakura-linked tickets to reach this threshold will crash identically forever without manual DB intervention. Found while executing TC-HLP-259 (large `TICKETS` value handling) in `HELPDESK_RAKE_TASKS.md`. **Retest 2026-09-18 (prod #120507 checked in): CONFIRMED FIXED** — `log_time_for_ticket` now rescues and skips the one over-budget entry instead of crashing; live-verified `TICKETS=300` re-run hit the same trigger 14 times and completed cleanly to a full summary. | Closed | High | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-054.md |
| BUG-HLP-055 | `rake redmineflux_helpdesk:check_emails` is completely non-functional on every project regardless of real mail configuration — identical dead-code pattern to BUG-HLP-044 (`auto_close_tickets`), reading 7 legacy `ProjectCustomField` keys that no longer exist on this instance instead of the real `RfHelpdeskEmailConfig`. Confirmed live: ran with 4 helpdesk-enabled projects present, including Alpha/Beta which have real, working incoming-mail config confirmed functional via the actual scheduled `EmailPollerWorker` — every single project, without exception, was reported "Incoming email disabled for this project, skipping...". This is the exact audit BUG-HLP-044's own Notes section (and `HELPDESK_MEMORY.md`) had already called for. Found while executing TC-HLP-249 in `HELPDESK_RAKE_TASKS.md`. **Retest 2026-09-18 (prod #120508 checked in): CONFIRMED FIXED** — task now reads `RfHelpdeskEmailConfig`; a live run correctly recognized Alpha/Beta as enabled (real protocol/server/username printed, real IMAP connection attempted) while Gamma/Redmineflux Helpdesk stayed correctly disabled. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-055.md |
| BUG-HLP-056 | `rake redmineflux_helpdesk:check_sla` correctly detects/marks every SLA breach (119 notifications sent) but every single escalation attempt fails (`Escalations: 0, Errors: 118` on one run) — the rake task passes a human-readable escalation-reason string (`'Response SLA breach'`) that fails `RfIssueSlaEscalationHistory`'s own inclusion validation (expects `response_breach`/`resolution_breach`/`manual`), while the real scheduled `Helpdesk::SlaMonitorWorker` passes the correct machine-readable value and escalates successfully. Confirmed live on ticket #301: SLA Journey shows only its pre-existing L1→L2 escalation from the seed data, no new entry despite a fresh resolution breach detected during this run. Distinct from BUG-HLP-044/055 (legacy dead-code reads) — this task operates on the real, current model, just passes the wrong argument to one method call. Found while executing TC-HLP-248 in `HELPDESK_RAKE_TASKS.md`. **Retest 2026-09-18 (prod #120510 checked in): CONFIRMED FIXED** — task now passes `'response_breach'`/`'resolution_breach'`; a live run showed `Escalations: 202, Errors: 0` (was `0`/`118`), with a genuine `[SLA][ESCALATION]` log line confirming a real L1→L2 escalation. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-056.md |
| BUG-HLP-057 | `export_helpdesk_reports` is a completely non-functional, vestigial permission — Reports access (viewing AND exporting) is gated purely by `manage_helpdesk`. Confirmed with two contrasting role configs: `view_helpdesk` + `export_helpdesk_reports` (no `manage_helpdesk`) gets a genuine 403 on `/rf_helpdesk/reports/tickets` — can't even view a report, let alone export one; `view_helpdesk` + `manage_helpdesk` (no `export_helpdesk_reports` at all) successfully views AND exports (CSV/Excel/PDF all verified). Directly contradicts `HELPDESK_USER_GUIDE.md` §20's permissions table, which lists it as its own independent permission. Found while executing TC-HLP-277 in `HELPDESK_REPORTING_AUTOMATION.md`. **Retest 2026-09-18 (prod #120539 checked in): CONFIRMED FIXED** — the gate now also accepts `export_helpdesk_reports`; live-verified `perm.test.agent` (that permission, no `manage_helpdesk`) can now view all 5 report tabs and export CSV. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-057.md |
| BUG-HLP-058 | REST API's `POST /tickets/:id/conversations` reply endpoint correctly triggers a real SLA side effect (a genuine new "✓ First Response Given" Activity Log entry, resolution deadline extended) but the reply itself never appears in the Helpdesk Conversion tab (count stayed unchanged) — instead it lands as a plain Note under the Notes tab, the reverse of the established rule from BUG-HLP-015 (a genuine agent reply should be excluded from Notes and visible in Conversion instead). Found while executing TC-HLP-270 in `HELPDESK_REPORTING_AUTOMATION.md`. **Retest 2026-09-18 (prod #120540 checked in): CONFIRMED FIXED** — the endpoint now sets the same thread-local signal the UI Reply Note uses; live-verified Conversion count went 0→1 with a real Outbound/"via Reply Note" entry (also still visible under plain Notes, noted but not blocking). | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-058.md |
| BUG-HLP-059 | The Swagger spec's own documented "existing Redmine browser session" auth method never actually works on any `/helpdesk/api/v1/*` data endpoint, for any role (confirmed with a full admin session, not just Customer) — combined with Customer accounts having no self-service way to obtain an API key at all (`/my/account` has no "API access key" section, `/my/api_key` redirects away), the spec's own "customers... restricted to their own tickets" clause describes access no real customer can ever reach. Found while executing TC-HLP-279 in `HELPDESK_REPORTING_AUTOMATION.md`. **Retest 2026-09-18 (prod #120543 checked in): PARTIALLY FIXED, kept OPEN** — both originally-cited causes (session auth, no self-service key) are genuinely fixed, but a live end-to-end retest found the customer still can't reach the API in practice: session-based access is now blocked by an unrelated `restrict_helpdesk_customer_access` filter never updated for `api/v1/*`, and key-only access reaches the API but returns zero visible tickets (including her own). Per explicit user instruction, production issue #120543 moved In QA → **Reopen** with a note detailing both remaining gaps; scope broadened in place rather than filing new bug IDs. **Retest 2026-09-21: PARTIALLY FIXED, still kept OPEN** — the `restrict_helpdesk_customer_access` allowlist gap is now confirmed fixed (session-based API access no longer redirects away), and a new partial improvement was found (single-ticket `show` lookup now works via key-only access, was a 404 before) — but the list endpoint (`GET /tickets`) still returns `total_count: 0` for a real customer with real authored tickets, confirmed on 2 different customers via both key and session auth, against an admin baseline that correctly returns 392. Root cause not pinned to an exact line this session despite extensive black-box testing; flagged for the developer. **Retest 2026-09-22: CONFIRMED FIXED** — the list endpoint now correctly returns `total_count: 29`/`2` for two different customers (matching their real authored ticket counts) via both key-only and session-based auth, admin baseline still correctly unfiltered at 392. All four originally-cited gaps now resolved. | Closed | Medium | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-059.md |
| BUG-HLP-061 | A sub-page's only hover icon in the Knowledgebase sidebar tree is Delete — no distinct "add child from this row" icon exists at all (CSS for one is already scaffolded, `.add-child-icon`, but never wired up in JS) — and the Delete icon, a genuine "×" SVG, is rendered at only 10×10px, small enough to be visually indistinguishable from a "+" (add). Found while retesting BUG-HLP-048: user flagged from a screenshot that hovering a sub-page shows what looks like a "+" that, when clicked, opens a delete confirmation. Root-caused via source and confirmed by rendering the icon at 200×200px (genuinely an X) vs. its actual 10×10px size (reads as a +). A real "Delete Page?" confirm dialog does stand between the click and any actual deletion, so not a silent-data-loss risk, but a real icon-clarity defect on a destructive action. **Reported to production 2026-09-21: #121011 (ztflux) checked in.** Linked to Test Case #119389 in Run #541, Environment "Win 10 + Chrome" — testcase marked Failed with this defect. **Retest 2026-09-22: CONFIRMED FIXED** — a real, distinct "Add sub-article" icon (a genuine "+" SVG, visually unambiguous vs. the delete "×") now renders per row, wired to a `treeAddParentId` mechanism that correctly nests the new article under the exact row clicked (confirmed via a 3-level breadcrumb), not just whichever page happens to be open. | Closed | Low | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-061.md |
| BUG-HLP-060 | Sidekiq's own internal `Scheduled::Poller` thread crashes with an `ArgumentError` within ~1 second of every boot (a `sidekiq` 7.3.9 / `connection_pool` 3.0.2 gem incompatibility, confirmed via full stack trace and installed gem versions) — permanently disabling all 3 documented cron-scheduled jobs (`email_checker`, `sla_monitor`, `auto_close_tickets`) for the life of the process, with the main Sidekiq process itself staying alive and showing no visible symptom. Live-confirmed on a real ticket with a 1-minute SLA: `response_breached` never auto-flipped to `true` after waiting past both the deadline and a full monitor cycle. A regression, not a permanent limitation — `HELPDESK_MEMORY.md` documents this exact cron mechanism working correctly earlier in this engagement. Found while executing TC-HLP-266/187 in `HELPDESK_REPORTING_AUTOMATION.md`. **Retest 2026-09-21 (prod #120545 checked in): CONFIRMED FIXED** — `connection_pool` now pinned to `~> 2.5` in the plugin's own Gemfile; live-verified a fresh ticket's SLA auto-breached and escalated with zero manual worker invocation, purely via the scheduled cron mechanism. | Closed | High | Local (redmine-docker-6, localhost:3012) | bugs/closed/BUG-HLP-060.md |

## Notes
- Open bugs: bugs/open/
- Closed bugs: bugs/closed/
- Screenshots: screenshots/<BUG-ID>/
