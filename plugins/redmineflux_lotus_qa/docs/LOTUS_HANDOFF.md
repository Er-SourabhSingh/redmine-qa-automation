# Handoff — Redmineflux Lotus Theme

## Last Session

- Date: 2026-09-18
- Redmine Version: 7.0.1.stable
- Environment: Forge — `https://flux-fccirp6sk49.forge.zehntech.com/`

## Incidental Finding (2026-09-22, local Docker redmine-docker-700) — BUG-LTS-007 filed

Not a planned Lotus test pass — found while investigating a user-reported UI issue on a Checklist-plugin
permission-test fixture project ("Checklist Perm Private"):

- The Project Overview page shows "This project is closed and read-only." **twice** for any closed project.
  Confirmed via DOM: two identical `<p class="warning">` elements. Confirmed via the Issues page for the same
  project (only 1 copy there) that the duplicate is specific to Overview.
- Root-caused by reading `redmineflux_lotus/app/views/projects/show.html.erb` (bind-mounted locally at
  `C:\redmine-docker-7.0.0\plugins\redmineflux_lotus\...`): the file has a top-level
  `<% unless Setting.ui_theme == 'redmineflux_lotus' %> ... <% else %> ... <% end %>` split (lines 1/161), and
  **both branches independently reimplement the `@project.active?` closed-warning check** (line 27 in the
  fallback branch, line 195 in the Lotus-active branch) — duplicating the warning Redmine core already renders
  once, globally, via the layout.
- **Confirmed NOT tied to the active Theme setting** — `Setting.ui_theme` on this instance is "Default" (not
  `redmineflux_lotus`), yet the duplicate still reproduces, since the plugin's view file still wins Rails' view
  lookup for this page once installed, regardless of the Theme setting's value. Don't assume switching the theme
  away from Lotus removes this plugin's influence on pages it overrides — see `LOTUS_MEMORY.md`.
- Filed as `BUG-LTS-007` (Medium). `bugs/open/` for this plugin is no longer empty.

## Completed This Session (2026-09-18) — BUG-LTS-003 retested again, mixed result: Tags-widget overlap now FIXED, edit-form full-width break REGRESSED

Retested `BUG-LTS-003` at 1280×720, Lotus theme, German language, Admin role, on a new Forge server (issue #260, the same fixture issue this bug's Tags-widget finding originally used — Sprint set to "Bug Bash", Story-Points set to 13):

- **Tags-widget overlap — FIXED.** No overlap between the tag editor and either the Sprint value or Story-Points label (bounding-box checks both null). This was the sub-finding the bug's title had been narrowed to since 2026-09-10 — it's now resolved.
- **Sprint/Story-Points value overlap and font/color/bullet mismatch — reconfirmed still fixed.**
- **Edit-form full-width break — REGRESSED.** Previously fixed on server `flux-f3lnytazd49` (2026-09-10), this reappeared on this server: Sprint/Story-Points selects both render at 682px (full grid width) vs. siblings' 241px (half-width). Confirmed numerically and visually.
- **User asked to retest and close the bug** — initially did not close it, since the edit-form full-width break was still reproducing. Reported the mixed result back to the user.
- **User reviewed the finding and made an explicit judgment call to close anyway**: since the remaining sub-finding is a width mismatch, not an overlap (no content is obscured or inaccessible), and is judged minor, the user directed closing the bug. Updated the bug file with this closure rationale, moved it to `bugs/closed/`, updated `bugs/_index.md`.
- Checked production issue #120218: already showed Status: Done, Done ratio: 100%, updated by Sourabh Singh directly in Redmine the same day ("Retested and closed" journal entry) — no production write needed from this session, the sync was already done outside the MCP tool chain.
- `bugs/open/` for this plugin is now **empty**. Per `CLAUDE.md` §10/§12, this triggers the requirement for a full final-cycle regression (`SENIOR_QA_STANDARDS.md` §27 — every TC in `LOTUS_GERMAN_LANGUAGE.md`, no automation specs exist yet for this plugin) before `STATUS.md` can be set to `Complete`. **Not yet run this session** — `STATUS.md` stays `In Progress` pending it.

## Completed Previous Session (2026-09-10, continued) — BUG-LTS-006 FIXED; BUG-LTS-003 narrowed further

Retested the 2 remaining open bugs at 1280×720, Lotus theme, German language, on a third Forge server this session, using a fresh test issue (Sprint = "Bug Bash", Story-Points = 89, plus a subtask and a related ticket — all deleted afterward):

- **BUG-LTS-006 — FIXED.** The subtask/related-tickets table's containing structure now includes a new `div.rf_issue_section_row` wrapper with `overflow-x: auto` — the table scrolls within its own ~703px-wide bounds (with a visible scrollbar/chevron affordance) instead of spilling out with `overflow-x: visible` into the Historie/Notizen sidebar. Bounding-box check confirmed a clean ~37px gap, no overlap. **Closed.**
- **BUG-LTS-003 — narrowed further.** The Sprint/Story-Points *value* overlap (the sub-finding reopened on 2026-09-09) is now genuinely FIXED: the Sprint value span renders at a normal 58.6px single-line width, no longer wrapping/spilling into the Story-Points label. Font/bullet and edit-form full-width fixes still hold. **However**, opening the Tags "Hinzufügen" editor still overlaps — not the Sprint value anymore, but the **Story-Points label** itself (`49.4×19.5px` confirmed overlap). Bug stays open, narrowed to just this one remaining overlap.
- `bugs/open/` now has only **1 bug** (`BUG-LTS-003`) — still not empty, final-cycle regression not yet triggered.

## Completed Previous Session (2026-09-10) — BUG-LTS-002 invalidated

The user pushed back on `BUG-LTS-002` (issue detail tab strip "clipping" at 1280×720), pointing out via screenshot that the "<"/">" buttons next to the tab strip are there specifically to scroll/page through it.

- Verified live on `flux-fhhcov1xf49.forge.zehntech.com`, issue #1, Lotus theme, 1280×720: the tab strip shows a "peek" of the next tab (partially cut off) at rest — a common, intentional carousel-style affordance — and clicking the right arrow correctly pages the strip, hiding earlier tabs (`display:none`, confirmed `x:0,width:0`, not a native `scrollLeft` — that stayed `0` throughout) and rendering the next tab in full. The final tab has only a ~5px residual overflow (vs. the ~23–174px originally described), effectively imperceptible.
- **Conclusion: not a bug — a working paginated tab strip.** Every tab is fully reachable via the provided controls. The original finding mistook the intentional "peek of next tab" hint for a broken layout. **Invalidated, moved to `bugs/closed/`.**
- `bugs/open/` now has 2 bugs (`BUG-LTS-003`, `BUG-LTS-006`) — down from 3, but final-cycle regression still not triggered (not yet empty).

## Completed Previous Session (2026-09-09 retest pass)

- Retested all 5 open Lotus bugs on a fresh Forge server (`https://flux-fhggkobjh49.forge.zehntech.com/`) after the previously-used server (`flux-fdrk6suoj49`) started returning HTTP 403 on every route mid-retest (user provided the replacement server and asked to select the Lotus theme first).
- BUG-LTS-001, BUG-LTS-004, BUG-LTS-005: all confirmed FIXED, moved to `bugs/closed/`.
- BUG-LTS-003: initially closed as fixed after checking only outer-container rects at 1920×1080. **Reopened same session** after the user flagged screenshots showing the overlap still happening on their own session — re-verified at 1280×720 with element-level bounding boxes and found 2 of its 4 sub-findings (Sprint/Story-Points text overlap, Tags-widget overlap) still genuinely reproduce at that resolution; confirmed Lotus-specific (clean under Default theme, same issue/resolution). The other 2 sub-findings (font/color/bullet, edit-form full-width break) are fixed at both resolutions. Moved back to `bugs/open/`.
- BUG-LTS-002: still reproduces (now worse — a 4th tab appears on this server, widening the overflow from ~23px to ~174px). Remains open.
- New informational (non-Lotus) finding: the subtask/related-tickets tables on the issue detail page overflow their container badly at 1280×720 — confirmed theme-agnostic (reproduces identically under Default), so not filed as a Lotus bug. See "Related Finding" section below.
- Per-bug regression (`SENIOR_QA_STANDARDS.md` §26) for the 3 fixed bugs: spot-checked adjacent/related surfaces sharing the same UI area — the sidebar-wrap fix confirmed on a second, unrelated admin page (`/roles`, not just `/admin`); the Sprint/Story-Points grid fix confirmed with no regression on the Agile Board card view (badges/story-points counter render cleanly, no overlap) — that check was itself only done at 1920×1080, which is part of why the 1280×720 reproduction was initially missed.
- `bugs/open/` is not yet empty (BUG-LTS-002, BUG-LTS-003 remain) — no final-cycle regression (§27) triggered this session; `STATUS.md` stays `In Progress`.

## Completed Previous Sessions

- Switched active theme to Redmineflux lotus (German language already active from prior Tag Plugin testing, both system default and account level).
- TC-LTS-001: home page hero widget + stat tiles — PASS (fully translated). Notable cross-environment discrepancy recorded: a different Forge server showed these same widgets untranslated with only the account-level language set (not system default) — flagged for re-verification there, not treated as fixed.
- TC-LTS-002: admin sidebar nav — FAIL. "Benutzerdefinierte Felder" and "LDAP-Authentifizierung" are clipped with an ellipsis under Lotus (fixed ~171px width, `nowrap`), while rendering fully under Default theme. Filed `BUG-LTS-001` (Medium).
- TC-LTS-003/004: retested Tag Plugin's issue-detail widget and admin Manage Tags page under Lotus — both already-known bugs (BUG-TAG-001, BUG-TAG-003/004) reproduce identically; confirmed theme-agnostic (same root cause, not new defects). No new Tag Plugin bugs from the Lotus pass itself.
- TC-LTS-005: retested Checklist Plugin (issue-detail widget, cross-plugin overlap check, admin template list + Delete modal) under Lotus — all PASS, no new bugs (only the already-known theme-agnostic `BUG-CHK-001` applies).
- TC-LTS-006: resolution testing (1280×720, 1920×1080) for the issue-detail right-column tab strip ("Historie"/"Notizen"/"Checklisten-Verlauf") — PASS at 1920×1080, **FAIL at 1280×720**: the tab strip's container is too narrow for the combined German label widths, clipping "Checklisten-Verlauf". Confirmed this does NOT happen under Default theme at the same resolution. Filed `BUG-LTS-002` (Medium) — a second, independent case of "longer German content overflows a Lotus fixed-width container."

Second session, on a **new Forge server** (`flux-f6nlrqpvk49` — the original server was replaced mid-project after being reported expired):

- TC-LTS-007: retested Gantt Plugin under Lotus (main view, Add Issue dialog, Settings panel, Edit Version modal, delete-confirmation dialog) — PASS, no new Gantt-plugin bugs; `BUG-GNT-001`/`002` confirmed theme-agnostic there too. Found a **third instance** of `BUG-LTS-001`'s root cause: the **project-level** sidebar nav (not just the admin nav originally found) clips "Aufgewendete Zeit" the same way (`scrollWidth` 118px > label-span `clientWidth` 111px). Added as an additional affected surface on `BUG-LTS-001` rather than a new bug.
- TC-LTS-008 [legacy pre-2026-09-22 ID, no longer in use] (Stage 6, combined with Gantt's own resolution retest): with Lotus still active, resized to 1280×720 and found the Gantt plugin's own timeline week-range date headers overlap into illegible text at this specific combination (cell `clientWidth` 99px vs `scrollWidth` 116px, vs. a clean 139px/139px under Default theme at the identical resolution) — this is a **Gantt-plugin** defect (its own fixed-pixel-width header cells), not a Lotus-theme defect, so it was added as a third variant to `BUG-GNT-002` (in the Gantt plugin's own bug tracker) rather than here, even though Lotus's reduced available width is what triggers it. Recorded here too since it's a genuine Stage 6 (Lotus + resolution combined) finding relevant to this theme's own test cycle.

TC-LTS-009 (explicitly asked: "did you tested all core pages of redmine with theme in german language"): extended the sweep to My Page, 7 Administration deep pages (Benutzer/Rollen und Rechte/Tracker/Ticket-Status/Aufzählungen/Benutzerdefinierte Felder/Workflow), the New Issue form, and Search — all correctly translated except already-known bugs reproducing (`BUG-AGB-001`'s Story Points, `BUG-TAG-002`'s Tag list, `BUG-LTS-001`'s sidebar clipping). Found one informational item: the Search page's "Apply issues filter" link is hardcoded English, confirmed reproducing under Default theme too — a genuine **core Redmine** i18n gap (missing upstream `de.yml` key), not owned by Lotus or any Redmineflux plugin, so not filed as a bug against either — noted for completeness only.

TC-LTS-010 (explicitly asked: "please cover all redmine pages do not miss any form also test lotus theme configuration page"): tested the Lotus plugin's own "Konfigurieren" link — **it 404s** (no settings page/route exists), confirmed via its real `href` and reproducing regardless of active theme. Filed as `BUG-LTS-005` (Medium) — the only new bug this pass. Also covered: Gruppen, LDAP-Authentifizierung (list + full new-source form), Applikationen, Plugins list, Informationen, Konfiguration's Integrations + Allgemein tabs, Mein Konto, bulk-edit issues, Copy Issue, issue relations (add), watchers (add), core Gantt, core Calendar — all correctly translated except already-known Redmineflux bugs reproducing (`BUG-AGB-001`'s Story Points, `BUG-TAG-002`'s Tag list) and a cluster of **core-Redmine-owned** (not Lotus/Redmineflux) i18n gaps in newer Redmine 7.0.1 features: "Integrations" tab label, "Enable webhooks", "Enable reactions", "Pandoc available (optional)", "Default queue adapter which is well suited only for dev/test changed" — all confirmed theme-agnostic and plugin-independent (no webhook/DevOps plugin installed here), so documented as informational only, not filed.

## In Progress

- A console 404 for `plugin_assets/redmineflux_lotus/plus-d08c3110.svg` was observed but its visual impact (if any) could not be pinned down in the time available — flagged, not filed as a bug.
- Dashboard plugin retest under Lotus still pending — the only remaining plugin-level item for this theme's cycle.

## Blockers

- None.

## Next Session Start Point

- `bugs/open/` is **no longer empty** — `BUG-LTS-007` (Medium, filed 2026-09-22) needs a fix + retest before the
  full final-cycle regression matters. When retesting, check the fix holds under **both** branches of
  `show.html.erb`'s `Setting.ui_theme` split (Default/blank and `redmineflux_lotus` explicitly selected) — the
  bug was present in both.
- Once `BUG-LTS-007` is fixed and `bugs/open/` is empty again: per `CLAUDE.md` §10/§12, the next step before
  `STATUS.md` can be set to `Complete` is the **full final-cycle regression** (`SENIOR_QA_STANDARDS.md` §27):
  re-execute all 13 TCs in `testcases/LOTUS_GERMAN_LANGUAGE.md` (no automation specs exist yet for this plugin,
  so this is fully manual). Not yet run — start here once the bug is clear.
- Note for whoever runs that regression: `BUG-LTS-003`'s sub-findings flipped fixed/unfixed across servers multiple times throughout this bug's history — if the edit-form full-width layout or any of its other 3 sub-findings resurface during the regression pass, that's expected server-dependent behavior, not necessarily a new defect; consult `LOTUS_MEMORY.md`'s Recurring Issues section.
- Dashboard plugin retest under Lotus is also still pending (see "Not yet covered this session" in the testcase file) — the last plugin-level item for this theme's full cycle.
- **Before filing a "content is clipped/cut off" finding on any tab strip, carousel, or similarly narrow container, check whether scroll/page controls (arrows, dots, swipe) are present and whether clicking them reveals the content in full** — `BUG-LTS-002` was invalidated because what looked like broken clipping was actually a working "peek of next item" affordance, confirmed by clicking through it.
- **When retesting BUG-LTS-003's remaining sub-findings, always check at 1280×720 as well as 1920×1080, and always inspect the actual rendered value/text elements (e.g. `.rf-show-display` spans), not just the outer `.attribute` container rects** — the outer containers can look non-overlapping while an inner value span still spills into a neighboring field's space. This exact mistake caused a bug to be closed as fixed and then have to be reopened same-day.
- Investigate the `plus-d08c3110.svg` 404's visual impact.
- Retest the Dashboard plugin under Lotus (its own Stage 1 default-theme pass is already done — this is the last plugin from the original list still needing a Lotus pass). Once done, this plugin's German+Lotus test cycle is essentially complete.
- The full core-Redmine page/form sweep requested via "do not left any page and any element please test carefully" is now done (TC-LTS-011), and the follow-up Create-vs-Edit-form + both-resolution gap the user flagged directly afterward is also closed (TC-LTS-012) — every item in `LOTUS_SCOPE.md`'s checklist is ✅ except a short deliberately-deferred list (password-change form, pre-auth login/registration, project archive/close/delete, file download/preview, outgoing-email test page, issue context menu) — see that file's Notes section for the reasoning on each; these are low-priority, not owed follow-up unless the user asks for them specifically.
- **Reusable pattern from TC-LTS-012**: when a user asks "did you test X" as a direct follow-up rather than a new task, treat it as a real coverage audit, not just a status check — it surfaced a genuine gap (Create forms tested, Edit forms mostly weren't) that the "test everything" pass before it had missed.
- When testing further plugins under Lotus, deliberately check narrow-container tab/nav elements at 1280×720 specifically — this is the second time a Lotus fixed-width container has clipped longer German text, so treat any tab strip, sidebar list, or similarly narrow label container as a suspect area worth checking at both resolutions, not just one. Also worth a quick sidebar-nav check even at 1920×1080 — `BUG-LTS-001`'s project-nav variant was found there, not at the smaller resolution.
- **New pattern to watch for**: TC-LTS-008 [legacy pre-2026-09-22 ID, no longer in use] found `BUG-AGB-007` not through a translation/layout defect but through a plugin's list view failing to resolve an enum value that its own Edit form resolves correctly — when sweeping core-Redmine-adjacent plugin tabs (like a project-settings sub-tab), compare list-view display values against the same record's Edit-form values, not just against other rows in the list.
- The environment's active theme was reverted to Standard (Default) at the end of TC-LTS-011 — confirmed via DOM (`link[href*="lotus"]` absent). Re-enable Lotus explicitly at the start of the next Lotus-focused session; don't assume it's still active.

## Open Bugs Found

- **BUG-LTS-007 (Medium)** — Project Overview page shows the "closed and read-only" warning twice, root-caused
  to a redundant reimplementation in `show.html.erb`'s own branches, not theme-selection-dependent. Found
  2026-09-22, local Docker. Reported to production as **#121062**, assigned to Vaishnavi Bhawsar.
  `plugins/redmineflux_lotus_qa/bugs/open/BUG-LTS-007.md`

## Closed Bugs (fixed, verified across sessions)

- BUG-LTS-001 (Medium) — admin sidebar + project sidebar labels clipped under Lotus theme. Fixed: sidebar now wraps long labels (`white-space: normal`) instead of clipping.
- BUG-LTS-004 (Low) — Inline Editor's Description-CKEditor "Save" button oversized (50px vs Cancel's 34px) under Lotus only. Fixed: both now render at 34px.
- BUG-LTS-005 (Medium) — the plugin's own "Konfigurieren" link led to a genuine 404. Fixed: settings page now loads with working Tracker-Rahmenfarben/Prioritätssymbole/Logo tabs.
- BUG-LTS-006 (was Medium, fixed 2026-09-10) — subtask/related-tickets table overflowed its container at 1280×720, overlapping the Historie/Notizen sidebar. Fixed: a new `div.rf_issue_section_row` wrapper (`overflow-x: auto`) now scrolls the table within its own bounds instead of letting it spill into the sidebar.
- BUG-LTS-003 (Medium, closed 2026-09-18) — Sprint/Story-Points integration under Lotus (font/color/bullet mismatch, value overlap, Tags-widget overlap) all confirmed fixed across retests. The last remaining sub-finding (issue-edit-form full-width layout, no overlap) was closed per explicit user judgment as minor/cosmetic rather than left open, despite still reproducing on the final retest server.

## Invalidated Bugs (not actual defects)

- BUG-LTS-002 — issue detail tab strip appeared to clip its last tab's label at 1280×720. **Invalidated 2026-09-10**: confirmed via click-through testing that this is a working "peek of next tab" pagination affordance — the "<"/">" controls correctly page through and reveal every tab in full. Not a bug.

## Related Finding — Confirmed Theme-Agnostic, Filed Anyway Per User Direction

- The subtask/related-tickets table overflow (see `BUG-LTS-006` above) was confirmed to reproduce identically under the Default theme during investigation. When asked where to file it, the user directed it be filed under Lotus as a "core issue detail page" finding rather than left as informational-only or attributed to a specific contributing plugin. Filed as `BUG-LTS-006`, with the theme-agnostic finding documented transparently in the bug file itself.

## Related Bug Found Against Another Plugin

- BUG-AGB-007 (Low, filed against Redmineflux Agile Board) — found while sweeping this theme's own core-Redmine pages (the project Settings → Sprints tab): the "Freigabe" column shows a raw untranslated enum value for some sprints. Confirmed theme-agnostic (reproduces under Default too) — root cause and fix live in the Agile Board plugin, not counted in this row's own bug total.

## Run History

| Date | Redmine Version | Environment | Tested By | Summary |
|------|-----------------|-------------|-----------|---------|
| 2026-09-07 | 7.0.1.stable | Forge (flux-fczk00paf49) | Claude (Playwright MCP) | Lotus theme applied (German active). TC-LTS-001 PASS, TC-LTS-002 FAIL (BUG-LTS-001), TC-LTS-003/004 confirm existing Tag Plugin bugs are theme-agnostic, TC-LTS-005 (Checklist Plugin) PASS, TC-LTS-006 (resolution testing) FAIL at 1280×720 only (BUG-LTS-002). |
| 2026-09-07 | 7.0.1.stable | Forge (flux-f6nlrqpvk49 — replaces flux-fczk00paf49, reported expired) | Claude (Playwright MCP) | TC-LTS-007: retested Gantt Plugin under Lotus — PASS, no new Gantt bugs. Found a third instance of `BUG-LTS-001`'s root cause on the project-level sidebar nav ("Aufgewendete Zeit" clipped), added as an additional affected surface. Theme reverted to Standard afterward for the Gantt plugin's continued Stage 1 work. |
| 2026-09-08 | 7.0.1.stable | Forge (flux-frmka2kzh49) | Claude (Playwright MCP) | TC-LTS-008 [legacy pre-2026-09-22 ID, no longer in use]: Stage 4 core-Redmine-under-Lotus sweep — Übersicht, Wiki, Roadmap, Kalender, News, Dokumente, Dateien, Aktivität, and all 9 project Settings tabs (Projekt/Mitglieder/Tickets/Versionen/Ticket-Kategorien/Repositories/Foren/Zeiterfassung/Sprints), plus a 1280×720 pass on the settings tab bar, Sprints table, and Roadmap. PASS overall at both resolutions, no new Lotus-owned bugs. Found `BUG-AGB-007` (Low) on the Agile Board plugin's own Sprints settings sub-tab, confirmed theme-agnostic — filed against that plugin, not Lotus. Also corrected Agile Board's own TC-AGB-115 finding: the sprint edit/delete UI does exist, at this Settings tab. |
| 2026-09-08 | 7.0.1.stable | Forge (flux-frmka2kzh49) | Claude (Playwright MCP) | TC-LTS-009: extended the core-page sweep to My Page, 7 Administration deep pages, the New Issue form, and Search. PASS overall, no new Lotus/Redmineflux bugs — only already-known bugs reproduce. Found one core-Redmine-owned i18n gap ("Apply issues filter" on Search, confirmed under Default too) — informational only, not filed. Stage 4 sweep is now complete; only the Dashboard plugin's own Lotus retest remains for this theme's cycle. |
| 2026-09-08 | 7.0.1.stable | Forge (flux-frmka2kzh49) | Claude (Playwright MCP) | TC-LTS-010: tested the Lotus plugin's own Configure page — 404, filed `BUG-LTS-005` (Medium). Also covered Groups, LDAP form, Applications, Plugins list, System Info, Konfiguration's Integrations/Allgemein tabs, My Account, bulk-edit, Copy Issue, relations, watchers, core Gantt, core Calendar — all clean except already-known bugs and a cluster of core-Redmine-owned (not Redmineflux) i18n gaps in newer 7.0.1 features, documented as informational only. |
| 2026-09-08 | 7.0.1.stable | Forge (flux-frmka2kzh49) | Claude (Playwright MCP) | TC-LTS-011 (explicit instruction: "do not left any page and any element please test carefully"): exhaustive final sweep — remaining 9 Settings tabs' own field content, real Wiki content page + history + index, News/Document/Version-detail pages, Repositories new-repo form (viewed only), Forums (forum+topic created/viewed/deleted), global Time Entries list+report, Custom Queries (created/saved/edited-link-confirmed/deleted), and confirmation that Move Issue is merged into bulk-edit (`/issues/move` 404s). PASS overall, no new Lotus/Redmineflux bugs — 8 more core-Redmine i18n gaps found (informational, running total 14, see `LOTUS_SCOPE.md`). Theme reverted to Standard at session end. Core-page/form coverage is now exhaustive except a short, explicitly-documented deliberately-deferred list. |
| 2026-09-08 | 7.0.1.stable | Forge (flux-frmka2kzh49) | Claude (Playwright MCP) | TC-LTS-012 (user asked directly: "did you tested edit and create form of each sections... also did you tested in both resolution"): closed a real gap — TC-LTS-011 mostly exercised Create forms only. Re-tested under Lotus, this time opening each section's own **Edit** form specifically: Wiki (Sidebar + main content page), News, Document, Forum, Forum Topic (both Create and Edit), Version (existing Version 5's Edit form + New form viewed), Issue Category (Create+Edit), Member (add-member modal + inline role-edit), Custom Query (actual Edit form, not just the link's existence). All correctly translated — zero new bugs. Also spot-checked overflow (`scrollWidth`/`clientWidth`) at 1280×720 on the News create form, Version edit form, Members table, and Query edit form — no clipping found; one `hidden-for-sighted` screen-reader-only label flagged then confirmed not a defect. Theme/viewport reverted to Standard/1920×1080 at session end. |
| 2026-09-08 | 7.0.1.stable | Forge (flux-frtsiofsm49) | Claude (Playwright MCP) | While retesting the Tags plugin's `BUG-TAG-006` (Tags-widget overlapping Sprint/Story Points) once the user enabled the Story Points custom field: confirmed under Standard theme the overlap is fixed, but it still reproduces under Lotus (tag editor's bounding box fully covers "Story Points:", confirmed via computed rects + screenshot on issue #260). Root-caused to the same gap as `BUG-LTS-003` (Sprint/Story Points sit outside Lotus's `rf_issue_attrs_grid`) — added as an additional affected surface on that bug rather than a new bug ID. `BUG-TAG-006` closed against the Tags plugin since its own Default-theme reproduction is genuinely fixed. |
| 2026-09-09 | 7.0.1.stable | Forge (flux-fhggkobjh49) | Claude (Playwright MCP) | Retest pass on all 5 open Lotus bugs (theme selected on this server first, per user instruction). Server switch mid-pass: the originally-used retest server (`flux-fdrk6suoj49`) began returning HTTP 403 on every route partway through `BUG-LTS-003`, so the user provided this replacement server and testing restarted here. BUG-LTS-001 (admin+project sidebar clipping), BUG-LTS-004 (button height), BUG-LTS-005 (Configure 404) — all confirmed FIXED, moved to `bugs/closed/`. `BUG-LTS-003` was initially closed too (checked only 1920×1080, outer-container rects only) but **reopened same session** after the user flagged screenshots showing the overlap still happening — re-verification at 1280×720 with element-level bounding boxes found 2 of 4 sub-findings (Sprint/Story-Points text overlap, Tags-widget overlap) still reproduce at that resolution, confirmed Lotus-specific (clean under Default theme); the other 2 (font/bullet, edit-form width) are fixed at both resolutions. `BUG-LTS-002` (tab strip clipping at 1280×720) — still reproduces, now worse (a 4th tab, "Eigenschaftsänderungen", appears on this server). New informational, non-Lotus finding: subtask/related-tickets tables overflow their container at 1280×720, confirmed theme-agnostic (reproduces under Default too) — not filed. `bugs/open/` not yet empty (BUG-LTS-002, BUG-LTS-003); `STATUS.md` remains `In Progress`. |
| 2026-09-09 | 7.0.1.stable | Forge (flux-fhggkobjh49) | Claude (Playwright MCP) | Follow-up in the same session: the subtask/related-tickets table overflow (previously left informational) was, at the user's explicit request, filed as `BUG-LTS-006` (Medium) — root-caused to the table's ~32-column set (many plugins each contributing one column, e.g. Agile Board's `sprint_craft`/`story_points`, Tags plugin's `tags`) rendering at ~2001px inside a ~548px container with `overflow-x: visible`, spilling into the Historie/Notizen sidebar column at 1280×720. Confirmed theme-agnostic (reproduces under Default) but filed against Lotus per explicit user direction rather than left informational or attributed to one contributing plugin. `bugs/open/` now has 3 bugs (BUG-LTS-002, 003, 006); `STATUS.md` remains `In Progress`. |
| 2026-09-10 | 7.0.1.stable | Forge (flux-fhhcov1xf49) | Claude (Playwright MCP) | User pushed back on `BUG-LTS-002` via screenshot, pointing out the "<"/">" buttons are a scroll control. Verified by clicking through the tab strip on issue #1 (Lotus, 1280×720): confirmed it's a working paginated "peek of next tab" affordance — earlier tabs are hidden (`display:none`, not scrolled) as you page forward, and each tab renders fully once paged to (final residual overflow ~5px, down from the originally-described 23–174px). **Invalidated — moved to `bugs/closed/`.** `bugs/open/` now has 2 bugs (BUG-LTS-003, BUG-LTS-006). |
| 2026-09-10 | 7.0.1.stable | Forge (flux-f3lnytazd49) | Claude (Playwright MCP) | Retested both remaining bugs at 1280×720, Lotus theme, fresh test issue (Sprint="Bug Bash", Story-Points=89, plus subtask + related ticket, all deleted afterward). **BUG-LTS-006 confirmed FIXED and closed** — a new `div.rf_issue_section_row` wrapper (`overflow-x: auto`) now scrolls the subtask/related-tickets table within its own bounds (~703px), no longer overlapping the Historie/Notizen sidebar. **BUG-LTS-003 narrowed further**: the Sprint/Story-Points value overlap is now fixed (Sprint value renders single-line, 58.6px, no wrap); font/bullet and edit-form fixes still hold. The Tags-widget overlap still reproduces, but now against the Story-Points *label* rather than the Sprint value (49.4×19.5px confirmed overlap) — bug stays open, narrowed to just this. `bugs/open/` now has only 1 bug (BUG-LTS-003). |
| 2026-09-18 | 7.0.1.stable | Forge (flux-fccirp6sk49) | Claude (Playwright MCP) | User asked to retest `BUG-LTS-003` and close it. Retested at 1280×720, issue #260 (Sprint="Bug Bash", Story-Points=13). **Mixed result, initially not closed**: Tags-widget overlap (the sub-finding the title was narrowed to) is now FIXED; Sprint/Story-Points value overlap and font/bullet mismatch reconfirmed fixed; but the edit-form full-width break — previously fixed on a different server (2026-09-10) — REGRESSED and still reproduced here. Reported the mixed result to the user. **User reviewed and explicitly directed closure anyway**, judging the remaining width-only (non-overlapping) layout gap as minor/cosmetic. Bug closed, moved to `bugs/closed/`. Production issue #120218 was already Done/100% (updated directly in Redmine by Sourabh Singh same day, outside this session) — no write needed. `bugs/open/` now **empty**. Full final-cycle regression (`SENIOR_QA_STANDARDS.md` §27) not yet run — `STATUS.md` stays `In Progress` pending it. |
| 2026-09-22 | 7.0.0 (Docker) | Local (redmine-docker-700, localhost:3010) | Claude (Playwright MCP) | Incidental finding, not a planned Lotus pass — user reported a duplicated "closed and read-only" banner on a Checklist-plugin test fixture project's Overview page. Investigated and root-caused: `redmineflux_lotus/app/views/projects/show.html.erb` overrides Redmine core's Overview view and both of the file's internal `Setting.ui_theme` branches independently reimplement the closed-project warning, duplicating what core already renders once globally (confirmed 1 copy on Issues page for the same project vs. 2 on Overview). Confirmed not theme-selection-dependent (reproduces with "Default" theme selected). Filed `BUG-LTS-007` (Medium). **User explicitly approved reporting it to production** — created as #121062, assigned to Vaishnavi Bhawsar. |
